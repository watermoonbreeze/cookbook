[unattended] data(生产线): P0 基础搭建 data-pipeline/ + 现有505首校体检单(roundtrip无损·揪5真错)

按冻结详细设计(预设食材数据生产线_详细设计.md)落地 P0:
- data-pipeline/ 六脚本(纯Python stdlib·无三方依赖):
  build_staging(建库) / import_seed(seed→staging·关联失败记flag) /
  export_seed(staging→等价seed·roundtrip用) / verify_roundtrip(首校编排+校验门+体检报告)
- schema.sql: staging schema(带 source/confidence/review 溯源·nutrient 长表逐字段可溯源)
- mappings/feature_tags.txt: 特征标签白名单(前缀规则区分 category 真分类 vs feature 特征标签)
- staging.db: 入git留数据快照(详细设计十一.1) · _export/__pycache__ gitignore

首校结论:
- roundtrip 语义等价 diff=0 → 现有 seed↔staging 无损·采集→存储→发布链路可信·可放心扩量(P1+)
- 体检 858 flag: 🔴合理性真错5(饱脂>总脂物理矛盾·培根/沙丁鱼/海带/田螺/羊排·satFat来自USDA而fat来自成分表口径不一致·7-22能量自检没覆盖的新发现) +
  🟡章鱼真疑(录kcal>Atwater·同7-22待核) + 低置信度41 + 缺guideline573(结构性·care/crowd seed无ref字段) + 离群223(启发式待人工过滤)
- 单位越界0(单位量纲干净)

红线守住: 只诊断不改任何 seed(详细设计十一.2 + 用户"等真实数据来了再调") · 忌口=规则非数据(缺guideline揪出无出处) · 脚本只产报告不动库

修 bug: sqlite3 同cursor边SELECT迭代边INSERT中断外层→低置信度/缺guideline/单位越界只写1条·改fetchall()后正确(41/573/0)

待用户拍板(记入unattended_decisions队列): 5条satFat真错怎么修/缺guideline是否补ref字段+按condition回填/P1 USDA采集(需key)/nlc精确匹配常规化

同步: 全数据源交叉核对_发现.md 补7-24复检 + 待办P0完成 + SESSION交接 + unattended_decisions

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
