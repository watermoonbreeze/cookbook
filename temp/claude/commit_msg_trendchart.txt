[unattended] feat(饮食报告): 营养走势折线(宏量克数·个人视角·报告专属)

方案拍板=曲线只放饮食报告(真实摄入)、周计划不做;画宏量克数单条+三色切换。
过 apple_ux_designer 门禁出规格 + Google 质量门禁(等价性逐字段验证·无阻断) + shared 单测 + 双构建绿。§9.40。

shared 数据层(纯逻辑·防漂移):
- NutritionTotals 加 operator times(f)(逐字段×系数·按成员 share 折算)
- DietReport 加 perDayNutrition: List<NutritionTotals?>?(逐日个人营养·已×share·空天null·长度=periodDays·家庭/无记录=null)
- aggregate() 重构:逐日序列作单一真相源、personal 均值由它 filterNotNull 聚合(同源防口径漂移)
- 防漂移单测:逐日序列均值≈personal.avgKcal + 空天null + 家庭视角null

UI:
- 新组件 NutritionTrendChart(Canvas 自绘):单条折线+三色切换chip(默认蛋白)·96dp·min=0归一·断线分段(空天/零值断点·不跨空连不补0)·周画点月不画·x轴锚点标签·"怎么看"解读行·空/少数据三态·复用 LocalExtendedColors 三色
- 守红线:无目标线/无刻度/无红/热量不做主轴/个人视角专属/免责由外层卡承载不重复
- DietReportScreen 营养摄入卡内(膳食纤维行后·免责前)插入折线子区(非独立卡)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
