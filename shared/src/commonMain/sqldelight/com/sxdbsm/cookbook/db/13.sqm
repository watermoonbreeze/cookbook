-- [AI生成] v13 -> v14：pantry 加份数列 serving_count。
-- 份数=可做几次菜(用户提供总量)；剩余/不足由派生占用实时计算，不落库。
-- serving_count 为新列，ALTER ADD 安全(非已有列)。
ALTER TABLE pantry ADD COLUMN serving_count INTEGER NOT NULL DEFAULT 0;
