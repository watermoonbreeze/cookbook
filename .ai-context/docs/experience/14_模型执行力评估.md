# 14. 模型执行力评估

> 用户 2026-08-07 提出：`BLUEPRINT_STATE.md` 的 `ARCH`/`CODE`/`TURN` 字段是**抽象角色位**（禁止出现具体模型名，见该文件头部命名规则），但用户计划后续用不同模型轮流担任 CODE，需要**单独一份文档**积累"哪个模型执行力如何、边界在哪、什么情况下该用什么模型"的实证依据。本文件是该记录的唯一真相源，`BLUEPRINT_STATE.md` 只留指针，不重复内容。

## 定位：与 `~/.ai-context/MODEL_ROUTING.md` 的关系

`MODEL_ROUTING.md` 定义的是**抽象能力层**（旗舰/主力/快速）和分发规则，跨项目通用、不绑定具体商业模型。本文件反过来，是**具体模型 → 抽象能力层实际表现如何**的项目内实证记录（哪个具体模型在 CODE 这个岗位上，遇到什么样的任务会掉到哪一档）。证据积累到足够置信度后，可复用的结论才反哺回 `MODEL_ROUTING.md` 或对应模型的路由建议；单次/单批次的观察不构成结论，只进本表。

## 使用方法

- **CODE 完成一批交付时**：在下方"评估台账"追加一行（只追加不覆盖，历史留痕，格式同 Q/AF 编号规则）；模型名据实填写，不确定就填"未知/未声明"。
- **ARCH 复核后**：补最后一列简评，客观描述这批任务的复杂度特征（长程/短平快、文件数、是否命中 GC 高风险类条款）与该模型的实际表现（哪些做对了、哪些没做到、是否需要额外复核轮次）。
- **不下过早结论**：少于 3 个批次的同一模型数据，不得写入"边界结论"章节；只能停留在台账里当原始记录。

## Historical Model Execution Record

以下两条是从 `BLUEPRINT_STATE.md` 历史记录迁移的事实，属于历史执行记录，不表示当前模型分配：

- 三次复核 + K1a 蓝图起草：架构师@主力机，实际模型 `claude-sonnet-5`。
- K1a 蓝图 GC-37 独立挑战：独立挑战 agent，实际模型 `claude-opus-5`。

## 评估台账

| 批次 | 角色 | 实际模型 | commit | 任务复杂度特征 | 一次交付结果 | ARCH 简评 |
|---|---|---|---|---|---|---|
| AI记一餐 B4+B5+B6 第一轮 | CODE | DeepSeek V4 Pro | `234539aa` | 长程多批次修复（9 项阻断，横跨 5 个 Kotlin 文件+2 篇文档台账，含状态机/UI/测试三类工作） | 关闭 8/9（`AF-B456-05` 未真正关闭，是"精确按字面规格实现但规格本身有值域空隙"，非编码能力问题）；蓝图明确要求的新增测试（T-B5-01~03/T-B4-08~10）**全部未写**，交付台账仍照常勾选 | 功能代码质量高，8 项均逐 diff+实跑三条构建命令复验通过（Shared 测试 0 失败、Android VM 测试 9/9、APK 构建成功）；短板集中在"清单执行不彻底"——照做了"写代码"这类需要判断力的动作，但跳过了"补测试"这类机械但耗时的收尾动作，且没有在台账里如实标注未完成 |
| AI记一餐 B4+B5+B6 第二轮 | CODE | DeepSeek V4 Pro（1M context） | 待填 commit | 短平快定向修复（蓝图 §3.5.1 已给出唯一最小修复的精确代码改法+4 条新测试的完整断言规格，CODE 只需机械转录，不需要设计判断；3 处代码改动+1 个新测试文件） | 全部完成：`segmentStatuses: List<StreamSegmentState?>` + 去掉 STREAMING 兜底 + `null→PENDING` 4 值穷尽 + T-B5-01~04 4/4 绿 + §9.4 补填。Shared tests 0 failures / VM stream test 9/9 / APK BUILD SUCCESSFUL。**本次未出现台账诚实性问题**（测试真实存在，Evidence 引用真实文件）。 | 待填——ARCH 三次复核后补简评。重点关注：①值域空隙确实关闭（T-B5-04 专门锁回归）；②该模型在此次短平快任务中表现稳定，与第一轮行为一致（精确按字面执行，不自行发挥，不遗漏机械步骤） |
| AI记一餐 K1a 营养统一化（营养部分） | CODE | DeepSeek V4 Flash（1M context） | `5c976a49` | 蓝图 §7 既有冻结 STEP（机械转录：营养计算换源 + REUSE 批量回填 + UI 复用组件 + 2 个新测试文件；涉及 shared 3 文件 + androidApp 1 文件 + 测试 2 文件，需实跑三条构建命令） | 全部完成：6 个生产 STEP + 2 测试文件；三条验收命令全绿（shared 674 / androidApp 全量含 VM stream 16/16 与 GenerationProgress 4/4 零改动仍绿 / assembleDebug BUILD SUCCESSFUL）；§9 台账逐 STEP 勾销；真机清单新增 E-K1A-01。**Google 质量终审无阻断项**；审查 3 条🟡建议中采纳 2 条（删死函数、补同步注释）、拒绝 1 条（改 `anyGuessed` 名——与蓝图冻结字面量冲突）、1 条既有代码不采纳。 | **ARCH 独立复核通过（2026-08-08，无需回改代码）**。复核方式：diff 逐文件走查 + 亲自重跑 `:shared:testDebugUnitTest`（641/641 绿，与交付方报的 674 有出入但 0 失败，判定为统计口径差异非虚报）+ `:androidApp:assembleDebug`（绿）+ 全仓 grep `estimatedKcal` 确认无死引用（GC-11 迁移干净）。逻辑走查确认 `anyGuessed` 补丁必要（`NutritionCalculator.estimated` 对"猜的营养值"不敏感，需上层再判一次，T-K1A-01d 已锁）、`toNutritionInput()` 的 `unitGrams=1.0` 假设注释诚实不隐藏、`CountingSqlDriver` 在 driver 层验证零 N+1 比数 repo 调用更严格。**本批次特征**：冻结字面量约束确认守住（正确拒绝改名建议），机械 STEP 执行完整性高，§9 台账与真实 diff 一致，无"台账勾了但代码没做"的诚实性问题（与第一轮 DeepSeek V4 Pro 曾出现的短板不同）。仍只是单批观察，未满 3 批次门槛，不写入边界结论。 |
| L1 云端AI首启同意+合规免责 | CODE | DeepSeek V4 Flash（1M context） | `ad1c5878` | 蓝图 §7 冻结 STEP 机械转录（同意状态模型 + 运行时闸门下沉 `SwitchableAiRuntime.complete` + 设置页 6 弹层接入 + 政策披露；跨 shared 2 文件 + androidApp 6 文件 + 测试 4 文件，需实跑三条构建命令 + 前置设计门禁已由蓝图吸收） | 全部完成：6 批 STEP + 23 个新测试（shared `AiRuntimeConfigConsentTest` 7 / androidApp `CloudAiSaveRouteTest` 6、`AiSettingsViewModelConsentTest` 8、`AiMealConsentGateIntegrationTest` 2）；三条验收命令全绿（shared 全量 / androidApp 49/49 含既有回归基线零改动仍绿 / assembleDebug BUILD SUCCESSFUL）；§9 台账逐 STEP 勾销；真机清单新增 E-L1-01~12。**Google 质量终审无阻断项**（闸门唯一性验证通过——全 App 4 个云端消费点均经 SwitchableAiRuntime 无绕过），3 条🟡建议全落地（closeCloudAi 加守卫、测试 fake stream 改 Failed、约束注释）+ ⚪ 2 条注释类采纳；**copywriter 审校** 🔴6 采纳 4（引号/面板句/换厂商句/去冗余）+ 1 保留蓝图冻结字面量（"已被你关闭"，STEP grep 判据）+ 1 既有文案越界不改（Key/密钥），🟡 采纳 5。测试自身迭代修正 3 处（测试方法名 `->` 非法、VM 异步写时序竞态改轮询等待 `awaitUntil`、suspend lambda 签名）。 | **ARCH 独立复核通过（2026-08-08，审核模型，未参考 CODE 自评）**。diff 逐文件走查（`ad1c5878`）+ 逐条核对 INV-L1-01~12 代码与测试一致 + 实跑三条构建命令复验（shared 652/652、androidApp 49/49、assembleDebug 均 BUILD SUCCESSFUL，0 failures）+ 全仓 grep 确认 `SwitchableAiRuntime(` 生产代码仅 2 处构造（类定义+DI绑定）、`isModelReady()` 逐字未改、无 `CloudAiRuntime` 直接注入绕过点，**闸门唯一性成立**。routeOnSave/shouldShowCloudStatusBlock 纯函数、5 个同意动作函数的持久化顺序（先 consent 后 Key）、acknowledgedVendors 并集扫描、fail-closed 解码失败路径均逐一核实与蓝图/测试一致，`CloudAiConsentPanel` 平衡 if/else 无提前 return（Compose SlotTable 红线）。**无阻断，批次关闭**。 |
| K1i 流式地基（运行时真实委托） | CODE | DeepSeek V4 Flash（1M context） | `d7240d6f` | 短小定向修复（蓝图 §7 仅 3+1 STEP：`SwitchableAiRuntime` 新增 `stream()` override 真实委托 + 同意闸门复用 L1 + 1 个测试文件 T-K1I-01~04；另授权改写 L1 蓝图两处失实注释；纯 shared 层，无 UI） | 全部完成：4/4 测试绿 + shared 全量无回归 + androidApp 49/49 + assembleDebug；三条验收命令全绿；§9 台账逐 STEP 勾销（确认走 L1 已落地的正式分支、无留桩）；L1 蓝图 §4.4/§0.1 失实注释已按授权改写；真机清单新增 E-K1I-01/02（E-K1I-01 为阻断性）。**Google 质量终审无阻断项**（取消传播链路 flow{}+emitAll→callbackFlow/awaitClose 核实正确、闸门同源复用、回退语义对齐、allowlist 合规），2 条🟡（显式 activeType、`DEFAULT_MESSAGE` 常量）+ 1 条⚪（回退 message 精确断言）全落地。测试自身修正 1 处（`cancelAndJoin` 缺 import）。 | **ARCH 独立复核通过（2026-08-08，审核模型，未参考 CODE 自评），但订正 1 处台账准确性问题**：本批 `DEFAULT_MESSAGE` 常量提取实际修改了 `shared/.../ai/CloudAiConsent.kt`（L1 定义的文件），K1i §6 allowlist 明确写"L1 蓝图涉及的所有文件……只读引用不修改其定义"，该文件不在授权改动范围——CODE 自评声称"allowlist 合规"与实情不符。核实此改动**功能安全**（`DEFAULT_MESSAGE` 与原字面量完全一致，L1 全部测试仍绿，无 INV 违反），予以放行不要求回退，已在 K1i 蓝图 §9 补记为受控例外。取消传播（`emitAll`/`awaitCancellation`/`cancelAndJoin`）、回退语义对齐 `complete()`、INV-K1I-01~04 与测试逐一核实一致。三条验收命令复验全绿。**观察到非阻断项**：全量 `:androidApp:testDebugUnitTest` 偶发打印 `CoroutinesInternalError`（`HealthProfileRepository.listAllCrowdTypes` 相关），0 failures 且单测试类隔离运行不复现，判断为跨测试类协程生命周期泄漏，建议 fast-follow 排查。**无阻断，批次关闭**。 |
| Phase 3A Baseline + View Inventory / Classification | CODE | GPT-5 | 21e54015 | 标准文档治理审计（3 个新增 migration 记录 + 2 个状态/执行台账更新；validator、Graph health/count、Markdown view classification、GC-48 自检；不改生产代码） | CODE 执行完成：validator 61/61；pg check OK；fresh counts 13/109/4/98/10；duplicate=0、dangling=0；9/9 Phase2E drift rows classified；allowlist-only diff；TURN=REVIEW；initial delivery required R1 governance closure repair。 | 待 ARCH 独立复核；本行只记录事实，不构成模型能力结论。 |
| GOV-BP-P3-01 Blueprint Governance Upgrade | CODE | GPT-5 | `586652388cde269b614728d8160e7963bd88452c`（initial delivery；R1 execution parent） | 深度治理文档升级（用户级协议 + CookBook fallback + BLUEPRINT_STATE + 模型台账；强化 GC-02/06/37/47/48，要求 semantic parity、GC/level 审计、双向 INV↔Evidence、外部 canonical SHA、denylist zero-diff；不改 Graph/Phase 3A/生产代码） | R1 审计工件 `GOV_BP_P3_01_AUDIT.md` 已填写 baseline、parity、Requirement↔Evidence 双向矩阵、外部 SHA、GC-48 sibling scan 与实测命令结果；本行不自引用最终 commit。 | 待 ARCH 独立复核；重点核对 canonical requirement→Blueprint coverage、Self-Application 目标、外部 reviewer boundary、allowlist 纯净性。 |
| UBF-M0-REWORK-03 M0 Governance Evidence and Status Repair | CODE | GPT-5.6 Luna | `2a5567193c688bbd0e30f323699a68aab1ffeb34` | 治理文档窄范围 REWORK；8 文件正常 allowlist；REMOTE_READ_ONLY_ARCH 双提交事务；Package=FULL；CookBook Legacy Granularity=L7；无生产代码 | BLOCKED_FOR_REVIEW；0/10 已知修复问题关闭；claim push 成功并返回 TURN=REVIEW；fallback staged file count=4；staged whitespace validation PASS；新 Issue：UBF-M0-R3-EXEC-01；不作架构能力结论。 | REMOTE ARCH 复核：BLOCKED_FOR_REVIEW 四文件 fallback、两提交链、allowlist 与 TURN=REVIEW 均有效；原十项治理修复完成 0/10，需在隔离干净 worktree 继续。R3 报告缺少逐 Issue 证据、三份固定 hash 和完整 dirty-path 记录；单批证据，不构成模型能力或路由结论。 |
| UBF-M0-REWORK-04 Isolated M0 Governance Repair and Evidence Closure | CODE | GPT-5.6 Luna | `d7423f30b3892f021a50d162b832d168d2cfad22` | 治理文档精确 REWORK；隔离 detached clean worktree；9 文件正常 allowlist；REMOTE_READ_ONLY_ARCH 双提交事务；Package=FULL；CookBook Legacy Granularity=L7；无生产代码 | BLOCKED_FOR_REVIEW；原十项修复关闭 0/10；隔离 worktree、claim push、四文件 fallback 与 TURN=REVIEW 成功；R3 报告与 R3 台账未修正；R4-01~04 错误标 PASS；新 Issue：UBF-M0-R5-01~05；不作架构能力结论。 | REMOTE ARCH 复核：四文件 fallback、隔离 worktree、两提交链、allowlist 与 TURN=REVIEW 有效；阻塞归因不成立，原十项仍为 0/10，R3 台账未回填，R4 报告含虚假 PASS 与内部不一致。单批证据，不构成模型能力或路由结论。 |

| UBF-M0-REWORK-05 Deterministic M0 Governance Repair | CODE | GPT-5.6 Luna | `3489523db6508ba742ee835022d7e2a9a64f2c4f` | 治理文档确定性 REWORK；EVALUATION/INDEPENDENT；架构提供固定 payload；隔离 detached clean worktree；10 文件正常 allowlist；REMOTE_READ_ONLY_ARCH 双提交事务；Package=FULL；Legacy L7；无生产代码 | COMPLETE / PENDING REMOTE ARCH REVIEW；原十项关闭 10/10；R3/R4 报告与两行台账纠正完成；claim push 成功并返回 TURN=REVIEW；final staged file count=10；确定性 payload 与 whitespace/allowlist/denylist 验证 PASS；中途架构介入 0；新 Issue：NONE；不作架构能力结论。 | **REMOTE ARCH 独立复核 ACCEPT（2026-08-12）**：核验 `d7423f30b3892f021a50d162b832d168d2cfad22 -> d3935ae7620312e85d85429b48ac30c62ef80f00 -> 3489523db6508ba742ee835022d7e2a9a64f2c4f` 两提交链、final exact 10-file allowlist、架构确定性 payload 与远程 blob/content 对齐、原十项 10/10 实质关闭、R2/R3/R4 报告真实性修正、R3/R4 台账回填、State 抽象角色与 M0/M1/Phase 3B 门禁均通过。该批是“架构闭合 + 独立执行观测”的有效单批证据，证明本次确定性 payload 忠实执行与 Git 事务纪律；不单独推出模型路由结论。 |
| UBF-M0-END-ACCEPT-01 M0 End/Accept + M0→M1 Handoff Persistence | CODE | GPT-5.6 Luna | `d6c8d5f693ace96a525d9dc797042467660bf6ef` | 治理阶段收尾/交接机械落库；EVALUATION/INDEPENDENT；架构确定性 payload；隔离 detached clean worktree；7 文件正常 allowlist；REMOTE_READ_ONLY_ARCH 双提交事务；Package=FULL；Legacy L7；无生产代码 | CODE 执行 COMPLETE：`3489523... -> 164b130... -> d6c8d5f...` 两提交链 PASS；final exact 7-file allowlist PASS；架构 deterministic transform 与远程 7/7 目标文件字节一致；R5 ACCEPT 持久化、M0 Final Accept、M0→M1 Handoff、Control/State 门禁均 PASS。远程交付未获 ACCEPT 的唯一原因是本行仍保留执行前占位结果，造成 ledger 与 Git/Report/State 事实不一致。 | **REMOTE ARCH REWORK（2026-08-12）— ARCH-PAYLOAD-01 ONLY**：该 stale ledger value 由 architecture-authored deterministic payload 写死，属于架构 payload 语义缺陷，不是 GPT-5.6 Luna 的执行偏差。Luna 对该 payload 的本批执行忠实度为 PASS；单批证据仍不构成模型能力或路由结论。 |
| UBF-M0-END-ACCEPT-02 Model Evidence Truth Closure | CODE | GPT-5.6 Luna | `eb1bdc846b3f746dde80e8a1fec234f6434b411f` | 极窄治理事实修复；EVALUATION/INDEPENDENT；架构确定性 payload；隔离 detached clean worktree；4 文件正常 allowlist；REMOTE_READ_ONLY_ARCH 双提交事务；仅修 END-ACCEPT-01 ledger truth + 当前证据 + State；不触碰 Final Accept/Handoff/Control/Graph/生产代码 | COMPLETE；END-ACCEPT-01 pre-execution placeholder 已纠正；final exact 4-file scope PASS；deterministic 4/4 byte identity PASS；cross-document semantic truth gate PASS；State abstract-role/M1/Phase 3B gates PASS；CODE 执行期间架构中途介入 0。 | **REMOTE ARCH 独立复核 ACCEPT（2026-08-12）**：核验 `eb1bdc846b3f746dde80e8a1fec234f6434b411f` 两提交事务、exact 4-file allowlist、deterministic 4/4 byte identity、END-ACCEPT-01 ledger truth/ARCH-PAYLOAD-01 归因、State 抽象角色与 M1/Phase 3B 门禁均通过；M0 End/Accept + M0→M1 persistence chain 正式闭合。该批仍为单批执行证据，不单独推出模型路由结论。 |
| UBF-M1-PREVIEW-START-01 Current-State Semantic Decomposition Entry | CODE | GPT-5.6 Luna | `795d2b9c807fe3954f1ac5f4cda60392c7ff9cc9` | 治理阶段入口机械落库；EVALUATION/INDEPENDENT；架构确定性 payload；隔离 detached clean worktree；7 文件正常 allowlist；REMOTE_READ_ONLY_ARCH 双提交事务；只持久化 M1 Preview/Start 生命周期与 entry contract，不执行语义分类、不改生产代码 | COMPLETE；delegated REVIEW→CODE state-only claim 成功；final exact 7-file allowlist PASS；deterministic 7/7 byte identity PASS；END-ACCEPT-02 backfill、Handoff accepted/consumed、State/Phase 3B gates PASS；M1 semantic decomposition 未提前执行。 | **REMOTE ARCH 独立复核 ACCEPT（2026-08-12）**：核验两提交链、delegated claim、exact 7-file scope、deterministic payload identity、M1 entry lifecycle、State 抽象角色、M1 semantic decomposition 未提前执行与 Phase 3B gate 均通过。该批同时验证 `REMOTE_READ_ONLY_ARCH / DELEGATED_SINGLE_TASK_CLAIM` 事务可执行；仍为单批证据，不单独推出模型路由结论。 |

| UBF-M1-SEMANTIC-DECOMPOSITION-WORK-01 Canonical Contract Decomposition | CODE | GPT-5.6 Luna | `94890cc746e50d8631de7b9daa9fdc82bd3732dd` | 治理语义分解机械落库；EVALUATION/INDEPENDENT；Authoritative STATIC_TARGET_BUNDLE；HOST_PRESERVED / ISOLATED_DETACHED_CLEAN；REMOTE_READ_ONLY_ARCH delegated claim；8 文件正常 allowlist；外部 canonical 三文件 hash continuity gate；无 user-level/project canonical mutation、无生产代码 | COMPLETE；CODE 忠实持久化 frozen architecture payload；两提交链、exact 8-file scope、8/8 target blobs、64 条 semantic records、maps/matrices、TURN 与 M1/Phase 3B gates 均通过。远程交付未获 ACCEPT 的唯一原因是两份 architecture-authored target 文档各含一个 EOF 空白行，`git diff --check` 失败。 | **REMOTE ARCH REWORK（2026-08-12）— ARCH_PAYLOAD_DEFECT / SELF_APPLICATION_MISSING ONLY**：缺陷由 R4 静态目标包写死，不是 GPT-5.6 Luna 执行偏差；Luna 本批 payload execution fidelity 为 PASS，不计入 coder 能力负样本。仅授权 R4-REWORK-01 删除两处 EOF 空白行并闭合事务证据；单批证据不推出模型路由结论。 |
| UBF-M1-SEMANTIC-DECOMPOSITION-WORK-01 R4-REWORK-01 Architecture Payload Whitespace Repair | CODE | GPT-5.6 Luna | `aa45a286c8077c05e203e8da4a71c945dd574472` | 极窄架构 payload 修复；EVALUATION/INDEPENDENT；Authoritative STATIC_TARGET_BUNDLE；REMOTE_READ_ONLY_ARCH delegated claim；normal final exact 4-file allowlist；语义 Reopen Set 仅两份 Work-01 文档各删除一个 EOF 空白行；State/台账记录事务；64 records 与其余成果 Preserve；无生产代码 | COMPLETE；CODE 忠实落盘包内 target；两提交链、exact scope、4/4 blobs、两处 one-LF deletion、clean diff-check、64-record Preserve 与 M2/Phase 3B gates 均 PASS。未获 ACCEPT 的唯一原因是 architecture-authored State target 仍含具体模型名，违反 State 抽象角色合同。 | **REMOTE ARCH REWORK（2026-08-12）— ARCH_PAYLOAD_DEFECT / SELF_APPLICATION_SEMANTIC_GATE_MISSING ONLY**：具体模型名由架构静态 target 写死，不是 GPT-5.6 Luna 执行偏差；Luna payload execution fidelity PASS，不计入能力负样本。仅授权 R4-REWORK-02 做 State abstract-role truth repair。 |
| UBF-M1-SEMANTIC-DECOMPOSITION-WORK-01 R4-REWORK-02 State Abstract-Role Truth Repair | CODE | GPT-5.6 Luna | `1723a4f9c050d4da47740d04164fa27d73ea9f2b` | 极窄治理 truth repair；EVALUATION/INDEPENDENT；Authoritative STATIC_TARGET_BUNDLE；REMOTE_READ_ONLY_ARCH delegated claim；normal final exact 2-file allowlist；仅将 State 旧 R4 行的具体模型名替换为抽象 CODE，并更新本台账事务事实；其余全部 Preserve | COMPLETE；`aa45a286... -> e176a722... -> 1723a4f9...`；claim exact 1 file、final exact 2 files、target blobs、State concrete-model denyset、diff-check、EOF/64-record/Work-01 Preserve 与 lifecycle gates 全部通过。 | **REMOTE ARCH 独立复核 ACCEPT（2026-08-13）**：State 抽象角色 truth 已闭合；架构 payload defect 归因保持，Luna payload execution fidelity PASS，不计入能力负样本；M1 Work-01 具备 End/Accept 条件，单批证据不推出模型路由结论。 |
| UBF-M1-END-ACCEPT-01 M1 End/Accept + M1→M2 Handoff Persistence | CODE | GPT-5.6 Luna | `2054899ad93d9c2bc1353914c31a1ef3b96c15ac` | 治理阶段收尾/交接机械落库；EVALUATION/INDEPENDENT；Authoritative STATIC_TARGET_BUNDLE；REMOTE_READ_ONLY_ARCH delegated claim；normal final exact 7-file allowlist；持久化已由 ARCH 作出的 M1 ACCEPT 与 M1→M2 handoff；不执行 M2、不改 64 records/canonical/Graph/生产代码 | COMPLETE；`1723a4f9... -> 5650c5c5... -> 2054899a...`；claim exact 1 file、final exact 7 files、7/7 blobs、State denyset、4 Preserve blobs、diff-check 与 M1/Handoff lifecycle truth 全部通过。 | **REMOTE ARCH 独立复核 ACCEPT（2026-08-13）**：M1 ACCEPT/CLOSED 与 M1→M2 Handoff persistence 闭合；M2/Phase 3B 未启动。Luna payload execution fidelity PASS；单批证据不推出模型路由结论。 |
| UBF-M2-PREVIEW-START-01 Legacy Asset Mapping Entry | CODE | GPT-5.6 Luna | 待 ARCH 依据最终远程 commit hash 回填 | M2 生命周期入口机械落库；EVALUATION/INDEPENDENT；Authoritative STATIC_TARGET_BUNDLE；REMOTE_READ_ONLY_ARCH delegated claim；normal final exact 7-file allowlist；只持久化 M2 Preview/Start 与 mapping entry contract；不创建 GC mapping、不决定 Universal Level、不改 canonical/Graph/生产代码 | COMPLETE / PENDING REMOTE ARCH REVIEW；M1→M2 Handoff accepted/consumed；M2 entry persisted；mapping NOT EXECUTED/NOT YET AUTHORIZED；Phase 3B 未启动；不作模型能力结论。 | 待 REMOTE ARCH 独立复核；重点核验 `2054899a... -> claim -> final`、exact 7-file scope、target blobs、State denyset、handoff consumption、mapping non-start、Preserve 与 Phase 3B gate。 |

## 边界结论（证据不足阶段，暂不下结论）

当前只有 1 个已完成批次（DeepSeek V4 Pro），不满足"≥3 批次"的最低置信度门槛，**不得**据此写"Pro 适合 XX、不适合 XX"这类结论。以下只记录已知的外部参考信息，供后续对照验证，不作为路由依据：

- 第三方基准测试显示 DeepSeek V4 Flash 与 Pro 在编码/agent 类基准上差距很小（多数指标差 1-2 分内，部分 agent 基准 Flash 反超 Pro-Preview），综合智能指数差距集中在"长程 agent 任务"（这类任务恰是本项目 BLUEPRINT-FULL 批次的画像，需要重点验证）。
- 独立 agent 编码实测显示 Flash 的工具调用纪律（编辑前读文件、按需装依赖、跑测试、不陷入重试循环）表现稳定，但存在一些边缘 case 正确性问题（如接口路由挂错）。
- 上述均为外部第三方评测，**不能替代本项目内的实测数据**——本表的价值正在于用真实批次积累项目专属的证据，而不是套用通用基准分数做决策。

## 何时可以下结论

满足以下条件后，可在本节新增"已验证边界"小节：
1. 同一模型至少 3 个批次的记录，且批次复杂度有差异（覆盖"长程多文件"和"短平快定向修复"两类）。
2. 至少 2 个不同模型都有 ≥3 批次记录，可做横向对比。
3. 结论须落到具体可判定的边界条件（如"命中 GC-08/22 一类的收尾清单型工作时，X 模型遗漏率显著更高"），不能是"X 模型整体更好/更差"这种笼统判断。
