# 业务功能

## 健康档案 = 家庭成员"我"（档案整合，2026-07-16）

- [2026-07-16] **个人健康档案与家庭成员"病种/健康状态"本是同一套 care 分类**(CrowdType:三高/痛风等)：`HealthProfileRepository`(老,单用户)和 `FamilyRepository` 成员 `careCategoryIds` 都引用同一 `food_category`(care 维度)；忌口/调养统一走 `FamilyRepository.allEnabledCareIds()`(member_care + legacy health_profiles 并集)。所以两者是**UI 双入口重复**，整合为**纯 UI 去重、低风险**(数据层不动)。
- **整合落地**：取消独立"个人健康档案"入口/弹窗；"我的"→"档案"组入口=家庭档案(唯一入口，从功能设置提到顶层)；用户卡健康标签源从 `HealthProfileRepository.observeEnabled()` 换成 `family.observeMembers()` 里 `isSelf` 成员的 careCategoryIds 映射名(`MineViewModel.healthCard` combine members+`listAllCrowdTypes`)；"病种"文案→"健康状态"(选项不变、已挂忌口调养)。**HealthProfileRepository 数据层保留**(legacy 并集向后兼容，老用户 legacy 已由 `ensureInitialized` 迁到成员"我")。达标/摄入按**关注成员(is_focus)**算，关注≠"我"时卡片提示"当前关注:XX"。

## AI 推荐下一餐（2026-07-09）

- **安全架构**：规则先筛(纯代码 `HealthRuleEngine`：可做性=非调料齐 + 犯忌 avoid 剔除 + 限量降权 + 去重)→模型只在安全候选里搭 2~3 菜组合→JSON schema 校验(dishId 必来自候选)→任何环节失败**纯规则兜底**。忌口由代码强校验，模型无权绕过；无 key/无网也能出规则推荐。
- **可插拔运行时**：`AiRuntime` 接口 + `SwitchableAiRuntime` 按 `AiRuntimeConfig` 路由 Mock/云端/端侧；云端 `CloudModels` 注册表多厂商(OpenAI 兼容)，加厂商零业务改动。key 按 vendor 存、AI设置UI 下拉选模型+弹框设/编辑。
- **取材模式**：`RecommendMode` PANTRY(在手食材)/RANDOM(整库,随机轮转)，同一引擎复用。
- **落地**：候选卡"选它"→ `addMealWithDishes` 路由带 dishIds → 加餐页 `configure(editDate, presetDishIds)` 在 `loadMealsForDateInternal` 末尾并入第一块(用户确认再存，符合"写操作需确认")。
- 方案文档 `feature/AI推荐下一餐落地方案.md` / `端侧AI能力接入方案.md`。

## 添加/编辑餐食

[2026-05-24]
- 添加餐食页不应按“每次只添加一餐”设计；用户目标是一次创建/编辑一天的餐食。
- UI 状态使用 `MealBlockUiState` 列表表达一天内多个餐食模块，每个模块包含：餐次、用餐时间、菜品列表、备注。
- 保存某天餐食采用“整日覆盖”策略：先删除该日期旧的 `meal_record_dish` 和 `meal_record`，再写入当前模块列表，避免同一天重复叠加。
- 进入添加餐食页时，如果指定日期已有餐食，需要直接回填为可编辑模块；没有数据时默认创建“早餐”模块。
- 固定餐次自动带出默认时间；`加餐` 这类非固定餐次不带默认时间，必须由用户手动选择。
- 首页计划卡片和食历卡片的编辑入口都跳转同一个添加/编辑餐食页，保持编辑体验一致。

## 图片与多图字段

[2026-05-24]
- 菜品/食材多图复用 `image_path` 和 `thumbnail_path` 字段，用 `|` 分隔多个图片路径，最多 3 张。[AI修改]
- `image_path` 保存原图文件路径，`thumbnail_path` 保存对应缩略图文件路径；两者按相同索引一一对应。[AI修改]
- 图片默认落到 `/sdcard/cookbook/img/`，文件名格式为 `yyyyMMddHHmmssS.jpg`；缩略图为 `yyyyMMddHHmmssS_thum.jpg`。[AI修改]
- App 启动时必须先获取公共存储权限；授权后图片统一写入 `/sdcard/cookbook/img/`，不再回退到 app 专属外部目录。[AI修改]
- 所有列表和封面默认展示 `thumbnail_path` 第一张；点击图片预览时先展示缩略图，再异步加载 `image_path` 原图，形成从模糊到清晰的效果。[AI修改]
- 缩略图压缩目标约 5KB，最大不超过 10KB；原图按较大尺寸压缩保存，避免列表直接解码大图。[AI修改]

## 菜品喜爱度

[2026-05-24]
- `dish.preference` 使用 `INTEGER` / Kotlin `Int` 累加，避免 `REAL/Double` 小数精度问题。
- 喜爱度总分上限 1000；每次菜品添加到餐食记录中，累加 1。
- 星级展示为 5 颗星，每颗 200 分，即 `preference / 200.0`。
- 用户可见文案统一为“喜爱/喜爱度”，不再显示旧“热度”或 `/1000` 总数。

## 食历分页与定位

[2026-05-24]
- 食历使用连续自然日窗口，不只展示有餐食记录的日期；无记录日期也返回空卡，方便定位当天。
- 默认窗口为最近一周：`today-6` 到 `today`，进入页面后定位到当天。
- 日期按正序展示：上方历史，下方计划；手指上滑看未来计划，下拉看更早历史。
- 顶部加载历史、底部加载计划，每次扩展 7 个自然日。
- 顶部前插历史日期后必须通过 `prependCount` 保持滚动锚点，避免列表跳动。

## 添加餐食默认日期

[2026-05-24]
- 新建餐食入口不再固定今天：先查已有餐食最大日期。
- 无记录或最大日期早于今天：默认今天。
- 最大日期为今天或未来：默认最大日期 + 1。
- 编辑入口指定日期优先，不能被新建默认日期逻辑覆盖。

## 食材权限规则

[2026-05-24]
- 食材按 `source` 区分权限：
- `source='preset'`：预设食材，不可编辑、不可删除，UI 不做明显文字标记。
- `source='user'`：用户自建食材，可编辑、可删除，UI 显示可长按的图标提示。
- UI 层和 Repository/SQL 层都需要限制该规则，不能只靠界面隐藏按钮。
- 删除用户自建食材前必须二次确认；删除时先清理 `dish_ingredient` 关联，再删除食材本身，失败要提示用户。

## 菜品食材分类

[2026-05-24]
- 新增/编辑菜品和菜品详情暂不展示“主料”标识。
- 新添加到菜品的食材默认 `isMain=false`；`dish_ingredient.is_main` 字段暂保留，后续做原料/调味料分类时再统一迁移。

## 食材层可复用实现模式（2026-07-08）

- **加新维度（如 season 应季）零 schema**：只需 ①food_categories.json 加该 dimension 节点 ②ingredients.json 给食材打该 dimension 的 code ③把 dimension 加进营养 Tab 的 `nutritionDimensions` 集合（VM）。无表结构变更、无迁移。[AI生成]
- **食材页全局搜索下拉+跳分类**：搜索复用全库 `searchIngredients`(name/alias/pinyin LIKE)；`jumpToIngredient` = 取食材最具体 general 分类→切常规 Tab→`buildGeneralTreeExpandedTo`(沿祖先链展开树)→选中+`animateScrollToItem`+`IngredientCard.highlighted` 高亮。切 Tab/分类清高亮。[AI生成]
- **详情按健康档案高亮**：把 `HealthProfileRepository` 注入 picker VM，`observeEnabled` 收集，`enabledCareCategoryIds = profiles.map{ it.crowdTypeId }`(现 crowdTypeId 存 care 分类 id)；详情忌口区 `careRule.categoryId in enabledCareCategoryIds` 置顶+高亮。[AI生成]
- 详情四区顺序：🍳做法(常见做法+相关菜品) > 🩺忌口(调养,健康档案置顶) > 🥗属性(品类/营养/应季按 dimension 拆分行) > 📋处理与保存+来源。[AI生成]

## 食材营养权威核准 + 营养表（2026-07-15）

- [2026-07-15] **营养数据权威来源(定档)**：主源《中国食物成分表》官方在线平台 `nlc.chinanutri.cn`(中国疾控中心营养与健康所，每食物页含 kcal/蛋白/脂肪/碳水/**钠钾钙**，能量以 kJ 计 → kcal=kJ÷4.184)；进口/缺项用 **USDA FoodData Central**；**GI** 用悉尼大学 GI 库(仅含碳水食物)；**嘌呤** 用《成人高尿酸血症与痛风食养指南(2024)》+常用食物嘌呤表(仅肉/禽/水产/内脏/豆/菌)。全部记档在 `.ai-context/docs/数据来源.md`。
- [2026-07-15] **agent 联网批量核准数据模式**：全量名单(`temp/claude/all_ingredient_names.txt`)按每片~30切片→每片一个后台 general-purpose agent(先 `ToolSearch select:WebSearch,WebFetch` 加载联网工具)逐个查证→输出 `temp/claude/nutri_auth_N.json`(每条含 ref 实际来源 + review=verified/pending)。禁编造出处/页码，查不到的字段省略、口径不确定标 pending 并在 ref 注明。440 食材 15 片，verified 278/pending 162。
- [2026-07-15] **覆盖升级式合并(merge_nutri.py)**：auth 权威值**覆盖**旧"整理"估算、**保留 auth 未覆盖字段**(不 null 老值)、ref+review 取 auth；key 映射 energy_kcal→kcal 等短键、piece_gram→pieceGram(seed 支持计件克重)。合并后必跑 `validateNutritionSeedForTest`(名字全解析)+`:shared:testDebugUnitTest`。
- [2026-07-15] **食材营养表**(我的·实用工具→独立页 `NUTRITION_TABLE`)：`NutritionRepository.allIngredientNutrition()`(`selectAllIngredientNutrition` 左连 ingredient+ingredient_nutrition，纯新查询免迁移)→`IngredientNutritionRow`。顶栏 ⓘ 弹「数据来源与免责」(SOURCE_TEXT 展示来源要点)，回应"让用户知道数据来源"。
