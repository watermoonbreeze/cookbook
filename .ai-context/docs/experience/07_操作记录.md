# 操作记录

## [2026-07-08~09] AI 推荐下一餐（S0-S3 + 云端接入 + 多厂商设置 + 库存/随机）

- **S0-S3**：`shared/ai/` 规则引擎 `HealthRuleEngine`(方案A 可做性=非调料齐,调料按「调味品/油脂类」分类) + 取数 `RecommendationDataSource` + `AiRuntime`抽象/`MockAiRuntime` + `RecommendationOrchestrator`(规则筛→模型组合→JSON校验→纯规则兜底) + Prompt/Parser；androidApp UI 三入口(首页卡/加餐页✨/我的AI助手) + 推荐页(3候选卡/换一换/选它落地加餐页预填) + 单测 17+。
- **云/端切换框架**：`AiRuntimeType(MOCK/CLOUD/ON_DEVICE)` + `AiRuntimeConfig` + `SwitchableAiRuntime`(按配置路由) + `CloudAiRuntime`(HttpURLConnection,零依赖) + `OnDeviceAiRuntime`占位。
- **多云端厂商**：`CloudModels` 注册表(智谱GLM-4-Flash默认免费/GLM-4-Air/DeepSeek/通义/Kimi，OpenAI兼容协议 `GlmProtocol`)；key 按 vendor 存(同厂共用)+迁移旧单一key；AI设置UI 三档+下拉选模型+弹框设/编辑key。
- **库存/随机模式**：`RecommendMode(PANTRY/RANDOM)`，随机=`selectAllIngredientIds`整库、随机轮转；FilterChip 切换。
- 预设菜隐藏编辑(DishMini 补 source)；食材搜索结果两模式(入库出库/选择已选)；换一换轮转修复。
- 文件：`shared/ai/*`、`androidApp/.../ui/ai/*`、`androidApp/.../ai/{CloudAiRuntime,OnDeviceAiRuntime}`。方案 `feature/AI推荐下一餐落地方案.md`；详见 `context_memory/2026-07-09_*`。

## [2026-07-08] 无人值守：P1 IO Dispatcher + 删除一致性 + 搜索跳高亮

- P1：`expect val ioDispatcher`(Android=IO)，91 处 `Dispatchers.Default`→`ioDispatcher`。
- 删除一致性：`selectDishesOfMealRecords` 去 `d.status=1`，菜品软删后食历保留当时吃的菜。
- 首页搜索点食材→`IngredientJumpBus`跨屏→食材页复用 `jumpToIngredient` 定位高亮(失效降级旧行为)。

## [2026-07-08] 烹饪计时可靠性改造（息屏/后台/多计时/前台服务/全屏提醒）

- **墙钟计时**（97fac70）：`CookingTimerItem.endAtElapsed`(elapsedRealtime)，tick 按 `remainingFrom(endAt-now)` 算剩余，修"息屏倒计时停走"。
- **系统级响铃**：`kitchen/TimerAlarm`(scheduler+`TimerAlarmReceiver`)，start 注册 `setExactAndAllowWhileIdle(ELAPSED_REALTIME_WAKEUP)`；多计时器唯一 requestCode/通知id + `Map<Int,Ringtone>` 分别停；前台完成 in-app 响铃并 cancel 闹钟去重。
- **前台服务 + 全屏提醒**：`CookingTimerService`(FGS specialUse) 常驻"进行中"通知+保活，`sync(context,endWalls)` 启停；`TimerAlertActivity`(showWhenLocked+turnScreenOn) fullScreenIntent 拉起；运行态持久化到 pref `cooking_timer_running`。
- **实测修**（2ae9025）：FGS 通知默认最多 10s 延迟显示→`FOREGROUND_SERVICE_IMMEDIATE`；重启 intent 为 null 自毁→`START_REDELIVER_INTENT`+lastEndWalls；到点 WakeLock 亮屏。
- 待办：A14 `USE_FULL_SCREEN_INTENT` 默认关需引导用户开；通知点击应跳计时页(而非首页)。文件见 `context_memory/2026-07-08_cooking_timer_reliability.md`。

## [2026-07-07~08] 全局搜索 + 应季/详情四区/内容/首页搜索(F2-F5)

- **食材页全局搜索**：搜索框改跨全库下拉(`searchIngredients` 全库)，`SearchResultsPanel` 弹出；点结果 `jumpToIngredient`→切常规 Tab、`buildGeneralTreeExpandedTo` 沿祖先链展开分类树、选中并网格滚动高亮(`IngredientCard.highlighted`)；网格与搜索解耦(reloadCurrentList 不再按 keyword 过滤)。
- **F2 应季**：food_categories 加 `season` 维度(应季→春/夏/秋/冬)，32 时蔬打标签，VM `nutritionDimensions += "season"` 使营养 Tab 出现「应季」。
- **F3 详情四区重排**：🍳做法>🩺忌口(健康档案病种置顶高亮)>🥗属性(品类/营养/应季分维度)>📋处理保存；picker VM 注入 `HealthProfileRepository`，`enabledCareCategoryIds = profiles.map{crowdTypeId}`。
- **F4 内容**：+18 食材+18 详情+4 菜(食材 243、详情 100%、菜 20)。
- **F5 首页搜索**：复核已完整(含预设菜/243食材)，补食材结果卡可点→`navigateRootTab(INGREDIENTS)`。
- 提交：全局搜索 edf518d(已push)；F2 721ba89 / F3 a8ce2d6 / F4 8eeb2d1 / F5 bad7fca([unattended])。

## [2026-07-06~07] 食材层落地：阶段0/1 + 阶段3 内容 + 健康档案统一 + 预设菜

### 阶段0 DB 地基
- 查实 SQLDelight 版本由 `.sqm` 数推导（build.gradle `version=8` 是泄漏的 Project.version、无效）；改 `10.sqm` 为无操作 + 恢复驱动 `ensureLegacyColumns` 幂等补 reason 列（修真机「初始化数据失败」，见 06）。

### 阶段1 UI/数据
- `11.sqm` 新增 pantry 库存表 + PantryRepository；`IngredientMainTab` 加 PANTRY「库存」Tab，接入加入/移出（详情按钮「入库/出库」）。
- `food_categories.json` 常规树重构为 13 大类（对齐《食材品类分类体系.md》），153 食材引用脚本 remap。
- 拆 1636 行 `IngredientPickerScreen` → 主 617 行 + 5 个同包文件（脚本按函数名切分、全部 internal、清理 import）。

### 阶段3 内容
- 食材 153→**225**、详情 44→**225(100%)**（3 批，脚本+引用校验+单测）。
- 「我的→关于」新增「数据来源」如实声明 AI 整理 + 免责。
- `dishes.json` 16 道预设经典菜 + `seedDishes`（补齐式幂等，主料 is_main/配料/步骤/烹饪方式），详情"相关菜品"区显示。
- 营养标注复核修 4 处（带鱼→高嘌呤、四季豆→低嘌呤、柿子/榴莲→中GI）；163 条调养规则复核通过。

### 健康档案统一（schema）
- `user_health_profile` 由引用 `crowd_type`(4项) 改引用 `food_category` 的 `care_*` 病种(17项)；`12.sqm` v12→v13 重建表 + 软删旧 crowd_${id}；seeder 停止生成 crowd_${id}；我的页标签横向滚动。Schema.version=13。

## [2026-07-06] 食材层整体方案梳理 + feature 文档「1总纲+4明细」重构（规划，无代码）

### 任务范围
- 依据 `chatlog.md`「食材」段 + 现有代码/DB 实测，梳理食材层实现方案，并把 feature 下食材文档重构为「1 总纲 + 4 明细」。[AI生成]

### 用户三项决策
- 营养/调养架构 = **轻方案**：复用 `food_category.dimension` + `ingredient_category`/`ingredient_care_rule`，**否决** attribute 三表（贴合「营养/调养只是常规分类的聚合，数据源唯一」）。[AI生成]
- 库存 = **独立 `pantry` 表**（quantity/added_at/expire_at/status），非 ingredient 加列。[AI生成]
- 文档整理 = 只归拢食材主题，其余大文档保留。[AI生成]

### 产出（`.ai-context/docs/feature/`）
- 新建总纲 `食材层总方案.md`；新建明细④ `食材数据模型与迁移.md`。[AI生成]
- 旧 `食材属性系统设计.md`（三表）→ 改写为 `食材维度体系设计.md`（轻方案，三表降级为未来预留附录），旧文件删除。[AI生成]
- `食材界面设计.md`/`食材品类分类体系.md` 对齐：`in_pantry`→pantry 表、`attribute_def`→dimension 措辞。[AI生成]
- 结论快照 `context_memory/2026-07-06_ingredient_layer_plan_result.md`。[AI生成]

### 关键盘点结论
- DB 侧几乎不用大改：`status/reason/last_referenced_at/alias/is_main/food_category.dimension/care_rule` 均已存在，本方案只新增 pantry 表 + 补维度节点。[AI生成]

## [2026-06-06] 首页主题弹框与 MVP 剩余项推进

### 任务范围
- 首页主题按钮改为直接弹出主题切换弹框，不跳转“我的”。[AI生成]
- 审计 MVP 剩余项，输出 `.ai-context/docs/feature/MVP剩余项审计与推进.md`。[AI生成]
- 无人值守完成 P0/P1：食历复用、新建菜品带回、健康档案 UI、备份/恢复 UI、收藏组合入口、慢病食材规则。[AI生成]

### 关键实现
- 抽取 `ThemeModeDialog`，首页和“我的”页共用；首页 `HomeViewModel` 接入主题偏好读写。[AI生成]
- 食历卡片增加复用按钮，可复用到今天/明天；`MealRecordRepository.copyDayMeals()` 复用整日替换语义。[AI生成]
- 新建菜品保存后通过导航 `SavedStateHandle` 回传新菜品 id，添加餐食页自动加入当前餐食模块。[AI生成]
- “我的”页补健康档案多选弹框，保存时启用/禁用 `user_health_profile`。[AI生成]
- “我的”页补备份管理弹框，支持创建、恢复和删除 `/sdcard/cookbook/backups` 下的备份。[AI生成]
- 新增 `FavoriteComboRepository` 和 `FavoriteCombo`，添加餐食页可保存当前餐食模块为组合，也可选择组合加入餐次。[AI生成]
- 新增 `crowd_rules.json`，Seeder 写入 `crowd_ingredient`，食材选择器的人群查询可显示 recommend/limit/avoid 角标。[AI生成]

### 验证
- `./gradlew :shared:testDebugUnitTest`：通过。[AI生成]
- `./gradlew :shared:verifyCommonMainCookbookDatabaseMigration :shared:verifySqlDelightMigration`：通过。[AI生成]
- `./gradlew :androidApp:compileDebugKotlin`：通过。[AI生成]
- `./gradlew :androidApp:assembleDebug`：通过。[AI生成]

### 保留项
- P2 的 iOS framework 编译能力、删除策略完整回归、图片持久化回归保留为专项验证项；其中 iOS target 当前按 MVP 策略注释，不能伪装为已验证完成。[AI生成]

## [2026-06-05] 基础数据 JSON 化与食材默认 emoji

### 任务范围
- 将基础食材和食材分类从 Kotlin 硬编码迁移为 JSON 维护。[AI生成]
- 在 JSON 中为每个预设食材维护默认 emoji，用户未拍照时优先展示该图标。[AI生成]
- 保留 `FoodEmoji.kt` 作为用户自建食材或 JSON 漏配时的兜底映射。[AI生成]

### 关键实现
- 新增 `shared/src/commonMain/resources/seed/food_categories.json`，分类使用稳定 `code` 管理父子关系。[AI生成]
- 新增 `shared/src/commonMain/resources/seed/ingredients.json`，食材使用分类 `code` 绑定多维分类，并为每条食材配置 `emoji`。[AI生成]
- `PresetDataSeeder` 改为读取 JSON：同名食材不重复插入，但会补齐 emoji 和分类；软删除同名预设食材会恢复有效。[AI生成]
- SQLDelight 升级到 v5，`ingredient` 新增 `emoji TEXT NOT NULL DEFAULT '🥗'`，迁移文件为 `4.sqm`。[AI生成]
- `Ingredient` 领域模型、`IngredientRepository`、菜品详情食材关系和 `IngredientCard` 均接入 `emoji` 字段。[AI生成]
- `FoodEmoji.kt` 通用兜底图标从餐盘调整为 `🥗`。[AI修改]
- `shared/build.gradle.kts` 显式打包 seed JSON 到 Android runtime 和 JVM unit test resources；`SeedResourceLoader` 增加测试文件路径兜底。[AI生成]

### 验证
- `./gradlew :shared:testDebugUnitTest`：通过；新增 JSON emoji、seed 幂等和分类补齐测试。[AI生成]
- `./gradlew :shared:verifyCommonMainCookbookDatabaseMigration :shared:verifySqlDelightMigration`：通过。[AI生成]
- `./gradlew :androidApp:assembleDebug`：通过。[AI生成]

## [2026-06-05] 基础食材与多维分类第一阶段

### 任务范围
- 记录基础食材与多维分类调研方案，并按阶段开始执行。[AI生成]
- 第一阶段落地基础分类树、常见食材 seed 和对应单元测试。[AI生成]

### 关键实现
- 新增 `.ai-context/docs/feature/基础食材与多维分类方案.md`，记录阶段计划、分类维度和数据库落地方式。[AI生成]
- `PresetDataSeeder` 从“表为空才插入演示数据”调整为“补齐式 seed”，旧库已有数据时也能补新增分类和食材。[AI生成]
- 扩展计量单位，补充碗、块、根、条、段、瓣、只等常用单位。[AI生成]
- 扩展 `food_category` 多维分类：日常分类、营养分类、GI、嘌呤、钠/脂肪、人群分类等。[AI生成]
- 第一批扩充 80+ 常见食材，覆盖主食、蔬菜、水果、肉类、水产、蛋奶豆、坚果、调味料、油脂，并绑定多个分类。[AI生成]
- 新增 `PresetDataSeederTest`，验证 seed 后基础食材数量、关键食材和多维分类存在。[AI生成]

### 验证
- `./gradlew :shared:testDebugUnitTest`：通过；shared 实际执行 7 个测试，0 failure，0 error。[AI生成]
- `./gradlew :shared:verifyCommonMainCookbookDatabaseMigration :shared:verifySqlDelightMigration :androidApp:assembleDebug`：通过。[AI生成]

## [2026-06-04] 修复13：菜品 Item 长按菜单与删除引用检查

### 任务范围
- 说明清空某天餐食的触发位置和内部逻辑。[AI生成]
- 菜品 Item 长按菜单去掉“取消”，新增“编辑”和“删除”。[AI生成]
- 删除菜品前检查是否被餐食引用，若被引用则提示具体日期和餐次。[AI生成]
- 说明 SAF 与 app 专属目录的含义和后续处理方向。[AI生成]

### 关键实现
- `DishesScreen` 长按菜单改为“编辑 / 基于此另存 / 删除”；点击弹框外部即可关闭。[AI生成]
- `MainScaffold` 为菜品页补充 `onEditDish`，长按编辑直接进入 `Routes.newDish(id)`。[AI生成]
- `Cookbook.sq` 新增 `selectMealReferencesByDish`，按菜品 id 查询引用它的餐食日期、餐次和用餐时间。[AI生成]
- `DishesViewModel` 新增 `DishActions` 日志、删除引用检查状态、删除确认和错误弹框。[AI生成]
- `DishRepository.deleteDish()` 删除前清理 `favorite_combo_dish` 关联，避免没有餐食引用但组合关联导致外键失败。[AI生成]

### 验证
- `./gradlew :shared:generateCommonMainCookbookDatabaseInterface :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin`：通过。[AI生成]
- `./gradlew :shared:verifyCommonMainCookbookDatabaseMigration :shared:verifySqlDelightMigration :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest`：通过；当前单元测试任务为 `NO-SOURCE`。[AI生成]

## [2026-06-04] 专项修复：菜品编辑页空白排查

### 任务范围
- 重点排查“每次编辑菜品都是空白”的问题，并按用户要求增加编辑 Toast 辅助诊断。[AI生成]

### 关键实现
- `NewDishScreen` 在编辑模式加载完成后弹出 Toast：成功时显示“正在编辑：菜品名 ID=xxx”，失败/空白时显示对应 id 和错误状态。[AI生成]
- `Cookbook.sq` 新增 `selectDishForEditById`，对详情/编辑主表字段使用 `COALESCE` 兜底旧库 NULL 文本字段。[AI生成]
- `selectIngredientsOfDish` 对食材名称、别名、拼音、图片、缩略图、单位名称进行 `COALESCE`，避免关联食材旧数据打断整个菜品加载。[AI生成]
- `DishRepository.observeDishById()` 与 `getDishById()` 切换到编辑安全查询，详情页和编辑页共享同一条更稳定的按 id 读取链路。[AI生成]

### 验证
- `./gradlew :shared:generateCommonMainCookbookDatabaseInterface :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin`：通过。[AI生成]
- `./gradlew :shared:verifyCommonMainCookbookDatabaseMigration :shared:verifySqlDelightMigration :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest`：通过；当前单元测试任务为 `NO-SOURCE`。[AI生成]

## [2026-06-04] 阶段性整体审核与无人值守优化

### 任务范围
- 按架构师、项目经理/产品体验、测试工程师视角审计当前 App 全流程。[AI生成]
- 自动落地低风险、高确定性优化，记录需要产品确认或专项实现的问题。[AI生成]

### 关键实现
- 图片选择保存失败增加错误提示；初始化失败页增加重试。[AI生成]
- 备份恢复路径切到 `/sdcard/cookbook/db` 和 `/sdcard/cookbook/backups`，不再指向内部数据库。[AI生成]
- Android 10/API 29 纳入读写存储权限申请流程。[AI生成]
- 保存整天餐食时，喜爱值只对当天新增菜品累加，避免重复编辑同一天导致喜爱值膨胀。[AI生成]
- 食材选择器重开时清空已选和旧错误态；添加餐食保存失败显示错误提示。[AI生成]
- 食历刷新任务增加取消旧任务逻辑，首页主题按钮进入“我的”页主题设置。[AI生成]
- `PreferenceRepository`、`HealthProfileRepository`、`MealRecordRepository.observeMealTypes()` 补充调度边界。[AI生成]

### 保留待办
- 添加餐食中“新建菜品后自动带回并选中”。[AI生成]
- 食历一键复用到今天/明天/选择日期。[AI生成]
- 编辑某天餐食时清空当天记录的删除语义。[AI生成]
- 菜品删除策略和 `/sdcard/cookbook` 上架合规策略。[AI生成]

### 验证
- `./gradlew :shared:generateCommonMainCookbookDatabaseInterface :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin`：通过。[AI生成]
- `./gradlew :shared:verifyCommonMainCookbookDatabaseMigration :shared:verifySqlDelightMigration :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest :androidApp:lintDebug`：通过；当前单元测试任务为 `NO-SOURCE`。[AI修改]

## [2026-06-04] 修复：编辑菜品首次进入异常

### 任务范围
- 复查“编辑菜品第一次不行，添加菜品后才行”的初始化时序问题。[AI生成]

### 关键实现
- `MainActivity.CookbookAppContent()` 增加初始化状态，先等待 `PresetDataSeeder.seedIfNeeded()` 完成，再读取主题偏好和加载 `MainScaffold`。[AI生成]
- 初始化完成前显示“初始化数据中...”，避免首页/详情/编辑页面在旧库 NULL 清洗和字典初始化前被操作。[AI生成]

### 验证
- `./gradlew :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin`：通过。[AI生成]
- `./gradlew :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest`：通过；当前测试任务为 `NO-SOURCE`。[AI生成]

## [2026-06-03] 修复12：权限门禁、食材 NPE、公共存储统一

### 任务范围
- 餐食卡片内菜品 block 点击进入菜品详情。[AI生成]
- 修复编辑菜品二次进入为空、新增菜品编辑为空、图片保存后丢失的相关链路风险。[AI生成]
- 修复添加食材时 `selectAllIngredients` SQLDelight 非空映射 NPE。[AI生成]
- App 启动时先获取 sdCard 权限，再创建数据库；旧 app 专属 `cookbook` 目录迁移到 `/sdcard/cookbook`，后续统一从公共目录读写。[AI生成]

### 关键实现
- `FoodTimelineScreen` 给 `DayMealCardView` 传入 `onDishClick`，复用既有 `DishMiniCard` 点击进入详情。[AI生成]
- `NewDishViewModel.start()` 每次按路由 id 重新加载编辑/导入数据，不再因同入口复用跳过加载。[AI生成]
- `Cookbook.sq` 食材查询改为显式列 + `COALESCE`，`IngredientRepository` 改用显式 mapper。[AI生成]
- `PresetDataSeeder` 开始时执行旧库 NULL 字段清洗，修复已升级数据库中遗留的 NULL 文本字段。[AI生成]
- `CookbookStorage` 统一 `/sdcard/cookbook` 权限检查、目录创建和旧 app 专属目录迁移。[AI生成]
- `CookbookApplication` 移除启动时 seed；`MainActivity` 授权成功后才迁移目录、创建 `db/img`、读取主题偏好、加载 `MainScaffold`。[AI生成]
- `DatabaseDriverFactory` 和 `ImagePickerButton` 取消 app 专属目录 fallback，统一使用 `/sdcard/cookbook/db` 和 `/sdcard/cookbook/img`。[AI生成]

### 验证
- `./gradlew :shared:generateCommonMainCookbookDatabaseInterface`：通过。[AI生成]
- `./gradlew :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin`：通过。[AI生成]
- `./gradlew :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest`：通过；当前测试任务为 `NO-SOURCE`。[AI生成]

## [2026-06-03] 修复11：菜品刷新、编辑回填、数据库与图片外置

### 任务范围
- 添加菜品后菜品 Screen 立即刷新，Tab 下列表支持下拉刷新。[AI生成]
- 修复编辑菜品标题正确但表单内容为空的问题。[AI生成]
- Android 数据库迁移到 `cookbook/db`，图片迁移到 `cookbook/img`，菜品/食材新增缩略图字段。[AI生成]

### 关键实现
- `DishesViewModel` 在空关键词时观察 `observeAllDishes()`，搜索态保留一次性查询；`DishesScreen` 在 `ON_RESUME` 刷新并增加下拉刷新提示。[AI生成]
- `NewDishViewModel` 新增 `loading/errorMessage/thumbnailPath`，编辑和导入加载失败时不再静默展示空表单。[AI生成]
- SQLDelight schema 升级到 v3：`dish.thumbnail_path`、`ingredient.thumbnail_path`，新增 `2.sqm` 迁移。[AI生成]
- Android 数据库优先使用 `/sdcard/cookbook/db/cookbook.db`，受系统限制时回退 app 专属外部目录；首次外置时复制旧内部数据库和 wal/shm。[AI生成]
- `ImagePickerButton` 保存原图和缩略图：原图写入 `image_path`，缩略图写入 `thumbnail_path`，缩略图目标约 5KB、不超过 10KB。[AI生成]

### 验证
- `./gradlew :shared:generateCommonMainCookbookDatabaseInterface`：通过。[AI生成]
- `./gradlew :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin`：通过。[AI生成]
- `./gradlew :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest`：通过；当前测试任务为 `NO-SOURCE`。[AI生成]

## [2026-06-03] 修复 SQLite version 2 降级到 1

### 任务范围
- 修复 App 打开时报错：`SQLiteException: Can't downgrade database from version 2 to 1`。

### 关键实现
- 定位到 `shared/src/commonMain/sqldelight/com/sxdbsm/cookbook/db/1.sqm` 已存在 v1 -> v2 迁移。
- `shared/build.gradle.kts` 未显式设置 SQLDelight 目标版本，当前包仍按 version 1 打开数据库。
- 在 `sqldelight.databases.create("CookbookDatabase")` 中添加 `version = 2`。

### 验证
- `./gradlew :shared:generateCommonMainCookbookDatabaseInterface :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin`：通过。
- `./gradlew :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest`：通过。
- 当前测试任务为 `NO-SOURCE`。

## [2026-05-30] 同步公司 Claude 配置到本机 Claude/Codex

### 任务范围
- 读取公司配置目录 `/Users/sxd/Documents/国安广传/work-record/国安广传/临时资料/.claude/`。
- 筛选可通用配置，分别同步到 `~/.claude/` 和 `~/.codex/`。
- 重新读取关键入口，确保后续任务按新规则执行。

### 关键处理
- 同步公司版自动任务编排：微任务快速通道、方案风险评审、返工分层、无人值守方案确认规则。
- Claude 侧更新 `~/.claude/CLAUDE.md`、`~/.claude/workflow_auto_orchestration.md`、命令、DEV 角色、memory、`maven_upgrade` 技能。
- Codex 侧转换更新 `~/.codex/AGENTS.md`、`~/.codex/memories/workflow_auto_orchestration.md`、`myinit`、`zongjie`、DEV 角色、memories、`maven_upgrade` 技能。
- 跳过 `adb_transfer` 技能：依赖 Windows 绝对路径，macOS 本机不可直接使用。
- 未同步 `auth`、history、sessions、cache、sqlite、telemetry、projects、file-history、paste-cache 等运行态和敏感目录。

### 备份与验证
- 备份目录：`~/.claude/backups/config_sync_2026-05-30/`、`~/.codex/backups/config_sync_2026-05-30/`。
- 已复查 Codex/Claude 关键入口文件和技能目录；`adb_transfer` 未安装到本机 Claude/Codex skills。
- 最终一致性复查：DEV 角色文件一致；业务技能一致为 `maven_upgrade`；Codex 额外 `.system` skills 为系统内置；命令文件保留 Claude/Codex 运行时语义差异。

## [2026-05-30] 修复10：首页空计划、菜品编辑状态、V2.1 主题

### 任务范围
- 首页计划为空时仍显示“计划”标签，内容显示“暂无计划”。
- 修复菜品详情点击编辑仍进入新建状态的问题。
- 新建/编辑菜品的标签和烹饪方式 chip 改为 `wrap_content`，添加按钮紧跟在后面。
- 备份现有主题色，并按 V2.1 全局视觉规范生成新的亮/暗色主题。

### 关键实现
- `HomeScreen` 将计划 `SectionHeader` 从 `plans.isNotEmpty()` 条件外提，空列表时展示 `EmptyState("暂无计划")`。
- `NewDishViewModel` 新增 `start(editingDishId, importDishId)`，统一处理新建/编辑/导入入口，先清理旧状态再加载目标菜品。
- `NewDishScreen` 合并编辑和导入的入口 `LaunchedEffect`，避免多个副作用互相覆盖。
- 标签和烹饪方式从铺满行的旧 `TagChip` 改为定高 `AssistChip`，不设置宽度，保持内容自适应。
- 主题备份到 `.ai-context/docs/theme_backup/2026-05-30_v2_1_before/`，运行主题替换为 V2.1 暖杏色系，并同步调整深色主题和标题字重。

### 验证
- `./gradlew :shared:generateCommonMainCookbookDatabaseInterface :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest`：通过。
- 当前 `:shared:testDebugUnitTest` 和 `:androidApp:testDebugUnitTest` 为 `NO-SOURCE`，无测试源。

## [2026-05-30] 修复9：搜索栏、多烹饪方式、菜品索引、食材编辑

### 任务范围
- 修复 SearchScreen 等搜索栏字体显示不全。
- 新建/编辑菜品的烹饪方式支持多次添加，标签/烹饪方式 chip 高度与添加按钮一致。
- 修复菜品详情点击编辑进入新建的问题。
- 菜品页取消“拼音”Tab，“全部”按首字母分组并提供右侧字母索引滑动定位。
- 食材选择页预设和自建食材都可编辑，预设不能删除；食材卡有图时图片铺满上半部分。

### 关键实现
- `AppSearchField` 改为 `BasicTextField + Row 色块容器`，避免 48dp 高度下 Material TextField 默认 padding 挤压文字。
- `Routes.NEW_DISH` 改为 `newdish/{dishId}/{importDishId}` 路径参数，`MainScaffold` 继续用 `NavType.LongType` 读取。
- `Cookbook.sq` 新增 `dish_cooking_method_rel`，`DishRepository.saveDish()` 同步多烹饪方式关联。
- `Dish` 增加 `cookingMethods`，`DishMini` 增加 `cookingMethodNames`，详情页和菜品 Item 展示多个烹饪方式。
- `DishesScreen` 的 “全部” 列表按 `dishInitial(name)` 分组，新增 `PinyinInitial.kt` 和 `LetterIndexBar`。
- `IngredientCard` 调整图片/文字布局，`IngredientPickerViewModel.updateIngredient()` 放开预设食材编辑，删除仍仅限自建。

### 验证
- `./gradlew :shared:generateCommonMainCookbookDatabaseInterface :shared:compileDebugKotlinAndroid :androidApp:compileDebugKotlin`：通过。
- `./gradlew :androidApp:assembleDebug :shared:testDebugUnitTest :androidApp:testDebugUnitTest`：通过。
- 当前测试任务仍为 `NO-SOURCE`。

## [2026-05-26] 修复6：食历定位、全局搜索、底栏加号和返回键

### 任务范围
- 食历只在首次打开时自动定位，用户操作后切回不再强制回到当天。
- 菜品列表喜爱星星下方显示喜爱值，0 不显示。
- 首页搜索图标进入独立搜索页，分组搜索菜品、食材、餐食。
- 添加餐食按钮融入底部导航栏正中。
- 物理返回键按路由、首页、双击退出统一处理。

### 关键实现
- 移除 `FoodTimelineScreen` 页面层的兜底自动定位，只响应 `TimelineViewModel` 发出的一次性滚动请求。
- `DishRow` 普通模式下星级下方显示 `preference` 数值。
- 新增 `SearchScreen` / `SearchViewModel`，复用 `DishRepository.searchDishes()`、`IngredientRepository.search()`、`MealRecordRepository.searchMealCards()`。
- `Cookbook.sq` 新增 `searchMealDates`，支持按日期文本或关联菜品名搜索餐食。
- `MainScaffold` 新增 `BackHandler`，并把加号按钮放入 `NavigationBarItem`。

### 验证
- `./gradlew :androidApp:compileDebugKotlin`：通过。
- `./gradlew :androidApp:assembleDebug`：通过。
- `./gradlew :shared:testDebugUnitTest`：通过，当前无测试源。

## [2026-05-25] 修复5：食历真实餐食日期分页

### 任务范围
- 修复食历总数不对：只展示 `meal_record` 中真实有餐食记录的日期。
- 默认定位规则：今天 -> 未来最近 -> 最大日期。
- 历史/计划分页每次加载 7 个有记录日期。
- 顶部日期范围移动到标题栏右侧，点击打开带小圆点标记的月历。

### 关键实现
- `Cookbook.sq` 新增 `selectTimelineDatesAsc`、`selectMealRecordsByDates`。
- `MealRecordRepository` 新增 `observeTimelineDates()`、`loadTimelineCardsByDates()`。
- `TimelineViewModel` 从连续自然日窗口改为真实日期索引分页。
- `FoodTimelineScreen` 顶部栏右侧显示全量最早/最晚餐食日期，并新增自定义月历弹窗。

### 验证
- `./gradlew :androidApp:compileDebugKotlin`：通过。
- `./gradlew :androidApp:assembleDebug`：通过。
- `./gradlew :shared:testDebugUnitTest`：通过，当前无测试源。

## [2026-05-25] 阶段性架构审核与性能优化

### 任务范围
- 首页/食历切换、添加菜品后返回、添加餐食保存返回等可感知卡顿。
- 不做 DB schema 迁移，不引入新图片库，不大改导航或页面结构。

### 关键实现
- `DishRepository` 使用批量标签查询和烹饪方式字典组装 `DishMini`，减少菜品列表/首页 N+1 查询。
- `MealRecordRepository` 批量读取多条餐食记录下的菜品、标签、烹饪方式，降低食历窗口和首页计划的组装成本。
- `saveDish()`、`save()`、`saveDayMeals()` 使用 `db.transaction`，避免保存时产生半写入和多次中间刷新。
- `DishesViewModel`、`DishPickerViewModel`、`IngredientPickerViewModel` 搜索加入防抖和旧任务取消；`DishPicker.refresh(force=true)` 保留立即刷新。
- `StoredImage` 增加有容量上限的 LRU 缩略图缓存，点击预览单独读取更清晰图片。

### 验证
- `./gradlew :androidApp:compileDebugKotlin`：通过。
- `./gradlew :androidApp:assembleDebug`：通过。
- `./gradlew :shared:testDebugUnitTest`：通过，当前无测试源。
- `./gradlew :shared:allTests`：工程未注册该任务，已在 `AGENTS.md` 中修正命令。

## [2026-05-24] 修复4无人值守完成

### 任务范围
- 菜品详情喜爱度展示。
- 菜品列表 item 样式。
- 食历正序、默认最近一周、当天定位、上下 7 天分页。
- 添加餐食默认日期规则。
- 新增/编辑菜品移除“主料”标识。
- 用户自建食材删除安全补强。

### 关键实现
- `DishRow` 改成左图、中间菜名+标签、右侧星级/Checkbox。
- `TimelineViewModel` 改为连续日期窗口，默认 `today-6..today`，提供 `loadPrevious()` / `loadNext()`。
- `FoodTimelineScreen` 使用 `LazyListState` 定位当天，并在前插历史后保持滚动锚点。
- `AddMealViewModel.configure(editDate)` 解决编辑入口和新建默认日期竞态。
- `MealRecordRepository.observeTimelineWindow()` 返回窗口内每个自然日的卡片。
- `IngredientRepository.deleteUserIngredient()` 事务内先删 `dish_ingredient` 再删用户食材。

### 验证
- `./gradlew :androidApp:compileDebugKotlin :androidApp:assembleDebug`
- 结果：`BUILD SUCCESSFUL`
- DEV_TEST 复核通过，DEV_REVIEW 终审通过。

## [2026-05-24] Codex 自动任务编排强制化

### 问题确认
- 用户指出修复4执行过程中没有看到智能体按自动任务编排流程参与。
- 复盘确认：当时由主线程直接分析和修改，没有真实分派 DEV_SA/DEV_ARCH/DEV_UI/DEV_CODE 等角色。

### Codex 侧规则修正
## [2026-06-06] 食材分类方案 A 落地 + 最近使用虚拟分类

### 功能调整
- 食材选择页左侧新增固定“最近使用”虚拟分类，位于“全部”下方，不落库、不可编辑、不可删除。[AI生成]
- “最近使用”查询来源为 `meal_record -> meal_record_dish -> dish_ingredient -> ingredient`，按 `meal_record.created_at` 最近引用时间倒序。[AI生成]
- 食材选择页新增“管理分类”入口；只允许维护用户自建普通分类，预设/营养/慢病分类保持只读。[AI生成]
- 新增分类、编辑分类、删除分类均经 `FoodCategoryRepository` 校验，UI 不直接判断数据库写入权限。[AI生成]
- 添加食材弹框分类下拉改为使用完整分类列表，不再依赖左侧树是否展开。[AI生成]

### 数据与删除规则
- 用户分类删除继续遵守软删除：`food_category.status=0`，并将相关 `ingredient_category.status=0`。[AI生成]
- 删除分类只解除食材分类关系，不删除 `ingredient` 记录本身。[AI生成]
- 有子分类时阻止删除父分类，避免形成不可见子节点。[AI生成]

### 验证经验
- SQLDelight 改 `.sq` 后先执行 `./gradlew :shared:generateSqlDelightInterface`。[AI生成]
- `:shared:testDebugUnitTest` 与 `:androidApp:assembleDebug` 不应并行执行；二者可能同时操作 `shared/build/tmp/kotlin-classes`，导致 `Could not delete .../shared/build/tmp/kotlin-classes/debug/com` 这类非代码错误。[AI生成]
- 本次最终顺序执行后：`./gradlew :shared:testDebugUnitTest` 成功，`./gradlew :androidApp:assembleDebug` 成功。[AI生成]

- 更新 `~/.codex/AGENTS.md`：
  - 开发类任务强制读取并执行 `~/.codex/memories/workflow_auto_orchestration.md`。
  - 任务启动前必须输出任务类型、执行深度、交互模式和 DEV 角色分派。
  - 标准/深度任务必须按需真实分派子代理。
- 更新 `~/.codex/memories/workflow_auto_orchestration.md`：
  - 新增 Codex 子代理落地规则。
  - DEV_SA/DEV_PM/DEV_ARCH/DEV_CAA/DEV_TEST/DEV_SEC/DEV_REVIEW 映射为 `explorer`。
  - DEV_CODE/DEV_UI/DEV_DB/DEV_UT 映射为 `worker`。
  - 阶段0必须输出智能体分派表。
- 更新项目 `AGENTS.md`：
  - Cookbook 项目开发任务必须按 Codex 自动任务编排流程执行。

### 上下文
- 新增 `.ai-context/docs/context_memory/2026-05-24_codex_auto_orchestration_enforcement.md` 保存流程修正结论。
- 未同步 Claude Code 侧；用户本次要求是 Codex 对应体系。

## [2026-05-24] MVP 第二夜闭环 + 工程规范 + Codex 命令机制

### 工程统一规范
- 新增 `.ai-context/docs/experience/09_工程统一规范.md`
- 更新 `AGENTS.md`，要求开发前按需读取工程规范
- 规范覆盖 KMP 分层、Android Material3、iOS SwiftUI、数据库、代码风格、验证流程

### 菜品选择与录餐闭环
- 新增 `DishPickerScreen.kt` / `DishPickerViewModel.kt`
- `AddDayFoodScreen` 接入 DishPicker 多选
- `AddMealViewModel` 新增 `addDishes()` 合并去重
- `NewDishScreen` 接入 DishPicker 单选导入
- `NewDishViewModel` 新增 `importFromDishId()`

### 菜品详情与另存
- 新增 `dishdetail/DishDetailScreen.kt` / `DishDetailViewModel.kt`
- `MainScaffold` 注册 `Routes.DISH_DETAIL`
- `DishesScreen` 长按“基于此另存”接入 `Routes.copyDish(dishId)`
- `Destinations` 增加 `copyDish()` 和 `importDishId` 参数

### 食材选择与新建食材
- `NewDishScreen` 的“添加食材”接入 `IngredientPickerScreen`
- `IngredientPickerScreen` 增加 `excludeIngredientIds`
- `IngredientPickerViewModel` 增加跨搜索/分类保留选中项
- `IngredientPickerScreen` 底部“添加食材”弹窗已可创建用户食材并自动选中

### 应用名
- 新增 `androidApp/src/main/res/values/strings.xml`
- `AndroidManifest.xml` 设置 `android:label="@string/app_name"`
- Android 显示名称改为 `CookBook`

### Codex 全局配置与命令
- 将 `~/.claude/CLAUDE.md` 的“AI 代码注释规范（全局通用）”补入 `~/.codex/AGENTS.md`
- 创建并安装个人插件 `personal-workflow@personal`
- 插件包含 `zongjie`、`fansi`、`myinit`、`create-android-project` 命令文件

### 验证
- 多次执行 `./gradlew :androidApp:assembleDebug`
- 最近一次应用名改动后构建结果：`BUILD SUCCESSFUL`

## [2026-05-14] 项目初始化 + 数据库设计

### 执行 /myinit
- 翻译 AGENTS.md 首行英文为中文
- 创建 `.codex/` 完整目录结构（commands/hooks/agents/docs/skills）
- 创建 `pre-tool-use.js` 危险命令拦截、`settings.json`、`code-reviewer.md`
- 追加上下文保护机制到 AGENTS.md

### 迁移 temp/codex/ 文件到 .ai-context/docs/feature/
- `MVP实施方案.md` → `.ai-context/docs/feature/MVP实施方案.md`
- `数据库设计方案.md` → `.ai-context/docs/feature/数据库设计方案.md`
- `界面探讨.md` → `.ai-context/docs/feature/界面探讨.md`
- 清空 temp/codex/ 目录

### 数据库设计方案三轮讨论
1. **第一轮**：基于用户的数据库想法，从 MVP 简单 5 表扩展为完整食材-菜品-食谱体系（17+ 张表），输出 `.ai-context/docs/feature/数据库设计方案.md`
2. **第二轮确认**：
   - dish.name 允许重复，新增 `dish_tag` + `dish_tag_rel` 标签体系区分同名菜品
   - 餐序从 3 个扩展为 6 个固定 + 用户加餐，新增 `meal_type` 表
   - 微量元素放一期，初步列出 6 大类约 30 种营养素
3. **第三轮确认**：
   - `meal_type.default_sort`(INTEGER) 改为 `default_time`(TEXT HH:mm)
   - `meal_record.sort_order` 改为 `meal_time`(TEXT HH:mm)
   - 时间天然有序，无需额外排序转换逻辑
