# 用户习惯

## 沟通风格

[2026-05-14]
- 用户倾向于先提出大致想法，然后让 AI 细化方案
- 喜欢分轮次确认，逐步完善设计（如数据库方案经历三轮讨论）
- 关注设计的实用性，倾向于简洁自然的方案（如用时间排序替代数字排序）

## 设计偏好

[2026-05-14]
- 偏好"表结构先建全，UI 按阶段暴露"的渐进式策略
- 重视数据来源区分（preset vs user），方便后续管理
- 倾向于把逻辑放在业务层而非数据库约束层（如菜名+标签唯一性）

[2026-07-06]
- 遇到"轻方案 vs 重架构"时**偏轻、偏贴合直觉**：食材维度选复用 `food_category.dimension`（分类聚合心智模型），否决 attribute 三表的强扩展方案。给方案时应把轻/重成本讲清并让其选。[AI生成]
- **先定方案后落代码**：复杂功能先要文档/实现方案，确认后再动手；方案阶段就明确定级。[AI生成]
- 关注**文档不膨胀**：feature 目录文档过多时要求按「1 总纲 + N 明细」重构，历史/重复的归档到 `_archive/`。[AI生成]
- 有明确的三层架构设想（界面层 / 数据层 / AI 层），要求"先搭框架与规范，内容后续用人工/爬虫/AI 增量补，加内容不动架构"。[AI生成]
- [2026-05-30] UI 控件希望有稳定“沟通名称”，例如菜品Block、菜品Item、餐食卡片、通用搜索栏、食材卡，后续修改时优先用这些名称定位。
- [2026-05-30] 用户倾向少边框、多色块和投影；固定高度输入控件要确保文字完整显示。

[2026-07-07]
- **数据来源诚实性（重要）**：这是面向慢病人群的健康 App。补的食材/营养/详情数据是 AI 依据通用常识整理、非权威核对。用户主动要求「关于」页列数据来源——**必须如实标注（AI 整理待核 + 免责声明 + 拟采用权威源），禁止编造权威数据库假装数据出自那里**。健康类数据宁可注明局限，不可虚构可信度。[AI生成]
- **大改动分批 + 每批验证**：内容补充/重构量大时，用户认可"分批校验(引用完整性)+单测+提交"的节奏；先做一批高质样板让其确认深度，再批量。[AI生成]
- **涉及 schema/数据模型的决策先对齐**：即便是"直接做"的任务，若发现要动 schema 或有架构分叉（如健康档案病种两套并存），先讲清根因+选项让用户拍板，再实现。[AI生成]
- 无人值守触发词："我吃饭了""下班了"等——按无人值守规则（升级深度、[unattended] 分阶段提交不 push、决策入 context_memory、≤15 文件、高风险入待确认队列）。[AI生成]

## 文档管理

[2026-05-14]
- 要求设计文档统一存放在 `.ai-context/docs/` 下管理
- 临时文件放 temp/codex/，稳定后迁移到 .ai-context/docs/feature/

## 流程要求

[2026-05-24]
- 用户要求“严谨流程模式”：先确认依据，再按小步实现，最后构建验证并记录上下文。
- 用户重视代码风格和技术架构统一，要求不同开发者接手也能保持一致。
- 新增或修改功能时应优先查项目规范、feature 文档、context memory，再动代码。
- 用户明确要求自动任务编排不能只由主线程“口头模拟”；标准/深度任务必须真实分派对应 DEV 角色智能体。
- 任务开始前必须先说明任务类型、执行深度、交互模式和智能体分派表；常规模式下方案阶段完成后要等用户确认再进入实现。
- 如果当前 Codex 会话没有可用多智能体工具，必须主动说明“流程降级”，不能假装已经分派智能体。

## Codex / Claude 双模式偏好

[2026-05-24]
- 用户希望 Codex 尽量保留 Claude Code 的工作习惯，例如 `/zongjie`、`/fansi`、`/myinit`。
- 需要明确区分：Claude 侧配置不作为 Codex 运行时依赖；Codex 侧应维护自己的 `AGENTS.md`、`.codex/`、plugin/commands。
- 对于 Codex 不能裸注册的 slash command，可通过普通文本触发或个人 plugin 命名空间命令替代。
