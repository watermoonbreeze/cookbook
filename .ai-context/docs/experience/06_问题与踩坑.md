# 问题与踩坑

## seed跳过/stateIn冻结/rotate随机/打分量纲（2026-07-15 深夜）

### seed"已存在即跳过"→关联永久残缺（凉皮0千卡根因）
- `seedDishes` 对已存在同名预设菜"`return@dish`跳过"→若菜在其食材入库前被seed过(不同代/私菜补全时序),配料没关联上;之后补了食材/营养,重seed(甚至force)仍跳过→**关联永久残缺→热量算0/看着没食材**。
- 修法=**补齐式**(只加不删零风险):已存在预设菜不直接跳过,查已关联食材id(`selectDishIngredientIdsByDishId`)、把seed里能解析但没关联的配料补挂上。**菜品热量算0/没配料先查dish_ingredient是否真关联上**,别只看seed数据(seed数据对≠运行时DB关联对)。
- seed**处理逻辑**变更(非JSON内容)要让老库跑一次:`SEED_LOGIC_VERSION`盐混入内容指纹并+1(不改JSON则指纹不变、老库跳过seed拿不到修复);用户侧"更新基础数据"(force)也可即时修。

### stateIn(WhileSubscribed) 无直接订阅者 → .value 冻结在初始值
- `val x = repo.observeX().stateIn(scope, WhileSubscribed(5000), 初始值)`,若UI只收另一个flow(如combine出的stats)、**没人直接collect这个x**,则x的upstream不激活、`x.value`永远返回初始值(冻结)。
- 坑:用`x.value`做**toggle方向判定**(`memberId !in x.value`)→x冻结在空集→每次都判"未缺席"→反复设同一方向、回不来("膳食统计没吃点了回不来")。
- 修:toggle类"读当前态再取反"的逻辑,**读实时DB**(repo加suspend查询/`toggleAbsent`原子翻转)或确保该flow真被订阅,别信`stateIn.value`。

### rotate随机分批会翻到"罚分末批"(整批忌口/低分)
- 候选按分数降序、忌口/低分罚到末尾;`rotate`按批切,RANDOM模式`rotation=Random.nextInt`→`rotation%batches`随机翻到**任意批含全忌口末批**("随机推荐出忌口菜")。PANTRY顺序`rotation++`不会先撞末批。
- 修:轮转批次只按"可接受(非忌口)"候选数算(`indexOfFirst{avoidNames非空}`为界),忌口仍保留末尾只作边界批尾部零星出现。

### 推荐打分:硬约束(忌口)用巨值混进加权score是冗余
- 忌口排末已由`sortedWith(compareBy({avoid非空},{isRecent},{-score}))`分层保证,`avoid=50`巨值在**排序上冗余**、只把score打到-50量级淹没其余因子(±5)、污染可解释性。降到象征值(5)让score回可比区间。同理单因子(onHandMain)无上限会线性碾压营养/偏好,`min(count,3)`封顶。(膳食算法工程师评审)

## 无人值守 UX 深挖 / 字典重命名连带bug / 健康免责（2026-07-15）

### 无人值守用"研究agent产方案 + 主线程落地安全项"模式
- 大范围体验/审计任务：派多个专家角色 agent(如 Apple-UX 工程师)按流程各自深挖一块(读真实代码、产 `优先级/风险/体量` + 现状/痛点/建议/落地/**安全项vs需拍板**的方案文件)，主线程读方案后**只落地安全项**(纯UI/VM/零迁移/无真机风险)、**高风险/涉产品语义/信息架构/健康语义项入待确认队列**，每项构建验证 + 独立 commit。方案文件写 `temp/claude/ux_*.md`。
- **安全项判定**：纯文案/排序/提示/默认值/空态/纯新查询 = 安全；改数据写入/迁移/删除可恢复性/信息架构重排/评级或推荐算法/健康提示强度 = 需拍板或真机。拿不准归需拍板。

### 字典项英文化/重命名的连带 bug（gramUnit 找"克"恒 null）
- 单位英文化(克→g,23.sqm rename)后，**按旧中文名硬编码查字典的地方会静默失效**：`gramUnit()` `firstOrNull{it.name=="克"}` 恒 null → 改克数时 `unitId` 丢失、克当量算不出、`unitName` 落到兜底"克"(与实际"g"不符)。
- **教训**：重命名字典项(单位/分类等)后，必须 grep **全仓按旧名字符串匹配的代码**(`=="克"`/`=="毫升"` 等)，改成兼容新旧(`it.name=="g"||it.name=="克"`)或按 id/code 查。改 seed/迁移只是一半，代码里的硬编码名是另一半。

### 健康提示守免责红线：只如实归纳、不推荐、不医嘱
- 加"缺什么/健康解读"类文字，措辞只**陈述已覆盖/缺哪类食物组**(如 `FoodGroup.nutritionGaps` 返回"优质蛋白/主食/蔬菜"里缺的)，**不推荐具体菜、不给营养承诺、不写成硬指标**；无数据(空)不显示；集中展示健康功能处补分组级免责"公开公式估算·仅供参考·非医嘱"。改评级/推荐口径等涉健康语义的**一律入待确认队列**，不无人值守擅改。

### combine 多源含 suspend：可内联到 combine 的 suspend transform
- `combine(f1,f2,f3,f4){...}` 的 transform **本身可 suspend**(可直接 `nutritionRepo.totalOf()`)，简单场景可内联、`return@combine null` 产出可空流，替代 `combine(...).mapLatest{}`(list+cast 的写法易错)。需"新值取消旧算"时才保留 mapLatest；本地库快查内联即可。四参 combine 有 typed 重载，别用 `listOf(Any)` + cast(类型对不上还漏 null 性,如 `observeFocusBody()` 返回**非空** BodyMetrics)。

## 偏好JSON存储 / 表单录入写回竞态 / 数字过滤（2026-07-14 晚）

- [2026-07-14] **小型结构化配置(身体数据等)存偏好 JSON 免迁移**：不必为身高/体重/年龄/性别/活动新建表/加列——序列化成一个 JSON 存 `user_preferences`(一个 key，`@Serializable data class` + `Json.encode/decodeFromString`)，`PreferenceRepository.observe/setBodyMetrics`。加/改字段零迁移(`ignoreUnknownKeys`)。适合单行、低频、非查询维度的配置。
- [2026-07-14] **表单多字段"改一个写回全部"的竞态**：字段 onChange 用 `flowValue.copy(该字段=新值)` 写回时，若 flowValue 来自异步偏好 flow(写→DB→回灌有延迟)，快速连改两个字段时后一个基于**尚未含前一个改动的旧值**copy→覆盖丢数据。修：所有字段写回**以本地 UI 态为单一真相源**(`build()` 带上所有本地字段值 + 仅覆盖显式变更项)，不读迟滞的 flow 值。
- [2026-07-14] **数字输入框过滤别只 `filter{isDigit()||'.'}`**：允许 `1.7.5`/`..`/整数字段的 `30.`→`toDoubleOrNull/toIntOrNull` 恒 null→依赖它的显示(如目标热量)静默消失、用户不知为何。小数字段限**最多一个小数点**(第二个点则保持原值)，整数字段(年龄)**禁小数点**(只 `isDigit()`)。
- [2026-07-14] **StateFlow 里结合多源+逐项异步计算用 `combine(...)+mapLatest{}`**：色系墙/今日营养要结合"餐食窗口 flow + 身体数据 flow"再对每天做 `NutritionRepository` 异步汇总——`combine` 合两源、`mapLatest`(可 suspend、新值到达取消旧计算)里批量 `dishNutrition(allIds)` 一次查再按 dishId map，避免逐天查。别用 `.map{}`(不能 suspend)。

## Compose 沉浸式 / LazyList / 状态重建的坑（2026-07-14）

- [2026-07-14] **沉浸式 edge-to-edge 下全屏页被系统导航栏遮挡**：app 开 `setDecorFitsSystemWindows(false)`+导航栏透明+各页 `contentWindowInsets(0,0,0,0)`，全屏页(无底部 Tab 栏)内容伸到系统导航栏下、底部被遮。统一在 `MainScaffold` 的 `NavHost` 对无底栏路由加 `Modifier.navigationBarsPadding()`(一处修全部)；Tab 页由 Material3 `NavigationBar` 默认 insets 自避让。**坑：嵌套 Scaffold 的 bottomBar 若也自加 `navigationBarsPadding()` 且没 `contentWindowInsets(0,0,0,0)`→底部按钮双重/三重下边距**(AiRecommendScreen 曾如此)。全项目统一"全屏页不自管系统栏、交给 MainScaffold"。
- [2026-07-14] **`LazyList.scrollToItem(idx)` 后同帧读 `layoutInfo` 居中会拿到旧布局**：`scrollToItem` 把 item 排到视口起始，但同一帧 `visibleItemsInfo` 未重排，`firstOrNull{index==idx}` 多半返回 null→居中分支静默跳过，只剩靠左。用 `snapshotFlow{listState.layoutInfo}.firstOrNull{ it.visibleItemsInfo.any{i->i.index==idx} }` 等目标列真正布局出来再 `scrollBy(itemCenter-viewportCenter)`。
- [2026-07-14] **VM 用"重建整个 UiState"替换 state 会丢未列出的字段**：`AiRecommendViewModel.recommend()` 用 `mapResult()` 返回全新 `AiRecommendUiState` 替换 → 每次推荐后 `recommendStyle/selectedSlot/recentWindowDays` 被重置回默认(用户"点偏新鲜又跳回综合")。修：结果 `.copy(那些粘性字段=旧值)`。**教训：mapResult 型的整体重建，务必 copy 回所有 UI 粘性选择；或改成在旧 state 上 copy 局部字段。**
- [2026-07-14] **字典/库软删 + 已选未同步 → 保存"复活"**：标签/烹饪方式库软删只作用 `source='user'`(预设不可删)。但删掉某项时若它**已被当前菜选中**(`state.tags`/`cookingMethodNames`)，保存走 `INSERT OR IGNORE` 又把它以 user 入库→复活。修：删库项时同步 `removeTag/removeCookingMethod`。
- [2026-07-14] **实验 Material3 组件在拆出的新 Composable 要各自 `@OptIn`**：把用了 `FilterChip` 的控件区抽成 `RecommendControls`/`LibraryPickerDialog` 后编译报 "material API experimental"——`@OptIn(ExperimentalMaterial3Api::class)` 不随外层函数继承，新函数要自己标。

## 餐食/食材大批量交互改造的坑（2026-07-13 无人值守）

- [2026-07-13] **`DishMini.mainIngredientNames` 长期未被填充→依赖它的功能静默失效**：`DishRepository.buildDishMinis` 与 `MealRecordRepository.buildDishesByMealRecord` 都没 set 该字段，恒为空。结果餐次分类图标(`FoodGroup.groupsOf`)、主食置顶判定、菜品列表主料副文本全部无效但不报错。修：新增 SQL `selectMainIngredientNamesByDishIds`(is_main=1)，两处构建都填。**教训：用某个派生 model 字段前，先 grep 它是否真被赋值——DishMini 有一堆默认空字段，别假设填了。**
- [2026-07-13] **Material3 1.1.2 无 `SelectableDates` API**：`rememberDatePickerState(selectableDates=...)` 编译不过(该 API 是 1.2.0+)。需要禁选某些日期(如已有餐食/早于下限)时，改在**确认回调**里校验+提示(setDate 拒绝并 warning)，而非 picker 层禁用。
- [2026-07-13] **详情弹层编辑区被 `!selectionMode` 硬挡**：`IngredientDetailSheet` 的编辑/删除区写成 `if (!selectionMode && ...)`，导致选择模式即使传了 onEdit 也不显示→"选择模式编辑不了自建食材"。改为 `if (onEdit != null || onDelete != null)`，权限由调用方(传不传 onEdit)决定。**教训：可复用组件的"能力显隐"应由回调是否传入决定，别在组件内用 mode 布尔硬编码。**
- [2026-07-13] **"复制"日期语义**：食历复制餐食的目标日期应是"**当前整个食历最晚餐食日期+1**"(接在所有餐食之后)，不是"被复制那天+1"。用 `mealRepo.dateRange().second + 1`。
- [2026-07-13] **餐食编辑状态机多标志易串**：`configured/userPickedDate/loadedFromDate/copyFromDate/copyConfigured/isEditingExisting` 协同。复制入口(configureCopy)必须用**独立守卫**(copyConfigured)而非公用 configured，否则会被 init 的默认 configure 抢跑 `if(configured)return` 吞掉；且复制/新增的 loadedFromDate=null，保证"改日期=移动删旧"只在真编辑既有日期时触发。**教训：多入口共享一个 ViewModel 时，每个入口用独立一次性守卫，别共用一个 configured。**

## 库存推荐"某菜没推出来"的真因排查（2026-07-12 无人值守）

- [2026-07-12] **真因是"忌口硬过滤"，不是匹配/排序**：用户反复反馈"库存有五花肉却不推红烧肉"，前一轮已改排序(ON_HAND_MAIN_BONUS/降 SHORTAGE 惩罚)仍没解决。加 `PantryRec` 诊断日志实机抓 logcat 才定位：菜确实进了候选(②预筛 29 道)，但 `HealthRuleEngine.evaluate` 把**含忌口(avoid)食材的菜直接 `return null` 剔除**(④评估只剩 5 道)。用户启用了健康档案(高血脂)，五花肉对高血脂是 avoid → 所有五花肉菜被过滤。**方法论：推荐"缺某菜"要分三层定位——匹配层(候选没有)/规则层(候选有但被 avoid 过滤)/排序层(有但排后)，看日志④评估候选一眼可辨；别只盯匹配和排序。**
- [2026-07-12] **健康忌口/限量只能作用于"非调料"食材**：盐对高血压是 avoid，若调料参与判定则几乎每道菜都忌口、忌口失去意义。改：`HealthRuleEngine` 的 avoid/limit/recommend 全从 `nonSeasoning` 计算；`gatherForPlan` 同步排除 `seasoningIds`。调料的忌口/限量**转做法提示**(`cookingCautions`：盐→少盐/白糖→少糖/生抽→少酱油/油→少油)，而非剔除菜。依赖：`selectSeasoningIngredientIds` 必须把盐/糖/酱油正确归到调料分类，否则老问题复现。
- [2026-07-12] **忌口菜"排后+标红告知"而非隐藏**(用户明确要求、家庭 app 家人也能做)：avoid 不再剔除，改 `AVOID_PENALTY=50` 大幅降权排最后 + `DishCandidate.avoidNames` → UI 标红"⛔忌口：X"。`AVOID_PENALTY` 远大于各正向加分，忌口菜必排最后。
- [2026-07-12] **食材 `name` 现有 UNIQUE 约束**(旧红线"无唯一约束"已过时)：写复现测试插第二个同名"五花肉"直接 `SQLITE_CONSTRAINT_UNIQUE` 崩——说明全新库五花肉只一个 id、两份红烧肉共用它；同名多 id 只可能出现在**老库经迁移升级**(未补加 UNIQUE)。教训：**写复现测试时 schema 约束会直接暴露真相**，比空想高效。
- [2026-07-12] **设备数据相关 bug 用日志打点让用户实机抓 logcat**：单测用全新 Schema.create 库复现不了老库脏数据，加 `CookbookDiag` 诊断日志(①库存②候选③不足④评估⑤展示)让用户跑一遍贴日志，一次定位，胜过盲改多轮。诊断日志用 `CookbookDiag.log{}`(lambda 延迟构造 + release 默认关，按 `FLAG_DEBUGGABLE` 开)，别裸 `CookbookLog.d` 逐条打(release 每次 gather 会跑上百次+构造字符串)。

## 推荐/菜系/自建菜打磨（2026-07-12）

- [2026-07-12] **自建菜不被库存推荐的根因=食材同名不同 id**：`createUserIngredient` 每次裸 INSERT 不去重，用户新建菜时又建同名食材(如"五花肉")→ 得到与预置食材不同的 id；库存推荐预筛按 `ingredient_id` 精确匹配 → 对不上、漏推该菜。修：①createUserIngredient 新建前按去空格名 `selectActiveIngredientIdByName` 复用已有 id(治本，复用时也补绑本次分类，INSERT OR REPLACE 幂等)②推荐 gather 按名扩展 pantryIds **和 shortageIds**(同名副本也算在手/不足，修存量脏数据)。教训：**凡按 id 匹配的地方，都要防"同名多 id"**；名扩展要成对做(在手 + 不足 一起扩，否则耗尽食材经副本被当充足推前无警示)。
- [2026-07-12] **data class 插入字段会打断"位置参数"调用方**：给 `PlanDish` 在中间插 `isMeat` 字段，测试里 `PlanDish(id, name, ..., avoid, breakfast)` 位置参数把 `breakfast` 错位映射到 `isMeat`，早餐逻辑静默失效、测试挂。改动 data class 字段顺序后，**全仓搜位置参数构造(尤其测试 helper)**，改用命名参数。
- [2026-07-12] **关键词判类别用裸单字会误伤同字异义**：判荤用裸"蟹"会把"蟹味菇"(食用菌)判成荤；"参/血/肚"同理。改用具体名(海参/鸭血/猪肚/田螺)。凡 `name.contains(单字)` 分类，先扫 seed 里有无"含该字的异义食材"。("鱼香茄子含鱼"经核实不误判，因只看主料名、味型不进主料)
- [2026-07-12] **推荐"换一换"分批**：旧 rotate 按平移 2 个 → 批次严重重叠。改按整批 N(=10)分页(rotation%批数, drop(start), 上层 take(N)): 下一批不重复、末批不足、全推完循环。≤1批时换一换回同批(符合"全推完再循环")。改分页语义后，旧"换一换必不同"的断言要按">N候选"重写。

## 推荐/搭配/分类打磨（2026-07-10）

- [2026-07-10] **Kotlin 字符串模板内插整个 data 对象**：`for (p in proteins)` 里 p 是 `PairIngredient`(data class)，hint 写 `"$p 配 $v"` → 输出 `PairIngredient(name=五花肉, role=PROTEIN) 配 ...`(用户可见)。items 用了 `p.name` 正确、唯独 hint 漏加 `.name`。单测只断言 items/method 没测 hint 故未暴露——**面向用户的文案字段务必单测断言其内容**(如 assert !contains("PairIngredient"))。
- [2026-07-10] **按分类名 contains 关键字判类别的两个坑**：①泛"豆"会把鲜豆类蔬菜(毛豆/四季豆/豆芽/荷兰豆)误判为豆(它们绑的是"蔬菜类+鲜豆类")→蔬菜判定要优先于豆，且豆只认"大豆/豆制品/坚果"②泛"菜"会命中"预制菜类"把料理包误判为蔬菜→用"蔬/菌/藻"而非"菜"。前提不变量：seed 每食材同绑**顶层大类 code + 叶子 code**，故顶层名(蔬菜类/菌藻类)含关键字能兜住；若未来只绑叶子会漏判(已加守护测试)。
- [2026-07-10] **`selectCategoriesByIngredient` 只返回直接关联、不含祖先链**：按类别匹配依赖 seed 是否把食材直连到含关键字的大类。这类"依赖数据形状"的逻辑要加**端到端守护测试**(seed→分类→非空)，防 seed 演进静默退化。
- [2026-07-10] **审查子代理会在项目目录写临时文件**：本次代理把分类扫描结果写到了 `seed/*.txt` 被 `git add -A` 误提交。教训：无人值守批量 `git add -A` 前 `git status` 扫一眼有无意外文件；发现即单独 commit 移除。

## 库存份数 + 推荐可做性（2026-07-10 打磨）

- [2026-07-10] **库存"加1份没反应"**：结算式 `addServings`(份数=当前剩余+delta,重置窗口) 在**当天有餐用到该食材**时，重置后当天餐立即把新加的扣回→剩余仍旧、视觉无变化。修：`份数=当前剩余+delta+今日已占用`(补回重置窗口会再次扣减的今日餐)，既保留结算式对"历史欠账"的正确性，又保证"加N份→剩余可见+N"。派生库存模型的加减，务必推演"今天餐/历史餐"两种占用对可见值的影响。
- [2026-07-10] **"某食材只对应1道菜就不推荐"是误判**：库存推荐不显示某菜的真因是 `HealthRuleEngine` 判可做性要求**该菜全部非调料食材都在库存**(`nonSeasoning.all{在手}`)，缺一味辅料整菜被 `mapNotNull` 丢弃(如有五花肉却不推红烧肉)。放宽为**主料齐即可**(缺辅料记 missingNames 降权+标"还需采购")，无主料菜按非调料齐兜底。排查"推荐里没有某菜"先看可做性门槛，而非找不存在的数量阈值。
- [2026-07-10] **改可做性语义会连带改单测断言**：HealthRuleEngine 单测直接编码了"非调料齐才可做"，放宽后 2 个用例(缺辅料剔除)必然失败——语义变更时同步改测试断言(缺辅料→仍可做+missingNames，缺主料→剔除)，别只改实现。

## 无人值守：采购清单 + shared 日志 + 5 轮审查（2026-07-10）

- [2026-07-10] **shared 用 android.util.Log(经 expect/actual CookbookLog) → :shared JVM 单测全红**：`android.util.Log` 是 stub，未 mock 时方法抛 `RuntimeException`。凡任何被测路径(seed/repository)会触达 CookbookLog，须在 `shared/build.gradle.kts` 加 `android { testOptions { unitTests.isReturnDefaultValues = true } }`。副作用：所有 android 桩返回默认值，未来若要测"依赖某 android 调用抛异常"的路径需改用显式 mock。
- [2026-07-10] **`LaunchedEffect(d.id)` 只用 id 做 key → 同 id 内容变化不重跑**：菜品编辑保存后 id 不变但 ingredients/steps 变了，洞察/相关菜品不重算(陈旧)。改用整个 data class 做 key `LaunchedEffect(d)`——data class 按结构 equals，内容不变不重启(不白跑)、内容变才重启；前提是 effect 内不回写触发该 Flow(否则自激循环)。
- [2026-07-10] **Compose body 里裸调冷流 `prefs.observeFlag(...).collectAsStateWithLifecycle()`**：每次重组新建 SQLDelight asFlow 冷流 → 反复取消/重订阅查库(抖动)。用 `remember(prefs){ prefs.observeFlag(KEY,default) }.collectAsState...` 缓存；或读进 VM 用 stateIn。
- [2026-07-10] **LazyColumn 手工维护 `animateScrollToItem` 偏移(字母索引跳转)**：新增/条件插入任一 item(如筛选 chip 行)都要同步偏移累加**并**把该 item 的存在性纳入 `remember` key，否则跳转全体偏位、且缓存不重算。数 item 时注意"一个 item{} 内含 LazyRow+Spacer 仍是 1 个外层 item"。
- [2026-07-10] **派生逻辑依赖内部 `DateTime.today()` → 无法用固定日期单测**：如采购/缺料按系统今天算，测试用固定过去日期会被排除、断言全错。把 today 提为方法参数(生产传 `DateTime.today()`)，既可测又与卡片同源。
- [2026-07-10] **构造字段计算了却漏传进 data class 构造**：`DishInsights(...)` 漏传 `related=` → 用了默认 `emptyList()`，"相关菜品"永不显示 + 白跑一次查询 + 未使用变量告警。Kotlin 默认值会掩盖此类遗漏、编译不报错——审查时重点核对"算了但没用/没传"的变量。
- [2026-07-10] **seed 写入列表与校验白名单硬编码两份 → 静默漂移**：`validateDishSeedForTest` 的方式/单位集合曾是 `seedCookingMethods/Units` 的逐字复制，改一处忘另一处则测试给假结论(该守的守不住)。提为共享常量 `PRESET_COOKING_METHODS/UNITS`，seed 与校验同源。
- [2026-07-10] **pantry 缺料多口径是设计非 bug**：详情页 `remaining()<=0`(窗口[入库日,今天]闭区间, 保守"现在能否做") vs 卡片/采购 `PantryAllocation.shortages`(rank 时间序, 乐观"排期餐是否轮到")。同天多餐/历史餐争用同料时可给相反结论，属预期，注释须写清避免后人误判。

## SQLDelight/JUnit/协程/跨设备合并（2026-07-10 选择性同步）

- **SQLDelight 单列 SELECT 返回列类型本身(List<String>/List<Long>)，不是行包装**：`SELECT d.name ...` 的 `executeAsList()` 是 `List<String>`，`.map{it.name}` 报"Unresolved reference: name / inferred Unit"。单列直接用，多列才生成数据类。[AI生成]
- **JUnit4 `@Test` 方法必须返回 void**：`fun x() = runBlocking { ... }` 若 block 末尾是**返回非 Unit 的断言**(如 `assertNotNull(v)` 返回 v)，则方法返回非 Unit → `InvalidTestClassError: should be void`。末尾补 `Unit` 或以 `assertEquals/assertTrue`(返回 Unit)收尾。[AI生成]
- **suspend 函数内的 local `fun` 不能调 suspend 函数**：需声明为 `suspend fun`(local)，否则"Suspension functions can be called only within coroutine body"。[AI生成]
- **跨设备合并按名匹配 + ID 重映射**：两设备自增 ID 不同，不能带 ID 直插。按自然键(name)匹配：命中复用其 id、缺则建，建"旧名→目标id"映射；依赖行(dish_ingredient/收藏/餐食)经映射重指。选某域**自动带依赖**(库存→食材, 收藏/餐食→菜品+食材)保证引用完整。整库=替换、选择性=合并。接收端**自动识别包类型**(zip 里有无 `data.json`/`cookbook.db`)避免改传输协议。[AI生成]
- **AlertDialog 里同向嵌套 verticalScroll 会互相冲突**(内层滚不动/底部被遮)：只在**一层**加滚动(通常外层弹框内容 Column)，内层去掉。[AI生成]

## 库存/占用类需求的建模方法论（2026-07-10）

- **"分配/占用/额度/余额"类需求优先做「派生态」而非「存储扣减」**：库存份数体系最终采用**不落库、实时按时间序算**。存储扣减会遇到：加回时不知该先补谁、编辑/删餐重复扣、触发时机难定。派生只需 `份数(用户改) + 餐食记录` 实时算，天然支持"加份数早的先恢复"。[AI生成]
- **派生占用要设「时间窗口」，否则历史会污染当前**：纯派生若把**所有历史餐**都算作占用当前库存，会出现"两年前用过还占今天份数"的荒谬。解法=**入库日窗口**：每食材记 `added_at`(持久化)，只算 `date >= added_at` 的餐。[AI生成]
- **余额型字段用「结算式」更新，避免累加+窗口重置的重复计入**：`serving_count` 累加(+delta)但窗口重置到now → 为0后重加会把已消耗的重复算进剩余。正确=加时先算当前剩余、再 `serving_count = 当前剩余 + delta` 并重置窗口(`activatePantry`)。[AI生成]
- **同类"状态标注"要覆盖所有展示入口，别只加一处**：缺料/采购最初只加在周期规划预览，餐食卡片(首页计划/食历)没有→用户以为没生效。应在**统一的数据组装点**(如 `MealRecordRepository` 组卡)算好，所有复用同一卡片组件的地方一并生效。[AI生成]
- **派生态天生"覆盖存量数据"**：因不落库、查询时实时算，新增此类功能后**现有历史数据自动被评估**，无需"创建新记录才生效"。回答用户"存量能否覆盖"时可据此确认。[AI生成]

## 库存份数 / SQLDelight方言 / 代码审核修复（2026-07-10）

- **SQLite 3.18 方言不支持 UPSERT**：`INSERT ... ON CONFLICT(col) DO UPDATE` 报 `',' expected, got 'ON'`(SQLDelight dialect `sqlite_3_18`，UPSERT 是 3.24+)。累加/幂等 upsert 改**两步**：`INSERT OR IGNORE ...`(靠 unique index 忽略重复) + `UPDATE ... SET x = x + :delta`，放**同一事务**(`db.transaction{}`)保证原子。[AI生成]
- **新迁移文件命名 = 当前最大 `N.sqm` + 1，别被"版本号"误导**：`N.sqm` 迁移 vN→vN+1。现有到 `12.sqm` → 当前 `Schema.version=13` → 新迁移文件是 **`13.sqm`**(不是 14)。看目录里最大序号 +1 命名，命名错会漏迁移/版本错乱。加列同时改 `.sq` 的 `CREATE TABLE` + 建 `N.sqm ALTER`(新列 ALTER ADD 安全)。[AI生成]
- **派生态 > 存储式扣减**（当需求含"撤销/按时间序恢复/重算"时）：库存"占用/不足"若真去改库存数字(存储扣减)，加份数时无法知道该先补哪个、且整日编辑会重复扣。改为**不落库、实时按序算**(份数只用户改；占用=遍历所有餐食菜品按 date→meal_time→created_at→sort_order 排队，rank>份数即不足)，加份数后自动重算天然"早的先恢复"。凡"分配/额度/占用"类需求先想能否派生。[AI生成]
- **DB 恢复必须原子(带回滚)**：`copyTo(currentDb, overwrite)` 覆盖主库后若后续步骤失败，原库已丢、新库不完整。覆盖前把现有库存到 staging 回滚区，覆盖失败即还原。恢复关的是**单例 driver**，close 后无热重建 → 恢复后需重启应用(UI 提示/或强制重启)。[AI生成]
- **Java 阻塞 socket 的 `accept()`/`read()` 协程 `cancel()` 无法中断**：靠 `close()` socket 抛异常解阻塞(cancel 只设标志)。持有 socket 引用供 reset/onCleared 关闭；读要设 `SO_TIMEOUT`(掉线未发 FIN 会永久阻塞)；区分"主动取消"与"真实异常"(用 volatile 标志)，否则真实错误被静默吞。[AI生成]
- **KMP：可测纯逻辑放 `shared/commonMain` 不放 androidApp**：androidApp 无测试源集→纯逻辑(协议编解码/阈值判定/分配算法)零覆盖。下沉 shared 后纳入 `:shared:testDebugUnitTest`，平台只留系统采集/UI。[AI生成]
- **Kotlin `if(x) a++; b++` 分号内联易被误改**：`?.let { if(c) a++; b++ }` 两句都在 lambda 内(当前对)，但后人若移出/删判空语义静默变。统计类逻辑用显式花括号。[AI生成]

## 存储合规 / 备份 / 双设备同传 / 相机（2026-07-09）

- **声明 `CAMERA` 权限后，`ACTION_IMAGE_CAPTURE`(即 `TakePicture()`) 也需运行时授予 CAMERA**：项目本用 `TakePicture` 拍照且**未**声明 CAMERA（委托系统相机，无需权限）。为加扫码引入 zxing → 其清单合并 CAMERA 到 app → 拍照从此需先授予 CAMERA，否则 `SecurityException`/失败。凡新增任何 in-app 相机(扫码/CameraX)导致声明 CAMERA，必须在**拍照前**也加 CAMERA 运行时申请。[AI生成]
- **KDoc/注释里出现 `/*` 会被 Kotlin 当嵌套块注释起始 → 编译 `Unclosed comment`(报在 EOF)**：如注释写 `img/*`、`a/*b`。Kotlin 块注释可嵌套，`/*` 必须配 `*/`。注释里描述路径通配别用 `/*`，改「img 目录全部」等表述。[AI生成]
- **图片/文件在 DB 存绝对路径 → 目录迁移或跨设备即失效**：Cookbook 图片原存 `imageFile.absolutePath`，P0 迁 app 专属目录 + 双设备同传时全裂。改为**只存文件名(相对)**，读取时按当前 img 目录解析(`resolveImageFile`：历史绝对路径存在则直用，否则按 basename 回落到当前目录=自愈)。凡 DB 存文件引用一律存相对名。[AI生成]
- **存储合规去 `MANAGE_EXTERNAL_STORAGE`**：数据放**app 专属目录** `getExternalFilesDir(null)/<app>`(无外部则 `filesDir`)，读写零权限、卸载即清、符合 Scoped Storage；随之删广义 WRITE/READ/legacy 与权限门禁页。**代价**：app 专属目录用户文件管理器不可见 → 需 SAF 导出/导入(`CreateDocument`/`OpenDocument`)让用户拿到数据。Android11+ 无 MANAGE 时**无法读旧 `/sdcard` 公共数据迁移**(悖论)，开发阶段可放弃旧数据。[AI生成]
- **完整备份必须含 DB + 图片**：只备 `.db` 会丢照片(图片是 DB 外独立文件)。用 `java.util.zip` 打 `.ckbk`(zip)= db(+wal/shm) + img全部 + `manifest.json`(带 `schemaVersion` 供导入版本校验)；恢复先 checkpoint、staging 解压、校验版本、关连接、覆盖 db 并清旧 wal/shm、释放图片。[AI生成]
- **zxing 扫码默认横屏**：`CaptureActivity` 默认走传感器/横屏。固定竖屏需自定义 `class PortraitCaptureActivity : CaptureActivity()` + Manifest `android:screenOrientation="portrait"` + `ScanOptions.setOrientationLocked(true).setCaptureActivity(...)`。[AI生成]
- **局域网直传零依赖**：`ServerSocket(0)` 取随机端口 + 4 位一次性校验码握手(接收端先发码，匹配才传)，本机 IP 用 `NetworkInterface` 取非回环 IPv4；发送端复用备份 `exportTo(流)`、接收端 `importFrom(socket流)` 直接落地恢复。ServerSocket.accept() 阻塞需 IO 调度 + 可取消(close 解阻塞)。[AI生成]
- **无 Context 的工具类要取 app 目录**：`object CookbookStorage` 在 `Application.onCreate` 存一次 application Context(`init`)，`requireSubDir(child)` 默认用它 → `LogFileManager` 等无 Context 类也能取目录，避免签名到处塞 Context。[AI生成]

## AI 云端接入 / 联网的坑（2026-07-09）

- **本地优先 App 缺 `INTERNET` 权限 → 云端调用静默失败回退**：Cookbook 之前无网络功能，Manifest 没声明 `INTERNET`，导致 CloudAiRuntime 的 HTTP 被系统拒、每次回退规则，表象是"选了云端没生效"。凡加联网功能先确认 `INTERNET`(+按需 `ACCESS_NETWORK_STATE`)。[AI生成]
- **改 Manifest 权限/组件必须重装 APK**：热更/增量安装不会重读新权限，用户装了旧包会一直没效果，排查前先让用户重装。[AI生成]
- **诊断"云端不通"先用 curl 直测 key**：`curl` 打厂商接口（智谱 v4 `Authorization: Bearer <原始apikey>` 直接可用，无需手动 JWT；响应 `{choices:[{message:{content}}]}`）。通了=问题在 App(权限/配置)，不通=key/接口/格式问题。curl 传中文易被 shell 编码搞乱(0xbb)，测试用英文。[AI生成]
- **国内大模型走 OpenAI 兼容接口**：智谱/DeepSeek/通义(compatible-mode)/Kimi 都提供 `chat/completions`，一个通用 runtime + 不同 endpoint/model 即可切厂商；key 按 vendor 分组存(同厂多模型共用)。[AI生成]
- **Compose 实验 API 需就近 @OptIn**：`FilterChip`/`ExposedDropdownMenuBox` 等是 `ExperimentalMaterial3Api`，用它的**每个** composable(含私有子组件)都要 `@OptIn(ExperimentalMaterial3Api::class)`，否则编译失败。[AI生成]
- **给列表模型(如 DishMini)加字段要透传所有构造点**：改 domain data class 后，所有 `DishMini(...)`/中间 `DishMiniSource(...)` 构造点 + 对应 `.sq` 查询的 `SELECT` 都要补该列(否则 `row.xxx` Unresolved)。[AI生成]
- **Claude 会话内环境变量缓存**：用户改了系统环境变量后，当前 Claude 会话的 Bash **仍读到旧值**(父进程启动时缓存)，需会话重启才干净；期间用 `unset VAR;` 兜底。[AI生成]

## Android 计时/后台提醒的四个坑（2026-07-08 烹饪计时）

- **倒计时不能用 `delay` 每秒递减**：息屏/后台时 Compose 协程(Main dispatcher 的 delay 基于 uptimeMillis)被系统挂起，倒计时"停走"。正解：记结束时刻(`SystemClock.elapsedRealtime()`，息屏也走时)，每次按 `endAt-now` 算剩余。[AI生成]
- **后台/息屏/被杀要准时响铃**：不能靠前台协程，用 `AlarmManager.setExactAndAllowWhileIdle(ELAPSED_REALTIME_WAKEUP, endAt, pi)` + Manifest 注册 `BroadcastReceiver` 播铃+通知。多任务各用唯一 requestCode/通知id，Ringtone 静态 `Map` 分别持有才能分别停。[AI生成]
- **前台服务通知"点开始不立刻出现、离开界面才出现"**：FGS 通知默认最多延迟 10 秒显示(防短命服务)。修：`NotificationCompat.setForegroundServiceBehavior(FOREGROUND_SERVICE_IMMEDIATE)`。A14 FGS 需声明 `foregroundServiceType`(计时用 `specialUse` + property + 权限)。[AI生成]
- **Android 14 全屏提醒/亮屏被限**：`USE_FULL_SCREEN_INTENT` 对非闹钟类 App 默认关闭，`fullScreenIntent` 降级为横幅、不亮屏。需引导用户到"设置→通知→允许全屏通知"或用 `ACTION_MANAGE_APP_USE_FULL_SCREEN_INTENT` 引导；WakeLock 亮屏在新版多为无效。全屏 Activity 用 `setShowWhenLocked(true)+setTurnScreenOn(true)`。[AI生成]
- 划掉应用后服务被系统重启时 `onStartCommand` 的 intent 可能为 null：用 `START_REDELIVER_INTENT` + 静态兜底数据，否则空 intent 会让服务误判"无运行"而自毁。国产 ROM"划掉即强杀"纯代码绕不过，响铃靠系统闹钟不受影响。[AI生成]

## 大规模改 seed 分类引用：用脚本+引用完整性校验，别手改

[2026-07-06]
- 重构 `food_categories.json`(general 树) 会牵动 `ingredients.json` 里 153 个食材的 `categories` code 引用，手改必留孤儿。做法：写 Python 脚本（DEFAULT 旧→新映射 + 逐食材 REPLACE 覆盖），转换后**校验**：每个 general 引用都能解析到新树节点、每个食材至少挂 1 个大类，全绿才落盘。脚本存 scratchpad 可复现。[AI生成]
- 兜底认知：`PresetDataSeeder` 对未知 category code 是 `?.let` **静默跳过**（不崩），所以孤儿引用只会少关联、不会 crash；但仍应靠校验清零。分类按文件顺序 seed，**父节点必须排在子节点之前**。[AI生成]
- 改 general 大类名（如 水果→水果类）会打断按名断言的测试（`PresetDataSeederTest` 查 `it.name=="水果"`），改分类名时同步搜 `it.name ==` 更新断言。[AI生成]

## Codex 自动任务编排容易停留在“引用文档”

[2026-05-24]
- 只在 `~/.codex/AGENTS.md` 写“流程定义：workflow_auto_orchestration.md”不够，模型可能把它当参考文档，而不是强制入口。
- 必须在全局入口写清楚：开发类任务开始前先执行阶段0评估，输出任务类型、深度、模式和智能体分派表。
- DEV 角色不是 Codex 内建 agent 名称，需要显式映射到 `multi_agent_v1.spawn_agent`：
- 分析/方案/审核类角色用 `explorer`，实现/测试落地类角色用 `worker`。
- 标准/深度任务如果有多智能体工具但没有真实分派，应视为流程偏差，必须暂停并修正流程后再继续。

## 任务前上下文快照不能只靠收尾保存

[2026-05-30]
- 原规则只要求“重要任务节点后保存上下文”，没有把“任务开始前保存上下文”写成入口门禁。
- 结果是执行任务时容易只在结束后保存，用户看不到任务前状态被固定下来。
- 修正方式：阶段0任务编排声明后，阶段1分析或任何实质操作前，先写 `.ai-context/docs/context_memory/yyyy-MM-dd_pretask_<任务短名>.md`。
- 快照内容必须包含最新需求、模式/级别、计划分派、已知状态、预计涉及文件、风险和待验证项。
- 纯问答不执行命令、不改文件、不做项目决策时可不写，但必须明确说明原因。

## Codex 与 Claude Code slash command 差异

[2026-05-24]
- `.codex/commands/zongjie.md` 已存在，不代表 Codex 客户端会注册裸 `/zongjie`。
- 用户输入 `/zongjie` 时，Codex UI/CLI 可能先拦截并提示“没有这个命令”，消息不会进入模型。
- 可行替代：
  - 直接输入普通文本“执行 zongjie / 总结经验”。
  - 用 Codex plugin 的 `commands/` 暴露命令，通常以 `/插件名:命令名` 使用。
- 已创建个人插件 `personal-workflow` 并安装 `personal-workflow@personal`，但新命令通常需要新开会话才会加载。

## IngredientPicker 跨列表选择问题

[2026-05-24]
- 原 `confirmSelected()` 只从当前 `ingredients` 列表筛选，用户跨搜索/分类选择后可能丢失之前选中的食材。
- 修复方式：在 `IngredientPickerUiState` 中维护 `selectedIngredients`，确认时直接返回该列表。
- 同时新增 `excludeIngredientIds`，避免已经加入菜品的食材再次展示。

## Android app label 回退包名

[2026-05-24]
- Manifest 未设置 `android:label` 且无 `app_name` 资源时，安装后可能显示包名/默认名。
- 修复方式：新增 `res/values/strings.xml`，设置 `app_name=CookBook`，并在 `application` 和 launcher `activity` 上引用。

## DatePicker 默认日期偏移

[2026-05-24]
- Material3 `DatePicker` 的 `selectedDateMillis` 语义更接近 UTC 日期毫秒。
- 如果用本地时区 `atStartOfDayIn(TimeZone.currentSystemDefault())` 初始化，在东八区等时区可能出现默认显示前一天的问题。
- 修复方式：用 UTC 零点毫秒初始化和解析，例如 `Instant.parse("${date}T00:00:00Z")`，确认时也按 UTC date 读取。

## 图片 URI 展示与持久化

[2026-05-24]
- 拍照返回的 URI 写入 `image_path` 后，界面不会自动显示真实图片，必须有统一解码组件读取 `content://` 或文件路径。
- 仅显示“图片1/图片2”会让用户误以为照片没有保存；图片选择区应展示真实缩略图。
- 相册返回的 `content://` 不能长期依赖临时授权；拍照 cache 原图也存在被清理风险。
- 处理方式：图片选择后立即在后台压缩并复制到 `filesDir/images/`，`image_path` 只保存压缩后的私有文件路径。
- 列表和详情不要直接解码原图；压缩图目标控制在 10KB 内，点击图片时再用弹框展示较大尺寸预览。

## 预设/自建食材权限

[2026-05-24]
- 用户明确规则：预设食材不可编辑、不可删除；自建食材才可编辑/删除。
- UI 需要给明显提示，避免用户不知道哪些能长按：
- 预设：不显示编辑图标，不弹长按菜单。
- 自建：显示可长按的图标提示，长按弹出编辑/删除菜单。
- 权限规则不能只写在 UI；SQL 的 update/delete 也要限制 `source='user'`，防止误操作。
- 删除自建食材不能只 `DELETE ingredient`；必须先处理 `dish_ingredient` 关联，否则可能外键失败或留下孤儿关联。

## 食历前插分页跳动

[2026-05-24]
- 食历顶部加载历史会在列表头部前插 7 天，若提前发布 `prependCount`，UI 会在旧列表上消费锚点，等新数据回来后仍然跳动。
- 正确做法：ViewModel 等新窗口数据返回后再发布 `prependCount`，UI 消费后调用 `consumePrependCount()`。
- 加载历史和计划必须共用 `loading` 门禁，避免两端加载互相取消 Flow 订阅。

## 阶段性性能优化边界

[2026-05-25]
- 无人值守做架构优化时，优先选择不改 schema、不引入新依赖、可编译验证的优化。
- Repository 层列表组装容易出现 N+1：菜品标签、烹饪方式、餐食记录下菜品都应按批次预取。
- 保存餐食和保存菜品要用 `db.transaction` 包裹主表、关联表和喜爱度更新，避免 Flow 收到半完成状态。
- 食历窗口裁剪会改变“滑过日期是否保留”的体验，不能和低风险性能优化混在一起悄悄落地。
- 当前工程没有 `:shared:allTests` 任务，shared 验证使用 `./gradlew :shared:testDebugUnitTest`；如果后续新增 multiplatform 测试任务，再更新命令文档。

## 食历真实日期分页

[2026-05-25]
- 食历“总数”不能按连续自然日计算，否则无餐食日期也会生成空 item。
- 正确数据源是 `meal_record` 的 `DISTINCT date`，列表按日期正序显示。
- 默认定位优先级：今天有餐食 -> 未来最近有餐食日期 -> 最大餐食日期。
- 顶部加载历史和底部加载计划，每次都加载 7 个“有餐食日期”，不是 7 个自然日。
- 日历标记要用全量有餐食日期集合，日期范围也必须来自全量最早/最晚餐食日期，不随当前分页窗口变化。

## Navigation query 参数编辑态丢失

[2026-05-30]
- 菜品详情点击编辑进入了新建菜品，根因是 `NEW_DISH` 使用 query 参数时，当前 Navigation 配置下 `dishId` 解析不稳定。
- 修复方式：把 `newdish?dishId={dishId}&importDishId={importDishId}` 改为路径参数 `newdish/{dishId}/{importDishId}`。
- 对编辑/复制这种必须可靠传递 id 的路由，优先使用路径参数；query 参数更适合可选搜索条件。

## SQLDelight schema 变更必须配套迁移

[2026-05-30]
- 新增 `dish_cooking_method_rel` 后，老安装包升级启动会崩溃：`SQLiteException: no such table: dish_cooking_method_rel`。
- 只改 `Cookbook.sq` 只能影响新建数据库；已安装用户必须新增 `.sqm` 迁移文件。
- 修复方式：在 `shared/src/commonMain/sqldelight/.../1.sqm` 创建新表，并从旧字段 `dish.cooking_method_id` 回填关联数据。
- 经验：任何 schema 变更完成后都要检查 `shared/src/commonMain/sqldelight` 下是否需要新增/更新迁移，并跑 `:shared:generateCommonMainCookbookDatabaseInterface`。

## SQLDelight 迁移文件必须同步提升 version

[2026-06-03]
- 报错：`SQLiteException: Can't downgrade database from version 2 to 1`。
- 根因：已有 `1.sqm` 表示 v1 -> v2 迁移，但 `shared/build.gradle.kts` 未配置 `version = 2`，导致当前包仍按 DB version 1 打开。
- SQLite 不允许从设备已有 version 2 降到 version 1，不能通过清库作为修复方案。
- 修复方式：在 `sqldelight.databases.create("CookbookDatabase")` 中显式设置 `version = 2`。
- 经验：每新增一个 `.sqm` 迁移文件，都要同步检查 SQLDelight `version` 是否等于目标 schema 版本；版本只能升不能降。

## Compose 页面复用导致编辑态串状态

[2026-05-30]
- 菜品详情点击编辑仍可能显示成新建，不能只检查路由参数是否传对，还要检查页面 ViewModel 是否复用旧表单状态。
- `NewDishScreen` 里分散使用多个 `LaunchedEffect` 分别加载编辑/导入，容易出现新建、编辑、导入互相覆盖。
- 修复方式：在 `NewDishViewModel.start(editId, importId)` 统一入口，先重置表单状态，再按“编辑优先、导入其次、新建为空”加载。
- 经验：编辑页标题、保存 id、表单数据必须来自同一个入口状态；保存完成的 `done=true` 也要在再次进入时清掉。

## Kotlin daemon + 中文路径问题

[2026-05-30]
- Gradle 编译时 Kotlin daemon 偶发连接失败，fallback 编译会把中文路径错误转义成 `/Users/sxd/Documents/u5DE5u4F5C/...`，导致大量 “source file not found”。
- 这类报错不一定是源码问题；先执行 `./gradlew --stop` 停掉 daemon，再复跑构建。
- 如果复跑后进入正常 Kotlin/SQLDelight 类型错误，再按真实源码问题处理。

## 菜品列表返回不刷新

[2026-06-03]
- 添加菜品后返回菜品页不刷新，根因是列表使用一次性 `searchDishes("")` 结果，未持续观察数据库变化。[AI生成]
- 修复方式：空关键词场景使用 `dishRepo.observeAllDishes()` 的 Flow 作为主列表；搜索关键词非空时仍走一次性搜索。[AI生成]
- 从新建/编辑返回时在 `ON_RESUME` 主动 `refresh()`，并给 Tab 列表增加下拉刷新入口，保证用户手动可恢复最新状态。[AI生成]

## 编辑页标题正确但表单为空

[2026-06-03]
- 标题显示“编辑菜品”只能说明路由 id 已传入，不能说明详情数据已成功加载。[AI生成]
- `loadForEdit()` 不能静默忽略 `getDishById()` 返回 null 或异常，否则用户看到的是空表单并误以为编辑功能失效。[AI生成]
- 修复方式：编辑/导入入口增加 `loading/errorMessage` 状态，加载成功后一次性回填表单，失败时禁用保存并显示错误提示。[AI生成]

## 图片字段新增后必须保持索引一致

[2026-06-03]
- `image_path` 和 `thumbnail_path` 都使用 `|` 分隔多图时，删除图片必须按索引同步删除，不能用路径去重或只删原图字段。[AI生成]
- 图片展示优先走缩略图，预览再加载原图；保存时要同时生成两个文件并一起回写数据库。[AI生成]
- SQLDelight 新增字段后，`insert`、`update`、查询映射、seed 插入参数都必须同步调整，否则会出现编译错误或运行时数据缺失。[AI生成]

## 公共 sdCard 目录必须先授权再建库

[2026-06-03]
- 如果目标是 `/sdcard/cookbook/...`，不能在 `Application.onCreate()` 里提前 seed 或读取偏好，否则数据库会在权限门禁前被创建。[AI生成]
- 正确流程：`MainActivity` 先判断/申请外部存储权限 -> 授权后迁移旧 app 专属 `cookbook` 目录 -> 创建 `/sdcard/cookbook/db` 和 `/sdcard/cookbook/img` -> 再加载主题、数据库和 HomeScreen。[AI生成]
- Android 11+ 写 sdCard 根目录需要 `MANAGE_EXTERNAL_STORAGE`，运行时普通读写权限不够；必须跳系统“所有文件访问权限”设置页。[AI生成]
- 数据库和图片路径必须统一从 `CookbookStorage` 获取，不要各模块各自 fallback 到 app 专属目录，否则用户会看到 db/img 不在指定目录。[AI生成]

## SQLDelight 非空字段遇到旧库 NULL 会崩溃

[2026-06-03]
- 闪退日志 `CookbookQueries$selectAllIngredients` 内 `cursor.getString(...)!!`，通常表示旧库中某个声明非空的 TEXT 字段实际为 NULL。[AI生成]
- 只给新字段 `DEFAULT ''` 不能修复已存在的异常旧数据；查询层要用 `COALESCE` 兜底，启动 seed 时也要执行一次 NULL 清洗。[AI生成]
- 改 `SELECT *` 为显式 `COALESCE` 后，SQLDelight 会生成自定义查询结果类型，Repository 需要用显式 mapper，不再套用表实体扩展函数。[AI生成]
- 后续新增 `NOT NULL` 字段时，必须同时满足：schema 中有 `NOT NULL DEFAULT <默认值>`，迁移文件 `ALTER TABLE ... ADD COLUMN ... NOT NULL DEFAULT <默认值>`，迁移后执行 `UPDATE table SET field = COALESCE(field, <默认值>)`，启动清洗逻辑也要纳入该字段。[AI生成]
- 新增字段如果会参与业务过滤，例如 `status`，所有常规查询必须同步加有效条件；只加字段不改查询等于没有实现业务语义。[AI生成]

## KDoc 路径通配符可能提前结束注释

[2026-06-05]
- Kotlin KDoc 里不要直接写类似 `seed/*.json` 的路径，因为其中的 `*/` 会被解析成注释结束符。[AI生成]
- 表现为编译报 `Unclosed comment`，并可能连带出现类无法解析的错误。[AI生成]
- 修复方式：改写成“`seed/` 下的 JSON 文件”，或拆开星号与斜杠，避免源码中出现连续的 `*/`。[AI生成]

## KMP Android 资源读取不能只假设 commonMain resources 自动可用

[2026-06-05]
- `shared/src/commonMain/resources` 在当前 Android unit test classpath 下没有自动出现，导致 JSON seed 读取为空。[AI生成]
- 修复方式：在 `shared/build.gradle.kts` 中显式把 `src/commonMain/resources` 挂到 Android `main/test` resources，并在 `SeedResourceLoader` 为 JVM 单元测试增加项目文件路径兜底。[AI生成]
- 经验：资源驱动的 seed 迁移必须加测试验证“资源确实读到内容”，不能只验证解析函数本身。[AI生成]

## 初始化异步抢跑会导致首次编辑异常

[2026-06-04]
- 授权后如果立即加载 `MainScaffold/HomeScreen`，而 `seedIfNeeded()` 仍在后台异步执行，用户第一次点编辑可能抢在旧库清洗和预置数据初始化前发生。[AI生成]
- 表现为：第一次编辑为空/失败，添加菜品或等待一段时间后再编辑正常。[AI生成]
- 修复方式：权限通过后先同步等待 seed 完成；初始化完成前只显示初始化页，不读取主题偏好、不加载导航和业务页面。[AI生成]

## 编辑页空白要区分路由 id 与数据加载失败

[2026-06-04]
- 编辑页标题显示正确、路由也带了 id，仍然可能因为 `getDishById()` 内部查询异常而显示空白。[AI生成]
- SQLDelight 表字段声明为非空时，历史库里残留的 `NULL` 文本字段会在 `cursor.getString(...)!!` 处中断，异常可能发生在主表，也可能发生在食材/标签等关联查询。[AI生成]
- 修复方式：详情/编辑这类关键按 id 读取链路使用显式 `COALESCE` 查询，不直接依赖 `SELECT *`；关联查询也要对旧字段兜底。[AI生成]
- 排查方式：编辑页临时 Toast 显示“菜品名 + ID”或“加载失败 + ID”，先确认是路由 id 丢失、查不到行，还是加载中途失败。[AI生成]
- Toast、Snackbar、导航这类一次性事件不能由 Compose 表单快照反推；初始空状态、重置状态和真实加载结果会在重组中交替出现，导致重复或错误提示。[AI生成]
- 正确做法：由 ViewModel 在加载成功/失败的分支发出一次性事件，UI 只消费事件；同时对相同路由参数做启动去重，避免重复 `start()` 清空已加载表单。[AI生成]
- ViewModel 初始化字典时不能写成 `_state.value = _state.value.copy(field = suspendCall())`；receiver 会先捕获旧 state，挂起查询完成后再把旧空快照写回，覆盖编辑加载成功后的表单。[AI生成]
- 正确做法：先执行挂起查询得到局部变量，再用 `MutableStateFlow.update { current -> current.copy(...) }` 基于最新状态合并结果。[AI生成]
- 复杂链路要主动打关键节点日志，尤其是导航参数、ViewModel 入口、Repository 查询结果、状态快照、一次性事件消费和异常分支。[AI生成]
- 日志需要使用统一可过滤 tag，例如本次 `NewDishEdit`，内容要包含业务 id、状态字段、集合数量和错误信息；不要只写“进入/成功”这类无法定位状态变化的空日志。[AI生成]
- 日志也要控制边界：不要输出完整图片路径列表、用户隐私、大对象内容或高频循环日志；必要时输出数量、id、摘要即可。[AI生成]

## 整体审核后的低风险优化经验

[2026-06-04]
- 数据库存储路径变更后，备份/恢复必须同步改到同一真实库文件；否则备份的是旧内部库，恢复也不会影响当前运行库。[AI生成]
- Android 10/API 29 不能简单归为“已授权”；如果仍要求 `/sdcard/cookbook`，至少要让权限按钮走读写权限申请，避免用户卡在权限页无动作。[AI生成]
- 整天餐食采用“先删后插”的编辑模型时，喜爱值不能对所有新菜品无条件累加；应先比较旧日期菜品集合，只对新增菜品加分。[AI生成]
- 弹窗选择器如果 ViewModel 由 Koin 复用，每次打开必须显式清空选择态和旧错误态；不要依赖 Composable 销毁自动清理。[AI生成]
- 保存操作必须有 `runCatching` 和可见错误提示，尤其是整日替换、图片保存、备份恢复这类可能受权限/数据库状态影响的操作。[AI生成]
- 分页刷新/Flow 刷新如果每次都新开协程，要保存 Job 并取消旧任务，避免旧结果晚到覆盖最新窗口。[AI生成]

## 功能完成后必须纳入单元测试

[2026-06-05]
- 只跑编译和 assemble 只能证明代码能构建，不能证明业务规则正确；之前 `NO-SOURCE` 表示测试任务没有实际测试源码。[AI生成]
- 后续功能完成后必须补充或更新对应单元测试，并运行相关测试任务，防止遗漏历史数据、软删除、迁移、状态覆盖等隐蔽因素。[AI生成]
- shared 层优先测试 Repository/UseCase/SQLDelight 行为；Android UI 层如难以直接单测，应把核心规则下沉到 ViewModel/Repository 后测试。[AI生成]

## 最近食材可见性要同时检查数据源与 UI 排除条件

[2026-06-10]
- 保存菜品成功且重新进入编辑页能看到食材数量增加，说明 `dish_ingredient` 主链路已落库；此时“最近使用”不可见不一定是 Repository 或 SQL 查询失败。[AI生成]
- 食材选择器存在 `excludeIngredientIds` 这类防重复参数时，会在查询结果之后再次过滤列表；排查最近/搜索/分类列表为空时必须检查 UI 过滤条件。[AI生成]
- 如果 ViewModel 已有去重兜底，例如 `NewDishViewModel.addIngredient()` 会忽略重复食材，选择器可以优先保证“已使用食材可见”，不要为了防重复展示而隐藏最近使用结果。[AI生成]
- 这类问题的低风险修复优先限定在 UI 入参策略，不改 schema、不改保存事务、不改最近食材查询口径；数据层已有单测时继续用单测证明业务规则未回退。[AI生成]
- 真机日志要同时覆盖保存事件和列表数据源结果；只记录 `new_dish_save success=true` 不足以判断最近列表为何为空，后续可在选择器加载最近分类时记录查询数量与排除数量。[AI生成]

## 改 .sq 表结构必须同步加 .sqm 迁移（否则旧库升级缺列，真机才暴露）

[2026-07-06]
- 给 `ingredient` 加 `reason` 列时只改了 `Cookbook.sq`（进入 `Schema.create`），**漏了对应 `N.sqm` 迁移文件**。结果：全新安装正常（建表带列），旧库升级后缺列，凡读写 `reason` 的查询报 `no such column: reason`（已失效列表、菜品读食材 `selectIngredientsOfDish`、软删写 reason 全挂）。[AI生成]
- **单元测试用 `RepositoryTestDatabase.create()` = `Schema.create(driver)`，直接建最新结构、不走迁移链**，所以测试全过而真机升级失败。改 schema 的改动，光靠现有单测无法发现缺迁移。[AI生成]
- 规则：**任何 `.sq` 表结构变更（加列/改列/建表）必须同时新增 `N.sqm` 迁移**；`Schema.version` 由迁移文件数决定（`gradle` 里的 `version=8` 是无效残留属性）。[AI生成]
- 补救幂等补列（本次采用）：在 `DatabaseDriverFactory.android.createDriver()` 建驱动后 `runCatching { driver.execute(null, "ALTER TABLE ... ADD COLUMN ...", 0) }`，列已存在时忽略 duplicate column。适合"schema 已含列但部分旧库缺列且版本号已被占用"的补救；正常演进仍应走 `.sqm`。[AI生成]
- 建议：长期可开启 SQLDelight `verifyMigrations`，让"schema 与迁移不一致"在构建期暴露，而不是等真机。[AI生成]
- **[2026-07-06 已证实·阶段0结论]** SQLDelight 2.0.1 的 database 块**没有 `version` 属性**；`Cookbook.sq` 的运行时 `Schema.version` **由 `*.sqm` 迁移文件数自动推导**（1~N.sqm ⇒ version=N+1）。旧代码 `create("CookbookDatabase"){ version = 8 }` 里的 `version` 泄漏成了 **Gradle 项目版本**（`Project.version`），与 DB 毫无关系——是"版本号"误解的根因，已删除。判断真实 DB 版本**只看生成的 `CookbookDatabaseImpl.Schema.version`**，别信 build.gradle。[AI生成]
- **[2026-07-06 已证实]** reason 列修复正解（在"重装/无历史旧库"前提下）：补 `10.sqm`（`ALTER TABLE ingredient ADD COLUMN reason ...`）让迁移链重建结果与 `Schema.create` 一致（version 自动升 11），随后**删除** `DatabaseDriverFactory.android.ensureLegacyColumns` 幂等补列（消除驱动侧重复 DDL）。⚠️ 若线上已有"该列已进 Schema.create 的旧库"，普通 ALTER 迁移会对已有列的库崩（duplicate column），此时才保留幂等驱动补列；本项目因重装无此约束。[AI生成]
- **[2026-07-07 实际踩中·教训]** 上条的"删除驱动补列"决策在真机翻车：用户设备旧库是 v10-已含 reason，升级跑 `10.sqm` 的普通 ALTER → `duplicate column: reason` → 迁移失败 → "初始化数据失败"。单测走 `Schema.create` 不跑迁移，永远测不出这类**迁移专属**失败。**正解**：把 `10.sqm` 改为无操作占位（`CREATE TABLE IF NOT EXISTS _noop; DROP TABLE IF EXISTS _noop;`，勿删文件否则版本空洞），reason 由 `Schema.create`(新装) + 恢复的 `ensureLegacyColumns` 幂等补列(旧库) 共同保证。**教训：改动只影响迁移路径时，务必单独推演"设备各历史版本升级"矩阵，别被单测全绿误导；"假设都重装"是危险前提。**[AI生成]
- 迁移空迁移占位别用 `SELECT 1;`：Android `execute()` 对 SELECT 的处理有歧义，用「建了即删的临时表」这类确定性 DDL 更稳。[AI生成]

## 先核对现有 schema/代码再出方案，避免过度设计与重复造表

[2026-07-06]
- 食材层重设计前，先实测 `Cookbook.sq` 与生成代码，发现 `status/reason/last_referenced_at/alias/is_main/food_category.dimension/care_rule` 均已具备——原以为要新建的表/列多数已存在。若不核对直接照文档设计，会重复造表、平添迁移。[AI生成]
- 方法论：**写数据层方案的第一步是 grep `CREATE TABLE` + 读现有列，再决定新增什么**；文档描述可能落后于真实 schema。[AI生成]

## SQLDelight 生成列名遇 Kotlin 关键字会加下划线

[2026-07-06]
- `user_preferences.value` 列在生成代码里的属性名是 `value_`（`value` 是 Kotlin 软关键字）；`selectPreference(...).executeAsOneOrNull()?.value_` 才对，写 `.value` 编译报 Unresolved reference。排查同类问题先看生成的 `*.kt` 行类型的实际字段名。[AI生成]

## 苹果化改造 / 营养色系（2026-07-14）

- [2026-07-14] **SQLDelight `sqlite_3_18` 方言 WHERE 不支持 `REPLACE`/`TRIM` 等字符串函数**：想在 `.sq` 里按去空格名比对(名称归一)会编译失败(`<expression> expected`)。改法：查出 id+原名(`selectActiveIngredientIdNames`)，在 **Kotlin 侧** `normalizeNameKey` 归一后比对；或加 `name_key` 冗余列。→ 也见 CLAUDE.md 已有 UPSERT 红线，字符串函数同属方言缺失。
- [2026-07-14] **删"死导入"翻车两处**：①`perl -0 -pe` 用 `$` 锚定行尾在 **CRLF** 文件上匹配不到(需 `\r?$`)；②凭肉眼判"没用了"删掉了**仍被引用**的 import(如 CuisineRail 还在用的 `pointerInput`/`detectDragGestures`)→编译红。结论：**删 import 用 Edit 逐个删、删前 Grep 确认全文件无引用**；死导入只是 warning，拿不准就留着，别为清洁去冒编译险。
- [2026-07-14] **`by collectAsStateWithLifecycle()` 报"has no getValue"**：缺 `import androidx.compose.runtime.getValue`(委托属性需要)。同类 `by remember{}.collectAsState()` 也要该 import。
- [2026-07-14] **Material3 1.1.2 能力边界**(反复踩)：有 `ModalBottomSheet`/`SwipeToDismiss`/`LargeTopAppBar`/`FilterChip`(实验但可用)，**无 `SegmentedButton`**(1.2+)、**无 `SelectableDates`**(1.2+)。分段控件自绘；禁用日期在确认回调校验。
- [2026-07-14] **UI 盲改要"UX 设计师 agent 分轮 + 每轮小改即 build"**：整屏苹果化不要一把梭全量重写，按轮(token→大标题→设置分组→动作表/步进器→卡片/间距→终审)推进，每轮改完 `assembleDebug` 过再下一轮、每屏一 commit 可回退；`SwitchRow`/`InsetGroup` 等新组件先建后铺开。

## [2026-07-15] 状态并发与迁移/诊断类

### 旧快照 copy 写回丢并发字段（真机踩过）
- 现象：食材编辑器默认单位下拉空（连中文单位都没有）。真机 logcat 埋点显示 `loadUnits` 已查到 22 个单位写入 state，但编辑器拿到 0 个。
- 根因：`IngredientPickerViewModel.selectMainTab` 启动时 `val current = _state.value`（此时 availableUnits 还空），DB 查询**挂起期间** init 的 `loadUnits` 把 availableUnits 填了，等 `_state.value = current.copy(...)` 写回时用的是**旧快照** current，把并发填入的 availableUnits 冲回空。
- 通用规律：**多个 init 加载器并发**（loadCategories/loadUnits/loadAllIngredients…各自 launch），任一处 `val x=_state.value` 捕获后经挂起再 `_state.value = x.copy(...)`，都会丢掉挂起期间别处更新的字段。
- 修法：写回一律基于**最新** `_state.value.copy(...)`，或用原子的 `_state.update { it.copy(...) }`（NewDishViewModel 字典加载就是用 `_state.update{}` 正确避坑的）。
- 排查手法：全项目 grep `(_state|_uiState)\.value = [a-zA-Z_]\w*\.copy\(`（写回用的不是 `.value.copy` 而是捕获的局部变量）+ `val \w+ = _state.value`（全量捕获点），逐个看是否"捕获→挂起→旧快照写回"。

### 真机诊断"数据有但没传到 UI"
- adb 多设备用 `adb -s <序列号>`；`adb devices -l` 看序列号。
- Compose 底部导航文本**不进无障碍树**，`uiautomator dump` 抓不到导航项文本；改用 `wm size` 按比例点或让用户操作。
- 华为等设备 shell **无 sqlite3**；`adb exec-out run-as <pkg> cat databases/xxx.db > local.db` 拉库到本地用 python sqlite3 读（注意关 App 让 WAL flush，否则拉出的可能不完整/"file is not a database"）。
- 定位链路最快：在"查(repo)→存(VM state)→读(UI)"三处各埋一条 `AppLogger.d(TAG,...)`/`CookbookLog.d(...)`，一次 logcat 就知道断在哪。定位后删日志。

### SQLDelight 迁移 rename 字典项保 FK
- 统一单位/字典命名（如 克→g、毫升→ml、升→L）：用 `UPDATE measurement_unit SET name='g' WHERE name='克'`——**保 id 不变**，`dish_ingredient.unit_id`/`pantry` 按 id 引用、FK 不断、历史数据不丢。
- name 有 UNIQUE：若目标名可能已存在（如此前 force-seed 加过的重复英文单位），先 `DELETE ... WHERE name IN(...) AND source='preset'` 删重复，再**带守卫**重命名 `UPDATE ... WHERE name='克' AND NOT EXISTS(SELECT 1 FROM measurement_unit WHERE name='g')`，防 UNIQUE 冲突让迁移崩（迁移崩=真机"初始化失败")。
- 同步改 seed 文件（dishes.json/ingredients.json 的 unit 字段）与 `PRESET_MEASUREMENT_UNITS`，并改按名断言的单测（`units["克"]`→`units["g"]`）；`unitNames()` 自动跟随 PRESET，引用完整性校验一致。

### 加列升级无损三件套
- `ALTER TABLE x ADD COLUMN col TEXT NOT NULL DEFAULT ''`：老行只补默认值、**零数据改动**；CREATE TABLE 同步加同列（全新装走 Schema.create）。
- 预设值回填放 seeder、**只填空的**（`WHERE col=''`）、按名/规则计算，不覆盖用户已选；每次启动幂等。
- 老用户数据的"补齐"靠"编辑时预填 + 保存即应用"，而非强制数据迁移（如老自建食材编辑时按名预选营养大类、点保存补挂分类）。

### 分类表无 code 列
- `FoodCategory` 模型与 `food_category` 表**都没有 code 列**，Group→分类只能**按 name 映射**（`FoodGroup.CATEGORY_NAME`）。注意红线：改 general 大类名会打断按名的映射/断言。

## 中文食材分类 classify 尾词优先（2026-07-15）
- **中文食材名的类别看末尾 head-noun，不是前缀修饰词**：用户纠正"看最后的名称(xxx菜/xxx肉/xxx牛奶)，不看前面说明(鸡毛菜只是长得像鸡毛的菜，始终是菜)"。初版 classify 按前缀关键词命中导致：脱脂纯牛奶含"牛"→红肉(应奶)、鸡毛菜含"鸡"→禽(应菜)、五色米/炕馍没命中主食。
- **改法**：`FoodGroup.classify()` 尾词优先——先查 `NAME_OVERRIDE`(用户确认的 54 项特例表)→`endsWith("油")→null`/`endsWith("腐")→BEAN`→`endsWith 蛋→EGG/奶→DAIRY/肉→(含鸡鸭鹅=禽 else 红肉)/菜|苗→VEGETABLE`→最后才前缀关键词兜底。DAIRY 判定必须在 FISH/meat 之前(牛奶别进肉/水产)、FUNGI 在 FISH 前、FRUIT 在 VEG 前。
- **NAME_OVERRIDE 可续加**：用户约定后续新食材"参考已有分类规律自主判断"，拿不准归类的特例直接加进 `NAME_OVERRIDE` map(name→Group)，比堆关键词更可控。
- **归类覆盖率**：392/440 有类，剩 48 全是纯调料/油/茶/糖(classify 返回 null 正常，营养大类=无)。预设食材启动 `backfillFoodGroups()` 重刷(覆盖式)、用户自建只填空的。

## 权威数据核准 / 合并（2026-07-15）
- **agent 联网批量核准数据**：数值核对(营养/GI/嘌呤等)交给分片后台 agent 联网查权威源(先 `ToolSearch select:WebSearch,WebFetch`)，每片写独立 `temp/claude/*_N.json`，主线程 python 合并。比主线程逐个查快且不炸上下文。
- **健康数据禁编造出处**：查不到的字段**直接省略**(不编数值)，口径不确定/非一手源标 `review=pending` 并在 `ref` 写明原因(如"用鲜品近似/品牌标签/USDA代")；一手权威成分表值才 `verified`。落库时 seed 的 `review=="pending"→1L`(需复核) 否则 0L。
- **覆盖升级式合并**(不是全量替换)：以旧 seed 为底，auth 提供的字段覆盖、auth 省略的字段保留旧值(避免把老估算 null 掉)、ref+review 取 auth。合并脚本先写好待命，最后一片 agent 回来再一次性跑，省得反复合并。
- **合并后必验**：python 能 `json.load` = 合法；跑 `validateNutritionSeedForTest`(名字全解析)+`:shared:testDebugUnitTest`。Windows 控制台显示中文乱码是 codepage 问题、不代表文件坏(文件 UTF-8，以 python 解析为准)。

## 无人值守：0千卡新变体 / adb取库 / 权威阈值国标（2026-07-16）
- **预设菜0千卡新根因(补齐只补"缺失关联"忽略"NULL用量")**：早期 seed 无 quantity，`dish_ingredient` 行**已关联但 quantity/unit_id=NULL** → 按克算营养恒 0(排骨海带汤)。原补齐分支 `if(ingredientId in linked) return@ing` 只补缺失关联，对已关联却 NULL 用量的行直接跳过 → 永不修。修法：新增 `fillDishIngredientQuantityIfNull`(UPDATE...WHERE quantity IS NULL,只回填空值不覆盖用户,纯新查询免迁移) + 补齐分支对已关联配料调用 + `SEED_LOGIC_VERSION` v2→v3。**软删的菜(status=0)reseed 会当新菜重插带新数据(selectPresetDishIdByName只认status=1)，未删的走补齐分支被跳过**——这解释"删掉再更新就好、没删的一直0"。
- **真机库验证修复法**：`adb pull` 拉库到本地 → python 模拟要跑的 SQL(按 seed json 回填) → 统计影响行数+抽查目标菜，先证明修复有效再改代码/交付。华为库模拟得 368菜/838行、只剩 user 自建菜 NULL(seeder 不动用户数据=正确)。
- **Git Bash 调 adb 访问 `/sdcard` 路径必须 `export MSYS_NO_PATHCONV=1`**：否则 MSYS 把 `/sdcard/...` 转成 `C:/Program Files/Git/sdcard/...` 致 `No such file`。
- **本项目 db 不在默认 `databases/`**：落在 `getExternalFilesDir(null)/cookbook/db/cookbook.db`(app专属外部目录,零权限)，`run-as ... cat databases/cookbook.db` 取不到；直接 `adb pull` 该外部路径即可(无需 run-as)。journal 模式(非 WAL)时主 .db 已含全部数据。
- **嘌呤"低/中/高"三级分级(25/150 mg/100g)非国标**：WS/T 560-2017 正文只按食物类别给"应避免/限制/可选择"**定性**建议、附录嘌呤含量单位是 mg/kg 且不设临界值；诊疗指南只作"限制高嘌呤"定性要求。25/150 来自教材/科普惯例、版本不一(存 <25 与 <50)。用则必标"非国标·惯例口径"。反例可信国标：钠(膳食指南5g/2000mg、高血压2400mg)、GI(FAO/WHO 55/70，**非** WS/T 652-2019——该标准仅规定测定方法)、GB 28050-2011 NRV 与低/高含量声称。
- **MMR 批内多样性**：`RecommendationOrchestrator.diversify()` 只对"正常菜层"(非最近非忌口)按主料 Jaccard 贪心重排；忌口/最近层是分层末位(红线)、原样保留不参与重排；λ 绑推荐风格(`RecommendationStyle.diversityLambda()`)控制强度、λ≥0.999 直接返回。纯确定性(无随机)可单测。

## 配色切换 / 今日卡 / 交互库审阅（2026-07-16 下半场）
- **Material3 1.1.2 无 `HorizontalDivider`**：那是 1.2.0+，本项目用 `Divider`(1.1.2)。同类版本坑见"无 SegmentedButton/SelectableDates"。
- **配色切换架构**：改主题色/加配色走 `AppPalette` 枚举(shared) + `androidApp/ui/theme/Palettes.kt`(每套 light/dark ColorScheme + 代表色) + `CookbookTheme(themeMode, palette)` + `PreferenceRepository.observe/setPalette` + MainActivity `collectAsState` 即时重组；**别把色值硬编码散落各处**。默认赤陶橘(复用 Color.kt LightColors/DarkColors)。语义色 error 全套统一、success 由 ExtendedColors 恒定。宏量三色放 ExtendedColors(`macroProtein/Fat/Carb`)固定不随主题。
- **色系墙保持纯结构、不关联热量/慢病**(用户拍板决策A)：热量是**个人**概念(要身体数据)→只放今日卡；慢病提示(钠等食物属性)→个人视角(今日卡)。`NutritionLevelEvaluator` 热量+钠只服务个人卡，色系墙 `FoodGroup.nutritionLevel` 维持不变。
- **调料默认克数**：调料加进菜品默认 100g 会让钠/油脂算爆。`SeasoningDefaults.defaultGramFor(name,isSeasoning)` **只对分类判定为调料(seasoningIngredientIds=调味品/油脂类)缩小**默认(盐3/酱油10…)，普通食材(含名字带油的油菜/油麦菜)一律 100g。
- **宏量渐变条别"实块+糊接缝"**(显假)：改整条从每段**中心纯色**平滑过渡(`Brush.horizontalGradient` colorStops 放段中心+两端锚首末色)；单段给同色浅→深细微光泽；位置强制严格递增防极小段反转。
- **未保存返回守卫用统一件**：`rememberUnsavedGuard(isDirty,onConfirmLeave)` **非包裹式**(返回 requestBack)，套进大 Scaffold 屏不用重排缩进；顶栏返回 onClick=requestBack。餐食页 dirty=加载完 `markBaseline()` 记内容签名、与 `sigNow()` 比。
- **UI/交互门禁**：界面/交互类操作**编码前先 spawn Apple-UX 设计 agent 出规范**(读设计方案+现状代码)，据方案编码；纯文案/复用 §九 小改可免。**做 UI 先查 `交互组件复用指南.md`**(需求→22 组件对照 + B 批映射)，能复用必复用。

## B 批 UX + 双门禁 + 代码审查整改（2026-07-16 收尾）
- **两条强制门禁**：①UI/交互先 Apple-UX 设计 agent 出规范再编码；②一批编码后 Google 资深工程师审查 agent 把关最佳实践、阻断项必修复复验。做 UI 先查 `交互组件复用指南.md`(需求→22 组件)+§九(9.1–9.17)，能复用必复用。
- **删/还原类操作重复抬喜爱度陷阱**(Google 审查阻断项)：`saveDayMeals` 用"该日旧菜集"作基线判"新出现的菜"抬喜爱度；删整天撤销时该日刚被删空→旧集为空→**所有恢复菜误 +1** 污染排序/推荐。修法=`saveDayMeals` 加 `bumpPreference:Boolean=true`，**撤销/纯还原传 false**。同类红线见"移动日期重复抬喜爱度"。凡"还原/撤销"走 save 路径都要想：这是不是"新记一餐"？不是就别抬统计。
- **可逆破坏操作=软删+撤销**(§9.12)，别用"不可撤销"硬确认 AlertDialog：先 `snapshotDay` 快照→删→`showUndo` 点撤销 `saveDayMeals(bumpPreference=false)` 还原。**快照读失败/为空(异常吞成空)不该照删**致无法撤销(`getOrNull()+isNullOrEmpty` 守卫)；撤销带 action 的 Snackbar 用 `Long`(非 Short，留足点击)；统一宿主 showMessage/showUndo **单 job 串行化**(cancel 前一个)防未点的撤销被新提示挤丢。
- **未保存返回守卫抽复用件**(§9.17)：`rememberUnsavedGuard(isDirty,onConfirmLeave)` **非包裹式**(返回 requestBack 给顶栏返回用)——套进大 Scaffold 屏不用重排缩进；比包裹式 content lambda 好用。dirty 用"加载完记内容签名 `sigNow()`、与当前比"。
- **卡片加可选能力槽**：`IngredientCard` 加 `footer:(@Composable ()->Unit)?=null`(库存Tab 就地加减 MiniStepper 用)——能力由回调/槽传入决定，null 默认不影响其它调用点(复用红线)。
- **统一 CompositionLocal 宿主**：`staticCompositionLocalOf<X?>{null}` + 各屏 `?.` 调用(宿主未挂不崩)，Provider 挂在 MainScaffold NavHost 外层覆盖全屏；注意 CompositionLocal **不跨 Activity/独立窗口**(Dialog 一般 OK)。
- **全屏 `Dialog` 遮住共享 Snackbar 宿主**：CompositionLocal 能被 Dialog 继承(能取到 `LocalAppSnackbar`)，但 `MainScaffold` 的 `SnackbarHost` 渲染在 Activity 内容里、**在全屏 Dialog 窗口下方→Snackbar 弹出不可见**。弹层内的即时反馈用 **Toast**(系统窗口层，盖在 Dialog 上；纯告知无跟进项 §9.12 允许)或在 Dialog 内自挂一个 SnackbarHost；别指望共享宿主。
- **表单"保存并继续"范式**(§9.13，B-6)：底部常驻 `FormBottomBar`(`primaryText/onPrimary`胶囊 + 可选 `secondaryText/onSecondary`次文字，仅两者都传才显=能力由回调决定)；主 CTA 从顶栏下移底部、滚动 Column 改 `weight(1f)` 让底栏常驻。存成功后**留页复位**难点：①字段 `remember(ingredient?.id)` key 恒 null 不 remount→须**显式 `resetForm()`** 逐项赋默认(含 `unitTouched/groupTouched=false` 恢复按名/按类预选、`moreExpanded=false`)，别只靠 remember key；②**关弹层信号与复位信号分流**——VM 加独立 `continueSavedNonce`(keepOpen 成功+1)，`keepOpen=true` 时**不置** `lastSavedIngredientId`(否则触发 screen 的关弹层 `LaunchedEffect` 误关)；弹层 `LaunchedEffect(nonce)`+`savingContinuation`+`lastHandledNonce` 守卫(本弹层点过且 nonce 真增才复位，不依赖"nonce 仅成功递增"隐蔽不变量)。复位后 `FocusRequester.requestFocus()` 包 `runCatching`。
- **健康定性(忌口/限量/调养)按主料 is_main 判定，别按"菜里是否含该食材"**(2026-07-16)：克数极少的辅料/点缀会主导定性失真(「咸肉炒木耳娃娃菜」木耳50g配料却显"忌木耳")。规则层无逐食材克数→用已 populated 的 `is_main`(role==MAIN)作占比代理，忌口/限量/调养只算主料；无主料标记的菜不触发定性(宁可少误报，家庭 app 有免责)。**多消费点须同口径**：`HealthRuleEngine.evaluate`+`RecommendationDataSource.gatherForPlan`(联动 `PeriodPlanner` hasAvoid 硬剔)+`DishDetailViewModel`(详情页展示)——漏一处会"详情显忌木耳、推荐不忌"自相矛盾。调料忌口仍走 `cookingCautions`("少盐/少糖")不判菜品忌口(不变)。主料语义统一=isMain。
- **"存为菜品/预填新建"复用 `NewDishPrefillBus`**：跨屏预填新建菜品统一走该总线(单例 `single`)——`request(NewDishPrefill(name/ingredients/cookingMethodName))`→导航到新建页，`NewDishScreen` 在纯新建分支 `LaunchedEffect` 消费 `pending.value` 调 `applyPrefill`。加做法预填=`NewDishPrefill` 加 `cookingMethodName` + `applyPrefill` 里 `addCookingMethod`(放 `markBaseline()` 前使预填计入基线，不误判未保存 §9.17)。食材名→Ingredient 用 `createUserIngredient`(按名复用 id)。异步解析(IO)+导航加**防抖门闩**(主线程 `var building`)防连点压双栈。自由搭配/食材"组成菜品"/搜索"点此新建"三处同构。
- **慢病定性评级(如痛风嘌呤)按主料关键词匹配**：无国标数值阈值的指标(嘌呤三级无国标)**不做数值分级、走定性清单**——`NutritionLevelEvaluator` 加"应避免"关键词表(内脏/浓肉汤/部分海鲜)+`matchHighPurineFoods(名单)`,evaluate 加可选 `highPurineHits` 参(默认空、旧调用零影响),GOUT 分支 gate `病种登记 && 命中`。**只匹配主料名**(与"健康定性按主料判定"口径一致),caller(HomeVM)从今日 mains 匹配。关键词 `contains` **避免单字泛词**(删"凤尾"防误伤凤尾菇、"肝/胰"单字须注释标依赖现网无非高嘌呤含字食材)。守免责(定性非数值、个人视角、仅供参考)。
- **"一键 chips 快捷加"交互模式**(2026-07-16 餐次常吃 chips)：高频"从候选里挑加进当前容器"(加菜/加食材)可在容器内直接放一行**横滚 FilterChip**,**点即提交并从列表过滤移出**(移出本身=天然"已加"反馈,无需常驻选中态`selected=false`)+`leadingIcon`加号示意可点+`widthIn(max)`+Ellipsis 防超长撑满+**无候选整行不渲染(不占位)**。候选流 `combine(observePopular(喜爱度), observeRecent兜底)` 去重 take,**过滤"本容器已选"放 UI**(每容器已选不同)、`remember(候选,已选)` 缓存派生。误点高频→**撤销优于确认**(Snackbar,不弹确认框)。设计门禁建议的 `SectionHeader` 若自带 padding 会在卡内双重内缩→改克制内联小标签(labelMedium)。少操作:4步(点添加→开选择器→多选→确认)压到1步。
- **撤销 Snackbar 必须"单 job 串行化"的又一场景=连续操作**(2026-07-16)：局部宿主也要守 §9.12——`var undoJob; undoJob?.cancel(); undoJob=scope.launch{ snackbar.showSnackbar(...) }` 抽成屏内 `showUndoLocal(msg,onUndo)` 供加/删共用;否则裸 `scope.launch{showSnackbar}` 连点多次(连加/连删)后**新提示无声挤掉旧撤销条**,只剩最后一次可撤,前几次撤销窗口被吞(数据无损但撤销名存实亡)。Google 审查按红线判为"唯一值得交付前顺手处理"。
- **加 DB 索引先盘"复合PK/唯一索引的非前导列"**(2026-07-16 v24)：SQLite 复合 PK/UNIQUE `(a,b)` 只服务**以 a 前导**的查询，按 **b 单独过滤/JOIN 则全表扫**。盘点缺索引=找"按非前导列查"的热路径：`ingredient_category` PK(ingredient_id,category_id)→按 category_id 列食材缺;`meal_record_dish` PK(meal_record_id,dish_id)→按 dish_id 反查(最近吃/做过几次)缺;`dish_ingredient` uq(dish_id,ingredient_id)→按 ingredient_id 反查/删缺;各多对多 rel 表(dish_tag_rel/dish_cooking_method_rel/favorite_combo_dish)同理。已被覆盖别重复加:name/nutrition.ingredient_id 等有 UNIQUE/PK。迁移=**纯 CREATE INDEX IF NOT EXISTS**(幂等·不改表结构不动数据=最安全,老列无"列不存在"风险,名全新无冲突)+ .sq 同步(新装 Schema.create);**不需 SEED_LOGIC_VERSION bump**(纯 schema 非 seed 逻辑)。软删表别急着上 `(col,status)` 复合索引(status 选择度差、写放大大,单列+status 过滤已够)。真机验:老库升级初始化正常 + EXPLAIN QUERY PLAN 确认走索引。
- **慢病数值软降:"无处不在的调料属性"(钠)不做菜级软降,"绑定主料的食材属性"(GI/嘌呤)才做**(2026-07-16 慢病软约束)：菜级 `sodiumMg` 含盐/酱油等调料无法拆、**几乎每道热菜都放盐**→按菜级钠软降=惩罚每道菜=退化成常数偏移=排序无变化=失效（红线"盐对高血压忌口让每道菜都忌口"的数值翻版）。钠靠 **care limit + `cookingCautions`(少盐/少酱油做法提示) + 今日卡钠占比**(已上线)覆盖高血压,别在推荐软降里做钠。GI/嘌呤是"绑定特定主料"(matchHighGi/Purine 按主料名),软降天然排除调料、不误伤普通炒菜,适合。判据:该指标是"调料带来的(无处不在)"还是"主料本底(绑定特定食材)"。
- **"静默的排序微调"在健康语境有操纵感→弱可感知的干预不该默认开、用 opt-in 风格作载体**(2026-07-16)：忌口能默认生效因**强可感知**(标红+排末,用户一眼看到 App 在干预);软降是**弱可感知**排序微调,"默认对病种静默重排"=用户没要也察觉不到、App 悄悄改选择顺序=paternalistic nudging without disclosure(信任风险)。裁决:软降因子**默认权重0**,**仅"营养优先"风格给正权重**(用户主动选风格=知情同意、语义自洽、零新增开关、无需现场提示)。越轻越隐蔽越不该默认开(反直觉但对)。
- **分层排序用布尔判据(非分值)→附加的减分因子"数学上不可能越层"**(2026-07-16)：`sortedWith(compareBy({avoidNames.isNotEmpty()},{isRecent},{-score}))` 分层判据是**布尔**(是否忌口/最近),与 score 数值无关→新加的软降/任何减分因子**只在同层内**动 score、**不可能**把正常菜推进忌口层。这比"penalty<avoid 数值量级"更强的保证(后者只保同层可比)。加软降类因子时,确认分层判据是布尔而非靠分值大小分层,就不必担心"减分把菜挤进错误分层"。
- **接 backlog 老项先"摸现状"再动手——可能已大半完成、真缺口不是描述那个**(2026-07-16 菜系分类)：菜系分类 backlog 写"方案A/B待拍板",实际 dish.cuisine 列+预设100%覆盖+菜系Tab+来源标注+底层管道**早已完成~70%**,真缺口只是"自建菜 cuisine 恒空→在菜系Tab看不到"。**多角色验证(产品+UX)**收敛出**最小打磨**(默认家常菜+去稀疏Rail)而非重做,避免过度设计。教训:老 backlog 项先探索现状,别照描述从零做。
- **"决策执行偏差":原决策本意 ≠ 落地方式,给合理默认优于留空**(2026-07-16)：`ab1ca32`本意"不强迫用户选菜系"(对,家庭菜手选川/粤是伪精度必填),但执行成"自建菜没有菜系"→制造菜系Tab可发现性漏洞。正确落地=**给合理默认值(家常菜,家庭菜天然家常=正确默认非妥协)**,而非留空。凡"不强迫填"的字段先想"有没有正确默认",别直接留空致下游割裂。
- **"内容决定结构"别把固定枚举列死当骨架**(2026-07-16 CuisineRail)：11 菜系固定竖列里徽菜4道/西餐1道=点进去只几道菜的稀疏死胡同,每点一次是挫败。改**从实际数据动态派生"有菜的项"**(`observed.mapTo(HashSet){it.cuisine}` 再按固定顺序 filter,空项自动不显、随数据增长自动出现)。派生源用**全量 observed 非搜索态 raw**(Rail 要稳定不随搜索变);选中项失效(该分类归零)要**局部回落**(`selected?.takeIf{ it in set }`,不回写 flow 避免再组合)防"无高亮+右侧空"自愈死角。
- **"全 App 开关去噪"类改动 gate 覆盖面必须做"消费点普查"、别只 gate 显式 Tab/入口**(2026-07-16 库存挂钩开关，Google 审查抓 2 阻断)：散落在**通用组件(详情弹层/卡片行/搜索面板/功能介绍页)**里的同类痕迹最易漏——库存开关首版漏了 IngredientDetailSheet 入库出库、SearchResultsPanel 入库出库、FeatureGuideScreen"库存推荐"介绍卡。做法：①先 grep 关键词(库存/入库/出库/采购/缺料/在手/份数…)**普查所有可见路径**再逐一 gate;②**抽 `rememberXxxFlag()` helper 统一读**(6+ 处 `koinInject+observeFlag+collectAsStateWithLifecycle` 逐字重复=改一处漏五处的一致性风险根因);③**能力显隐由回调决定**的组件(IngredientDetailSheet)关时**传 null 回调**即可、不改组件;④**变灰(缺料 alpha)必须与标注同生同灭**(库存关无"缺料"概念,还灰=莫名);⑤**依赖同一数据源的衍生功能一起处理**(食材自由搭配依赖在手食材→库存关一并隐藏,别留非功能态空壳);⑥VM 默认态是隐性入口(推荐 mode 默认 PANTRY→start() 关时也要 RANDOM 起步,否则首帧仍触达"已隐藏"路径)。**default=true 保留现有行为**(既有功能的可选关闭,老用户升级不丢),各消费点 observeFlag/collectAsState 初值都传 true。
- **同一慢病信号在多消费点=两套来源分工互补别重复**(2026-07-16 详情页GI/嘌呤)：详情页已有"逐食材 care 忌口"(人工策展`care_category`清单 avoid/limit)，再叠加 data-driven 的 GI≥70/嘌呤关键词定性→**必然重叠**(白米饭既在糖尿病 limit 又 gi≥70)。做法=**GI/嘌呤只补 care 漏网的**：命中后 `filterNot{ 已在 avoid∪limit }` 去重(care 更强定性优先)，两块信息各司其职、用户不见同一食材两次。**去重两侧统一 trim 归一比对**(别依赖"两侧都不 trim"隐含前提,日后一侧加归一会静默失效——Google 审查最高优先建议)。UI 上 care 是 `when` 互斥单行、GI/嘌呤是**独立维度**(可与忌口并存)另起独立 `InsightLine`。
- **同信号多页口径差异属"视角不同"可不强统一**(2026-07-16)：今日卡按**关注成员**(family focus)病种、详情页按**全家 enabled 档案**病种——差异合理(详情=菜品对全家档案、今日卡=关注成员当日摄入),关键是**详情页与其自身 avoid/limit 同源自洽**(都来自 profiles),别为对齐今日卡改口径致页内两套标准。注释说明差异即可,别硬统一。
- **androidApp 无 VM 单测台→把有分支的逻辑下沉 shared 纯函数补测**(2026-07-16)：详情页 VM(androidApp)无测台,聚合+去重这条唯一有分支的逻辑抽成 `NutritionLevelEvaluator.dishQualitativeHits(纯函数)` 放 shared,VM 只做"取依赖→调纯函数→塞字段"胶水;shared 纯函数加单测覆盖 gate/去重/边界,补上无测台盲区(架构角色建议)。
- **慢病评级"定性(关键词) vs 定量(按名查真实值)"看有无实测数值**(2026-07-16 DIABETES GI 分支)：与痛风嘌呤(无国标阈值→关键词定性)相反,**GI 有已在库实测/惯例数值**(`ingredient_nutrition.gi`)+FAO/WHO 明确阈值(低≤55/中/高≥70,**非 WS/T652-2019**——该标准只规定测定方法)→应**按名查真实 gi 值判高GI**,别用关键词。落地同 P4 骨架:`matchHighGiFoods(mains, giByName)`(精确 `Map` 查、`giByName[n.trim()]>=70`)+evaluate 加可选 `highGiFoods` 参(默认空、缺数据不下调向后兼容)+DIABETES 分支 gate `病种登记 && 命中`+caller gate 病种才查表(省无谓全表加载)。`NutritionRepository.giByName()` 名→gi **同名多形态取较高值**(偏保守·高GI提示宁多勿漏)、**key trim 归一**(红线:去空格比对,精确 Map 查比痛风 `contains` 严须两侧 trim 防漏匹配)。提示措辞"可换低GI或控量"(个人视角·非医嘱)。**至此高血压/痛风/糖尿病三维均进 evaluator 各有单测**。
- **首次启动引导**：`PreferenceKeys.HAS_SEEN_GUIDE` 标记 + `MainScaffold` `LaunchedEffect(Unit){ if(!observeFlag(...).first()){ setFlag(true); nav.navigate(FEATURE_GUIDE) } }`——**先置标记再导航**防重组重复触发；复用现有功能介绍页(不新建 onboarding UI)、已看过不打扰、仍可从"我的"手动看。
- **双模式复用组件(如 `IngredientPickerScreen` Tab落地/弹窗选择器)加大标题**：按 `selectionMode` **分叉顶栏**——Tab 模式(selectionMode=false)用 `LargeTopAppBar`+`scrollBehavior`，弹窗模式(true)保留原 `TopAppBar`(返回+整行搜索)；**共享主体(搜索面板/主Tab/网格/底栏/详情弹层)完全不动，只改顶栏那一段**，弹窗核心流程零回归。`LargeTopAppBar` **可非 Scaffold 用**：`scrollBehavior` + 容器(Surface)挂 `nestedScroll(scrollBehavior.nestedScrollConnection)`，滚动子(含 `LazyVerticalGrid` 在 `Row(weight)` 内)的 nestedScroll 冒泡即驱动折叠；内容不足一屏优雅退化为常驻大标题(功能不坏)。弹窗模式 `.then(if(!selectionMode) Modifier.nestedScroll(...) else Modifier)` 不挂。

## 2026-07-18 会话·可复用踩坑（图片/计时/推演/序列化）
- **到点全屏只弹第一个、后来的没进/停不掉**：Activity 已在前台时，后来通知的 `setFullScreenIntent` **不再拉起 Activity、也不触发 `onNewIntent`**（系统对已有前台的全屏意图抑制）→ 依赖 intent 刷新列表就漏掉第2个。解法：让接收器 companion 持 `MutableStateFlow<List<(id,名)>>`，`fire`/`stop` 变更即 `emit`；Activity `collectAsState()` 实时增删、停到空 `finish()`——**不靠 intent 驱动多实例聚合，用共享 StateFlow**。（华为不弹全屏≠代码问题，是 A14 `USE_FULL_SCREEN_INTENT` 需授权 + 华为"后台弹窗/锁屏通知"OEM 限制，小米默认给了才弹。）
- **到点全屏"多个没逐个进/停不掉铃"更深根因=两套"到点响铃"机制打架**(2026-07-18 修 StateFlow 版后仍复现)：前台服务保活进程→`CookingTimerScreen` 里每秒 tick 的 `LaunchedEffect` while 循环**息屏后仍在跑**(进程没冻结)。每个计时器到点走**两条完成路径**：①系统闹钟 `TimerAlarmReceiver.fire`→`putActive`+`emitActive`→全屏 `activeAlerts` 显示；②Compose 前台循环调 `TimerAlarm.cancel(id)`(内部又 `TimerAlarmReceiver.stop` **把刚进 activeAlerts 的项删掉+停铃**)，改用 Compose 本地单例 `activeRingtone` 自响。→第2个一进 activeAlerts 就被①删(bug1)；真响的是 Compose 单例铃、全屏"停止"只停 `receiver.active`(已空)→停不掉(bug2)。解法：**响铃+告警列表收敛到 `TimerAlarmReceiver` 单一真相源**——`fire` 抽 companion `ring()` 供闹钟与前台循环共用；前台到点改 `cancelSchedule`(只撤调度不停铃)+`ring`，用 `if activeAlerts.value.none{id}` 守卫防双响(`putActive` 内先停旧铃兜底去重)；收集**多个**同 tick 归零逐个 ring(别只处理最后一个)；移除 Compose 本地 `activeRingtone`；加对账 `LaunchedEffect(activeAlerts)` 把已不在响的 FINISHED 复位。教训：**同一"到点"事件别开前台/后台两套响铃机制**，前台保活会让 Compose 循环在后台照跑、与系统闹钟撞车。
- **进程级共享单例状态被"广播线程+Compose协程"两路读写→必须加锁或显式线程约束**(2026-07-18 Google审查抓阻断)：`TimerAlarmReceiver` companion 的 `active` map + `_activeAlerts` StateFlow，被 `onReceive`(系统广播·主线程)与前台 tick 循环(Compose Main 协程)两路 `putActive/stop`(多步非原子:停旧铃→put→emit 遍历 map)。**"当前恰都在主线程"是隐式假设、无代码约束**——循环日后挪 `Dispatchers.Default` 或广播 `goAsync()` 即 `ConcurrentModificationException`/铃句柄泄漏(偶发难复现)。解法：`synchronized(lock)` 包 put/stop/emit(emit 命名 `emitActiveLocked` 标注须持锁)，**慢/外部调用移出锁外**(`NotificationManagerCompat.cancel` 放 synchronized 之外，锁只护内存态)。教训：companion/object 里被多执行上下文读写的可变状态，别赌"都在主线程"，加锁做硬保证。
- **取铃失败(getRingtone null)别让告警"到点像没反应"**(2026-07-18)：`ring` 里响铃包 `runCatching{}`，若 `ActiveAlert` 只在 `ringtone!=null` 时 `putActive`→取铃失败该 id 永不进 activeAlerts→对账 Effect 见其 `!in ringingIds` 立刻把 FINISHED 复位 IDLE=到点没动静。解法：`ActiveAlert.ringtone` 改**可空**、**无论取铃成败都 `putActive`**(可空=静默告警,通知仍发、全屏仍显示可停)。凡"有铃才登记"的告警态，要想"取不到铃时告警是否还该成立"。

## 2026-07-18 会话·菜名推食材"推出即自动加入 + 库外保存时自建"
- **本地推演识别"库外食材"=库内匹配后剩余段 + 停用词剔除**(无 AI 无大词典)：`DishNameIngredientGuesser.guessDetailed` 三步——①库内食材名长名优先子串占位匹配(inLibrary=true)②剔除停用词(内置烹饪/口味词"红烧/香辣/宫保…" + 调用方传**烹饪方式字典**作 extraStopWords)占位③剩余连续中文段(≥2字)作库外候选(inLibrary=false·待自建)。停用词**刻意不含本身是食材的词**(冰糖/红油/咖喱)——"宁漏勿误"，避免 garbage 自动建库(因为库外候选会 createUserIngredient 落库)。AI 更准的分词归 backlog。
- **"待自建/占位食材"用负 id 作信号、保存时按名换真 id**：库外候选加入清单时用 `Ingredient(id=负数, name)` 占位(`pendingIdSeq--`)，UI `pendingCreate = ing.id<=0`(真实食材恒 id>0·AUTOINCREMENT)显"待自建"胶囊；`save()` 前把 id≤0 项 `createUserIngredient(name)` 换真 id(自动按名关联分类+营养)、失败丢弃不写坏 FK。`saveDish` 直接用 `di.ingredient.id` 落库，占位负 id 必须换掉否则 FK 坏。
- **占位按名解析可能撞上清单已有真实食材→`INSERT OR REPLACE` 覆盖用户已调克数**(Google 审查抓)：`uq_dish_ingredient(dish_id,ingredient_id)` 唯一索引 + `INSERT OR REPLACE`，若清单里已有真实"排骨"(用户调 300g)、又自动加了占位"排骨"(默认 100g)、占位解析复用到同 id→两条同 id→后者覆盖前者克数。解法：换 id 后**按 id 去重、优先保留"非占位"(用户手动/已调克数)那条**(`LinkedHashMap`+`fromPlaceholder` 集，遇非占位覆盖占位)。凡"占位/自动生成项最终会解析成真实 id"的场景，落库前必按真实 id 去重、且定义好"撞车留哪条"。
- **归一比对要两侧一致**(Google 审查抓·呼应老红线)：guesser 候选名 `replace(" ","")` 去了空格，VM 里 `search(name).firstOrNull{it.name==c.name}` 用**严格 ==** 匹配原始库名→含内部空格的库名("西 红柿")漏配、甚至误落库外分支自动建重复食材。解法：VM 侧也按去空格归一名比对(`it.name.replace(" ","").trim()==c.name`)。改一侧归一必改另一侧。
- **"推出即自动加入"防抖 + 仅新建模式 + 只加不删**：`setName`→`scheduleAutoAddFromName`(cancel 旧 job + `delay(350)` 防抖，避免逐字命中弹多次)→`autoAddFromName`(suspend)。编辑既有菜(`editingId!=null`)**双重早退**(schedule + autoAdd 都判)，且 `loadForEdit`/`importFrom` 不经 setName→不会把用户删掉的食材加回来。自动加入**只 `s.ingredients + add` 不删**(用户可能已调克数)；挂起期(search 多次)后 `_state.update{}` 内用最新 state 重算去重(守"别捕获快照经挂起写回"红线)。Snackbar 一次性(`autoAddSerial` keyed)、库外项文案说明"将在保存时加入食材库"。

## 2026-07-18 会话·通知崩溃/特殊人群/菜名打磨
- **A12 从 Service(非Activity上下文)启 Activity 的通知 openPi 缺 `FLAG_ACTIVITY_NEW_TASK` 会闪退**(真机报)：前台服务通知 contentIntent 点击→`PendingIntent.getActivity` 只给了 `FLAG_ACTIVITY_CLEAR_TOP`→A12 抛 `AndroidRuntimeException(Calling startActivity() from outside of an Activity context requires FLAG_ACTIVITY_NEW_TASK)`。修：补 `NEW_TASK or CLEAR_TOP`(与到点闹钟通知 openPi 一致·那个有 NEW_TASK 所以不崩)。凡从 Service/Receiver 的 PendingIntent 启 Activity 都要 NEW_TASK。
- **健康状态扩展特殊人群走"轻正向"零 schema**：crowd 维度(`food_category dimension=crowd`)本就设计承接"人群"，加人群=加 crowd 分类 seed + `ingredient_care_rules.json` care 规则(avoid/limit/recommend)，**`selectCareCategories` 返回全部 crowd→健康档案自动可选**(无硬编码)、经 `HealthRuleEngine` 生效忌口并集+调养推荐，**不进 `NutritionLevelEvaluator`/色系墙**(正向达标数据不牢+健康敏感→隔离)。care 规则用分片 agent 按《膳食指南2022》各专册整理(每食材名须在库·校验0不匹配·宁缺勿滥·守免责禁"助孕/长高/预防"断言·孕婴遵儿保科)。
- **菜名推食材打磨=扩双字做法停用词 + 拒绝以做法/连接字开头的库外候选**(真机报"醋溜排骨→醋溜、毛豆烧鸡→烧鸡"误当食材)：`COOKING_LEADING_CHARS`(烧炒煮蒸焖…包夹酿…)只拒**开头**命中(叉烧/面包做法字在词尾仍作合法候选)。单字主料(鸡/鸭/鱼)因≥2字过滤+不在库,救不回=库缺(记"对话食材补充清单")。本地启发式局限,更准归 AI。

## 2026-07-18 会话·健康状态深挖·P3高血脂数据覆盖补齐
- **新加评级维度别只加列+逻辑,要查数据覆盖率——否则"评级半残"静默不触发**：P3 高血脂加了 `saturatedFat`/`cholesterol` 列+评级分支,但实测营养 seed 覆盖率仅 15%/10%(新列旧食材全 NULL)→**85-90% 的菜缺数据向后兼容=不下调=高血脂评级形同虚设**。深挖健康维度先跑覆盖率统计(`python 数字段计数`),找"逻辑齐全但数据空"的半残维度。GI/纤维 63-65% 同理待补。
- **补权威健康数据用分片后台 agent 联网(115条/5片)**:守红线——中国食物成分表优先/USDA 兜底、**素食胆固醇=0(权威事实可直接填)**、饱和脂肪多数成分表只给"脂肪酸占比"需×总脂折算(标 pending)、复合食品(饺子/馄饨)查不到**省略不硬凑**、一手权威才 verified。python **覆盖式合并只填缺失(None)不覆盖已有值**、`ref`/`review` 保留老值(sat/chol 是补充字段·全局免责已覆盖估算)。
- **合并健康数据必做"物理约束校验"防呆**：`saturatedFat ≤ fat`(饱脂不可能超总脂)——合并时拦下 agent 给的错值(羊排 sat 7.0>脂 3.9 被弃),复检还揪出**存量**错误(培根 sat 14.4>脂 9.0·前批遗留)删掉。凡合并外部数据,先想"有没有该字段间的物理不等式"当校验闸。
- **营养 seed 是内容变非逻辑变→指纹自动变→老库重 seed,无需 SEED_LOGIC_VERSION**;`upsertIngredientNutrition` 按名全字段更新,补的数据能到老用户。

## 2026-07-18 会话·连续多段倒计时(手动接力)
- **多段"手动接力"状态机=每段一个 endAtElapsed + currentSegmentIndex**：`CookingTimerItem` 加 `segments`/`currentSegmentIndex`；`runSegments`(空退化单段·恒≥1元素保 `.first()` 安全)。运行流：onStart 从当前段(`runSegments[currentSegmentIndex].seconds`)起 schedule；段到点→tick 置 FINISHED+ring(复用统一 ring)；**onNextSegment**(用户点"停铃·开始第N段"才进)=cancel 当前段铃 + index+1 + schedule 下一段 + RUNNING；onStop=停止全部复位段0;末段回归纯"停止"。每段各自守 elapsedRealtime 墙钟(非 delay 递减)。单段(`isMultiSegment=false`)完全走原路径零回归。
- **cancel 触发对账 Effect 与状态迁移之间禁插挂起点**(Google 审查 P1·呼应"cancel→activeAlerts 变→对账"红线)：onNextSegment/onStart/onStop 都先 `TimerAlarm.cancel`(→`receiver.stop`→`_activeAlerts` 变→对账 `LaunchedEffect(ringingAlerts)` 排队重跑)，紧接着**同步** `timers=map{}` 置 RUNNING/IDLE。对账重跑读到的已非 FINISHED 故不复位——但这依赖"cancel 与赋值之间无挂起点"，**必须显式注释该契约**(后人插 `scope.launch`/suspend 即误复位)。对账复位判据加 `atLastSegment`：多段非末段完成态**保留**(等用户点接力/停止全部)，只复位单段/末段。
- **多段运行态持久化必须存 currentSegmentIndex**(Google 审查 P2·中危真实缺陷)：running 持久化原只存 `id:endWall`，多段跑到第3段被杀重开→currentSegmentIndex 恒 0→段指示/进度点显"第1段"但 remaining 是第3段的=**自相矛盾**，且 onAcknowledge 从错误段接力→段数错乱。修：格式扩 `id:endWall:segIndex`(parse `getOrNull(2)?.toIntOrNull()?:0` 兼容旧格式)、恢复 `coerceIn(0,lastIndex)` 防越界、持久化 Effect key 加 currentSegmentIndex。凡"运行态跨进程恢复"，**所有决定后续行为的状态位都要持久化**，只存时间不够。
- **Compose 段编辑器用"普通类 `by mutableStateOf` + `remember{mutableStateListOf}`"**：`SegmentDraft`(name/minText/secText 各 `by mutableStateOf`)放 `remember(timer.id){mutableStateListOf(...)}`，字段改直接 `draft.x=`(本地态单一真相源·守多字段回写红线)、保存统一从 drafts 读；`removeAt(idx)`(idx 来自 forEachIndexed 当帧)SnapshotStateList 删后自动重组重索引，安全。key `remember(timer.id)` 保证换 timer 重建。
- **名称归一修饰词剥离过度**：`NutritionGuesser.coreName` 的 MODIFIERS 含单字"土"(用于土鸡蛋/土猪肉)，把**"土豆"剥成"豆"(1字)** → `core.length<2` 跳过匹配 → "暂无同名食材"(其实预设有土豆)。解法：匹配**同时用原名 raw(不剥词)+剥词 core 两把 key 取更高分**(raw 兜"土豆/山药/大枣"整词以修饰字开头的精确命中)、且 coreName 剥离后**须仍≥2字**。凡"整词恰以修饰字开头"都要防误剥。
- **kotlinx.serialization `encodeToString/decodeFromString` reified 重载歧义**(commonMain)：`json.encodeToString(list)` 可能解析到 2 参重载报 "No value passed for 'value'"。用**显式 serializer**：`ListSerializer(X.serializer())` + `json.encodeToString(ser, v)`/`json.decodeFromString(ser, s)`，避免 reified 依赖 import。
- **缩略图糊 = 存储小 + 显示再降采样双重**：缩略图存 `THUMB_MAX_SIDE=360` 已小，`StoredImage` 显示又 `inSampleSize=4`(→~90px) → 放大糊(原图清晰=另一路)。解法：存储提到 800、显示解码 `/4→/2`、`LruCache` 从固定条数改**按字节** `sizeOf=w*h*4`(大图别用条数缓存)。缓存 key 混采样级(`thumb2:`)防日后串味。
- **Compose foundation 1.5 `HorizontalPager`**：pageCount 通过 `rememberPagerState(initialPage){ count }`(**尾 lambda**)传，不是 `HorizontalPager(pageCount=...)`(那是旧重载·报"use overload without pageCount")；`@OptIn(androidx.compose.foundation.ExperimentalFoundationApi::class)`。
- **中医食养术语守免责**：用户维度叫**"食养"不叫"食补"**(食补=进补见效义、踩疗效暗示)，食材标签用官方原词**"药食同源"**；食养/药食同源**物理隔离慢病评级**(只正向排序展示、绝不接忌口/限量/评级/罚分)，且 grep 确认 `ai/` 全包无 `MedicinalFoods` 引用。药食同源=卫健委法定食品认定**非疗效背书**，目录举例要补"不代表更营养或该多吃"。
