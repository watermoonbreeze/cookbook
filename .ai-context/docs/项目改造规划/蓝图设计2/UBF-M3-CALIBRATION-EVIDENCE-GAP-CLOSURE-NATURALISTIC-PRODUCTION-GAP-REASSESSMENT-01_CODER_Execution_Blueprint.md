# UBF M3 Naturalistic Production Gap Reassessment 01 — CODER Execution Blueprint

Task: `UBF-M3-CALIBRATION-EVIDENCE-GAP-CLOSURE-NATURALISTIC-PRODUCTION-GAP-REASSESSMENT-01`
Revision: `R1`
Actor authority: abstract `CODER`
Handoff Parent: `f4744068092a8af89e44f0d1920b14a4050e3887`
Review mode: `REMOTE_READ_ONLY_ARCH`
Payload mode: `AUTHORITATIVE_STATIC_TARGET_BUNDLE / ADAPTER_INDEPENDENT`
Normal final scope: exact `10 paths = 5A + 5M` (`9` static exact blobs + `1` runtime-provenance schema target)

## 1. Objective

Mechanically persist the already-completed ARCH reassessment: backfill Family-C Pair-Seal ACCEPT, record both NP gaps as `OPEN_WITH_AUTHORIZED_NON_MANUFACTURING_CAPTURE_PROTOCOL`, install the passive capture contract, preserve H4 and keep reanalysis/later stages unauthorized.

CODE does not search for evidence, reinterpret candidates, run a probe, manufacture a production event, add a corpus row, or choose any Universal Level/Profile/Selector/routing result.

## 2. Entry and isolation

Parent `TURN=REVIEW` is expected. This package grants one exact State-only REVIEW→CODE claim. Dirty/ahead/behind/diverged host state and a failed pull are not Hard STOPs; preserve the host and execute in a clean temporary clone or isolated detached worktree at the exact parent. A valid in-flight R1 State-only claim may be resumed without a second claim.

No `ARCH_PRIVATE/`, Blind Reveal, concrete sealed actor, response, nonce or capability result is required or authorized. Requesting such input is a protocol error.

## 3. Exact execution

1. Fully extract/read the package and verify inventory.
2. Verify remote `origin/master` equals the Handoff Parent or the exact valid R1 claim.
3. Materialize, commit, push and remotely verify the State-only claim.
4. Apply the exact nine-file static target bundle.
5. Generate truthful runtime provenance using the supplied schema adapter.
6. Stage exactly `10 paths = 5A + 5M`; verify target blobs, runtime schema, Preserve, no private leakage, clean diff and lifecycle coherence.
7. Commit/push the final and return only the full final hash.

## 4. Semantic gates

- Pair-Seal `f4744068…` is persisted as ARCH ACCEPT / consumed by this reassessment.
- Lane MC remains complete at two qualifying families.
- Existing qualifying NP G05/G06 clusters remain `0/0`.
- Both gaps remain OPEN under passive non-manufacturing capture.
- This task adds `0` acquisition runs, `0` empirical rows and `0` manufactured events.
- Existing corpus/analysis/BAP/family Truth/reveals/commitments remain Preserve.
- H4, zero Universal decisions, reanalysis prohibition, M4/M5 and Phase 3B gates remain exact.

## 5. Hard STOP boundary

Hard STOP only for a true remote parent/in-flight-claim mismatch, invalid package/revision/delegation identity, required parent preimage/Preserve mismatch, inability to establish safe isolation, inability to prove the exact final changed set/blob/schema, confidentiality breach, or failed remote publication. Helper/path/tool incompatibility uses the package-authored native fallback and is not itself Hard STOP.

After a valid claim, a true Hard STOP uses the exact State-only abort artifact and returns REVIEW. No semantic improvisation, amend, rebase, reset, force-push or history rewrite is authorized.
