# UBF-M3-CALIBRATION-EVIDENCE-GAP-CLOSURE-PREVIEW-START-01 — CODER Execution Blueprint

Document Role: Mechanical Persistence Blueprint
Revision: `R1`
Handoff Parent: `bbd8bbbd5c97a9faef62fde50971a586322e625d`
Review Mode: `REMOTE_READ_ONLY_ARCH`
Execution Actor: `CODER`
Concrete Model Binding: `NONE`

## 1. Purpose

Mechanically persist the ARCH-authored Evidence Gap Closure Preview/Start contract. Do not acquire evidence or reinterpret H4.

## 2. Abstract actor contract

This package binds authority to `CODER`, not Luna/Terra/DeepSeek/any other concrete model. A concrete model is runtime provenance only. Capacity/availability substitution does not require repackaging while package/revision/parent/target/turn truth remains valid.

## 3. Valid holder transaction

Parent `TURN=REVIEW` is expected.

- `PARENT_REVIEW` -> materialize exact State-only claim -> push+remote verify -> CODE becomes effective.
- If the same revision already has an exact valid State-only claim pushed, a replacement concrete coder may resume the same `CODER` turn without a second claim.
- final/authorized abort returns REVIEW.

Dirty/behind host worktree is not a blocker. Preserve it; use isolated detached clean execution.

## 4. Final scope

Exact final scope: **9 paths = 4A + 5M**.

Eight static authoritative targets must match exact expected blobs. One Runtime-Provenance JSON is schema-constrained runtime evidence.

## 5. Non-scope

No empirical row, no evidence acquisition run, no controlled probe, no H1/H2/H3/H4 rewrite, no Level/Profile/Selector/model ranking/routing decision, no M4/M5, no CookBook Phase 3B.

## 6. STOP

Hard STOP only for remote-parent/claim identity failure, invalid delegation/package identity, target preimage mismatch, inability to isolate safely, or inability to prove final Git contents. Concrete-model capacity is soft compatibility, not a semantic STOP.
