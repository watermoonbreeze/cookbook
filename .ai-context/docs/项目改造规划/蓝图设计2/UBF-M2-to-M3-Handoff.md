# Universal Blueprint Framework — M2 → M3 Handoff

Document Role: Generated View / Stage Handoff

Parent Stage: `M2 — ACCEPT / CLOSED`

Child Stage: `M3 — Empirical Calibration Corpus`

Handoff Status: `PERSISTED / PENDING REMOTE ARCH REVIEW`

M2 Accepted Review Target: `84cd8508e213e3664ec898cd2b9a783570b28de5`

Persistence Task: `UBF-M2-END-ACCEPT-01`

M3 Status: `NOT STARTED / NOT AUTHORIZED`

CookBook Phase 3B: `NOT AUTHORIZED TO START`

## 1. Meaning

This generated view transfers accepted M2 boundaries into the future M3 entry gate. It is not canonical Truth, does not start M3 and does not authorize corpus construction before the persistence delivery receives remote ARCH acceptance.

## 2. Inherited accepted inputs

Future M3 must preserve and consume:

- M2 Final Accept and exact 48-record mapping artifact;
- 48 stable GC identities with project history, provenance, recurrence and exact Legacy grouping;
- 48 × `PRESERVE_IDENTITY + REDEFINE_LEVEL_RELATION`;
- 48 × Universal Mapping `UNRESOLVED`;
- M1 accepted 64-record semantic decomposition and its contradiction/gap/preserve boundaries;
- UBEA-v2 execution architecture and adapter-independent evidence contract;
- user-level canonical identity boundaries, project overlay authority and source-identity-discovery requirement;
- model evidence ledger as evidence-only Truth, not routing conclusion.

## 3. M3 objective

M3 will build an empirical calibration corpus to determine whether a one-dimensional Universal Level can form a monotonic, comparable ladder of architecture closure versus safely preserved coder decision space.

The future corpus must cover multiple task families and risk/complexity shapes; successful execution, REWORK, structured Q/STOP, scope escape and Blueprint/ARCH-payload defects; coder-supplied reasoning that improved architecture; different actor capability observations; and FULL/LITE carrier differences without treating package profile as Level.

## 4. Frozen M3 entry rules

- corpus schema, sample eligibility, evidence minimum, labeling authority, confound controls and acceptance tests must be architecture-authored before CODE persistence;
- historical outcomes must preserve attribution: architecture-authored package defects are not coder negative samples;
- adapter compatibility failures are not semantic/capability failures;
- GC/Legacy Level, risk, FULL/LITE and actor routing remain orthogonal observed variables, not preselected Universal Level answers;
- `UNRESOLVED` mappings remain unresolved until empirical evidence and later architecture adjudication support a ladder;
- no sample may silently upgrade an Acceptance Snapshot, generated view or model anecdote into canonical Truth;
- M3 Preview/Start is a separate transaction after this handoff is remotely accepted.

## 5. M3 Preview/Start prerequisites

Before a separate entry task may be issued, remote ARCH must verify this persistence transaction's parent→claim→final chain, exact seven-file scope, target blobs, State denyset, Preserve Set, whitespace, M2 acceptance truth, M2→M3 handoff truth and remote ref.

## 6. Non-scope

This handoff does not authorize M3 Preview/Start, corpus rows, Universal Level count/names/mappings, final Task/Capability Profile schema, selector logic, canonical mutation, State split, routing mutation, Project Graph mutation, production work, M4/M5 or CookBook Phase 3B.

After remote acceptance of this persistence, architecture may issue a separate `UBF-M3-PREVIEW-START-01`. No M3 work begins directly from this handoff.
