# UBF-M2-END-ACCEPT-01 Luna Execution Blueprint

Document Role: Architecture-authored Mechanical Execution Task

Mechanism: `STATIC_TARGET_BUNDLE`

Review Mode: `REMOTE_READ_ONLY_ARCH`

Delegation: `DELEGATED_SINGLE_TASK_CLAIM`

Execution Model: `GPT-5.6 Luna`

Handoff Parent: `84cd8508e213e3664ec898cd2b9a783570b28de5`

Expected Return TURN: `REVIEW`

## 0. Frozen architecture decision

Remote ARCH accepts M2 Work-01 at `84cd8508e213e3664ec898cd2b9a783570b28de5`. CODE does not adjudicate acceptance.

The review verified chain `c72a19b257550de7bb75dc9361b9f939fc220cb9 -> 416e36199cb47eacba0b10de0170af5082b42c83 -> 84cd8508e213e3664ec898cd2b9a783570b28de5`, claim exact one State path, final exact seven paths, 7/7 target blobs, State concrete-model denyset, clean whitespace, five Preserve blobs, remote ref and lifecycle truth. The mapping recount is total=48, unique=48, missing=0, duplicate=0, Legacy counts L1=9/L2=8/L3=9/L4=7/L5=5/L6=4/L7=6, `UNRESOLVED=48`, preserved lifecycle=48. Open issue register: `NONE`.

This batch only persists that acceptance and creates the M2→M3 handoff. It does not execute or start M3.

## 1. Required final lifecycle

```text
M0/M1/M2: ACCEPT / CLOSED
M2→M3 Handoff: PERSISTED / PENDING REMOTE ARCH REVIEW
M3: NOT STARTED / NOT AUTHORIZED
M4/M5: NOT STARTED
CookBook Phase 3B: NOT AUTHORIZED TO START
TURN: REVIEW
```

## 2. Exact normal final allowlist

1. `A .ai-context/docs/项目改造规划/蓝图设计2/UBF-M2-END-ACCEPT-01_Luna_Execution_Blueprint.md`
2. `A .ai-context/docs/UBF-M2-END-ACCEPT-01-Execution-Report.md`
3. `A .ai-context/docs/项目改造规划/蓝图设计2/UBF-M2-FINAL-ACCEPT.md`
4. `A .ai-context/docs/项目改造规划/蓝图设计2/UBF-M2-to-M3-Handoff.md`
5. `M .ai-context/docs/项目改造规划/蓝图设计2/universal_blueprint_framework_implementation_control.md`
6. `M .ai-context/docs/experience/14_模型执行力评估.md`
7. `M .ai-context/docs/context_memory/BLUEPRINT_STATE.md`

No eighth path is allowed.

## 3. Frozen acceptance meaning

M2 is complete because it non-destructively mapped all 48 legacy GC assets with the required metadata and preserved uncertainty honestly. `Universal Mapping=UNRESOLVED` for 48/48 is an accepted M2 output, not missing work: M2 had no authority or empirical corpus to invent the Universal ladder.

Accepted M2 outputs:

- stable identity, Origin, Current Authority, Decision Category, Task Profile candidate, Closure Effect, Preserved Coder Discretion, Evidence Type, Legacy Level, Universal Mapping, Lifecycle and references for every GC;
- 48 × `PRESERVE_IDENTITY + REDEFINE_LEVEL_RELATION`;
- exact Legacy grouping and project provenance retained;
- no direct inference from GC number/order/count/Legacy Level to Universal Level;
- M1 64-record decomposition remains closed and preserved.

Unresolved by design and transferred to later stages: empirically calibrated Universal Level count/names/envelopes, final Task/Capability Profile schemas, selector behavior, canonical mutation and verification.

## 4. Preserve Set

Preserve M2 Work-01 mapping artifact and execution blueprint/report, the canonical GC registry, M1 64-record artifact, M1 Final Accept, M1→M2 Handoff, UBEA-v2, user-level canonical files, Project Graph, stable entry, production code/tests/data/build/configuration and unrelated documentation.

State must contain none of `GPT-5.6`, `Luna`, `DeepSeek`, `Claude`, `Gemini`. Concrete model identity belongs only in the model evidence ledger and execution report.

## 5. Transaction

From an isolated clean environment at the exact Handoff Parent:

1. materialize only the exact claim State target;
2. verify package/blob identity, exact one-path staged set, State denyset, whitespace and clean residue;
3. commit `docs(governance): claim UBF M2 accept handoff turn`;
4. push and remote-verify; this commit becomes Execution Parent;
5. materialize all seven exact final targets without editing;
6. verify exact seven-path scope relative to claim and parent, seven staged target blobs/modes, State denyset, whitespace, tracked+untracked/index residue, Preserve blobs and lifecycle truth;
7. commit `docs(governance): persist UBF M2 accept and M3 handoff`;
8. push, remote-verify, return evidence and STOP.

Do not use metadata-preserving copy as Git change evidence. If filesystem mtimes cause expected tracked paths to disappear from the staged set, treat that adapter as soft compatibility, re-materialize with an equivalent exact-byte adapter, force refresh and re-run staged blob/scope gates. Never commit a partial staged set.

If every authorized adapter is exhausted after claim, materialize only abort State, commit `docs(governance): record UBF M2 accept handoff compatibility block`, push, verify and STOP.

## 6. Compatibility / Hard STOP

Path separators, console encoding, Python/PowerShell/Bash differences, optional adapter startup, nonessential tool absence and Git stat-cache behavior are soft compatibility when an equivalent preauthorized adapter remains.

Hard STOP on remote-parent/delegation/package/preimage/isolation failure, allowlist escape, target/staged blob mismatch, Preserve mutation, State denyset/whitespace failure, invalid parent chain, unverified remote push/ref or an unclosed semantic choice.

## 7. Evidence and STOP boundary

Return exact parent/claim/final-or-abort identities and parents, remote refs after each push, NUL-safe tracked+untracked/index sets, target/preimage/Preserve blobs, whitespace and State denyset results, M2 acceptance/lifecycle assertions and explicit STOP confirmation. Adapter PASS text alone is insufficient.

Do not create M3 Preview/Start, calibration corpus, Universal Level/Profile/selector decisions, canonical mutations, State restructuring, routing mutation, Project Graph mutation or production changes. Do not start CookBook Phase 3B.
