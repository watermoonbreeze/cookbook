# 菜品推荐算法 · 深挖分析与增强路线

> [AI生成] 2026-07-18 推荐算法全链路深挖。基于代码全量摸底。健康相关守免责红线。

## 一、现状全貌（打分→多样性→AI融合）
- **打分**(`HealthRuleEngine.evaluate`)：可做性筛选(至少一味在手非调料)→多因子加权和：base + 在手主料(封顶3) + 调养推荐 + 营养互补 + 偏好画像 − 限量 − 最近 − 库存不足 − 缺辅料(封顶3) − 主料重复 − 慢病软降 − 忌口(×5)。**分层排序**：`compareBy(忌口, 最近, -score)`(布尔分层·稳健)。
- **权重**(`RecommendationWeights`)：已因子化·4 风格预设(BALANCED/FAMILIAR偏熟/FRESH偏新/NUTRITION偏营养)切换。默认值来自旧常量。
- **多样性**(`RecommendationOrchestrator.diversify` MMR·λ 由风格定)：正常菜层内贪心打散，保留首位；~~原只按主料 Jaccard~~ **[本轮扩维:主料0.6+菜系0.2+做法0.2]**。最近/忌口层保持末位。
- **轮转**(`rotate`·每批10)：按非忌口候选数算批·防翻到罚分末批·循环。去重窗口 7 天(可 1-4 周)。
- **营养互补**(`NutritionBalance.score`)：**今日已吃**为基线(非近7天均值)·三宏量占比缺口×候选偏向·归一[-1,1]。
- **AI**(云端 GLM)：规则筛安全候选→MMR→轮转→Prompt→模型选菜组合→校验(只能选候选内·1-3菜/餐)→任何失败规则兜底。忌口由规则强校，模型无权绕。

## 二、✅ 本轮已落地
- **MMR 多样性扩维**(`321d20b`)：加菜系+做法维度，避免一批全川菜/全红烧。纯算法·无产品决策·单测。
- **时间衰减**(2026-07-18)：`RecommendationWeights.decayPreferenceByStaleness`（仅 FRESH 开）+ `HealthRuleEngine.stalenessDecay`（指数衰减·半衰期 30 天：0天=1.0/30天=0.5/90天≈0.125）。FRESH 下 `preference`(常做)加分乘衰减系数，久没做的常做菜降权→不固化老菜；从没做过(不在 `lastCookedDaysAgo`)不衰减。新查询 `selectLastCookedDatesByDishIds`(不限窗口·仅 FRESH 且有候选才查·IN 候选 id 无 N+1)。其余风格不衰减。单测覆盖。
- **口味画像**(2026-07-18)：新增纯函数 `TasteProfile`(菜系0.4+做法0.3+主料0.3，按各维最高频次归一→匹配度[0,1]，做法/主料取最强一味不稀释)。因子 `RecommendationWeights.tasteProfile` 四风格分级(偏熟悉0.6>综合0.4>偏营养0.3>偏新鲜0.2)。取数层按 90 天历史窗口聚合菜系/做法/主料频次(3 新查询·仅开口味因子的风格才查)。空历史→空画像→因子中性(向后兼容)。Google 审查无阻断。单测覆盖。
- **算法会诊角色**(2026-07-18)：新增公共 `algorithm_engineer`（高级算法工程师会诊团队·四镜头营养/健康/膳食/基础算法·可多实例并行会诊后收敛），供后续算法深挖/调参/性能优化调用。

### ✅ 2026-07-21 算法打磨轮（方法论10·3镜头会诊+收敛+Google质量审·纯算法/正确性·无产品决策）
> 由膳食营养/基础算法工程/真实吃法oracle 三镜头 `algorithm_engineer` 并行会诊真实代码→收敛→落地"安全自主"批(commit `34e49d3`+`15ba793` 区间)。需拍板项(慢病默认微开/菜级营养字段/当日能量/validate补菜/recommend封顶/早餐软硬透传/无库存兜底/周计划GI+重油)全部记入 `context_memory/待拍板队列.md`,未擅自做。
- **🔴RANDOM 999 变量崩溃修复**(正确性红线·`34e49d3`)：RANDOM 分支原走 `findDishesByIngredients(整库食材id)` 的 `IN :ingredientIds`,食材库过 999 触发 SQLite 变量上限崩溃(扩库即中招)。新增无 IN 查询 `selectAllDishesForRandom`(纯查询无迁移)+`DishRepository.listAllDishMinis`,RANDOM 走全库 DishMini(语义等价·gather 本就丢弃 match_count)。顺带排序 match_count(复杂菜优先·意外副作用)→preference。**对应待拍板 C组10 必修·已闭环**。
- **MealCompositionScorer 抽取**(防漂移·`34e49d3`)：荤素/主食组合补分两处同构(`combineScore`+`PeriodPlanner.score`·BALANCE0.7/STAPLE0.9)→单一真相源纯函数。空餐不给荤素补分守卫内建(等价 PeriodPlanner 原 isNotEmpty·主食分空餐照常)、f.balance 只缩放荤素→两处零行为变化。7例单测锁定。
- **MMR 重油族维度**(多样性·`15ba793`)：做法名 Jaccard 只在名完全相同才相似(红烧≠干煸≠煎)→感知不到"都很油"。加 `cookingHeaviness` 纯函数(清淡/中性/重油·烹饪常识非健康断言)+相似度"油腻度族相同则相似"维(两菜都有做法才比·空退化)。五维权重重配·和=1.0·主料仍主导0.45(主料0.45+重油0.15+荤素0.15+做法名0.13+菜系0.12)。
- **fallback 一餐内主料不重复**(`15ba793`)：`combineScore` 加 MAIN_OVERLAP_PENALTY(0.5<BALANCE0.7)防"一餐两道五花肉"(贴真实吃法·不压倒荤素/主食补齐)。
- **组合完整性喂 prompt**(`15ba793`)：模型路径 validate 只校 id+菜数、无组合约束(会诊 C#F1/A#2:两荤无素漏网)→ prompt system 加"每餐荤素+主食"软引导 + user 候选标 荤/素/主食(不改 validate/模型自由度·强版补菜属口径记待拍板)。
- 均过 Google 质量审(见 experience)+shared 单测全绿+构建绿。守健康免责红线(重油族=烹饪常识·忌口硬约束未动)。**真机待验**:RANDOM 大库不崩、一批菜油腻度更错落、一餐不两道同主料。

#### 第2轮(2026-07-21·巩固+口径统一·纯算法·Google审无阻断)
- **PeriodPlanner 跨餐重油去重**：单餐已有 MMR 重油族打散、周计划无(口径不一致·顿顿煎炸红烧)。给 `PlanDish` 加 `cookingMethodNames`(gatherForPlan 批量 `selectCookingMethodsByDishIds` 分组填充·无 N+1)+`usedHeavyToday`(**按天**重置·非全程,避免后几天饿死重油菜)：一天内每多一道重油菜(cookingHeaviness==2),后续重油候选按数递减(`f.repeat×HEAVY_REPEAT_PENALTY0.4×count`)。清淡/中性不罚。与单餐同 `cookingHeaviness` 口径。证伪单测(FRESH 下一餐不两道重油)。
- **diversify head 防御化**：`takeWhile{正常}`+`drop` → `filter{正常}`+`filter{补集}`,去掉"正常层是连续前缀"的隐式排序依赖(当前 evaluate 排序下严格等价·filter 保序;未来改排序 key 也不会静默截断错层)。
- **NutritionBalance clamp 护栏**：既有 NutritionBalanceTest(2026-07-18)已覆盖无基线均衡/偏斜只奖不罚/候选无数据中性/补缺口正分;补缺的 `raw×SCALE` clamp 边界[-1,1]一例(极端偏斜基线×候选·证伪有效)。
- Google 审逐条核验:diversify 零行为变化✓、周计划去重正确✓、PlanDish 无遗漏构造点✓、无 N+1✓、测试真证伪✓。无阻断。

## 三、增强路线（分级·多需你定口径）

### 🟢 可自主（纯算法/数据·无产品决策·后续可做）
1. **营养因子数据补全**：营养覆盖~65%，缺数据菜营养因子中性。同 P3 法权威补齐→营养驱动更强。(数据活)
2. **多样性再扩**：加"荤素/营养结构"维度(避免一批全高油)、"简易vs复杂菜"搭配。(小·算法)

### ✅ 用户已拍板(2026-07-18)·实现进度
3. ✅**时间衰减**(2026-07-18 落地·见"本轮已落地")：FRESH 下常做加分随距上次做天数指数衰减、不固化老菜。
4. ✅**负反馈「踩」**(2026-07-18夜落地)：显式"踩"——菜卡**长按→ActionSheet"不再推荐这道菜"**(§9.20)→就地灰态(alpha0.5·不移除·隐勾选圈)+Snackbar撤销(防误标)+**菜品详情"恢复推荐"**闭环。存储用独立 `dish_dislike` 表(仿 dish_favorite·`27.sqm`·**零牵连 dish 表/SELECT ***)；`gather`/`gatherForPlan` 用 `selectDislikedDishIds` **过滤**(踩的不再出现·非罚分沉底,更符合"不再推荐"语义)；只用户显式标才降(不自动"未选=不喜欢")。过 Apple-UX+Google 门禁+DB往返单测。
5. ✅**口味画像**(2026-07-18 落地·见"本轮已落地")：学菜系+做法+主料频次→匹配加分·纯本地统计。
5. **慢病软约束权重**：现高GI/嘌呤罚分仅0.42(远小于忌口5)、且默认权重0(仅营养风格生效)。是否调强？(你已答"维持现状"·此项关闭)
6. **钠分级软降**：现钠不做菜级(调料难拆)。是否按每100g钠分梯度罚？(改进余地小·现用做法提示回避)

### 🟠 较大/需方案（跨切面或新能力）
7. **口味画像学习**：现只"常做+收藏"，无菜系/做法/食材偏好向量。学用户爱吃的菜系(川菜偏好)/做法/主料→个性化。(中)
8. **成员级推荐**：现面向全家混合约束。为特定成员推(排他人忌口)。**依赖多人关注重构**(已出方案待实现)。
9. **AI-规则融合深化**：规则的多因子分数/理由未透传 Prompt(模型只"从候选选")。把"缺蛋白+0.8/常做+0.6/在手主料多"因子分解喂 AI→更懂为何推。(中)
10. **时段/季节因子**：晚餐宜清淡/应季食材加权(`ingredient.seasonTag` 已有)/气温倾向。(中)
11. **推荐理由回馈 + 冷启动**：新用户全 0 信号→纯规则；可"新菜保护期加成"、首启口味问卷、推荐理由解释加速数据积累。(中)

## 四、建议下一步
先做 🟢 自主项(营养补全/多样性再扩)。🟡 时间衰减/负反馈价值高但需你定口径(与风格/误踩的取舍)——建议我出方案带选项给你选。🟠 口味画像/AI融合/时段季节属"让推荐更懂你"的中长期增强，逐个方案化。成员级推荐等多人关注重构落地后做。
