# 自动化基础能力层 Phase 1-4 代码审查反馈

> 审查时间：2026-08-02
> 审查范围：`shared/src/commonMain/.../domain/autogen/` + `ai/meallog/MultiDayRecorder.kt` + 单测
> 审查角色：Google 资深工程师（代码质量）
> 状态：**全部已修复** — 2026-08-02 同 session 修复完毕，见 commit `d59814ac`

---

## 本 Session 已完成项（可通过 git log 验证）

| 完成内容 | Commit | 涉及文件 |
|---|---|---|
| Koin 接线·IngredientAliasResolver + MultiDayRecorder 注册 | `77099cf7` | `shared/.../di/SharedModule.kt`<br>`shared/.../ai/meallog/MultiDayRecorder.kt` |
| A1/A2/B1/B2/B3 全部 5 项修复 | `d59814ac` | 见下方各条说明 |

`MultiDayRecorder` 构造函数已改：`autoGenContext: AutoGenContext` → `db: CookbookDatabase`，`recordAll()` 内部每次 `AutoGenContext.load(db, aliasResolver)` 获取新鲜字典。

---

## 🔴 阻断（已修复）

### A1 ✅ eaten_ratio 全链路丢失 — commit `d59814ac`

**修复文件**：
- `shared/src/commonMain/kotlin/com/sxdbsm/cookbook/domain/autogen/AutoGenModels.kt`
- `shared/src/commonMain/kotlin/com/sxdbsm/cookbook/domain/autogen/DishAutoGenerator.kt`
- `shared/src/commonMain/kotlin/com/sxdbsm/cookbook/domain/autogen/DayAutoGenerator.kt`

**修复内容**：
1. `AutoGenModels.kt`：`DishPreview` 新增 `eatenRatio: Double? = null` 字段
2. `DishAutoGenerator.kt`：`preview()` REUSE/CREATE 两分支均透传 `input.eatenRatio` 到 `DishPreview`
3. `DayAutoGenerator.kt`：`commit()` 收集 `eatenRatioMap(mealTypeId×dishName→ratio)`（仅非 1.0 的），`saveDayMeals` 后 `loadDayMealsForEdit` 匹配 `setEatenRatio`（参照 `AiMealRecorder.backfillEatenRatios` 同模式）

---

### A2 ✅ 单测双 DB 实例，commit 测试形同虚设 — commit `d59814ac`

**修复文件**：
- `shared/src/androidUnitTest/kotlin/com/sxdbsm/cookbook/domain/autogen/IngredientAutoGeneratorTest.kt`

**修复内容**：
- 全面重写为 `@BeforeTest setUp()` 共享单一 `db` 实例
- `ctx = AutoGenContext.load(db, aliasResolver)` 和 `generator = IngredientAutoGenerator(IngredientRepository(db), NutritionRepository(db))` 均基于同一 DB
- T07 零写库断言改为同 DB before/after count 比对（不再建第三个 DB-C）
- 新增 T08 两个 commit 测试（CREATE 落库验 `source="auto"` + `nutrition.ref="自动估算"`，REUSE 不增行）
- 新增"盐"食材种入调味品分类，确保 T03 调料量断言可达

---

## 🟡 建议（已修复）

### B1 ✅ isSeasoning 误把未分类食材当调料 — commit `d59814ac`

**修复文件**：`shared/src/commonMain/kotlin/com/sxdbsm/cookbook/domain/autogen/IngredientAutoGenerator.kt`

**修复**：CREATE 分支 `isSeasoning = nameKey in ctx.seasoningNames || group == null` 改为 `isSeasoning = nameKey in ctx.seasoningNames`

---

### B2 ✅ 多天结果压平成单条 today — commit `d59814ac`

**修复文件**：`shared/src/commonMain/kotlin/com/sxdbsm/cookbook/ai/meallog/MultiDayRecorder.kt`

**修复**：先 `val autoGenPreview = dayGen.preview(...)` capture 各天 date，再 `dayGen.commit(preview=autoGenPreview)`，按 `autoGenPreview.days[].date` 构建 `DayRecordResult`

---

### B3 ✅ dishIdCache reused 统计漏计 — commit `d59814ac`

**修复文件**：`shared/src/commonMain/kotlin/com/sxdbsm/cookbook/domain/autogen/DayAutoGenerator.kt`

**修复**：`commit()` 中分离 `alreadyCached = dish.inputName in dishIdCache` 先判断，再 `dishIdCache.getOrPut` 执行缓存/创建，统计每次出现独立计数（`alreadyCached||REUSE → reused++`，首建 `id>0 → created++`）

---

## ✅ 不变量核验结论（全部满足）

| 不变量 | 结论 |
|---|---|
| INV-01 dedup（同名不建重复） | ✅ |
| INV-02 热量>0（有食材即有热量） | ✅ |
| INV-03 unitId 非 NULL | ✅ |
| INV-04 careFlag（PENDING/INHERITED）| ✅ |
| INV-05 ref="自动估算" | ✅（T08 单测已验证） |
| INV-06 两阶段 preview/commit | ✅（T07/T08 单测已验证） |
| INV-07 source="auto" | ✅（T08 单测已验证） |
| INV-08 别名 JSON 过滤 _description | ✅ |

---

## 构建验证

```
:shared:testDebugUnitTest  BUILD SUCCESSFUL
:androidApp:assembleDebug  BUILD SUCCESSFUL
```

---

## 总结

架构分层清晰、复用得当。原 5 项问题集中在：
- **功能回归**：eaten_ratio 全链路在 Phase 3 平移时被截断（注释声称有、代码实际无）
- **测试质量**：双 DB 实例致关键 commit 测试形同虚设

所有问题已在 commit `d59814ac` 一次性修复，自动化基础能力层达到进入 P2-1 UI 阶段的质量基线。

*本文件由 2026-08-02 session 的 Google 资深工程师代码审查 agent 生成，同 session 已全部修复完毕。*
