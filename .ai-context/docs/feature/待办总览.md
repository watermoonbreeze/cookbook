# 待办总览（Backlog）

> [AI生成] 2026-07-08 建立。项目当前待办的**单一跟踪入口**，跨文档汇总。
> 维护约定：完成后把状态改为 ✅ 并注日期；新待办往对应分类里加；细节仍散落在各方案/`context_memory`，这里只做索引与优先级。
> 已完成的大块：MVP 三大核心、食材层（品类树/库存/详情/预设菜/健康档案/全局搜索/应季）、烹饪计时可靠性改造，均已落地，不在此列。

## 图例
- 优先级：🔴 高 / 🟡 中 / ⚪ 低
- 体量：S 小 / M 中 / L 大
- 状态：⬜ 待做 / 🔄 进行中 / ✅ 完成

---

## A. 功能类

| 状态 | 优先级 | 体量 | 项 | 说明 / 出处 |
|---|---|---|---|---|
| ⬜ | 🔴 | L | **AI 层：按在手食材推荐** | 输入现有食材→AI 组合做法/膳食建议，核心差异化。方案见 `端侧AI能力接入方案.md`。建议先出小范围方案再动手 |
| ⬜ | 🟡 | S | 首页搜索"跳到具体食材并高亮" | 现仅跳食材页；需跨屏传 ingredient id + 食材页 `jumpToIngredient` 处理 |
| ⬜ | 🟡 | M | `selectionMode` 布尔重构 | 管理/选择模式改参数/回调区分，去散落分叉（`食材界面设计.md`） |
| ⬜ | 🟡 | M | 维度区通用组件 `DimensionSection` | 详情/编辑属性区（品类/营养/应季）通用化（`食材维度体系设计.md`） |
| ⬜ | ⚪ | — | 内容持续补充 | 更多食材/预设菜/营养·调养标注（纯数据，按 `食材数据规范.md`） |

## B. 工程 / 分发基础（详见 `工程优化待办.md`）

| 状态 | 优先级 | 体量 | 项 | 说明 |
|---|---|---|---|---|
| ⬜ | 🔴 | M-L | **P0 存储权限合规** | 去 `MANAGE_EXTERNAL_STORAGE`；DB/图片/日志迁 app 专属目录（`getExternalFilesDir`）；保留旧 `/sdcard/cookbook` 迁移；导出走 SAF。**分发/上架前必做** |
| ⬜ | 🟡 | S-M | P1 Shared IO Dispatcher | `commonMain` 定义 KMP `ioDispatcher`；Android=Dispatchers.IO；替换 Repository 里 SQLDelight IO 的 `Dispatchers.Default` |
| ⬜ | ⚪ | M | P2 SQLDelight 写入 Diff | 菜品标签/食材分类/调养规则等"全量清空再插入"改差异更新，降无效写入与 UI 刷新 |
| ⬜ | ⚪ | S | P3 KMP 日志 | 评估 Napier/Kermit；`commonMain` Repository/Seed/迁移可日志；与 Android `AppLogger` 整合 |
| ⬜ | ⚪ | S | DI 平台化 | `expect fun platformModule(): Module`，平台依赖注册移入各平台 module |

## C. 平台 / 验证

| 状态 | 优先级 | 体量 | 项 | 说明 |
|---|---|---|---|---|
| ⬜ | 🟡 | M | iOS shared 编译能力 | 当前 iOS target 注释；启用前补 `actual`（资源读取等）与静态 framework 验证 |
| ⬜ | 🟡 | S | 回归验证：删除一致性 | 被历史餐食引用的菜品/食材软删除后历史展示不破坏（真机专项） |
| ⬜ | 🟡 | S | 回归验证：图片持久化 | 拍照/相册图片重启后仍显示、列表缩略图/详情原图（真机专项） |
| ⬜ | ⚪ | S | 烹饪计时：A14 全屏通知引导 | Android 14+ `USE_FULL_SCREEN_INTENT` 默认关，需引导用户开或 `ACTION_MANAGE_APP_USE_FULL_SCREEN_INTENT`。等有 A14 设备再补（A12 已验证正常） |
| — | — | — | 烹饪计时：国产 ROM 保活 | 划掉即强杀属 ROM 行为，纯代码绕不过；响铃靠系统闹钟不受影响。仅需引导用户开自启动/后台。**非代码项** |

---

## 建议推进顺序
1. **推产品价值** → A·AI 层（先出小方案定范围）
2. **为分发铺路** → B·P0 存储合规
3. **快速清理** → P1 IO Dispatcher + A·首页搜索跳高亮 + P3 日志（都偏小）
