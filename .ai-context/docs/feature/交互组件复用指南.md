# 交互·组件复用指南（权威版）

> [AI生成 2026-07-16] Apple-UX 审阅 §九 + 22 组件 + 全库真实用法后产出。**后续所有 UI 编码前先查此表：能复用必复用**。配套 §九 新增 9.12–9.17。

## 一、组件复用对照表（需求 → 用哪个现成组件）

| UI 需求 | 复用组件 | 用法要点 |
|---|---|---|
| 二级/详情/表单顶栏(带返回) | **`AppTopBar`** | `title`+`onBack`+`actions`；16 屏应收敛到它 |
| 一级落地页大标题 | **M3 `LargeTopAppBar`** | 参照 `MineScreen`；右上单主操作 |
| 搜索输入框 | **`AppSearchField`** | 全 App 唯一搜索样式 |
| 主 CTA(保存/完成) | **`CapsuleButton`** | 一屏一个 |
| 互斥选择 2~5 项 | **`SegmentedControl`** | 项多(7+)仍用滚动 chip |
| 设置/详情分组卡 | **`InsetGroup`**+`InsetDivider` | |
| 无边框白卡 | **`PlainCard`** | |
| 底部多操作/破坏确认 | **`ActionSheet`**+`SheetAction` | `destructive=true` 红字 |
| 主题切换 | **`ThemeModeDialog`** | |
| 空态 | **`EmptyState`** | 已加可选 action 槽(9.6) |
| 就地数量增减 | **`MiniStepper`** | 带单位；大跨度传 `onValueClick` |
| 食材卡(多模式) | **`IngredientCard`** | 能力由回调开关 |
| 菜品列表行 | **`DishRow`**(+`SourceBadge`/`TagChip`) | 多选 `showCheckbox` |
| 菜品小卡/网格 | **`DishMiniCard`**/**`MealDishGrid`** | |
| 一天餐食卡 | **`DayMealCardView`** | |
| 选图/拍照 | **`ImagePickerButton`** | 存相对名+EXIF 校正 |
| 图片展示 | **`StoredImage`** | |
| 星级(只读) | **`StarRating`** | |
| 表单标签/区块标题 | `UiText.kt`: `FormFieldLabel`/`SectionHeader`/`EmptyLineText` | `SectionHeader(action=)` 兼折叠触发 |

## 二、新增的复用组件（本轮建）

| 组件 | 用途 | 规格 |
|---|---|---|
| **`UnsavedGuard`** | 未保存返回守卫统一封装(抽自 NewDishScreen 内联) | `UnsavedGuard(isDirty, onConfirmLeave){ requestBack -> ... }`：内部 `BackHandler`+"放弃未保存的更改？"弹框(放弃红字/继续编辑) |
| **`AppSnackbarHost`** | 统一保存/撤销反馈，终结每屏各建 `SnackbarHostState` | 挂 `MainScaffold`，`showMessage(text)`/`showUndo(text,onUndo)`；撤销走 `ActionPerformed` |
| **`EmptyState` +action** | 落实 §9.6"给下一步" | 加可选 `actionLabel`+`onAction`，有则文案下渲染次按钮 |
| **`FormBottomBar`** | 统一"保存/保存并继续"摆位 | `primaryText`+`onPrimary`(胶囊)+可选 `secondaryText`+`onSecondary`(次文字)；`navigationBarsPadding`+`WindowInsets(0)` |

## 三、B 批逐项 → 复用结论

| B 任务 | 复用/新建 | 结论 |
|---|---|---|
| 餐食页未保存返回守卫 | 新建 `UnsavedGuard`，与 NewDish 共用 | 先抽封装再套两处 |
| 食材"保存并继续" | 范式 9.13 + `FormBottomBar` | 存后清空复位留页+Snackbar |
| 保存反馈统一 Snackbar+撤销 | `AppSnackbarHost` + 9.12/9.16 口径 | Toast 仅纯告知；删除类改 Snackbar 撤销 |
| 库存 Tab 就地加减份数 | `MiniStepper` 直接复用 | 零新组件 |
| 食历卡三图标收 ActionSheet | `ActionSheet`+`SheetAction` | "⋯"触发 |
| 菜品页大标题+整行搜索+Tab简化 | `LargeTopAppBar`+`AppSearchField`+`SegmentedControl` | 全现成 |
| 编辑菜品放开改期 | VM 逻辑，UI 复用 | 数据改动 |
| 删整天软删可撤销 | 9.12 口径 + `AppSnackbarHost` | 删 Home/Timeline 的"不可撤销"AlertDialog，改软删+撤销 |
| AppTopBar 收敛 16 屏 | `AppTopBar`/`LargeTopAppBar` 按 9.15 判定 | Tab 落地→大标题，带返回→AppTopBar |

## 四、§九 已增补 9.12–9.17（见 `苹果风格UI设计方案.md`）
保存反馈 Snackbar 优先 / 表单 CTA 与保存并继续 / MiniStepper 统一 / 大标题页判定 / 多操作 ActionSheet / 未保存守卫封装。

## 关键不一致点(落地前必知)
- 反馈体系最乱：`已保存` NewDish/AddDayFood/IngredientPicker 用 Toast，AiRecommend/Shopping 用 Snackbar，AddDayFood 两者并存——统一宿主是前置基建。
- 未保存守卫只在 `NewDishScreen` 内联(78-83、410-424 行)，未抽复用。
- 删整天硬确认(`HomeScreen`:286、`FoodTimelineScreen`:200)违反 §9.9，须改软删+撤销。
