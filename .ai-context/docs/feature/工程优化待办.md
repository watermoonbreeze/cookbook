# 工程优化待办

> 创建时间：2026-06-25。[AI生成]
> 来源：`temp/codex/optimization_plan.md` 评估结论。
> 状态：先记录待办，后续按阶段再拆任务实施。

## P0：存储权限合规迁移

- [ ] 去除对 `MANAGE_EXTERNAL_STORAGE` 的依赖。
- [ ] 将数据库、图片、日志等默认迁移到 app 专属目录，例如 `Context.getExternalFilesDir()` 或 `Context.getFilesDir()`。
- [ ] 保留旧 `/sdcard/cookbook` 数据迁移方案，避免已有测试数据丢失。
- [ ] 如需导出/备份，使用 Storage Access Framework 让用户选择导出位置。

说明：这是后续上架或普通用户分发前的最高优先级合规项。

## P1：Shared IO Dispatcher

- [ ] 在 `commonMain` 定义 KMP `ioDispatcher`。
- [ ] Android actual 使用 `Dispatchers.IO`。
- [ ] iOS actual 暂用 `Dispatchers.Default` 或后续平台专用调度器。
- [ ] 替换 Repository 中用于 SQLDelight IO 的 `Dispatchers.Default`。

说明：当前 shared 层大量数据库操作使用 `Dispatchers.Default`，建议近期处理。

## P2：SQLDelight 写入 Diff 优化

- [ ] 评估菜品标签、食材分类、调养规则等“全量清空再插入”的链路。
- [ ] 对高频编辑链路改为差异更新。
- [ ] 减少无效数据库写入和 UI 刷新。

说明：MVP 阶段可暂缓，数据量或编辑频率上来后再做。

## P3：KMP 日志能力

- [ ] 评估引入 Napier 或 Kermit。
- [ ] 为 `commonMain` Repository/Seed/迁移逻辑提供跨平台日志。
- [ ] Android 现有 `AppLogger` 与 KMP 日志做边界整合。

说明：等 iOS 或 shared 层排查需求增加后处理。

## P4：Koin 模块解耦

- [ ] 评估 `expect fun platformModule(): Module`。
- [ ] 将平台相关依赖注册移入各平台 module。
- [ ] `sharedModule` 只保留共享 Repository、Database、Seeder 等注册。

说明：当前结构可用，不是阻塞问题。

## P5：Shared ViewModel 长期化

- [ ] 等 Android UI 稳定后，评估将部分 ViewModel 迁移到 shared。
- [ ] 优先选择业务稳定、双端复用价值高的模块。
- [ ] 引入 KMP ViewModel 前先确认 iOS 页面同步计划。

说明：当前不建议立即做，避免在 Android UI 快速变化期增加复杂度。
