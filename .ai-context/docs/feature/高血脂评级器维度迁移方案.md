# 高血脂评级器维度 · DB 迁移方案（待审，不执行）

> [AI生成] 2026-07-17。高血脂 care 规则已落地(`9cee959` 51条)；**评级器负向维度**需给 `ingredient_nutrition` 加饱和脂肪/胆固醇两列（DB 迁移=高风险，SQLDelight 迁移红线多）。本文档是**实施方案**，供用户审核后再执行，不擅自改代码。

## 一、目标与口径

给 `NutritionLevelEvaluator` 加**高血脂(HYPERLIPIDEMIA)负向维度**：当登记高血脂病种、当日饱和脂肪/胆固醇偏高时，温和下调级别+提示。与钠/GI/嘌呤同为负向罚，守免责（仅供参考·非医嘱）。

**权威口径（待核准）**：
- 饱和脂肪：中国居民膳食指南 2022 / 血脂管理指南 2023——饱和脂肪供能 <10%（约 <20g/日，2000kcal 基准）。
- 胆固醇：膳食胆固醇 <300mg/日（一般）/ <200mg/日（高胆固醇血症）。
- **均为定性/建议阈值**，用则标口径来源+免责；缺数据→不触发（同钾/纤维退回口径）。

## 二、Schema 变更（红线：一次改全）

**当前**：`ingredient_nutrition` 无饱和脂肪/胆固醇列（有 fat_g/cholesterol 无）。最新迁移 `24.sqm`。

1. **改 CREATE TABLE**（`Cookbook.sq`）：在 `fat_g` 后追加两列（列序与迁移一致、追加末尾前的合适位置——实际两处都追加到 `purine_mg` 附近保持语义聚集，但**新装走 Schema.create 用 CREATE 的列序**）：
   ```
   saturated_fat_g REAL,   -- 饱和脂肪(g/100g)
   cholesterol_mg REAL,    -- 胆固醇(mg/100g)
   ```
   放在 `calcium_mg` 后、`gi` 前（营养素聚集）。**注意 CREATE 与 ALTER 的列最终集合一致即可，SQLite 加列都在末尾**——为避免混乱，CREATE 也把两列加在**末尾 status 之前**与 ALTER 顺序一致更稳妥。
2. **新建 `25.sqm`**（名=最大24+1）：
   ```
   ALTER TABLE ingredient_nutrition ADD COLUMN saturated_fat_g REAL;
   ALTER TABLE ingredient_nutrition ADD COLUMN cholesterol_mg REAL;
   ```
   REAL 可空、无 DEFAULT（老行为 NULL=缺数据，评级器缺数据不触发，天然安全）。**幂等性**：`ALTER ADD COLUMN` 对全新列安全；不可对已存在列跑（首次迁移，无此问题）。
3. **Schema 版本**：由 `.sqm` 文件数推导（25 个）→ 自动。

## 三、领域模型与查询同步（红线：所有调用点）

- `IngredientNutritionRow`/`NutritionTotals`（`Nutrition.kt`）：加 `saturatedFatG`/`cholesterolMg` 字段（可空，默认 null）。
- 显式列 SELECT（`selectIngredientNutrition*` 等 `.sq` 查询）：手动加两列。
- `insertX/updateX ingredient_nutrition`（seeder 入库）：传参加两列。
- `NutritionCalculator`：折算逻辑加两维（同 fat_g 按克当量折算）。
- `NutritionRepository.totalOf/dishNutrition`：汇总加两维。
- **所有构造 NutritionTotals/Row 处**：grep 全仓补两字段（漏则 "Cannot find parameter"）。
- 契约：`ingredient_nutrition.json` 加 `saturated_fat`/`cholesterol` 字段解析。

## 四、数据回填（seeder·只填空）

- `ingredient_nutrition.json` 为高价值/高饱和脂肪食材补两值（分片后台 agent 联网核准一手成分表→covered=verified，缺省略不编造）。
- seeder 回填**只填 NULL**（`WHERE saturated_fat_g IS NULL`，不覆盖用户），`SEED_LOGIC_VERSION`+1（逻辑变更让老库跑一次）。

## 五、评级器分支（`NutritionLevelEvaluator.evaluate`）

加高血脂负向分支（比照钠/GI）：
- 当 `HYPERLIPIDEMIA in conditions`：饱和脂肪占比 ≥ 阈值→下调级别+"饱和脂肪偏高·高血脂留意"；胆固醇同理。
- 缺数据(null/0)→不触发。措辞守免责、避"降脂/降胆固醇"医疗断言。
- 常量：`SAT_FAT_DAILY_G=20`、`CHOLESTEROL_DAILY_MG=300`（标口径来源），占比阈值比照 WARN/MID_RATIO。
- 单测：加高血脂维度用例（含缺数据不触发）。

## 六、风险与测试（红线：迁移必推演）

- **风险**：迁移链断→真机"初始化数据失败"。缓解：ALTER 加可空列无 DEFAULT（最安全形态）；单测走 Schema.create **不跑迁移链**，须**额外推演**：模拟老库(v24)→跑 25.sqm→验证列存在、老行值为 NULL、评级器缺数据不崩。
- **测试清单**：①`:shared:testDebugUnitTest` 全绿 ②迁移链升级测试(v24→v25) ③营养引用完整性 ④评级器高血脂维度单测 ⑤真机：老库升级不崩+新装正常+登记高血脂→高饱和脂肪菜显提示。
- **回滚**：改动集中在 nutrition 链路，未动核心表；若真机异常，回滚该批 commit（迁移文件删除+版本回退需谨慎，故**务必先真机验证再发**）。

## 七、执行顺序（审核通过后）
1. Schema(CREATE+25.sqm) → 2. 领域模型/查询/调用点同步 → 3. 契约解析 → 4. 数据回填+SEED_LOGIC_VERSION → 5. 评级器分支+单测 → 6. 迁移链推演单测 → 7. 构建+全量单测 → 8. Google 代码审查 → 9. 交用户真机验证。

> **等你审核**：口径阈值(饱和脂肪20g/胆固醇300mg)是否认可？是否现在执行迁移，还是先补数据/care 已够用？
