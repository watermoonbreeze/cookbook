# 一期收官路线图与决策清单（待用户一次性拍板）

> [AI生成] 2026-07-19。用户三项指令统一梳理：①首页推荐下一餐具体菜品 ②一期加账号体系(看留存活跃) ③商业规划有利功能列出加待办。**目标：用户一次性拍板下方决策清单 → 全速开工，省来回讨论。**
> 关联方案：`首页推荐下一餐方案.md`、`账号体系与用户统计方案.md`、`换一换缓存策略方案.md`(第二批会诊已修正)、`竞品对比与商业化规划.md`(§三汲取/§五运营)。

---

## 一、建议开发顺序（阶段1→6·待 A1 确认）

| 阶段 | 内容 | 体量 | 为什么这个位置 |
|---|---|---|---|
| **1 手头收尾** | **换一换第二批**（会诊完成·设计已修正）：子步骤1 gather候选缓存+忌口每次重取 → 子步骤2 关GLM思考+独立预取校验+均衡档预取 | M | 推荐链路优化已启动、会诊结论新鲜，先闭环 |
| **2 留存杠杆** | **首页推荐下一餐**（#1·复用现有引擎） | M | 直接服务留存 aha、复用现成引擎、高杠杆。=商业 §5.2 每日推荐首屏化 |
| **3 能 measure** | **账号体系·匿名统计埋点**（#2）：客户端埋点可先做，后端搭建依赖服务器资源 | M | 先有提留存的功能(#1)、再上统计才看得到效果；可与阶段2并行 |
| **4 护城河可视化** | **成员化红绿灯**（商业#1）+ **病种切换解读视角**（商业#3） | L+M | 差异化护城河的直观化，紧随核心留存功能 |
| **5 地基·后台持续** | **中式食材库/营养数据补全**（商业#5 + 营养补全·分片 agent） | L | 贯穿始终后台跑，最慢但优先级最高的差异化地基 |
| **6 其余 P1 + 大项按价值** | 条码扫描/采购闭环/人群适配/GI筛选导航；AI对话生成菜品(XL·方案已出)、药膳一期最小集(方案已出)、放开AI限制(需会诊) | 各异 | 体量大或非核心，排到一期核心留存验证后按数据决定 |

> 说明：阶段6 三个 XL/L 大项（AI对话生成、药膳、放开AI限制）方案多已出，但体量大、且更适合"留存验证达标后"再投入——建议**本期先不启动，留作阶段6按留存数据决定**。若你要提前，见决策 A1。

---

## 二、完整决策清单（每点已给【推荐】·你只需确认或标例外）

### A. 全局节奏
- **A1 开发顺序**：认可阶段1→6？重点子问：①#1首页推荐 与 #2账号统计 谁先/并行（【推荐】#1先行、#2紧随可并行）②三大项(AI对话/药膳/放开AI)本期启动 or 留阶段6按留存数据决定（【推荐】留阶段6）。

### B. 账号体系（详见 `账号体系与用户统计方案.md`）
- **B1 一期范围**【核心】：只做匿名统计 / 也做完整登录账号 / 暂缓。【推荐】**只做匿名统计**（看留存活跃不需登录·完整账号留二期 AI 订阅/云同步时）。
- **B2 后端选型**：自建轻量服务 / 友盟系三方 / Firebase。【推荐】**自建**（最贴本地优先隐私招牌·需一台服务器资源）；无后端人力则友盟系；**不用 Firebase**（数据出境）。
- **B3 首启同意默认策略**：默认开+透明可关 / 首启询问+默认不勾选。【推荐】**询问式、默认不勾选**（最强隐私姿态·代价样本略少）。
- **B4 上报事件最小集**：采纳方案 §2.1（app_open/first_launch/meal_logged/recommend_requested/recommend_adopted/avoid_hit_shown/feature_used/health_profile_set·全去标识化）。【推荐】**采纳**。
- **B5 关闭统计开关**：给。【推荐】**给**（PIPL 撤回权+反暗黑），无争议。
- **B6 匿名标识**：首启随机 UUID（卸载即换·不可复原身份）。【推荐】**随机 UUID**。
- **B7 二期登录方式**（仅登记·一期不做）：微信/手机号/两者 → 二期再定。

### C. 首页推荐下一餐（详见 `首页推荐下一餐方案.md` D1–D10）
> 大多为交互细节、已给推荐，**建议 D1–D10 全部按方案推荐执行**，你只需标例外。关键三项单列：
- **C1(=D1) 落点**：把现有"AI 推荐入口卡"原地升级为"下一餐推荐卡"（展示 3 道具体菜）。【推荐】**是**。
- **C2(=D4) 「记」落点**：点行尾「记」进 AddMeal 编辑页（给确认/加菜机会）/ 静默写库+撤销。【推荐】**进编辑页**。
- **C3(=D9) 色系墙下移**：推荐卡插入后色系墙下移到今日区之后（推荐=向前看优先）。【推荐】**是**。
- 其余 D2(3道)/D3(整行详情+行尾记双动作)/D5(卡底免责行)/D6(不就地切餐次)/D7(新用户示例态)/D8(一句依据说明)/D10(首页预览+全页全量) → 【推荐】**全按方案**。

### D. 换一换第二批（详见 `换一换缓存策略方案.md` 第二批会诊修正）
> 会诊已把技术必做项定死（忌口每次重取红线、关 GLM 思考、独立预取校验+组间去重+本地补足、两视图以组合段为主轴、RANDOM 不预取、降级链），**不需你定**。仅两项风格取舍确认：
- **D1 新鲜感档位**：你此前已选**均衡档**（首次预取多批+本地轮播+用尽再联网）。【确认】保持均衡档。
- **D2 预取组数默认**：9 组（覆盖约 3 次换一换·候选池 20 道·temperature 0.6）——【推荐】**先用 9，标 pending 待埋点校准**。

### E. 商业 P0 汲取项本期取舍
- **E1 护城河可视化本期做**：成员化红绿灯 + 病种切换解读视角 是否纳入阶段4本期做。【推荐】**纳入**（差异化核心·护城河#1/#2 直观化）。
- **E2 地基持续**：中式库/营养数据补全 后台持续跑（分片 agent 联网核准《成分表》）。【推荐】**持续**。

---

## 三、拍板后执行约定
每项走标准无人值守流程：门禁(Apple-UX/copywriter 按需)+多方会诊 → 编码 → 强测试(正+逆+边界+异常) → Google 审查(阻断必修) → 存上下文 → 经验 → 飞书(4段) → commit(`[unattended]`) → push → 下一个。UI 改动(首页推荐卡)编码前先 Apple-UX 门禁细化 dp；账号统计涉 INTERNET 权限+后端+PIPL 合规文案(copywriter)。

---

## 四、✅ 用户已拍板（2026-07-19）——按此执行

- **A1 开发顺序**：✅ 按阶段 1→6。
- **B1 账号一期范围**：✅ **只做匿名统计**（不逼登录·完整账号留二期 AI 订阅/云同步时）。
- **B2 后端选型**：✅ **暂无服务器 → 走第三方 SDK = 友盟+（U-App·免费·境内·留存/活跃看板现成）**（2026-07-19 用户定）。免自建后端、当天见看板。落地：①客户端**内部埋点抽象层**(`Analytics`/`Tracker` 接口·只暴露 §2.1 八事件·杜绝健康明细越界·SDK 可换) ②友盟 **同意后才 init** + 关非必要设备采集 + 隐私政策披露 ③"关闭统计"开关在抽象层统一拦截。二期再评估迁自控 BaaS/自建。
- **B3/B4/B6**：✅ 按推荐（首启询问式默认不勾选·§2.1 最小事件集·随机 UUID·给关闭开关）。
- **E1 护城河项**：✅ **本期纳入**（成员化红绿灯 + 病种切换解读视角）。
- **三大项（含 AI 框架）**：✅ **本期都要**，但 **AI 框架特殊 gating**——
  - 用户战略：**一期就把整体 AI 框架搭好**（用户作为"内置默认 AI"提供方也要用、要测试），但**用户侧默认关闭 AI**，等**用户的 AI 订阅方案**就绪后一键开放。→ **一期包含 AI（供测试），用户侧默认关**。
  - 落点：`放开AI限制`（云端档自由创菜+自动建库·2026-07-19 已定向）+ `AI对话生成菜品`（方案已出）= "AI 框架"，共用前置基建 `AiRuntime 扩多轮 messages`。
  - **红线**：`放开AI限制` 编码前须先按用户专项要求**多方算法会诊出"AI 模型推荐待办方案"**（ai_engineer+algorithm_engineer+健康+copywriter）→ 拍板 → 再编码。
  - **药膳一期**（药食同源白名单·非 AI 框架）当普通功能纳入（方案 `药膳食补维度方案.md` 已出）。
  - "用户侧默认关 AI"落地：现状已是"配了云端 key 才走云端"（无 key=离线规则），天然 opt-in；框架层新增能力（自由创菜/对话）挂在同一 gating 后，未配置/未开放即不可达。

## 五、更新后的执行顺序（含 AI 框架·2026-07-19 拍板后）
1. **阶段1** 换一换第二批（收尾·会诊已修正）
2. **阶段2** 首页推荐下一餐（#1·Apple-UX 门禁→编码）
3. **阶段3** 账号匿名统计·客户端埋点层（后端待服务器）
4. **阶段4** 商业 P0 护城河：成员化红绿灯 + 病种切换解读视角
5. **阶段5** 中式库/营养数据补全（后台分片 agent 持续）+ 药膳一期最小集
6. **阶段6 AI 框架**（一期搭好·用户侧默认关·供测试）：前置 `AiRuntime 扩多轮` → `AI 模型推荐待办方案` 会诊拍板 → 放开AI限制(phased) + AI对话生成菜品 MVP
7. **阶段7** 其余 P1（条码/采购闭环/人群适配/GI 筛选导航）按价值排

> 状态：✅ 拍板完成，**按阶段1（换一换第二批）全速开工中**。
</content>
