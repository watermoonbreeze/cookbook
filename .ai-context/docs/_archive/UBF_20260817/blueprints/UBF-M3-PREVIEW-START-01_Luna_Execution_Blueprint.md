# UBF-M3-PREVIEW-START-01 Luna Execution Blueprint

Document Role: Architecture-authored Mechanical Entry Task
Mechanism: `STATIC_TARGET_BUNDLE`
Review Mode: `REMOTE_READ_ONLY_ARCH`
Delegation: `DELEGATED_SINGLE_TASK_CLAIM`
Execution Model: `GPT-5.6 Luna`
Handoff Parent: `0cb6d95057485bebb088523a6fd44a7e5ef1c2a4`
Expected Return TURN: `REVIEW`

## 0. Frozen architecture decision

Remote ARCH accepts the M2 End/Accept + M2→M3 Handoff persistence at `0cb6d95057485bebb088523a6fd44a7e5ef1c2a4`. Verified architecture evidence: `84cd8508... -> d8d72673... -> 0cb6d950...`, claim exact one State path, final exact seven paths, 7/7 target blobs, seven Preserve blobs, State concrete-model denyset, clean whitespace, remote ref/lifecycle truth and M2 mapping recount `48 total / 48 unique / missing 0 / duplicate 0 / UNRESOLVED 48`. Open issue register: `NONE`.

This task only consumes the accepted handoff and persists the M3 stage-entry contract. It creates **zero empirical corpus sample rows** and performs no calibration analysis.

## 1. Required final lifecycle

```text
M0/M1/M2: ACCEPT / CLOSED
M2→M3 Handoff: ARCH ACCEPTED / CONSUMED
M3 Preview/Start: PERSISTED / PENDING REMOTE ARCH REVIEW
M3 Corpus: NOT STARTED / NOT YET AUTHORIZED
M4/M5: NOT STARTED
CookBook Phase 3B: NOT AUTHORIZED TO START
TURN: REVIEW
```

## 2. Exact normal final allowlist

1. `A .ai-context/docs/项目改造规划/蓝图设计2/UBF-M3-PREVIEW-START-01_Luna_Execution_Blueprint.md`
2. `A .ai-context/docs/UBF-M3-PREVIEW-START-01-Execution-Report.md`
3. `A .ai-context/docs/项目改造规划/蓝图设计2/UBF-M3-PREVIEW-START.md`
4. `M .ai-context/docs/项目改造规划/蓝图设计2/UBF-M2-to-M3-Handoff.md`
5. `M .ai-context/docs/项目改造规划/蓝图设计2/universal_blueprint_framework_implementation_control.md`
6. `M .ai-context/docs/experience/14_模型执行力评估.md`
7. `M .ai-context/docs/context_memory/BLUEPRINT_STATE.md`

No eighth path is allowed.

## 3. Frozen M3 corpus-entry semantics

The exact M3 schema, eligibility, evidence minimum, labeling/adjudication authority, defect attribution, compatibility classification, confound controls, coverage dimensions, programmatic recount and acceptance gate are frozen in `UBF-M3-PREVIEW-START.md` and in this package's architecture design.

Mechanical invariants for this entry and every future M3 corpus package:

- one empirical sample = one bounded delegated execution episode, not one GC/file/checklist/view;
- `CALIBRATION_ELIGIBLE` requires independent reviewed outcome plus reconstructable closed/residual decision boundary and complete evidence minimum;
- semantic labeling/adjudication belongs to ARCH; CODE only persists frozen rows and runs recount;
- only independently verified `EXECUTION_DEVIATION` may support a negative coder capability signal after confound review;
- `ARCH_PAYLOAD_DEFECT`, `BLUEPRINT_DEFECT`, `SOFT_COMPATIBILITY`, `COMPATIBILITY_EXHAUSTED` and `EXTERNAL_TRUTH_CHANGE` are never coder-negative capability samples;
- linked retries/reworks share a Root Incident/Episode Cluster ID unless independence is proven;
- FULL/LITE, mechanism, risk/novelty, GC/Legacy and Actor Routing are orthogonal observed variables, not Universal Level proxies;
- raw corpus rows may not decide Universal Level count/name/threshold/envelope/mapping or final Task/Capability Profile/Selector;
- disputed labels remain `UNRESOLVED` until explicit ARCH adjudication;
- recount must make duplicate/missing/forbidden-negative/forbidden-Level/generated-view-only evidence violations mechanically visible.

This entry itself must recount empirical sample rows = `0`.

## 4. Source identity and routing boundary

Package source identity is bound to the fixed repository parent and the verified user canonical hashes recorded in the manifest. Root `MODEL_ROUTING.md` is shared coarse Actor/Capability Routing. Provider-specific routing files, when present, are distinct provider mappings, not assumed copies or drift. M3 observes actor/model facts as evidence only and mutates no routing source.

## 5. Preserve Set

Preserve M2 Final Accept, M2 mapping Work-01, M1 64-record decomposition, UBEA-v2, canonical GC registry, `.ai-context/PROJECT.md`, user canonical, routing, Project Graph, production code/tests/data/build/configuration and unrelated docs.

State must contain none of `GPT-5.6`, `Luna`, `DeepSeek`, `Claude`, `Gemini`.

## 6. Transaction

From an isolated clean environment at the exact Handoff Parent:

1. verify package inventory, manifest, fixed parent/ref and present/absent preimages;
2. materialize claim State only; stage exactly one path; verify expected staged blob, State denyset, whitespace, Preserve and clean tracked/untracked residue;
3. commit `docs(governance): claim UBF M3 preview start turn`, push and remote-verify; the claim commit becomes the Execution Parent;
4. materialize all seven final targets byte-for-byte with no semantic editing;
5. explicitly stage all seven allowlisted paths and re-read the index; do **not** infer scope from filesystem copy success or mtimes;
6. verify exact seven-path staged set, 7/7 staged blobs, create/modify status, State denyset, whitespace, zero empirical sample rows, Preserve, no untracked residue and final lifecycle assertions;
7. commit `docs(governance): persist UBF M3 preview start`, push, remote-verify, return evidence and STOP.

Do not use metadata-preserving copy or Git stat-cache behavior as evidence that a tracked path changed. If an expected path is absent from the staged set, treat the adapter as soft compatibility, re-materialize exact bytes through the next preauthorized equivalent adapter, force index refresh/re-stage and re-run scope/blob gates. **Never commit a partial staged set.**

If every authorized equivalent adapter is exhausted after claim, materialize only the exact abort State, commit `docs(governance): record UBF M3 preview compatibility block`, push, verify and STOP.

## 7. Compatibility / Hard STOP

Soft compatibility includes path separators, console encoding, Python/PowerShell/Bash differences, optional helper failure, nonessential tool absence, copy metadata/mtime behavior and Git stat-cache behavior while an equivalent exact-byte adapter remains.

Hard STOP is limited to remote-parent/continuity/delegation/TURN failure, unsafe isolation, package or inventory failure, preimage/source-authority mismatch, allowlist escape, target/staged blob mismatch, Preserve mutation, untracked/index residue that cannot be proven safe, State denyset/whitespace failure, invalid commit chain, unverified remote publication or any unclosed semantic choice.

Architecture-authored payload defects and soft compatibility events must not be recorded as coder capability negatives.

## 8. Adapter-independent evidence contract

Remote review evidence must include exact full parent/claim/final-or-abort identities and parent relations; remote refs after each push; NUL-safe tracked/untracked/index sets; package and preimage hashes; expected versus staged Git blobs for every claim/final/abort target; Preserve blob checks; State concrete-model denyset; whitespace; zero-sample recount; handoff consumption; M3 corpus non-start; M4/M5/Phase 3B gates; and explicit STOP confirmation.

Adapter PASS text alone is never sufficient evidence.

## 9. Independent canonical-requirement challenge

| Requirement | Blueprint location | Result | Evidence method |
|---|---|---|---|
| M2→M3 accepted handoff is consumed but corpus not started | §0–§3 | PASS | exact final Handoff/Preview/State targets |
| corpus schema and evidence/authority boundary preclosed | §3 + Preview §§2–10 | PASS | semantic inspection + target blob identity |
| no empirical sample rows in entry | §3, §6 | PASS | programmatic row-pattern recount = 0 |
| architecture/package/compat defects excluded from coder negatives | §3, §7 | PASS | frozen attribution rules + semantic assertion |
| Legacy/GC/FULL-LITE/risk/routing not used as Universal Level | §3–§4 | PASS | forbidden-inference assertions |
| no final Task/Capability Profile or Selector decision | §3 | PASS | semantic non-scope gate |
| no user canonical/GC registry/routing/Graph/production mutation | §5 | PASS | Preserve/denyset hashes + exact allowlist |
| REMOTE_READ_ONLY_ARCH delegated State-only claim | §6 | PASS | exact claim artifact and commit-chain contract |
| stat-cache/metadata-copy partial-stage failure prevented | §6 | PASS | explicit stage/index/blob recount before commit |
| M4/M5/Phase 3B remain prohibited | §1, §8 | PASS | final Preview/Handoff/State/control assertions |

## 10. Self-Application mutation audit

- `Stable Identity`: no accepted GC/task/model identity is renumbered or silently repurposed.
- `Contract / Semantic`: only the new M3 entry contract is created; user canonical and UBEA-v2 remain Preserve.
- `Lifecycle State`: M2→M3 moves from ARCH accepted to consumed; M3 Preview/Start moves to persisted/pending review; M3 corpus stays not started.
- `Acceptance Snapshot`: prior M2 persistence row is backfilled to `0cb6d95057485bebb088523a6fd44a7e5ef1c2a4` with remote ARCH ACCEPT evidence; current row remains pending review.
- `Generated View`: M2→M3 Handoff and M3 Preview/Start reflect the same lifecycle and do not create authority beyond the entry.

Package Self-Application must pass before release. CODE must not repair architecture semantics inside execution.

## 11. STOP boundary

Do not create empirical corpus rows, relabel current historical cases as samples, decide Universal Level count/name/threshold/envelope/mapping, finalize Task Profile/Capability Profile/Level Selector, mutate user canonical, canonical GC registry, routing, State ownership split, Project Graph, production code/tests/data/build/config, or start M4/M5/CookBook Phase 3B.

After successful final push, return the full final commit hash and required evidence, then STOP for remote ARCH review.
