# UBF-M1-PREVIEW-START-01 Luna Execution Blueprint

Document Role: Architecture-authored Mechanical Stage-Entry Package
Task ID: UBF-M1-PREVIEW-START-01
Blueprint Revision: R2
Date: 2026-08-12
Repository: cookbook
Review Operation Mode: REMOTE_READ_ONLY_ARCH
Execution Mode: EVALUATION / INDEPENDENT
Execution Model: GPT-5.6 Luna
Execution Worktree Mode: ISOLATED_DETACHED_CLEAN
Package Profile: FULL
CookBook Legacy Granularity: L7
Payload Mode: DETERMINISTIC_ARCH_AUTHORED_TRANSFORM
Handoff Parent: `eb1bdc846b3f746dde80e8a1fec234f6434b411f`
Expected Initial TURN: REVIEW
Execution TURN: CODE
Expected Return TURN: REVIEW
TURN Transfer Contract: REMOTE_READ_ONLY_ARCH / DELEGATED_SINGLE_TASK_CLAIM

## 0A. Frozen TURN Delegation Authorization

This task runs under `REMOTE_READ_ONLY_ARCH`. The remote architecture reviewer cannot directly mutate the repository to hand over the TURN. Therefore, for **this task only** and **this exact Handoff Parent only**, this Blueprint is the explicit architecture authorization for Luna/CODE to perform one delegated TURN claim before implementation.

The expected pre-claim state is:

```text
TURN=REVIEW
holder=ARCH / 架构师
Handoff Parent=eb1bdc846b3f746dde80e8a1fec234f6434b411f
```

Under this operation mode, that `TURN=REVIEW` state is the **required legal preimage**, not a blocker. Luna must **not stop merely because TURN is REVIEW** when all of the following are true:

1. `Review Operation Mode` is exactly `REMOTE_READ_ONLY_ARCH`;
2. repository HEAD / `origin/master` is exactly the fixed Handoff Parent;
3. the fixed State preimage hash matches Section 6;
4. the worktree used for the claim is isolated and clean;
5. no unauthorized path is changed.

When those conditions hold, Luna is explicitly delegated to execute the Section 9 claim:

```text
REVIEW / ARCH holder
        ↓ delegated state-only claim
CODE / Coder holder
```

The claim authorization is constrained as follows:

- it is single-task, single-parent, and single-use;
- it authorizes **only** the `BLUEPRINT_STATE.md` mutation generated by the package claim command;
- the claim commit message is fixed by Section 9;
- the claim commit must be pushed to `origin/master` and its remote visibility verified before the final transform proceeds;
- after a successful claim, that claim commit becomes the `Execution Parent`;
- Luna must not `/clear` between claim and final delivery;
- on normal or fallback completion, Luna must return the TURN exactly as specified: `CODE → REVIEW`, then push and STOP;
- this delegation does not authorize M1 semantic decomposition, M2, CookBook Phase 3B, any other task, any other parent, or any broader repository mutation.

If the initial TURN is not REVIEW, the parent/preimage does not match, the claim would touch anything other than State, or the push/remote verification fails, Luna must STOP under the package failure gates.

**Mode distinction:** this delegated self-claim is valid only because the batch is `REMOTE_READ_ONLY_ARCH`. Under `WRITE_CAPABLE_ARCH`, CODE must not infer an equivalent self-claim authority from this document.

## 0. Architecture Decision Entering This Batch

Remote ARCH independently reviewed `UBF-M0-END-ACCEPT-02` final delivery `eb1bdc846b3f746dde80e8a1fec234f6434b411f` and returned **ACCEPT**.

Verified facts entering this task:

- M0 remains `ACCEPT / CLOSED`;
- the M0 End/Accept + M0→M1 persistence chain is accepted;
- END-ACCEPT-02 exact 4-file delivery and deterministic 4/4 byte identity passed;
- `ARCH-PAYLOAD-01` is closed and remains attributed to the architecture-authored END-ACCEPT-01 payload, not to Luna execution deviation;
- M1 Preview/Start is now authorized as a separate batch;
- CookBook Phase 3B remains `NOT AUTHORIZED TO START`.

This batch **starts only the M1 stage-entry lifecycle**. It does not perform the M1 semantic decomposition itself.

## 1. Stage-Entry Meaning

M1 is `Current-State Semantic Decomposition`.

The architecture objective is to decompose existing governance into semantic objects before any policy rewrite. The eventual M1 outputs are:

- current-clause inventory;
- per-clause `Identity / Contract / Lifecycle / Snapshot / View` classification;
- current-state map for Level, GC, FULL/LITE, coder role, review and promotion;
- contradiction / gap / preserve matrix against the target architecture;
- CookBook-specific rules that must remain project-overlay material.

Every future candidate change must be classifiable as:

`PRESERVE / REDEFINE / MOVE / SPLIT / DEPRECATE-CANDIDATE`

with authority and evidence.

The current batch only persists this entry contract and lifecycle transition. Luna must not independently classify clauses, redesign Universal Level, or rewrite governance semantics.

## 2. Frozen Semantics / Preserve Set

The following remain frozen in this batch:

1. M0 `ACCEPT / CLOSED` and `UBF-M0-FINAL-ACCEPT.md` content.
2. R5 accepted review target `3489523db6508ba742ee835022d7e2a9a64f2c4f`.
3. END-ACCEPT-02 accepted final delivery `eb1bdc846b3f746dde80e8a1fec234f6434b411f` and its ARCH ACCEPT disposition.
4. Stable Identity / Contract / Lifecycle State / Acceptance Snapshot / Generated View distinction.
5. CookBook Legacy L7 and GC-01～GC-48 historical identities/evidence.
6. No Universal Level count is frozen by M0.
7. No CookBook Legacy L7 → Universal Level mapping is pre-decided.
8. Universal Level must not be defined by GC count, risk score, document length, or historical L1～L7 topic order.
9. Project overlays may set a safe floor but may not redefine Universal Level semantics.
10. M0 Truth Pack/Supplement and accepted historical reports remain evidence snapshots unless an explicit erratum task reopens them.
11. Project Graph and CookBook Phase 3A/3B state are outside this batch.

## 3. Exact Normal Delivery Allowlist — 7 Files

The final commit may contain **exactly** these paths:

1. `A .ai-context/docs/项目改造规划/蓝图设计2/UBF-M1-PREVIEW-START-01_Luna_Execution_Blueprint.md`
2. `A .ai-context/docs/UBF-M1-PREVIEW-START-01-Execution-Report.md`
3. `A .ai-context/docs/项目改造规划/蓝图设计2/UBF-M1-PREVIEW-START.md`
4. `M .ai-context/docs/项目改造规划/蓝图设计2/UBF-M0-to-M1-Handoff.md`
5. `M .ai-context/docs/项目改造规划/蓝图设计2/universal_blueprint_framework_implementation_control.md`
6. `M .ai-context/docs/experience/14_模型执行力评估.md`
7. `M .ai-context/docs/context_memory/BLUEPRINT_STATE.md`

No eighth path is allowed.

## 4. Fallback Delivery After Claim — Exact 3 Files

If the TURN claim has already been pushed but the deterministic final transform cannot safely complete, the only fallback scope is:

1. `A .ai-context/docs/项目改造规划/蓝图设计2/UBF-M1-PREVIEW-START-01_Luna_Execution_Blueprint.md`
2. `A .ai-context/docs/UBF-M1-PREVIEW-START-01-Execution-Report.md`
3. `M .ai-context/docs/context_memory/BLUEPRINT_STATE.md`

Fallback must return `TURN=REVIEW` and keep:

- M0 `ACCEPT / CLOSED`;
- M0→M1 handoff not consumed by a successful M1 entry;
- M1 semantic decomposition `NOT STARTED`;
- CookBook Phase 3B `NOT AUTHORIZED TO START`.

Do not modify Handoff, Control or model ledger in fallback.

## 5. Zero-Diff Deny Set

The following are not authorized to change:

- `.ai-context/docs/项目改造规划/蓝图设计2/UBF-M0-FINAL-ACCEPT.md`;
- M0 Truth Pack and Supplement;
- all R2/R3/R4/R5 blueprints and reports;
- END-ACCEPT-01 and END-ACCEPT-02 historical execution reports/blueprints;
- `.ai-context/docs/experience/12_多模型协作与实施蓝图规范.md`;
- `.ai-context/docs/experience/INDEX.md`;
- `.ai-context/project_graph/**`;
- all production code, tests, build/configuration files;
- all user-level files outside the repository.

No amend, reset, rebase, force push, history rewrite, or allowlist expansion is permitted.

## 6. Fixed Parent Preimages

Before TURN claim the deterministic script verifies these exact Handoff Parent snapshots:

- `UBF-M0-to-M1-Handoff.md` SHA-256: `038a1e074f118a4d192ead10d50dc9ef5ea9ab8fdfde9370943bcd56982f6359`
- `universal_blueprint_framework_implementation_control.md` SHA-256: `22709a7266ae26989ee29cae0ed5561cf59122244200dcaa935ebe5b8bb5d4ef`
- `14_模型执行力评估.md` SHA-256: `47b4d04defe47d01e00cfdf3afb1d46716534f00cd2548c936382e89e8fd16e6`
- `BLUEPRINT_STATE.md` SHA-256: `92fceaad1b3f1791fc5e9a0e2a077b5f100d3485178992e1b296200e4ddae7a5`

Any mismatch before claim is `NON_PUBLISHABLE_STOP`.

## 7. Package Integrity

The package contains:

- this Blueprint;
- `m1_preview_start_01_apply.py`;
- `MANIFEST.sha256`.

Run first:

```bash
python <PACKAGE_PATH>/m1_preview_start_01_apply.py package-check
```

Every entry must report `OK`. Do not rewrite the script or Blueprint.

## 8. Remote Preflight and Isolated Worktree

Use the repository's configured `origin`; never copy the full origin URL into reports or chat.

```bash
git fetch origin
git rev-parse origin/master
```

It must equal exactly:

```text
eb1bdc846b3f746dde80e8a1fec234f6434b411f
```

Otherwise stop before TURN claim. Do not reset/rebase the user's worktree.

Create an isolated detached clean worktree at the fixed Handoff Parent. The original worktree/index/untracked files remain untouched.

## 9. TURN Claim — Commit 1

**Architecture authorization:** execute this section when the preflight proves the expected initial `TURN=REVIEW` State at the exact Handoff Parent. In `REMOTE_READ_ONLY_ARCH`, this state-only claim is the authorized handoff operation and must not be rejected merely because ARCH currently holds REVIEW.

From the isolated worktree at `eb1bdc846b3f746dde80e8a1fec234f6434b411f`:

```bash
python <PACKAGE_PATH>/m1_preview_start_01_apply.py claim --repo .
git diff --check
git diff --name-status
```

Only State may change.

Then:

```bash
git add -- .ai-context/docs/context_memory/BLUEPRINT_STATE.md
git diff --cached --check
git diff --cached --name-status
git commit -m "docs(governance): claim UBF M1 preview start"
git rev-parse HEAD
git push origin HEAD:master
git fetch origin
test "$(git rev-parse origin/master)" = "$(git rev-parse HEAD)"
```

The remote equality check is mandatory. Save the full claim hash as `<CLAIM_COMMIT>` only after it passes. The successful claim commit is now the Execution Parent and the authorized execution TURN is CODE.

## 10. Deterministic Final Transform

At clean `<CLAIM_COMMIT>`:

```bash
python <PACKAGE_PATH>/m1_preview_start_01_apply.py final --repo . --claim <CLAIM_COMMIT>
```

The fixed transform performs only these semantic operations:

1. adds this exact Blueprint and deterministic Execution Report;
2. adds the M1 Preview/Start stage-entry document;
3. records END-ACCEPT-02 final commit `eb1bdc846b3f746dde80e8a1fec234f6434b411f` and REMOTE ARCH ACCEPT in the model ledger;
4. appends exactly one current M1 Preview/Start CODE evidence row whose execution result is `COMPLETE / PENDING REMOTE ARCH REVIEW` while its own final commit/ARCH assessment remain pending by contract;
5. updates the M0→M1 Handoff from stale pending-review lifecycle text to `ARCH ACCEPTED / CONSUMED BY M1 PREVIEW/START-01`, without changing inherited M0 evidence semantics;
6. updates Implementation Control to the M1 stage-entry lifecycle and removes the obsolete statement that M1 is not yet authorized;
7. updates State to return `TURN=REVIEW` with M0 closed, accepted handoff consumed, M1 Preview/Start persisted/pending review, **M1 semantic decomposition not yet executed**, and Phase 3B still unauthorized.

## 11. Architecture Semantic Truth Gate

Both gates must pass:

### Payload Identity Gate

- exact parent preimages match;
- claim is State-only;
- final changed-file set is exactly 7;
- generated files equal the architecture-authored transform;
- `git diff --check` passes.

### Lifecycle / Truth Gate

- END-ACCEPT-02 ledger row contains `eb1bdc846b3f746dde80e8a1fec234f6434b411f` and `REMOTE ARCH ... ACCEPT`;
- current M1 row result is COMPLETE/PENDING REVIEW, never an execution-before placeholder;
- Handoff no longer says `PERSISTED / PENDING REMOTE ARCH REVIEW`;
- Handoff records the accepted persistence review target `eb1bdc846b3f746dde80e8a1fec234f6434b411f`;
- Control says M1 Preview/Start is the current stage entry, while semantic decomposition is not yet executed;
- State contains no concrete model names;
- State retains M0 `ACCEPT / CLOSED`;
- State marks M0→M1 Handoff accepted/consumed;
- State marks M1 Preview/Start pending remote review;
- State explicitly says M1 semantic decomposition is not yet executed;
- State and stage-entry document keep CookBook Phase 3B unauthorized;
- no Universal Level count/mapping or GC redesign is introduced.

## 12. Final Validation / Commit — Commit 2

Run:

```bash
git diff --check
git diff --name-status
git status --short
```

Stage exactly the seven normal paths and verify `git diff --cached --name-status` contains no eighth path.

Commit:

```bash
git commit -m "docs(governance): persist UBF M1 preview start"
git rev-parse HEAD
git push origin HEAD:master
```

Do not amend either commit.

## 13. Completion Contract

Normal completion requires:

- `eb1bdc846b3f746dde80e8a1fec234f6434b411f` → claim → final exact two-commit chain;
- claim State-only;
- final exact 7-file allowlist;
- deterministic identity and semantic truth gates PASS;
- END-ACCEPT-02 ARCH ACCEPT backfilled;
- M0 remains closed;
- M0→M1 Handoff becomes accepted/consumed;
- M1 Preview/Start becomes persisted/pending remote ARCH review;
- M1 Current-State Semantic Decomposition **has not yet been performed**;
- Phase 3B remains unauthorized;
- TURN returns to REVIEW.

CODE must not self-ACCEPT this stage entry and must not begin M1 semantic classification.

After a safe push, return only the full final commit hash to the user.
