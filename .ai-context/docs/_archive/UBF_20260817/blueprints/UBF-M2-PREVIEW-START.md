# Universal Blueprint Framework — M2 Preview / Start

Document Role: Generated View / Stage Entry Contract
Stage: `M2 — Legacy Asset Mapping`
Entry Task: `UBF-M2-PREVIEW-START-01`
Exact Handoff Parent: `2054899ad93d9c2bc1353914c31a1ef3b96c15ac`
Entry Status: `ACCEPT / CONSUMED BY UBF-M2-LEGACY-ASSET-MAPPING-WORK-01`
M1 Status: `ACCEPT / CLOSED`
M1→M2 Handoff: `ARCH ACCEPTED / CONSUMED BY THIS ENTRY`
M2 Mapping: `WORK-01 ARCHITECTURE FROZEN / PENDING MECHANICAL PERSISTENCE AND REMOTE ARCH REVIEW`
M3: `NOT STARTED`
CookBook Phase 3B: `NOT AUTHORIZED TO START`

## 1. Entry meaning

This document defines the M2 stage-entry contract. It does not contain mappings and is not canonical Truth. Remote ARCH accepted this entry at `c72a19b257550de7bb75dc9361b9f939fc220cb9`; it is consumed only by the separately architecture-authored Work-01.

## 2. Objective

M2 will non-destructively map CookBook Legacy L1～L7 and GC-01～GC-48 without erasing identities, history, provenance or accepted evidence.

Each future GC record must carry stable identity, Origin, Current Authority, Decision Category, Applicable Task Profile candidate, Closure Effect, Preserved Coder Discretion, Evidence Type, Legacy Level, Universal Level Mapping (`UNRESOLVED` allowed/expected), Lifecycle Status and evidence/contradiction references.

## 3. Frozen interpretation rules

- Current L1～L7 describes historical GC coverage, not future Universal Level.
- Universal Level measures remaining coder decision space and architecture closure.
- No mapping may be inferred from GC number/count/order, Legacy Level, risk score or document length.
- FULL/LITE, Risk/Novelty, Actor Routing and Universal Level remain orthogonal axes.
- Project overlay supplies project Truth/evidence/floors but cannot redefine Universal semantics.
- Authority conflicts remain explicit; unknown mapping is recorded as `UNRESOLVED`, never guessed.
- Root/per-model routing sources require identity discovery before comparison.

## 4. Mapping-work acceptance contract

A later architecture-authored Work-01 must preclose the record schema, complete GC inventory, source precedence, contradiction handling, allowlist, Preserve Set and evidence tests. It must cover GC-01～GC-48 exactly once, with missing=0 and duplicate=0. CODE may mechanically persist frozen records but may not invent mapping values.

## 5. Preserve and non-scope

Preserve all M1 outputs, GC identities/history/provenance, Legacy evidence, canonical files, Project Graph and production assets.

This entry does not authorize mapping rows, Universal Level count/names, Task/Capability Profile schema, selector logic, M3 corpus, canonical mutation, fallback synchronization, State split, routing mutation, Project Graph repair or CookBook Phase 3B.

## 6. Next gate

Entry gate is closed. `UBF-M2-LEGACY-ASSET-MAPPING-WORK-01` now carries the exact 48-record architecture result. Its persistence and remote acceptance are separate gates; this entry does not authorize M2 closure, M3 or CookBook Phase 3B.
