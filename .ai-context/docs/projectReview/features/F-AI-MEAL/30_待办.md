# F-AI-MEAL · 待办

- 真机验证详单见 `真机验证/真机待验证清单_<最新>.md`（`E-L1-01~12`/`E-K1I-01~02`/`E-B4/B5/B6-*`/本批新增项 等 ~40+ 项），本文件不重复摘抄。
- **cuisine/meal_slots 真透传（决策项，需先扩协议）**：NDJSON 事件加可选 `cuisine`/`meal_slots` 字段 + prompt 明确"不确定不填"，透传时加空值守卫；`meal_slots` 若收，只能做"菜名推断兜底 ∪ AI 给的值"的并集（归一后为空要退回兜底），不能让 AI 的窄口径替换掉宽口径兜底。
- **确认页展开 UI（需先过 Apple UX 设计门禁）**：展示新建菜的做法/标签/食材清单摘要，建议与下一条"prompt 补 `cooking_step` 等事件"合并成一批——否则做出来的展开区在真机上恒空、验不出效果。
- **AI 从未产出 steps/tags/description 事件**：`AiMealPrompt.NDJSON_SYSTEM_PROMPT` 目前只要求 `meal`/`dish`/`ingredient`/`seasoning` 事件，未要求 `cooking_step`；即使 B7 修复了透传链路，AI 现在也没有内容可透传。是否要求 AI 产出这些内容（token 成本 vs 内容完整性）待决策。
- **死代码整组清理**：`AiMealParser.kt`/`SchemaValidator.kt`/`SchemaMigration.kt`/`AiMealInputSchema.kt` 已确认零生产调用方，本批只加了警示注释；完整删除需先理清 `FlatToDayMealConverter`（仍被 `StreamingMealParser` 用）/`MealParseCanonicalizer`（仍被 `RuleMealParser` 用）的共用关系，避免误删还在用的依赖。
- **dish_id 本地序号的边界情况**：当前实现里，无 `dish_id` 也无 `dish_name` 的子事件（食材/调料/做法步骤）仍无法归属，这是协议本身的限制（AI 必须提供至少一种归属信号），非本批引入。
- **HTTP 类 AI 失败原因文案带内部代号**（2026-08-19 用户核验 E-CFG-03 时发现）：确认页"本次结果：规则解析（AI 解析失败：xxx）"里，`xxx` 若来自 HTTP 类失败（401/500 等），会带出 `STREAM_HTTP_ERROR` 这个内部代号后缀（如"HTTP 401 STREAM_HTTP_ERROR"），`AiMealInputViewModel.humanizeWarning()`（`androidApp/.../ui/ai/AiMealInputViewModel.kt:634`）未覆盖这个模式；`AiMealInputViewModelStreamTest.kt` 的 `T-B3-05` 目前按现状文案断言（改文案需同步改断言）。非阻断，晚点交给 `copywriter` 角色统一规划这类 AI 失败原因文案（含其余未覆盖的技术代号），别单独零敲碎打。
- **`google_quality_engineer` 复核遗留 4 项建议（非阻断，本批未做）**：①`summarizeDiagnostics` 的 8 个分类桶穷尽性靠人工维护，新增 `DiagnosticCode` 时容易漏接（已加通用 ERROR 兜底缓解，但精细分类计数仍需手动维护，建议改 `when` 穷尽式让编译器强制处理）；②`handleDishChildEvent`/`handleCookingStepEvent` 等子事件按"最近一次解析出的本地 dishId"指针挂靠，dish_id 被复用后**迟到**的子事件仍可能挂错菜（`dish_name` 精确匹配优先级应高于"最近一次"指针，当前是反过来的）；③`summarizeDiagnostics` 无专门单测，目前靠 `AiMealInputViewModelStreamTest` 间接覆盖；④二轮复核新发现：`summarizeDiagnostics` 的未分类 ERROR 兜底分支是**原文透传**（未做人话化），且无条数上限——极端情况下会在诊断区刷出十几条开发者协议措辞（如"NDJSON 行缺少 segment_id，已丢弃: …"），建议后续把 `SEGMENT_MISMATCH`/`INVALID_DATE` 也纳入分类桶 + 兜底列表加条数上限（如最多 3 条+"等 N 条"）。

## 迁移自跨功能待办文件（2026-08-19）

| ID | 状态 | 项 | 说明 |
|---|---|---|---|
| 战略会商-5430 | 📌 | 拍照扫码系/条码录入 | 条码优先级上提(确定性映射)。拍照识菜缓做。 |
| K1b | 🔄📄 | AI解析带入家庭健康档案做膳食评价（逐成员化） | 蓝图已起草+GC-37独立挑战完毕（11阻断+9建议待处置），2026-08-08 用户叫停"先做这个"、改为等 AI快捷记主线（真机验证+L1/K1e/K1h/K1i）收尾后再拾起。蓝图见 `AI记一餐_K1b膳食健康评价逐成员化_实施蓝图.md` §10，拾起时直接处置阻断项，不必重新起草。 |
| K1g | 📄 | AI记餐周期记+NDJSON流式解析改造 | 已定方案：快速记≤200字；周期记按周/日期分段；max token 按场景放大到覆盖一周；确认页发送后即进入生成态，NDJSON/JSONL 边解析边展示，整体 JSON 作为兼容输入规范化。见 projectReview/21 与 D-16。 |
| K15 | ⬜ | AI 分段解析与可控降级 | 见 `AI记一餐_分段解析与可控降级方案.md`：语音→AI；失败保原文、用户确认转模板；按天/餐次分段防 token 截断。 |
| K1c | 🔄 | 规则引擎日期推算+星期偏移 | weekdayHint→推算未来最近对应星期几的 date_offset。 |
| K1d | ⬜ | JSON Schema客户端与服务端双端兼容 | 生成 `.schema.json`+抽象入库编排流程+FlatMealJson 优先作为交换格式。 |
| J14 | ⬜ | 菜名解析自动补调料/主食食材 | "红烧肉"→自动加酱油/糖/料酒+"大排饭"→补大米。守 T1 事后留痕。 |

## 2026-08-20 真机反馈批新增

| ID | 状态 | 项 | 说明 |
|---|---|---|---|
| B7F-VOICE | ⬜🟡 | 语音输入报"语音引擎繁忙" | 真机反馈（E-B6-VOICE-01）：点击语音输入按钮不断提示"语音引擎繁忙"。**本轮已暂时隐藏语音入口**（`AiMealInputSheet.kt` 顶部 `VOICE_INPUT_ENABLED = false`，代码保留未删），此项是隐藏之外还要修的根因 bug——排查 `VoiceRecognizer` 实例生命周期/系统识别服务占用释放逻辑，修好后再把开关打开。 |
| AIMEAL-UX-REDESIGN | ⬜🔴 | **AI 快捷记界面重构**（方案讨论级，需先出交互方案再动手；2026-08-20 用户确认这就是他后面要做的那个"AI快捷记界面重构"，本条是唯一入口，不再另开） | 用户原话："再加上一个方案讨论的待办(AI快捷记界面要调整)"，后续追加："后面还有一个是我要对AI快捷记进行界面重构的，你一起归类一下"。**已知需要一并纳入讨论的具体问题**（按发现顺序）：<br>①**输入框长按不响应系统复制/粘贴菜单**（快速记+周期记均如此，`E-B4-03`/`E-B5-10`/`D13`）——2026-08-20 代码+字节码级排查：`AiMealInputSheet.kt` 的输入框全部渲染在 `ModalBottomSheet` 内容里；反编译 `material3-1.1.2.aar` 确认该版本 `ModalBottomSheet` 用 `ModalBottomSheetWindow`（`WindowManager.addView` 的裸浮层窗口，非 `Dialog`/Activity Window）承载内容。用户追问"API Key 输入框/搜索输入框为什么可以"后逐一核实：`KeyDialog`（`AiSettingsScreen.kt:522`）用的是 `AlertDialog`（真 Dialog 窗口），`AppSearchField` 四个调用点全部是整页路由，**全项目扫下来没有第二处"可编辑文本框长在 `ModalBottomSheet` 里"**——反例反而印证了假设，不是巧合。真正的修法大概率是"升级 Material3（≥1.2 之后 `ModalBottomSheet` 改用 Dialog 实现，M3 1.2+ 才有 `SelectableDates`/`SegmentedButton` 等组件，见 `CLAUDE.md` 已知版本限制）"或"这个输入流程换成非 Sheet 的承载方式（`AlertDialog`/全屏页，同 API Key 输入框、`AppSearchField` 的路子）"——两条都是架构级选择，需要和"要不要顺带做别的 UI 调整"一起讨论，不该单独当小 bug 改。通用踩坑已记 `experience/06_问题与踩坑.md`"真机待验证清单批量处置 + ModalBottomSheet 长按排查 session"节，**这个重构启动时必须处理，别只做视觉调整绕过去**。<br>②因为①暂时无法保证系统长按菜单可用，**自制"📋 粘贴"按钮本轮先保留未删**（用户原话要求删掉它，但删掉后若长按确实不可用会导致用户在 AI 快捷记里完全没法粘贴，是比现状更差的回归）——`appendText`/`onVoiceResult` 绕过截断的 bug 已修（`E-B6-05`/`E-B6-TRUNC-01`），且经 `google_quality_engineer` 复审又抓出"Sheet 内 Snackbar 大概率被浮层窗口遮挡看不见"这条关联阻断（同一 `ModalBottomSheet` 浮层窗口根因的另一种表现），已改为 Sheet 内联文字提示（不再依赖 Snackbar），单测已补（`AiMealInputViewModelStreamTest.kt` 4 条 B6-fix2 用例）。<br>③**新发现（`google_quality_engineer` 复审）**：`RuleMealParser` 对"差不多一碗面"这类模糊量词开头的输入，`QUANTITY_UNIT` 正则是 `^` 锚定的，"差不多"挡在最前面会让"一碗"**识别不到份量**、且"差不多"本身**原样留在菜名里**（菜名变成"差不多一碗面"而非"面"），这是比 F5-3 原本关心的 eaten_ratio 更实质的解析质量问题——现有代码已有"吃了/喝了/准备吃"这类动词剥离的先例（`RuleMealParserRegressionTest.kt` FIX-1/FIX-2），"差不多/大概/大约"这类模糊量词前缀理论上可以照同一套模式处理，重构/规则解析模板梳理（`AIMEAL-RULE-TEMPLATE`）时可以一并评估要不要做。 |
| AIMEAL-RULE-TEMPLATE | ⬜🟡 | 规则解析（非 AI 兜底路径）输入模板需要重新规划 | 用户原话（`D11`/`D14`）："规则解析的模板需要更新，你记到待办中，待办中先注明当前规则解析模板怎么样的，我看下如何调整比较好"。**当前模板现状**（说明弹窗 `HelpSheet`，`AiMealInputSheet.kt:767-770`「🗓️ 多天菜单模板」小节，原文）：`日期/周/天` + 换行 + `餐次（早、中、晚、加餐）：菜品（食材, 食材），菜品`，菜与菜之间用逗号分隔、同菜食材写在括号内（逗号/+/顿号均可），示例："周一\n晚饭：乌冬面（番茄炒蛋），清蒸桂鱼（桂鱼），清炒生菜（生菜）"。**已知这段模板文案本身已经过期**：文案第一行还在教用户"日期/周/天"写在输入文本里，但按本轮批量决策（见 `08_决策记录.md` 相关条目），AI 快捷记的日期现在完全由"快速记/周期记"结构本身固定，**不再支持在输入文本里写日期**——这段模板连同示例都需要跟着改，不只是"调整"这么简单，是要先明确新模板长什么样。实际解析规则在 `TextSegmenter`/`RuleMealParser`（`shared/.../ai/`，具体文件名待接手时 grep 确认），改模板前建议先过一遍解析器实际能认的语法，再决定文案怎么写、要不要顺带放宽/收紧语法本身。 |
