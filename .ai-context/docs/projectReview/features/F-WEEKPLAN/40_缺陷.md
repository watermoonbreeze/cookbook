# F-WEEKPLAN · 已知问题

（暂无未修复项）

## 已修复

- **2026-08-20**：`PeriodDayBlock.kt`（周期记/周计划每日输入块，AI 快捷记周期记与周计划共用）截断提示改内联文字，不再依赖 Snackbar——根因见 `F-AI-MEAL/30_待办.md` `AIMEAL-UX-REDESIGN`：该组件恒在 `ModalBottomSheet` 内使用，Snackbar 挂 Activity 主窗口，层级被 Sheet 遮挡大概率看不见。同批移除了 `onTruncated` 回调参数（组件改自包含），调用方 `AiMealInputSheet.kt` 已同步。真机验证详单在 `真机验证/真机待验证清单_<最新>.md` 的 E-B6-TRUNC-01。
