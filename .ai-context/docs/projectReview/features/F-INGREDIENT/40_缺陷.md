# F-INGREDIENT · 已知问题

（暂无）

## 迁移自待办_Bug修复.md（2026-08-19）

| ID | 状态 | 项 | 说明 |
|---|---|---|---|
| Bug修复-5305 | ✅ | 添加食材：清空预填后手动选营养分类→仍被自动classify覆盖 | Sol 已裁决 `ARCH_ACCEPTED / AUTOMATED_GATES_PASS`，无 AF。既有 `groupTouched` guard 已收敛为纯策略并补 `T-5305-01~04`：名称变化/分类加载不得覆盖手选，表单 reset 后才恢复自动候选。`:androidApp:testDebugUnitTest --rerun-tasks`（BUILD SUCCESSFUL，4m13s）、`:androidApp:assembleDebug --rerun-tasks`（BUILD SUCCESSFUL，4m）及质量检查均通过；工作树未提交。真机 `DEV-5305-01` 见 `真机待验证清单_202608270943.md`，仍为 `PENDING_DEVICE_VERIFICATION`。 |
