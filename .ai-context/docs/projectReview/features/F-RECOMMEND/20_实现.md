# F-RECOMMEND · 现状与实现

## 现状
✅ 已落地：规则引擎 + 云端 GLM + 成长画像 + MMR 多样性 + 逐菜理由 + 推荐风格；餐次查关联表 + 口味汇总喂 prompt + 药膳本地重排缓存。

## 代码落点
- 单餐(库存/随机): A ui/ai/AiRecommendScreen,AiRecommendViewModel; S ai/RecommendationOrchestrator,RecommendationDataSource
- 组合评分(荤素/主食/餐次差异·真相源): S ai/MealCompositionScorer
- 菜数/份量/餐次: S ai/MealPortion,MealSlot
- 自由搭配: A ui/pairing/FreePairingScreen,FreePairingViewModel; S ai/FreePairingEngine
- 风格/口味/去重/菜系: S ai/RecommendationStyle,TasteProfile,CuisineFamily,CookingHeaviness

---
2026-08-27 回写：`cd782504`（统一流程状态恢复）与 `4c0bdd48`（推荐路由 Trace 诊断）仅补状态/诊断与边界测试；未改推荐算法、规则降级、数据源或用户可见推荐策略。

2026-08-27 回写（L2）：`RecommendationDataSource.gatherConstraints()` 新增真实 category-id 回归测试，确认 `care_fatty_liver` 经全家 `member_care` 并集仍按既有 avoid/limit/recommend 三档消费；未改推荐数据源实现、排序、提示或算法。

最后更新：2026-08-29 · `HealthRuleEngine` 在既有“正常→最近→忌口、score”排序键末尾补 `dish.id` 升序同分 tie-break；`RecommendationOrchestratorTest` 新增 T-RR-01~05 固定 fixture，锁定候选顺序、轮转批次、四种 style 与 avoid/recent 分层可复现性。`:shared:testDebugUnitTest`（含 `RecommendationOrchestratorTest`/`RecommendationDataSourceTest`）与 `:androidApp:assembleDebug` 通过；未改权重、DataSource、云端、schema、UI。
