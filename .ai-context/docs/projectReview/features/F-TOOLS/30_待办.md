# F-TOOLS · 待办

> 2026-08-20 UEN 批次：悬浮导航栏与独立“+”已落地，当前转入真机验收 `E-UEN-14~15`；`MATERIAL3-UPGRADE` 仍为独立评估项。

- 端侧本地 AI 接入（当前占位）。
- `OBS-NEXT`：自动化门已通过；`E-OBS-NEXT-01~07` 与既有 MDC3 运行时项统一保留在唯一真机清单，全部代码批次完成后集中执行。未执行前不得标记运行时 PASS/关闭。

## 迁移自跨功能待办文件（2026-08-19）

| ID | 状态 | 项 | 说明 |
|---|---|---|---|
| J16 | ⬜ | 采购清单入口移到食材-库存页 | 悬浮购物车图标+份数徽标(99+)。复用 §九 FAB+贴角圆角标。 |
| U3 | ⬜ | 设置适老版 | 字号、触达、密度、对比度；复用共享适老规范。 |
| 功能算法-5502 | ⬜ | AI S4 端侧本地模型 | OnDeviceAiRuntime 已占位，接 LiteRT-LM。 |
| I-Mine | ⬜ | "我的"页重新归类 | 高频在前、低频折叠、可合并的收进弹框，简洁易找。需 Apple-UX 门禁。 |
| I-About | ⬜ | "关于·已为你开启"突出开启内容 | 让用户一眼看到开了哪些功能。与"我的归类"合并做。 |

## 2026-08-20 方案讨论新增

| ID | 状态 | 项 | 说明 |
|---|---|---|---|
| NAV-COMPACT | ⬜🔴 | 底部导航栏改悬浮胶囊栏 + 独立悬浮"+"按钮 | 方案讨论级，需先过 `apple_ux_designer` 门禁出精确 dp/间距再动手。用户原话："导航栏的高度需要调小一点，太大了，调试要减少至少1/4，然后字体样式都要调整"。讨论后拍板方向（用户选定）：**导航栏体脱离屏幕边缘悬浮成胶囊形**，"+"号从栏内正中（`MainScaffold.kt` `BottomBar`/`CenterPlusFab`）改成**独立悬浮的卫星按钮**（不再等比跟随栏体缩小，保留核心入口的视觉权重，位置待设计阶段定，常见落点右下角或栏体上方悬浮）。当前栏体是 Material3 `NavigationBar` 默认高度（约80dp量级）+ `labelMedium` 标签，目标压至少缩 1/4（约56-60dp量级，贴近 iOS 49pt 体感），标签建议 `labelMedium`→`labelSmall`、图标同步收窄。**改动面提示**：悬浮栏脱离边缘会改变现有 `navigationBarsPadding()` 避让逻辑（`MainScaffold.kt:171-175` 无底栏路由的系统栏避让、`showBottomBar` 判断），以及沉浸式系统栏颜色跟随（`SideEffect` 里 `navigationBarColor`），需要一并核对不能只改 `BottomBar` 组件本身。与 `AIMEAL-UX-REDESIGN`"+"号统一入口方案同批设计（悬浮"+"按钮点击后打开的就是统一入口新页面）。 |
| MATERIAL3-UPGRADE | ⬜🟡 | Material3 1.1.2→1.3.x 升级可行性（独立评估项，不阻塞当前统一入口/导航栏改造） | **背景**：`ModalBottomSheet` 装可编辑文本框长按不响应系统复制粘贴菜单的结构性限制（详见 `AIMEAL-UX-REDESIGN`），当前解法是把 AI 快捷记输入框单独迁到全屏页绕过；升级 Material3 能从根上解决，且一次性惠及全项目**10个文件、34处 `ModalBottomSheet` 调用点**（`AiMealInputSheet.kt`/`AiMealInputViewModel.kt`/`AiSettingsScreen.kt`/`AddDayFoodScreen.kt`/`AiPlanScreen.kt`/`AiRecommendScreen.kt`/`EatenAdjustSheet.kt`/`NutritionTableScreen.kt`/`ActionSheet.kt`/`PeriodDayBlock.kt`），包括交接文档提过还没排查的 `EatenAdjustSheet`/`ActionSheet` 撤销 Snackbar 遮挡同根因隐患。**`dev_research_engineer` 2026-08-20 调研结论**（部分因本环境网络限制未能闭环，动手前必须补验，不可直接采信）：<br>①**高置信**：`ModalBottomSheet` 从 **Material3 1.3.0**（稳定版）起改用真正的 `ComponentDialog` 承载（androidx 源码 `ModalBottomSheetDialogWrapper : ComponentDialog(...)` + commit `33c32f6`"Migrate bottom sheet to dialog implementation from popup"，2024-04-19，直接确认）。<br>②**版本链**：`compose-material3` 可升到已发布的 `1.3.0`/`1.3.2`；`compose-ui`（当前1.5.4）大概率要跟到约1.7.0（**精确版本号未核实**）；**Kotlin（当前1.9.20）大概率要升到 2.0.0+**（**本次调研最大未闭环项**，因 `developer.android.com` 官方页面本环境反复读取失败/摘要矛盾，未拿到一手确证——这是 Kotlin 编译器主版本跳跃，涉及 Compose Compiler 从独立 artifact 切到 Kotlin 自带的 Compose Compiler Gradle Plugin 机制变化，不是改个版本号数字那么简单）；AGP 保持 `8.2.2` 不用动（已核实在 Kotlin 2.0 官方兼容区间 7.4.2-8.3 内）。<br>③**破坏性变更排查（已本地实测）**：`Divider(` 全项目40处/21个文件调用，1.2.0-alpha04起软废弃（`@Deprecated`不阻断编译）；`FilterChip`/`LargeTopAppBar` 未发现确定性破坏；`SwipeToDismiss` 本项目实际0处调用（CLAUDE.md"可用实验组件清单"是通用说明非本项目已用）。<br>④minSdk 21 不受影响（已核实）。<br>⑤**遗留未闭环项（真要做之前必须先在分支里实测钉死，不可直接照此报告动手）**：Kotlin 是否强制2.0+、Material3 1.3.0精确对应的compose-ui版本号、`issuetracker.google.com/issues/327085717`（疑似同名已知问题）的具体状态与官方修复版本号（该页需登录，本环境读不到）。<br>**决策建议（非拍板，供后续评估）**：价值真实（一次性根治10个文件同类隐患，比现在的个案绕过更彻底），但 Kotlin 主版本跳跃风险点没摸透之前不建议顺手做；等评估窗口时先开分支实测补验上述遗留项，再决定是否/何时升级。 |
