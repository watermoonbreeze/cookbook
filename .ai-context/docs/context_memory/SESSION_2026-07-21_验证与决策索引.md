# 📑 SESSION 2026-07-21 · 验证与决策索引（你先读这份·逐个看）

> 超长无人值守 session 收束。本文是**单一入口**：①要你确定的方案 ②要你真机验证的全部内容 ③产出文档索引。全部代码已 push（origin/master `e18a04d`）。

---

## 一、📋 要你确定的方案（我据此继续）

| # | 决策 | 选项 | 我的推荐 |
|---|---|---|---|
| D1 | **无 VM 页 VM 化专项**（CookingTimer/FamilyEdit/CookMode 把业务态+数据访问下沉 Composable=UDF 越层） | (a)现在做·分批(CookingTimer→FamilyEdit→CookMode)·你后续真机验计时/持久化 (b)等你有设备时段再做 | **(a) 但 CookingTimer 最后做**（它含倒计时/AlarmManager/息屏恢复·风险最高·先做无副作用的 FamilyEdit/CookMode） |
| D2 | **AiSettings「模型来源」组 InsetGroup 卡化**（中等视觉改·需 Scaffold 灰底） | (a)做(我再走一次 Apple-UX 交互确认) (b)不做(低频页·能用不丑) | (a) 做·顺手对齐设置族 |
| D3 | **DietReport levelColor/结构级别色收敛**（膳食均衡度色）：现 0=红-alarm，色系墙 0=灰/1=琥珀(gentle) | (a)收敛到色系墙 gentle 色系(去红·合"不制造焦虑"健康准则) (b)保持现红-alarm(更醒目) | (a) 去红→与色系墙一致+合健康克制准则(但需你确认"膳食不均衡"不用红) |
| D4 | **慢病软降·周计划 GI/嘌呤梯度(F5)**：单餐已有软降(偏营养风格)+知情引导，周计划无 | (a)周计划也接同口径软降(仅偏营养风格) (b)暂不做 | (a) 与单餐口径一致 |
| D5 | **待拍板算法项**（见 `待拍板队列.md`）：无库存兜底/早餐软硬透传/菜级营养字段打散/当日能量/validate补菜/recommend封顶 | 逐项 green-light 或全部暂缓 | **无库存兜底**值得做(高频场景)·其余暂缓 |
| D6 | **AI 生成内容落库功能**（放开AI限制/AI对话生成菜品·你已定"生成的都落库"） | 何时启动方案+开发 | 需云端模型+真机·建议一期功能稳定后专项 |

> **我下一步默认**：若你不特别指定，我按 D1(a·FamilyEdit 先)→D2(a)→D4(a)→D5 无库存兜底 顺序做（都过设计/Google 门禁，真机项攒给你验）。

> **📌 你确定后 + 交接时后台核实（2026-07-21）**：你已拍板 **D1 全做/D2 卡化/D3 去红/D4 周计划软降/D5 无库存兜底**。交接时两后台 agent 回：
> - **D2**：`apple_ux_designer` 交互规范**已产出**（精确 dp，见 `SESSION_交接.md ⏭ D2`），下 session 直接实现。
> - **D5**：`Explore` 核实**「无库存兜底」非真 gap——RANDOM 模式已覆盖**（库存空→空态→手动切随机→全库推荐+忌口/慢病约束全生效）。原 `requireOnHand` 重构无必要；仅剩可选小 UX（库存空自动提示切随机·非必需）→ **D5 实质不做**，腾给 D1–D4。
> - 故实际推进序：**D3(快·去红)→D4(周计划软降)→D2(实现)→D1(无 VM 页·CookingTimer 真机关键)**。

---

## 二、✅ 要你真机验证的全部内容（git 拉取 `e18a04d` 后逐类验）

### V1 推荐算法（无 UI 变化·验行为）
- RANDOM 推荐在大食材库**不崩**（原 999 变量崩溃已修）
- 一批推荐**油腻度错落**（不再整批煎炸红烧）
- 一餐**不堆两道同主料/两道主食**
- 周计划**不顿顿重油**
- 云端 AI 推荐每餐更**荤素+主食完整**（配了云端模型才验）

### V2 F1 粘性字段（推荐页）
- 换餐次/换风格/换一换后，**已选的菜不丢**、慢病横幅**不闪掉**、药膳过滤重排**保留勾选**

### V3 慢病知情引导（新功能·推荐页顶部）
- **已登记痛风/糖尿病** + 当前非"偏营养"风格 → 推荐页顶部出现**可关闭横幅**
- 点「切到偏营养」= 切风格 + Snackbar + **永不再显**；点 × = 只关不改风格 + 永不再显
- **未登记这两种病的用户永不出现**该横幅

### V4 拍照（菜品/食材编辑·崩溃敏感）
- 菜品、食材编辑都能**拍/选 3 张**（原被压成 1 张）
- 首图 **16:9 大封面** + 下方 **64dp 缩略图条** + 未满显"再加一张"
- **删首图→第 2 张顶上成封面**；删空→回引导卡
- **反复进出编辑页不崩**（历史闪退场景·封面空→出图重组）
- 缩略图 **center_crop**（填满不变形）

### V5 家族化 P4 色
- **FamilyStats / DietReport / 首页色系墙** 三处宏量色（蛋白绿/脂肪琥珀/碳水蓝）**一致**
- 深色模式下宏量色/收藏星**适配不刺眼**

### V6 早前 session 未验项（如仍未验）
- 见 `SESSION_交接.md` 文末 11 项（菜品编辑不崩/崩溃友好界面/首页推荐图/食材编辑折叠/热量修复/两选择页/字母条/忌口红黄/AI 餐次预选/更新记录 v1）

---

## 三、📚 产出文档索引（要你看的架构/方案文档）

| 文档 | 是什么 | 为什么给你看 |
|---|---|---|
| `feature/跨平台架构策略.md` | **★架构评估**（Google×Apple 会诊·数据驱动准则是否最优+iOS/鸿蒙/桌面路线） | 你质疑的架构准则·结论+平台表+分歧记录 |
| `CLAUDE.md`「架构与代码质量准则」 | 精修后的**两条正交准则**（UDF UI层 + 共享边界·MVVM+UDF 措辞） | 你要加的准则·最终版 |
| `context_memory/数据驱动UI架构审计_2026-07-21.md` | **全局架构审计** findings（符合度中偏高·F1 已修·无 VM 页专项 backlog） | 代码健康度体检+整改路线 |
| `feature/推荐算法深挖分析.md` | 推荐算法**3 轮打磨记录**（含会诊/落地/弃项/"剩余见底"结论） | 算法这块做了什么+还剩什么(需拍板) |
| `context_memory/待拍板队列.md` | 算法**需你拍板**的项（慢病默认/GI/菜级营养/能量/无库存兜底…） | D4/D5 决策依据 |
| `feature/苹果风格UI设计方案.md` §9.31–9.33 | 新增 3 范式（一次性知情引导横幅/封面strip多图/家族化对齐边界） | 本 session UI 新范式沉淀 |
| `context_memory/家族化专项_决策与进度.md` | 家族化 P4 进度+延后项原因 | D2/D3 决策依据 |

---

> **用法**：你按①逐个确定 D1–D6（可在此文件直接批注，或口头告诉我），按②真机验证，按③挑文档看。我在你验证期间按你确定的方案继续，产出继续追加到本索引。
