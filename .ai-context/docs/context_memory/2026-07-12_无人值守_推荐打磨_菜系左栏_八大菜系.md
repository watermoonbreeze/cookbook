# 无人值守：推荐打磨 + 菜系左栏 + 八大菜系 + 自建菜修复（2026-07-12）

用户多点需求后离开(无人值守)：修自建菜未推荐、库存推荐批次、餐次选择、周期荤素、菜系一级分类、扩八大菜系；完成后审核+上下文+经验+APK+push+飞书。

## 交付（提交）
- **自建菜未被库存推荐修复** `a8d3d24`：根因=`createUserIngredient` 每次裸 INSERT 不去重，用户新建菜时又建同名食材(五花肉)得不同 id，与库存预置食材对不上，预筛按 ingredient_id 匹配漏菜。修：①createUserIngredient 同名(去空格)复用已有 id(治本，复用时补绑本次分类)②gather(PANTRY) 按名扩展 pantryIds+shortageIds(修存量脏数据)。
- **推荐批次分页** `d1894ce`：rotate 由"平移2个"(换一换严重重叠)改为按 DISPLAY_BATCH=10 整批分页(rotation%批数, 下一批不重复, 全推完循环)；MAX_ITEMS=10。
- **餐次选择** `1c9f7b9`：`MealSlot`(全部/早/上午/中/下午/晚/宵夜=对应 meal_type)+`MealSlotMatcher`(按菜名启发式适配)；gather(mealSlot) 过滤；库存+随机都显示餐次行，全部=不筛；换餐次 rotation=0 重推。
- **周期荤素搭配** `d1c60d2`：`PlanDish.isMeat`(MealSlotMatcher.isMeatByMains 关键词判荤)；PeriodPlanner.score 加 BALANCE_BONUS(本餐荤/素偏少一方补分)，鼓励一餐荤素兼有(营养搭配待营养规则)。
- **菜系左侧一级分类栏** `6ca2d88`：菜品页改双栏 Row{ CuisineRail(竖向:全部+家常菜+八大菜系+西餐/其他, 固定完整列表, 选中高亮) | Box(右侧内容 weight1f) }；替换原横向菜系chip；VM.selectCuisine(null=全部)。
- **扩八大菜系** `6b8e86e`：脚本加 20 道家常做法(仅用已有食材)，51→71，覆盖川鲁粤苏闽浙湘徽，分布家常41/川8/粤8/鲁4/苏3/浙2/湘3/闽1/徽1。
- **审查修复** `228ef88`：复用补绑分类、shortage 按名扩展、isMeat 收窄(去裸"蟹/参/血/肚"防蟹味菇误判)、清死代码(availableCuisines/toggleCuisineFilter)。

## 审查（Round1基线 + 2代理 + 修复 + 最终clean）
真实问题全部已修：食材复用丢分类、名扩展shortage不一致、isMeat误伤蟹味菇、死代码。菜系左栏Row闭合/weight/下拉刷新正确、71道数据合法、红烧肉(苏)与用户自建同名因 source='preset' 不冲突。最终 clean 回归 105 单测绿。

## 关键事实
- 红烧肉(苏菜)预设 与 用户自建"红烧肉" 因 `selectPresetDishIdByName`/`updatePresetDishCuisineByName` 均限 source='preset' 作用域，共存不冲突。
- 名扩展语义：库存有某名食材 → 同名所有 id 都算在手(兼容历史重复)；createUserIngredient 去重已阻断新重复。
- MealSlot/isMeat 均按菜名/主料名关键词启发式(菜品无餐次/荤素属性)，非精确、可后续细化。

## 待用户/后续
- 真机验证：自建菜推荐、批次换一换、餐次筛选、菜系左栏、荤素搭配、菜系迁移旧库升级。
- 营养搭配(周期规划)待营养规则接入；自由搭配 AI 生成新菜谱二期。
- DishesViewModel 仍可考虑把 selectedCuisine 与其它筛选进一步整理(本次已清死代码)。
</content>
