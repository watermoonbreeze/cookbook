# 上下文快照：UX 深挖 + 无人值守落地（2026-07-15 傍晚起）

> 用户下班授权无人值守"继续推进"，按 代码质量→UX打磨→食材/菜品/餐食深挖→健康膳食深挖→收尾。`[unattended]` 提交，未 push(除非用户要)。

## 一、方法
- 4 个 Apple-UX 工程师研究 agent 深挖，产出 `temp/claude/ux_{ingredient,dish,meal,health}.md`(方案含现状/痛点/建议/落地/优先级/风险/安全vs需拍板)。
- 主线程落地**安全项**(纯UI/VM/零迁移/无真机风险)，**高风险/需拍板项入待确认队列**(见 `待办总览.md` D节 + `unattended_decisions.md`)。每项构建验证 + 独立 commit。

## 二、已落地(均 [unattended] commit，构建/单测通过)
**代码质量**：代码本就扎实(旧快照写回全仓已达标)，小清理(营养表透明度抽常量)。跳过 _state.update{} 批量统一(低价值高churn)。
**餐食**：新建默认餐次按当前钟点智能选(晚上→晚餐不再恒早餐,`AddMealViewModel.pickDefaultMealType`纯函数)、食历"今天"回当天、移除"请选时间"死红字。
**菜品**：加食材去"已存在"弹窗(静默去重)、克数点数值直接输入(MiniStepper加onValueClick+GramStepper弹整数框+VM setIngredientGrams)、移除内测"编辑诊断Toast"、**修真bug**(单位英文化克→g后gramUnit()仍找"克"恒null致改克数丢unitId)、DishPicker常做置顶、筛选"清除"一键回全量(clearFilters)。
**食材**：网格空态(最近/库存/家庭/分类各Tab引导,IngredientGridEmptyState)、搜索无结果"＋新建关键词"直达(预填名,IngredientEditorDialog加initialName)、按营养大类预选默认单位(蛋/果→个/奶→ml/其余→g,unitTouched守卫)。
**健康膳食**(守免责红线,如实非医嘱)：今日卡"今天还缺X"解读(`FoodGroup.nutritionGaps`纯函数,缺优质蛋白/主食/蔬菜三支柱,无记录/齐不显)、修家庭统计"谁没吃"过时文案(说不保存实则已持久化day_absentee)、功能设置健康分组加免责总说明。

## 三、待确认队列(用户回来定夺，详见待办总览 D节)
- 🔴 菜品详情主CTA"记这道菜"、编辑未保存"放弃更改"保护、首页"今天"节前置、餐次块内联"最近常吃"chips。
- 🔴 健康：营养级别评级纳入热量达标+慢病指标(修误导性绿色,P3核心)、推荐补齐"今日缺口"。
- 🟡 菜品大标题搜索栏/四Tab简化/编辑改期/删整天软删撤销/食历卡ActionSheet/库存Tab就地加减/长表单折叠/搜索下拉浮层；健康忌口"对谁"标注/独立膳食统计页/宏量目标线/可信度分级。
- ⚪/待讨论：AppTopBar收敛16屏、详情减负、预设菜另存、空步骤收起；主料语义/菜系分类/详情四区顺序(产品语义)。

## 四、收尾(阶段5,进行中)
- ✅ 本快照 ②/zongjie ③提交 ④飞书通知。用户要求"全部完成后"做这四件。
