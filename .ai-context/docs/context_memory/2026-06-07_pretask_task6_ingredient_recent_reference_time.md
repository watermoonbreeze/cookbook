# 2026-06-07 任务6前置上下文

## 任务编排

- 模式：深度级别 / 数据模型修正模式。[AI生成]
- 原因：用户重新定义“最近食材”为“只要食材被加入/保存到菜品中就算最近使用”，需要从餐食引用链改为食材自身最近引用时间字段。[AI生成]

## 用户需求

- 编辑菜品时添加食材后，食材选择页“最近使用”应立即看到该食材。[AI生成]
- 最近食材不应只依赖餐食记录，而是只要食材被菜品引用就进入最近。[AI生成]
- 建议在 `ingredient` 表增加字段记录食材最后被引用时间，最近使用查询该字段非空并倒序。[AI生成]

## 当前实现差异

- 当前“最近使用”来自 `meal_record -> meal_record_dish -> dish_ingredient -> ingredient`，只统计今天及以前真实餐食。[AI生成]
- 该实现不会覆盖“菜品编辑中刚添加食材但菜品还没被餐食引用”的场景。[AI生成]

## 处理原则

- 新增字段要兼容历史数据：默认值、迁移、NULL 清洗都要处理。[AI生成]
- 删除/软删除仍遵守 `status` 规则。[AI生成]
- 完成后补单元测试并运行 shared 测试和 Android 构建。[AI生成]
