# 上下文快照：五项无人值守任务 + 综合审查修复（里程碑1，2026-07-15）

> 承接私人菜单数据补全(Phase1-3)后，完成用户五项计划 + 一轮综合审查修复。全部已 push gitee master。

## 交付内容

### 数据补全（Phase1-3，已 push）
食材 279→440、菜品 147→516(全家常菜/去重/带做法)、营养 65→100、食材详情 243→299(含火候技法)、6 组味型配料组。详见 `2026-07-14_私人菜单数据补全.md`。

### 五项任务
1. **①导航栏沉浸式**：`MainActivity` 透明系统栏后加 `isNavigationBar/StatusBarContrastEnforced=false`（SDK_INT≥Q 守卫），去掉半透明遮罩，内容真正贴底。
2. **②UX 苹果风深挖**：审计文档 `UX深挖审计与待办.md`(Top10+跨屏发现)；安全落地——加餐默认当前时间(AddMealVM newBlock)、同步弹窗去 markdown 字面；新建 `AppTopBar` 统一顶栏组件，已接 FeatureSettings/FreePairing(起步，余屏待收敛)。
3. **③健康膳食统计维度体系**：方案文档 `健康膳食统计维度体系.md`——定量化营养/定性分类两类维度、指标←数据关联表、nutritionLevel 公式、预设+自定义统一口径、自定义接入路径。
4. **④自定义食材纳入统计**：`IngredientEditorDialog` 加「营养素(每100g)」录入区(热量/宏量/纤维/钠/钾/钙/GI/嘌呤/单件克重)，营养色系开关开时显影响提示；VM `saveIngredientEditor` 收 nutrition 参数、`upsertNutrition` 入库；DI 加 `NutritionRepository`。
5. **⑤多人家庭档案**：方案草案 `多人家庭档案方案.md`(family_member 表/成员管理/忌口并集/迁移/待确认点)——**待用户确认记菜粒度与忌口口径后再实现**，当前仍单人 BodyMetrics。

### 综合审查修复（本轮）
- **#1 真缺口**：自定义食材只填「单件克重/GI」被 `hasAny` 守卫丢弃 → 加 `IngredientNutrition.hasAnyInput`(含 gi/pieceGram)供保存判断，`hasAny`(纯营养素)留作菜品营养覆盖率口径。
- **#2 文档一致**：编辑器补钾/钙录入框(维度体系文档承诺过)，重排整齐4行。
- **#3 校验守卫**：新增 `validateDetailAndCategorySeedForTest`——详情引用/食材+调养规则分类 code/各 JSON 内部 name·code 重复 全校验(补 validateDish/Nutrition 未覆盖面)，防后续补数据静默丢关联。
- **#4 死导入**：清理 FeatureSettings/FreePairing 接入 AppTopBar 后悬空的 import。

## 回归结论
- 代码级：多轮增量 + clean 全量构建 + 全单测套件均绿（含新增数据完整性守卫，补全数据分类 code/重复/详情引用全干净）。
- 可视化 UX 5 轮：headless 无法执行，需真机/用户操作，已把发现沉淀成待办。

## 审查未阻断但待办（下批处理）
- AppTopBar 收敛剩余 ~10 屏；PlainCard/InsetDivider/ActionSheet 扫尾；H2 速记模式等 UX 审计余项。
- `DayMealCardView` 每卡独立订阅 `observeBodyMetrics()` 冷流 → 上提 VM `stateIn` 共享(效率优化)。
- ⑤多人家庭档案实现(待用户确认方案)；统计 P2 膳食统计页。

## 下一步（里程碑2）
整体架构/UX/UE/UI/代码质量全面审查 + 修复 → 保存上下文 → 飞书。
