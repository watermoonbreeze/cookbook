# 上下文快照：营养数值权威核准 + 食材营养表（✅已完成，2026-07-15）

> 本轮"数据核对"三块(食材归类/权威营养/营养表功能)全部完成并提交 master。下一步转 D 代码质量。

## 一、已完成（本会话，已提交 master）
- 家庭档案 P1/P2/P3、五项调整、私人菜单数据补全、单位库、单位英文化(克→g/kg/ml/L，23.sqm rename)、修食材单位下拉为空bug(selectMainTab旧快照)、D10旧快照排查。详见 `2026-07-15_归类单位与旧快照排查.md`、`2026-07-15_五项调整与家庭档案P1.md`。
- **A1 自定义食材归类**：`ingredient.food_group` 列(22.sqm) + 营养大类必选预选 + 映射分类树 + classify显式覆盖。
- **食材归类核准(C-1)**：classify 改**尾词优先** + `NAME_OVERRIDE` 特例表(用户确认54项)。归类392/440,剩48全是调料油茶糖(正常null)。预设启动重刷。commit cbf437d。
  - **用户约定**：后续新食材"参考已有分类规律自主判断"，NAME_OVERRIDE 可续加。

## 二、✅ 营养数值权威核准（全量440 + GI/嘌呤，已入库 commit 3777a4a）
- **源**：《中国食物成分表》官方平台 `nlc.chinanutri.cn`(kJ→kcal÷4.184,含钠钾钙)；进口/缺项 USDA FDC；GI 悉尼大学库；嘌呤 2024痛风食养指南。
- **方法**：15 组 agent 联网逐个核对(每片30,共440)，输出 `temp/claude/nutri_auth_1..15.json`(ref填实际源+review=verified/pending)。
- **合并**：`scratchpad/merge_nutri.py` 覆盖升级式(auth值覆盖旧估算/保留auth未覆盖字段/ref+review取auth)。结果 **440/440升级,verified 278/pending 162**,补齐 pieceGram。`:shared:testDebugUnitTest` 通过(营养引用完整性440名全解析)。

## 三、✅ 食材营养表功能（我的·全量营养总览，已提交 前一commit）
- **入口**：我的→实用工具→「食材营养表」独立页。
- **实现**：`selectAllIngredientNutrition`(左连,纯新查询免迁移) + `IngredientNutritionRow`(Nutrition.kt) + `NutritionRepository.allIngredientNutrition()` + Android `NutritionTableViewModel`(combine5:all/query/groupFilter/sortKey/sortDesc→filter+sort) + `NutritionTableScreen`(AppTopBar+搜索+大类FilterChip+横滚可排序表+顶栏ⓘ数据来源弹窗) + Route NUTRITION_TABLE + MainScaffold + MineScreen入口 + DI。
- **数据来源展示**：顶栏ⓘ弹「数据来源与免责」(SOURCE_TEXT硬编码要点) + `.ai-context/docs/数据来源.md` 记档全部来源。

## 四、剩余待办（下一步）
- **D 代码质量**(当前)：pantryCardFlags全量重算、_state.update{}统一、魔法数抽常量。详见待办总览。
- **B UX打磨**：AppTopBar收敛剩余屏、删除弹窗统一ActionSheet、巨型Composable拆分(NewDishScreen)、图标化。
- 待验证清单 `.ai-context/docs/待验证清单.md`(用户逐项真机验证)。P3(分餐/口味画像)搁置。

## 五、流程约定(用户定)
- 每完成一轮自动:①保存上下文(本目录) ②总结经验(/zongjie)。
- 红线只进**项目 CLAUDE.md**。升级无损。真机由用户自己装(除非让抓日志)。
