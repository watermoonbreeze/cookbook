# 无人值守决策记录

## 2026-07-27 成员化红绿灯 Phase 2（无人值守·深度级·10文件·166+/20−）
- **背景**：用户说"成员红绿灯继续做这个。现在无人值守，有问题你自己会商不要问我，取会商的最优方案执行"
- **三件事全部交付**（commit `0c14fc5f`·构建+单测全绿）：
  1. **列表逐项徽章**：DishPickerScreen 每道菜旁 8dp 小圆点（红/黄/绿），基于当前查看成员约束批量评估
  2. **详情全家并集补个人忌口**：DishDetailViewModel.avoidNames 从仅病种忌口→病种+个人忌口并集
  3. **成员名可点跳健康档案**：详情页"适合谁吃"区块成员名变可点击，跳 Family 页面
- **自主决策**：
  - 列表徽章缓存策略：成员约束只查一次+gatherConstraintsForMember，菜品配料走 batch SQL（免N+1）
  - 灯色口径：忌口红/限量黄/安全绿，不做GI/嘌呤定性（列表徽章空间小，详情页已有完整判定）
  - 成员选择：取当前查看成员（focus集合首位），无健康约束不显灯（降噪）
  - 导航：成员名点击→Family页（暂不精确定位具体成员，二期可做）
  - 新依赖全用可选参数（`?= null`）+ Koin 自动注入，老路径零影响
- **未做/待确认**：
  - 菜品列表页（DishesScreen）未加徽章（本次聚焦选菜场景·后续可扩展）
  - 推荐页（AiRecommendScreen）未加徽章（同上）
  - GI/嘌呤定性未纳入列表灯（仅忌口/限量·后续可补）
  - 成员名点击→精确跳该成员编辑器（当前跳Family列表页）

---

## 2026-07-15 家庭档案 P2/P3（用户真机验证 P1 通过后"吃饭去了"，嘱继续 P2/P3，完成后 UX 测试）
- **模式**：无人值守，按阶段 build+单测+`[unattended]` 提交，逐块 push。
- **P2 计划**：①膳食统计页(家庭/成员切换,今日+近7天热量宏量达标) ②缺席微调「谁没吃」(per-day 在场成员→份额分母排除,影响个人摄入估算)。
- **P3 计划**：①成员口味偏好画像(推荐按关注成员偏好，谨慎——改推荐核心，若风险高则做轻量版并记待确认) ②分餐精确(某餐某菜谁吃/几份，最重，评估后决定深浅)。
- **风险控制**：不改毁库操作；推荐核心大改若不确定则保守+记待确认队列；每阶段单测守。
- **收尾**：全部完成后整体 UX 交互设计测试(≥数轮) + 保存上下文。

---


## 2026-07-15 SyncRepository 导入原子性 —— 选「弹性合并」而非「全量事务」
- **背景**：审查指出 `import()` 无总事务，中途失败半完成残留。
- **决策**：不做"整段 `db.transaction{}` 包裹"，改**逐行 `runCatching` 隔离 + skipped 计数**。
- **理由**：核心写入方法(`saveDish`/`createUserIngredient`/`mealRepo.save`)均 `suspend`+`withContext+db.transaction`，而 SQLDelight `transaction{}` 是**非挂起**块，无法在其中调 suspend 方法——真做单事务需给每个核心写入链抽"非挂起内部变体"，动核心路径、风险高。而 import 是**幂等合并**(按名 upsert/复用/同名跳过)，单行失败隔离 + 可重导补齐，比大重构更安全且同样消除"半写后抛错中断"。
- **落地**：`SyncImportResult` 加 `skipped`；六大合并环逐行 runCatching；`meals` 环补 `parseDate` 保护(原唯一未防的抛点)。新增单测 `弹性合并-单条坏数据被跳过不中断整批且skipped计数`。
- **遗留**：真正的"全有或全无原子回滚"若未来需要，仍需核心写入链非挂起变体重构，记入待办。

---


> 用户 2026-07-08 下班启动无人值守。指令：按待办顺序逐步执行；**暂无上架计划，P0 存储合规不做**；其他继续。
> 规则：深度执行；高风险/需真机 → 待确认队列不擅自做；每阶段 `[unattended]` 提交不 push；≤15 文件/阶段。

---

## 2026-07-10 第二次无人值守（下班）

> 指令：从待办挑可做项，逐个「实现→审核→回测→过了再下一个」。
> 全做完后：**整体回归测试+代码审核+架构审查 至少 5 轮**确保零问题 → 保存上下文 → 总结经验 → **编译 debug APK(给路径)** → 提交 push → 飞书通知完成。
> 补充约束：**iOS 暂不做，只做 Android**（用户明确）。库存*配置开关*用户本日已定"先占位、之后再做"，本轮回避该配置项。

### 本轮回避（不擅自做）
- 库存/份数**功能开关配置**（用户本日明确先占位）
- iOS 编译（用户明确暂不做）
- 端侧 AI Step2（上游端侧模型未接入，阻塞）
- 营养素+每日卡路里+热力图（L，需补营养数据 + 健康数据免责红线，范围大不宜无人值守）
- 各"真机待验"项（无设备）

### 本轮候选执行（自包含/可 :shared 单测回测/对现有行为零破坏优先）
| 顺序 | 任务 | 判定 | 状态 |
|---|---|---|---|
| 1 | 采购清单聚合(购物清单) | 加法型，复用 pantry 口径，可测 | ✅ 完成(89单测绿)`6ca7943` |
| 2 | P3 KMP 日志 | expect/actual 无依赖；接入 seed/采购诊断点 | ✅ 完成`9030640` |
| — | P2 SQLDelight 写入 Diff | 改写 seed 写库逻辑，中高风险 | ⏸ 停做(与"零问题"目标冲突，留待确认) |
| — | DI 平台化 | Koin 重构低收益、有 wiring 风险 | ⏸ 停做(同上) |

**决策**：完成 2 个自包含加法项后停止新增功能，转入用户要求的「5 轮整体回归+审核+架构审查」，把风险预算留给"确保零问题"。P2/DI 留待用户确认再做。

### 5 轮整体审查记录
- 每轮：全量 build + :shared 单测 + 多维代码审查(正确性/架构一致性/Compose/数据安全/KMP)，发现即修复再回测。
- **Round1** 基线：clean 全量 build SUCCESSFUL + 89 单测绿。
- **Round2** 5 代理多维审查→修复(`9bcfce6`)：①相关菜品漏传 related(真 bug 修复) ②LaunchedEffect(d) ③observeFlag remember ④热度前3取全局 ⑤空态文案分场景 ⑥步骤图 key+注释。DI 全匹配无崩、seed/采购口径一致、无迁移。build+89 绿。
- **Round3** 对抗验证修复(1-4 全通过, 5 步骤图 key 遗留=与既有一致的理论边界不可达) + 深挖 pantry/shopping：无必修 bug，采购三处等价、缺料 rank vs remaining 属合理设计差异、窗口无 off-by-one、日期回填不会错。仅精修 DishDetailViewModel 缺料口径注释(零风险)。
- **Round4** sweep→修复(`71bf766`)：①A-Z字母跳转偏移未计筛选chip行(D2真bug)修复 ②删 DishesViewModel 死依赖 mealRepo ③seed 方式/单位提常量与校验同源防漂移 ④采购文案。注释/集成/命名/完整性 critic 均通过。build+89 绿。
- **Round5** clean 全量回归 BUILD SUCCESSFUL + 89 单测绿；最终对抗验证 Round4 四处修复全部正确(字母跳转 index 逐项对齐=5, 无新问题)。
- **审查结论**：9 代理×5 轮，真实问题全部已修回测；pantry/采购/详情口径无必修 bug、无迁移、无 off-by-one。**通过**。

---

## 2026-07-10 第三次无人值守（食材/菜品/推荐打磨）

> 用户提 4 点后离开：①库存加份数没反应/"共X份"含义 ②菜品加"菜系"分类(网络权威来源+关于页) ③库存推荐"≥2菜才显示"/红烧肉没推荐/推荐能否食材自由搭配 ④主打磨食材菜品推荐逻辑。完成后 APK+上下文+经验+push+飞书。

### 已落地（[unattended] 提交）
- **#1 库存加份数 bug** `14d5604`：根因=结算式 addServings 当天有餐时加1份被当天餐立即扣回→无变化。改公式 `份数=当前剩余+delta+今日已占用`(保留结算式对历史欠账的正确性)，保证"加N份→剩余可见+N"；详情加"1份=够做一次…"说明；单测覆盖。
- **#3 库存推荐放宽** `5203d2a`：查清**不存在"≥2菜"过滤**；红烧肉不出现真因=`HealthRuleEngine` 要求**全部非调料食材齐**才可做，五花肉齐但辅料没齐→整菜丢。改为**主料齐即可推荐**(缺辅料记 missingNames 降权、标"🛒还需采购")，齐备菜排前。DishCandidate 加 missingNames，engine/prompt/VM/单测同步。

### 留待用户拍板（不擅自做，见 `feature/菜系分类与推荐打磨方案.md`）
- **#2 菜系分类**：涉及主观菜系归类(51道)、建模选择(标签 vs 独立字段+迁移)、用户自建菜归类、来源标注——产品/编辑决策应用户定。已出方案(A标签快速/B独立字段, 八大菜系+家常菜, 保守归类, 关于页来源)。
- **#3 后半 食材自由搭配推荐**：现状三种推荐都只从已有 dish 挑、AI 也不造新菜。方案：①规则轻搭配提示(离线) ②AI 生成新菜谱。分期，待定方向。

## 执行计划（安全优先，可编译/单测验证的工程任务先做）

| 顺序 | 任务 | 判定 | 处置 |
|---|---|---|---|
| 1 | P1 Shared IO Dispatcher | 低风险纯重构，build+test 可验 | ✅ 执行 |
| 2 | DI 平台化 `platformModule` | 低风险，build 可验 | ✅ 执行 |
| 3 | P3 KMP 日志 | 加依赖(Kermit)+集成，中等 | ✅ 执行(评估后) |
| 4 | P2 SQLDelight 写入 Diff | 改写库逻辑，涉数据，中高 | ⏸ 评估风险后定 |

## 待确认队列（高风险/需真机，未擅自执行）
- **首页搜索"跳到具体食材并高亮"**：跨底部 Tab 传参 + 高亮，Compose 跨 Tab 状态易出错，无真机难验证。
- **回归验证（删除一致性 / 图片持久化）**：本质是真机专项验证，无设备无法执行；仅可做代码级审阅。
- **iOS shared 编译能力**：启用 iOS target 需补多处 actual + framework 验证，改动大且无 iOS 环境验证。
- **selectionMode 完整两入口拆分**：高风险重构，picker 在菜品选食材关键流程，需真机回归。
- **AI S4 端侧模型(LiteRT-LM)**：大工程，占位已留，待专门规划。
- **P0 存储权限合规**：用户明确暂不做（无上架计划）。

## 本轮收尾总结（2026-07-08 无人值守）

**已完成并提交（本地，`[unattended]` 前缀，未 push）：**
1. P1 IO Dispatcher（91 处 → 专用 IO 池）
2. 删除一致性修复（菜品软删后食历保留当时吃的菜）
3. 首页搜索点食材 → 跳到该食材并高亮（`IngredientJumpBus`）

**共 4 个功能/修复提交 + 若干文档提交，全部 build/单测通过。均未 push（等用户回来）。**

**结论**：待办里**清晰安全、可编译验证**的项已做完。其余项（见下方待确认队列）都需用户**决策/真机/依赖抉择**，无人值守下按规则不擅自动，等用户返回定夺。

## 决策/进展日志

- **阶段1 ✅ P1 IO Dispatcher**（commit `[unattended]`）：新增 `expect val ioDispatcher` + Android actual=Dispatchers.IO；8 repo + RecommendationDataSource 共 91 处 `Dispatchers.Default`→`ioDispatcher`；build+单测过。
- **阶段2 ⏸ DI 平台化 → 待确认**：shared 只有 koin.core（无 koin-android）。移平台注册进 shared androidMain 需加 koin-android 依赖 + 用 androidContext()，且 DI 解析错误是**运行时崩溃**、无设备编译验不出——无人值守不擅自做。
- **阶段3 代码审计 + 修复**（删除一致性 + 图片持久化）：
  - ✅ **修复(中危)删除一致性**：`selectDishesOfMealRecords` 去掉 `d.status=1` → 菜品软删后食历仍显示当时吃的菜（与食材保留引用 pattern 一致；菜品库另用带 status=1 的查询不受影响）。build+MealRecord 测试过。**属行为变更，用户可 review**。
  - ⏭ **不做(低危)图片路径去重**：审计建议用 zip 按对 distinct，但会假设 image/thumbnail 两列表等长；旧数据可能长度不等→zip 截断丢图，引入回归。原问题近不可能触发，不冒险。留待需要时按"补齐长度再配对"做。
  - ✅ 图片持久化整体**无问题**：存 `/sdcard/cookbook/img/` 绝对路径、原图+缩略图、重启可回源。
- **阶段4 ✅ 首页搜索"跳到具体食材并高亮"**（原待确认项，重估后安全落地）：
  - 新增 `IngredientJumpBus`(单例) 跨屏传食材；搜索点食材 → request()+跳食材页；IngredientsScreen 消费 → 复用成熟 `jumpToIngredient` 定位高亮 → consume。
  - **失效模式优雅**：若跨 Tab 时序未收到，只退回"跳到食材页"当前行为，**无回归风险**——故安全执行。
  - build 过。**跳转/高亮的视觉效果需真机确认**（时序类逻辑编译验不出，但复用已验证的 jumpToIngredient，且失败降级到旧行为）。

## 无人值守（2026-07-09 下班后）：审核+测试+优化

**触发**：用户"把能做的都做完，不要问；代码架构审核测试多跑几轮，能优化的优化好，符合软件工程规范"。已先保存上下文 + 经验总结#14。

**本轮已完成（`[unattended]` 提交，未 push）**：P0存储合规、图片相对路径、完整zip备份、SAF导出导入、双设备局域网同传+二维码、端侧自测Step1、AI推荐打磨——见 `2026-07-09_p0存储_备份同传_端侧自测.md`。

**待确认队列（无人值守不做，需用户/真机/依赖抉择）**：
- 端侧 AI Step2：接 native 运行时(建议先 MediaPipe LLM Inference)+模型导入+实装推理。需真机+大模型，无法在此验证。
- 真机验证：P0重装/备份导出导入/双设备同传(两台同WiFi)/拍照CAMERA授权/删除一致性/图片持久化。
- iOS shared 编译、selectionMode 完整拆分：高风险暂缓。
- DI 平台化：需加 koin-android 依赖 + 运行时才验得出，暂缓（同上轮结论）。

**本轮动作**：对本会话改动跑 4 路并行代码审核(存储备份/双设备同传/图片AI/架构规范)，汇总后修确认的真实问题，多轮 build+单测，符合工程规范的优化一并做。findings 与修复见下方续记。

### 审核结果与修复（4路并行审核 → 5批修复）

**4 路独立审核**（存储备份/双设备同传/图片AI/架构规范）共发现真实问题，已按"数据安全>真实bug>工程规范"修复：

**批1 备份数据安全**（`fix(backup)`）：恢复原子化+回滚(防中途失败破坏现有db)；TRUNCATE checkpoint 只打包主库(消除wal/主库版本错配)；importFrom失败清理半成品；zip白名单解压；exportTo/expect KDoc订正。
**批2 同传健壮性**（`fix(sync)`）：接收/握手读 SO_TIMEOUT(防掉线永久阻塞/OOM)；持有receiveSocket可主动关闭；cancelling标志区分主动取消vs真实异常(不再吞错)；localWifiIp过滤link-local+wlan优先+私网段(修多网卡选错)；扫码后二次确认(覆盖不可逆)。
**批3 图片/AI**（`fix(image/ai)`）：PlanOrchestrator统计改花括号消分号隐患；DeviceAiGrade阈值7.5→7.0；过时"存储权限"文案→"请重试"；类注释路径订正；删未用常量；图片文件名加随机后缀防同毫秒碰撞。
**批4 工程债+测试**（`refactor(test)`）：SyncPayload(协议)、DeviceAiGrade+gradeFor(阈值) 下沉 shared，补 SyncPayloadTest/DeviceAiGradingTest 单测（androidApp原零测试覆盖）。

**验证**：clean 后完整构建 + 全部单测 = **14类68用例0失败**；androidApp assembleDebug 通过。

**复核判为非bug（未改，记录）**：`deleteTempCameraFile` 实际可删(FileProvider `camera_cache` 根名映射，lastPathSegment 即文件名)；两列表 distinct 因文件名带时间戳唯一实际不触发。

**待确认/后续（无人值守不做）**：
- 恢复后驱动热重建/自动重启进程：现关单例 driver 无重建，UI已提示"重启应用"、本轮已补原子化防损坏；彻底解决需改 DI 单例生命周期或恢复后强制重启，属架构改动+需真机验，留待确认。
- iOS BackupManager actual：iOS target 当前注释、不编译，不影响；启用 iOS 时补。
- 过时 `/sdcard/cookbook/log` 注释(AppLogger/LogFileManager/MineViewModel/AndroidModule 各1处)：纯注释、零风险，为守 ≤15 文件规范本轮未动，5分钟可扫尾。
- 备份并发写 torn-page：minSdk21 无 VACUUM INTO；手动触发低并发 + TRUNCATE checkpoint 已缓解，接受。
- StoredImage 预览无缓存、二维码主线程生成：低价值性能项，留后。

**结论**：审核发现的**数据安全与真实bug已全部修复并测试通过**；工程债(可测逻辑下沉+单测)已补；其余为需真机/架构决策/纯注释项，按无人值守规范记入待确认。均 `[unattended]` 本地提交，未 push。

## 无人值守（2026-07-10 吃饭）：搜索入库份数 + 周期规划采购提示

**触发**："我吃饭去了，你自行决定吧"。两需求：
1. 食材搜索界面入库时也能选份数(现搜索面板入库=默认1份, 无选择)。
2. 周期规划菜品：库存有但份数不够→"缺料"(灰); 库存里没有→"采购"(灰, 同缺料样式)。

**自主决策**：
- 请求1：搜索入库复用份数选择(弹份数 stepper 后 addServings)，与详情表 PantryServingSection 一致体验。
- 请求2：**采购只判主料**(盐/油等调料不标, 避免噪音, 延续"非库存食材不标"精神但对主料放开为采购提示)。作用于**周期规划预览**(AiPlanScreen)：按当前库存快照，每道菜主料 不在库→采购、在库但可用份数≤0→缺料，缺任一则灰显。不跨规划自身多天分配(规划本就"涉及后续采购")。
- 若 PlanDish 无食材 id，则给 PlanDish 补主料 ingredientId 列表(gatherForPlan)，AiPlanViewModel 计算采购/缺料标到 PlannedDish 供 UI 展示。

**验证**：每步 build + 单测；≤15 文件；[unattended] 提交不 push。探查结果与实现细节续记于下。

### 实现结果（2026-07-10 吃饭时段）
- **请求1 搜索入库份数** ✅：`SearchResultsPanel` 入库→`PantryServingDialog`(1~99份)→addServings。
- **请求2 周期规划采购/缺料** ✅：`PlannedDish` 加 shortageNames/purchaseNames；`PantryPlanAnnotator`(纯,可测,按天序分配剩余份数, 只判主料)；`RecommendationDataSource.annotatePlanWithPantry`(查主料+库存快照); AiPlanViewModel 生成后标注; AiPlanScreen 缺/采购菜半透明+"🛒采购:X"/"⚠缺:X"。
- **自评修复**：库存完全为空(未用库存功能)时**不标注**，避免整份规划全标采购灰显打扰。
- 验证: 全部单测 78 用例 0 失败(+PantryPlanAnnotatorTest); androidApp 构建通过。
- 决策: 采购只判主料(盐油等调料不标); 规划按当前剩余份数为预算跨天分配(规划本就"涉及后续采购")。
- 均 [unattended] 本地提交, 未 push。

## 无人值守（2026-07-14 下班）：一批 UI/功能调整（6 项）

**触发**："我下班了，完成后审核测试→UX体验测试至少5轮→保存上下文→总结经验→提交push→飞书通知"。

本次 6 项需求（前半在有人时已 push，后半 T3/T4 在无人值守下 `[unattended]` 提交）：
1. **收藏问题(Q1)**：仅回答——⭐=收藏，体现在菜品页「收藏」Tab + 列表置顶 ★。无代码。
2. **加餐默认日期(T5)**：`resolveNewMealDate` 改为「有今天/未来餐食→最后餐食+1；否则按时段 <12点=今天、≥12点=明天」。自主取舍：morning/afternoon 分界取 **12 点**；不加"禁选过去"限制(保留补记昨餐)。
3. **底部导航栏沉浸式(T2)**：去 `NavigationBar` 的 `clip`，背景铺满到屏幕底边。
4. **全屏页导航栏遮挡**：`MainScaffold` 对无底部栏路由统一加 `navigationBarsPadding`(一处修全部全屏页)。
5. **标签库/烹饪方式库(T3/T4)** `[unattended]`：可复用 `LibraryPickerDialog`(库直选+输入存库+编辑态删自建)。自主取舍：软删只作用 `source='user'`(预设不可删)；`CookingMethod`/`DishTag` 加 `preset`；选中做 toggle。**无 schema 变更**(仅新增查询+软删)，无需迁移。补 `DishLibraryTest`。
6. **色系墙固定本年+往年平均行**(前置消息批次，已 push)：见 `2026-07-14_苹果UI改造_营养色系墙.md` 增补。

**高风险/禁止项**：无。文件数在阶段限额内。
**收尾**：UX 5 轮审查→(修真实问题)→保存上下文→总结经验→push→飞书通知。

## 无人值守（2026-07-14 深夜）：私人菜单数据补全 + 5大深挖任务

**触发**：用户给 temp/foods.md(私人多年菜单)，要补全食材/菜品/做法(去重、家常菜、重口照加、技法写详情)，然后无人值守做5项：
1. 导航栏沉浸式处理。
2. 全界面及控件深挖，严格苹果 UX 风格。
3. 深挖 食材/菜品/餐食/AI推荐/健康膳食统计——重点**健康膳食统计不准确**：先定**维度**，定**哪些食材关联哪些指标**，让预设+**自定义**食材/菜品都能用同套指标算(用户会继续补充)。
4. **自定义食材/菜品如何像预设一样统计出数据**：列方案；创建新食材/菜品时若"营养色系"开关开，显示**填哪些值影响哪些营养统计(含热量)**，方便填写。
5. 功能设置用户档案 → **家庭多人吃饭场景**，先给方案保存。

**流程**：按序一个个做，每项测试审核过再下一个；全做完回归+UX测试≥5轮→保存上下文→push→飞书。最后整体 架构/UX/UE/UI/代码质量 全面审查+修复→保存上下文→飞书。

**数据补全进度(已 [unattended] 提交，未 push)**：
- Phase1 食材 279→440(+161)：豆制品/地方叶菜/腌腊内脏/水产/腌菜辣椒/杂粮/水果/油脂茶饮等；分类code全校验、命名变体用"具体名+通用别名"。
- Phase2 菜品 147→516(+369)：去重、全家常菜、食材名全匹配、5道带做法。
- Phase3(进行中)：配料组+6组味型(葱姜蒜辣/樟树椒洋葱香菜/凉拌汁/酸菜鱼料/水煮料/煎肉香料)；食材详情agent补新食材技法(莲藕焯水冷冻/面筋控量/桂鱼清蒸等)。
- 用户补充理念：一菜多做法=多条菜品用做法区分(Phase2已按method拆)；变体子类用通用名+二级名(别名)。

## 无人值守（2026-07-22）：菜品食材剂量默认值专项（深度）

**触发**：会话继续接「数据/编辑」session，做待办 #30(BUG单位/剂量)+#31(智能默认克数)合并专项。深度级·无人值守。

**真机库证实(GCL0220212004523)**：损坏行**全部是 user 自建菜**(青椒肉丝/蒸土豆/鸭腿饭/猪肚包鸡 + 若干测试垃圾菜)；无 preset 损坏。P3「牛肉汤炖白菜豆腐粉丝」经查是**软删菜(dish390 status=0)残留**、活跃 dish530 数据正确→**非 bug**。

**自主决策**：
1. **P1 修 race**(NewDishViewModel)：`unitsReady:CompletableDeferred`+`cachedGramUnit`；`autoAddFromName` await 单位就绪；加食材 `unitId=gram?.id`(移除落到 `defaultUnitId` 的错兜底·bug根因)。取舍：`saveIngredientAsDish` 平行路径不加守卫(用户触发时序已就绪+P4兜底·加注释)。
2. **P2 智能默认克数**(SeasoningDefaults)：非调料按 `FoodGroup.classify` 给经验默认(蛋50/菜150/奶200…·惯例非权威可改)·判不出退100。
3. **P4 数据修复** `[需关注·改数据中风险]`：seeder 幂等 UPDATE `repairDishIngredientGramUnitForDefault`(quantity=100 且 unit 空/计件→gram)·`SEED_LOGIC_VERSION` v9→v10。**纯 UPDATE 无 schema 迁移**(免.sqm)。真机库副本模拟验证：精确修10行/144合法行不动/幂等残留0。取舍：quantity=100.0 精确匹配(避免误伤合法计件)·极端"真录100个计件"误伤罕见可接受(已.sq注释)。
4. **透明准则**：changelog.json 加 v2 告知用户(修单位显示+分类默认克数)。
5. **quantity=NULL 的8行 user 菜**(0营养·另一根因)→记待办跟进·本次不动。

**门禁**：google_quality_engineer 审查(无阻断·2建议已收口:units加载 runCatching 防 unitsReady 永挂 + saveIngredientAsDish 注释) + 叠加独立 /code-review。构建 `:shared:testDebugUnitTest`+`:androidApp:assembleDebug` 均绿。

**off-type 进待办(未做·UI类单开session)**：①添加食材vs添加菜品两编辑页不统一(字体色/名称范式·抽共享FormField) ②营养走势折线三线同显+周月视图统一评估(用户2026-07-22提)。

**高风险/禁止项**：无。文件数(7)在阶段限额内。

### 续做 A1+A2+C（用户指定·同数据/编辑 session）

- **A1 修 quantity=NULL 配料(0营养)**：真机库 8 行 user 自建菜配料 quantity=NULL(用户加食材没设量)→营养算0。seeder 幂等修:`selectUserDishIngredientsWithNullQuantity`(**仅 source='user'**·preset 空量走 seed 循环填正确值不能被抢填)→按食材名 `SeasoningDefaults.defaultGramFor`(蛋50/菜150/奶200…)+`fillDishIngredientQuantityIfNull`(quantity+克单位)·`SEED_LOGIC_VERSION` v10→v11。真机库副本验证:精确命中8行/排除4行preset软删。加 `PresetDataSeederTest` A1 回归测试(鸡蛋空量→50g+克·绿)。**小改自评**(复用已测原语)免单独 Google agent。
- **A2 关待办悬案**:查证 `dish_ingredient(dish_id,ingredient_id)` **有 `uq_dish_ingredient` UNIQUE 索引**(CREATE TABLE+真机库均有)→待办 E组标结论关闭·零代码。
- **C 查证已修**:待办 line92「RANDOM 模式 IN 全食材展开 999崩溃隐患」**早已修**(`RANDOM→listAllDishMinis`/`selectAllDishesForRandom` 无 IN·`Cookbook.sq:1145` 注释含原理·pantryIds 仅内存Set·candidateIds≤100)→**先查证再改**未重复造轮子·待办标结论关闭·零代码。

**取舍**:C 本是待办列的"待做"，查证发现已修→如实标关闭不虚做(省 token+诚实)。A1 只修 user 菜(preset 由 seed 填)。**高风险/禁止项**:无。

---

## 无人值守（2026-07-22 傍晚）：数据健康 session 按序推进

**触发**：数据健康 session，用户"无人值守开始"。按「📊 数据/健康类」backlog 顺序自主推进·不拍板·每项完成本地 commit(`[unattended]` 前缀)不 push·存疑入待确认队列·健康数据守「联网核实权威指南原文·不过度」红线·≤15 文件/项。

**backlog 顺序**：①✅营养计算规则成页(已 push `caeca84`·用户在场时) ②🔄四项数据待核(玫瑰花/章鱼/年糕/生蚝·联网核准 agent 跑中) ③忌口补漏剩余边界(鸡毛蛋卫生/鱼油/植物高嘌呤) ④GI/纤维覆盖率补齐 ⑤菜品加食材智能默认剂量 ⑥食材库/预设菜扩充 ⑦AI规则推荐 MID-1(dish.commonness)。

**决策**：
- **D1**：四项数据(改 ingredient_nutrition)与忌口补漏(改 care_rules/crowd_rules)**不同文件→可并行**联网 agent；GI/纤维与四项数据同文件→**串行**(四项合并后再做)。
- **D2**：健康忌口「判定口径=规则非数据」红线→忌口补漏 agent 只产出**联网核实建议**(该加/不加/level·带指南 ref)，据结论**保守落地(宁少勿过度)**、存疑记待确认，不让 agent 自主拍板加 care。
- **D3**（用户 2026-07-22 补充·方法论红线）：**联网只适合「少量+验证类」，大批量数据获取要脚本化或权威数据源，别逐条联网硬抓**。据此重判 backlog：②四项数据(4项验证)③忌口补漏(几类边界·核实指南口径)=少量验证→**联网合适·继续**；④**GI/纤维覆盖率补齐(~几百项·大批量)→不逐条联网**：先摸缺口→看能否脚本化(《成分表》结构化补纤维;GI 很多食材本无权威实测值→缺的**保守标 pending 不硬凑**)，无脚本化数据源就标"需权威数据源·待核"记待确认，不做大批量联网。见 memory `web-verify-small-not-bulk-scrape`。

**四项数据现状(核准前)**：生蚝(verified·嘌呤239高)/章鱼(verified·嘌呤89.8中)/年糕(verified·GI87高)/玫瑰花(**pending**·干玫瑰花蕾非成分表原书值·gi=0 存疑将建议省略)。

### ⚠️ 待确认队列（用户"都做一下"后已处理 Q1/Q4·Q2/Q3 守红线保持）
- **✅忌口-Q1 植物高嘌呤误判红 已修**（`IngredientCrowdCare`·[unattended]）：食材百科红绿灯对"植物来源(蔬菜/菌菇/豆/主食/水果)实测高嘌呤"从"慎选红"降"留意(植物嘌呤利用率低)"、动物来源仍红。据《食养指南2024》。**只改展示级·不碰忌口**(忌口按 care·植物本无 care avoid)。**藻类(紫菜/海带)存疑从严排除**(不降·按数值判)。Google 审无阻断+单测(黄豆/干香菇降·沙丁鱼/紫菜仍红·压制不破)。
- **❌忌口-Q2 鱼油高血脂 recommend 守红线不做**：omega-3 降甘油三酯是"处方级制剂"结论·市售鱼油≠处方·写"推荐/降脂"踩健康免责红线→保持不加。待用户定是否只作中性说明。
- **❌忌口-Q3 鸡毛蛋 preschool/lactation 守红线不做**：无针对性指南原文点名·加了=想当然过度(违反"判定口径=规则·必须有指南据"红线)→保持只加 pregnancy/infant=avoid。待用户定或找到指南再加。
- **🔨GI-Q4 面米主食 GI 部分补**（[unattended]·少量验证符合D3）：**已补3项**(速冻馒头88/粘米粉65[籼米粉非糯米·守取65不取87]/坚果麦片56·权威GI库)；**7项复合食品留空**(速冻包子/小笼包/馄饨/手抓饼/八宝粥/酒酿/玉米什锦[=杂蔬非甜玉米]·带馅带油无标准GI·宁留空不编造)。单测绿。

### 各项完成存档补充
- **③忌口补漏 🔨**（[unattended]·改 care_rules 1文件）：联网核实(2个 agent 少量验证类·符合D3)。**落地**：鸡毛蛋 care_pregnancy=avoid + care_infant=avoid(食品安全·孕期指南据·鸡毛蛋名在 ingredients.json 已验·单测绿)。**保守未做(有指南反证)**：植物高嘌呤不做痛风忌口(2024食养指南·现状正确保持)、鱼油不做痛风忌口。**4项存疑入待确认队列(Q1-Q4)**。守"判定口径=规则非数据·宁少勿过度"红线。
- **④GI/纤维覆盖率 🔨复核不硬补**：实测 GI75%/纤维95%(旧记63-65%过时)。真缺口多为合理空缺(调料无GI/动物无纤维)·不为凑数硬补(调料补GI有害)。真缺口(面米主食GI)记 Q4 可选后续。守 D3(不为覆盖率大批量硬抓)。
- **⑤智能默认剂量 ✅查证已实现(零代码)**：`SeasoningDefaults.GROUP_GRAMS` 已有非调料按 `FoodGroup.classify` 分类经验默认(菜150/蛋50/奶200…)+调料按名·之前 session(P2·07-16/22)已做·待办未同步→查证标关闭·不重复造轮子。
- **⑥食材扩充 / ⑦AI家常度列 → 停做·记待确认**：⑥无明确边界(需产品定方向)；⑦涉迁移+几百菜**主观家常度标注**(口径=产品决策)+推荐接入·中等工程→无人值守不自主做迁移+主观数据·需先定口径再排期。

## 本轮无人值守收尾（数据健康 session·2026-07-22 傍晚）
**实质推进 5 项**：①营养计算页(已push `caeca84`)·②四项数据(`d571ad6`)·③忌口补漏(`dd4b021`)·④GI/纤维复核·⑤智能剂量查证。`[unattended]` 本地 2 commit 未 push(`d571ad6`/`dd4b021`)+本文档更新。⑥⑦需产品决策入待确认。**方法论(用户 D3)**：联网只适合少量验证·大批量用脚本/权威源→④据此没为凑覆盖率硬抓(诚实判合理空缺)。**待确认 Q1-Q4** 待用户定。

### 各项完成存档
- **②四项数据 ✅**（[unattended]·改 ingredient_nutrition.json 1 文件）：联网核《中国食物成分表》第6版一手值。**生蚝**钙131→35mg(旧值严重偏高·官方35)+钾200→375·verified；**年糕**钠20→56.4/钾42→81/钙19→31/纤维0.8→0(官方未检测)·GI87保留(糯米饭口径高GI)·verified；**玫瑰花**删gi(0→省略·干花蕾无GI意义·防误读低GI)+ref标非成分表原书·**维持pending**(成分表6版/nlc均无干花蕾条目)；**章鱼**逐项核对确认无误·不改。改JSON→指纹自重跑·老库 upsert 更新。:shared 单测绿·数据来源页已含成分表6版/GI/嘌呤出处(无需新增)·care层不受数值修正影响。
