# 跨模型项目上下文审计

> 阶段：审计与入口收敛完成
> 时间：2026-08-05

## 需求

- 核对项目通用知识、当前状态和交接入口是否均位于项目 `.ai-context/`。
- 确保另一模型首次打开仓库时，可按入口直接掌握项目全貌和当前工作状态。

## 模式与范围

- 级别：标准任务；交互：直接审计与修复模式。
- 协作：`document-curator` 只读检查入口、索引、会话交接和仓库外置项目知识。
- 本轮只允许补充/修正项目 `.ai-context/` 内的文档与索引；不移动或删除现有文件，不修改业务代码。

## 审计范围

- 根入口：`AGENTS.md`、`.ai-context/README.md`、`.ai-context/docs/projectReview/00_导读与索引.md`。
- 项目状态：`.ai-context/docs/context_memory/SESSION_交接.md` 与最近主题快照。
- 可发现性：`.ai-context/docs/功能路径索引.md`、`feature/`、`experience/` 的索引。
- 外置检查：根目录 `docs/`、`.claude/`、`.codex/` 和项目说明文件中是否残留唯一项目知识。

## 已知风险

- 旧文档可能仍被误当作唯一入口；未跟踪的上下文文件可能未被主交接引用。
- 上一轮新增的周期记/NDJSON 开发规范必须能从全景图和交接直接定位。

## 结论与落地

- 新增 `.ai-context/PROJECT.md`：跨模型首读顺序、真相优先级、当前关键任务、文档分层和代码近旁资料边界。
- 重写 `docs/AI-交接文档.md` 为工具中立的接手指南；`README.md`、`AGENTS.md`、`CLAUDE.md` 均指向新入口。
- 根 `docs/` 的 5 份历史项目资料已迁入 `docs/feature/_archive/legacy_root_docs/` 并标注非当前依据；不再保留根目录项目知识双源。
- 旧 `.ai-context/docs/待验证清单.md` 已归档；当前只认 `feature/真机待验证清单_<yyyyMMddHHmm>.md` 的时间最新唯一文件。
- 项目地图、项目现状、待办索引和当前交接已挂接周期记/NDJSON 开发规范与新的跨模型入口。
