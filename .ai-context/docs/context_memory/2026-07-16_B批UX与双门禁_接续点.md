# 上下文快照：B 批 UX + 双门禁 + 接续点（2026-07-16 收尾）

> 新 session 从这里接续 B-6/7/8。本段全部已推送 master(到 a30c75d)。

## 一、两条已确立的强制门禁（写进 CLAUDE.md，做任何 UI 前后必守）
1. **UI/交互设计门禁**：界面/交互类操作**编码前先 spawn Apple-UX 设计 agent**出规范（读 `苹果风格UI设计方案.md`+现状代码），据方案编码。纯文案/复用 §九 小改可免。
2. **代码质量门禁**：一批编码+构建+单测过后，**spawn Google 资深工程师审查 agent**（正确性/并发/性能/复用/边界等），**阻断项(🔴)必修复+复验**再交付。
- 做 UI 先查 `交互组件复用指南.md`（需求→22 组件对照 + B 批映射）+ §九(9.1–9.17)，能复用必复用。

## 二、B 批进度（B-1~B-5 已双门禁完成，已推送）
- **B-1 餐食页未保存返回守卫**：新复用件 `UnsavedGuard`(`rememberUnsavedGuard(isDirty,onConfirmLeave)` 非包裹式返 requestBack) + AddMealVM 基线签名(`sigNow/markBaseline/isDirty`，两加载路径末尾 markBaseline)。
- **B-2 统一 Snackbar 宿主**：`ui/component/AppSnackbar.kt`(`AppSnackbarController.showMessage/showUndo` + `LocalAppSnackbar`)，MainScaffold 挂单一 `SnackbarHost` + `CompositionLocalProvider` 包住 NavHost。showUndo 用 Long + 单 job 串行化。
- **B-3 库存Tab就地加减份数**：`IngredientCard` 加 `footer` 槽；库存Tab 每卡底 `MiniStepper`(±份数，setServings/addServings)。
- **B-4 食历卡三图标收 ActionSheet**：`DayMealCardView` 复制/编辑/删除→"⋯"开 `ActionSheet`(SheetAction，删除 destructive)。
- **B-5 删整天软删可撤销**：`MealRecordRepository.snapshotDay(date):List<DayMealDraft>` → `deleteDayMeals` → `showUndo` 点撤销 `saveDayMeals(...,bumpPreference=false)` 还原。Home/Timeline 两屏 `deleteDayUndoable`，删掉"不可撤销"硬确认。
  - **Google 审查阻断项已修**：撤销 saveDayMeals 原会因该日刚删空→oldDishIds 空→所有恢复菜误 +1 喜爱度污染排序。修法=`saveDayMeals` 加 `bumpPreference:Boolean=true`，撤销传 false；补回归单测 `MealRecordDeepTest.删整天撤销还原_不重复抬喜爱度`。另修：快照读失败/空不照删、撤销失败落提示。

## 三、B 批剩余（新 session 接续，各需双门禁）
- **B-6 食材"保存并继续"**：复用 §9.13 + 待建 `FormBottomBar`(primaryText/onPrimary胶囊 + 次 secondaryText/onSecondary)。难点=存成功(异步 `ui.lastSavedIngredientId` 变化)后**重置表单**——`IngredientEditorDialog` 字段用 `remember(ingredient?.id){...}`，需加 `resetNonce:Int` 作附加 key(replace_all `remember(ingredient?.id)`→`remember(ingredient?.id, resetNonce)`)，保存并继续时 nonce++ 重置 + 留页 + Snackbar"已保存「X」，继续添加"。screen 现 `onSave=vm::saveIngredientEditor`；两处编辑器(create 504 / edit 525)。VM `saveIngredientEditor` 成功设 `lastSavedIngredientId`。
- **B-7 菜品页大标题+整行搜索+四Tab简化**：§9.15 `LargeTopAppBar`(参照 MineScreen 的 exitUntilCollapsed)+`AppSearchField`+`SegmentedControl`。`DishesScreen` 现为带搜索的普通 TopAppBar。**先过 Apple-UX 设计**。
- **B-8 AppTopBar 收敛 16 屏**：§9.15 判据(Tab 落地→LargeTopAppBar；带返回二级页→`AppTopBar`)。逐屏甄别是否有意大标题。

## 四、本 session 其它已落地(供新 session 知情，均已推送)
- 配色切换 7 套(赤陶橘默认 + 6 套 iOS 系统色鲜亮款)、今日卡宏量条(分段实色，渐变已回退)+慢病提示个人视角(色系墙保持纯结构)、数据来源页/参考资料组、功能介绍页(Apple 欢迎范式)、调料默认克数、组合部分选、营养级别评估核心(NutritionLevelEvaluator，方案 `营养级别评级方案.md`)。
- 未做待办：调养/忌口判定加剂量占比门槛、AI设置云端模型配置指南、用户上报菜品(联网后)、首次启动引导浮层。

## 五、构建/验证
- 构建 `scripts\build-cli.bat :androidApp:assembleDebug`；shared 单测 `:shared:testDebugUnitTest`。全绿。
- 装机待验：7套配色、今日卡、功能介绍、数据来源、B-1~B-5 各交互。
