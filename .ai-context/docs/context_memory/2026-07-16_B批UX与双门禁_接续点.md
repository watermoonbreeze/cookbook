# 上下文快照：B 批 UX + 双门禁 + 接续点（2026-07-16 收尾）

> 新 session 从这里接续 B-6/7/8。本段全部已推送 master(到 a30c75d)。

## 一、两条已确立的强制门禁（写进 CLAUDE.md，做任何 UI 前后必守）
1. **UI/交互设计门禁**：界面/交互类操作**编码前先 spawn Apple-UX 设计 agent**出规范（读 `苹果风格UI设计方案.md`+现状代码），据方案编码。纯文案/复用 §九 小改可免。
2. **代码质量门禁**：一批编码+构建+单测过后，**spawn Google 资深工程师审查 agent**（正确性/并发/性能/复用/边界等），**阻断项(🔴)必修复+复验**再交付。
- 做 UI 先查 `交互组件复用指南.md`（需求→22 组件对照 + B 批映射）+ §九(9.1–9.17)，能复用必复用。

## 二、B 批进度（B-1~B-5 已双门禁完成，已推送）
- **B-1 餐食页未保存返回守卫**：新复用件 `UnsavedGuard`(`rememberUnsavedGuard(isDirty,onConfirmLeave)` 非包裹式返 requestBack) + AddMealVM 基线签名(`sigNow/markBaseline/isDirty`，两加载路径末尾 markBaseline)。
- **B-2 统一 Snackbar 宿主**：`ui/component/AppSnackbar.kt`(`AppSnackbarController.showMessage/showUndo` + `LocalAppSnackbar`)，MainScaffold 挂单一 `SnackbarHost` + `CompositionLocalProvider` 包住 NavHost。showUndo 用 Long + 单 job 串行化。
- **B-3 库存Tab就地加减份数**：`IngredientCard` 加 `footer` 槽；库存Tab 每卡底 `MiniStepper`(±份数，setServings/addServings)。
- **B-4 食历卡三图标收 ActionSheet**：`DayMealCardView` 复制/编辑/删除→"⋯"开 `ActionSheet`(SheetAction，删除 destructive)。
- **B-5 删整天软删可撤销**：`MealRecordRepository.snapshotDay(date):List<DayMealDraft>` → `deleteDayMeals` → `showUndo` 点撤销 `saveDayMeals(...,bumpPreference=false)` 还原。Home/Timeline 两屏 `deleteDayUndoable`，删掉"不可撤销"硬确认。
  - **Google 审查阻断项已修**：撤销 saveDayMeals 原会因该日刚删空→oldDishIds 空→所有恢复菜误 +1 喜爱度污染排序。修法=`saveDayMeals` 加 `bumpPreference:Boolean=true`，撤销传 false；补回归单测 `MealRecordDeepTest.删整天撤销还原_不重复抬喜爱度`。另修：快照读失败/空不照删、撤销失败落提示。

## 三、B 批剩余（新 session 接续，各需双门禁）
- **B-6 食材"保存并继续" ✅ 已双门禁完成**（本 session，未提交，待用户指示）：
  - 新建复用件 `ui/component/FormBottomBar.kt`(`primaryText/onPrimary`胶囊 + 可选 `secondaryText/onSecondary`次文字，`navigationBarsPadding`只此消费一次 + 顶 1dp Divider)。
  - `IngredientEditorDialog`：主 CTA 从顶栏**下移**到底部 `FormBottomBar`(顶栏只留返回)；滚动 Column 改 `weight(1f)` 让底栏常驻；加参 `canSaveAndContinue`(create 传 true/edit 默认 false)、`onSave` 尾参加 `keepOpen:Boolean`。次操作"再记一个"仅新建态，显隐由回调传入决定。
  - **重置未用 resetNonce，改显式 `resetForm()` lambda**(逐项赋默认，含 `unitTouched/groupTouched=false` 恢复按名预选、`moreExpanded=false`)——因字段 `remember(ingredient?.id)` key 恒 null 不会 remount，须显式复位。
  - **双信号分流**(关键)：VM state 加 `continueSavedNonce:Int`；`saveIngredientEditor(...,keepOpen=false)` onSuccess 里 `lastSavedIngredientId`(关弹层信号) 与 `continueSavedNonce`(复位留页信号) 依 keepOpen 分流——keepOpen=true 不置 lastSavedId(不触发 screen 关弹层 effect)、`continueSavedNonce+1`。弹层 `LaunchedEffect(continueSavedNonce)` + `savingContinuation` + `lastHandledNonce` 守卫(只本弹层点过且 nonce 真增才复位)触发 `resetForm()`+聚焦(`FocusRequester`)+Toast。
  - **Toast 而非统一 Snackbar**：全屏 Dialog 遮住 MainScaffold 共享 Snackbar 宿主(不可见)，纯告知无跟进项 §9.12 允许 Toast。
  - Google 审查**无阻断项**；采纳建议 1(nonce 守卫解耦)、2(提 `formValid` 消除主/次 enabled 重复)。
- **B-7 菜品页大标题+整行搜索 ✅ 已双门禁完成**(本 session，未提交)：
  - `DishesScreen` 普通 TopAppBar→`LargeTopAppBar`(大标题「菜品」Bold+右上 Add，`exitUntilCollapsedScrollBehavior`+Scaffold 挂 `nestedScroll`)。
  - **搜索行/四Tab SegmentedControl/计数/筛选下沉进 LazyColumn 头部**(新 `LazyListScope.dishHeaderItems` 扩展=搜索/Tab/计数 3 item + filters 1 item)作折叠燃料——否则大标题不折叠。菜系档右侧列表驱动折叠(CuisineRail 独立 scroll 不干扰)。
  - **字母跳转 `letterIndexMap` 偏移基数 0→4**(头部 4 item)+纳入 remember key(踩坑红线)。filters 无条件加(空也占槽)保证基数恒 4。
  - **全屏 Overlay 遮搜索行**问题：`DishSearchOverlay` 加 `onKeywordChange` + 自带聚焦 `AppSearchField`(iOS Mail 式)。`AppSearchField` 加可选 `focusRequester` 参数(`.then(if..Modifier)`)。
  - `stickyHeader` 本 compose 版本导入未解析→回退普通 `item`(原字母头也非吸顶，行为不变)。Google 审查**无阻断**，采纳建议(then 写法、注释锁 letterHeaderCount)。
- **B-8 AppTopBar 收敛 ✅ 已完成 7 屏**(本 session，未提交，审查中)：
  - `AppTopBar` 加可选 `subtitle` 参数(AI 推荐"推荐来源")。
  - 转 AppTopBar：NewDishScreen(仅主屏,子对话框保留)、AddDayFoodScreen、AiRecommendScreen(+subtitle)、AiSettingsScreen、WeekPlanScreen、CookingTimerScreen、FoodTimelineScreen(onBack 可空双路由天然支持)。删 3 条死导入 ArrowBack。
  - **合理豁免不转**：NutritionTableScreen/SearchScreen(标题是搜索框)、FeatureGuideScreen(空标题+透明栏欢迎页)、CookModeScreen(Close 图标全屏模式)——AppTopBar 只接 String title/固定 ArrowBack/固定 background 色，无法承载。
- **B-8 AppTopBar 收敛**：§9.15 判据(Tab 落地→LargeTopAppBar；带返回二级页→`AppTopBar`)。**Explore 摸底结论(2026-07-16)**：
  - 已合规：首页/我的(LargeTopAppBar)、菜品详情/采购清单/家庭成员/膳食统计/功能设置/食材自由搭配/膳食参考依据/数据来源(AppTopBar)。
  - **11 个内联 TopAppBar 二级页→改 AppTopBar**：NewDishScreen、AddDayFoodScreen、AiRecommendScreen、AiSettingsScreen、WeekPlanScreen、CookingTimerScreen、CookModeScreen(动态标题)、NutritionTableScreen、FeatureGuideScreen；**SearchScreen(标题是搜索框，AppTopBar 只接 String title→需甄别，可能不适合直接换)**；**FoodTimelineScreen(有 onBack→AppTopBar；无 onBack 的 timelineAt 路由→保持/LargeTopAppBar)**。
  - DishesScreen→LargeTopAppBar(即 B-7)。Dialog 选择器(IngredientPicker/DishPicker)保持现状不动。
  - 逐屏替换时保留各自 actions/标题/onBack；非纯机械屏(Search 搜索框标题、Timeline 双路由、CookMode 动态标题)单独判断。B-8 属"复用已确立 AppTopBar 同类小改"可免设计 agent，但仍走代码审查门禁。

## 四、本 session 其它已落地(供新 session 知情，均已推送)
- 配色切换 7 套(赤陶橘默认 + 6 套 iOS 系统色鲜亮款)、今日卡宏量条(分段实色，渐变已回退)+慢病提示个人视角(色系墙保持纯结构)、数据来源页/参考资料组、功能介绍页(Apple 欢迎范式)、调料默认克数、组合部分选、营养级别评估核心(NutritionLevelEvaluator，方案 `营养级别评级方案.md`)。
- 未做待办：调养/忌口判定加剂量占比门槛、AI设置云端模型配置指南、用户上报菜品(联网后)、首次启动引导浮层。

## 五、构建/验证
- 构建 `scripts\build-cli.bat :androidApp:assembleDebug`；shared 单测 `:shared:testDebugUnitTest`。全绿。
- 装机待验：7套配色、今日卡、功能介绍、数据来源、B-1~B-5 各交互。
