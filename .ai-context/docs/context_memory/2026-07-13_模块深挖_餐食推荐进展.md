# 模块深挖进展（餐食 / 推荐 / 待续）— 2026-07-13

按用户"深入其他模块，一个个来"推进：① 餐食 → ② 推荐 → ③ 食材库存。前两项已完成推送。

## 已完成并推送（gitee master）

- **① 餐食模块**（`eba3bba`）
  - 真 bug：移动餐食到新日期会重复抬喜爱度。`saveDayMeals` 加 `incrementBaselineDate`（默认本日；移动传来源日 `loadedFromDate`），只对相对基线新出现的菜 +1。
  - 清死代码：`occupiedDates` 字段及其 init 9999 行查询、无调用的 `deleteMealRecord(id)`、save() 误导性 `?: return` 改 `!!`。
  - 测试：`MealRecordDeepTest`（5 个，含守护移动不重复 +1）。

- **② 推荐链路 · H2**（`e9737c1`）
  - 真 bug：`RecommendationOrchestrator.rotate` 返回 `drop(start)` 未 take，把整条长尾喂给 prompt/validate（prompt 膨胀、模型可选到 UI 列表外的菜）。修：`rotate` 末尾 `.take(DISPLAY_BATCH)` 只返回当前一批。
  - 测试：`RecommendationOrchestratorTest` +2（候选不外溢、模型选批外菜被过滤）。

- **② 推荐链路 · H1**（`40beb18`）—— 用户拍板"真正消费 suggestions"
  - 此前配云端模型时点"开始推荐"会真发一次云端调用产出分餐组合，却被 UI 整个丢弃（只用规则候选扁平列表）→ 空跑。
  - 改：`AiRecommendViewModel` UiState 加 `suggestionGroups`，`mapResult` 在 `source==MODEL && suggestions非空` 时按候选 byId 解析组装 `SuggestionGroupUi`（理由/做法/菜列表）；加 `toggleGroup`。`AiRecommendScreen` 加 `SuggestionGroupCard`（方案N+理由+🍳做法+可勾单菜/整套）。规则兜底/离线仍走扁平列表。两条路共用 `selectedIds` + `onPickMeal` 契约。

## 待续（下次继续）

- **M3/M4 判定关键词表收敛**（用户已同意"顺手收敛带测试"，本次下班收尾未做）：
  荤素/主食/早餐判定散落多套关键词表口径不同步——
  `MealSlot.kt` 的 `MealSlotMatcher.MEAT`、`domain/FoodGroup.kt`、`RecommendationDataSource.BREAKFAST_KEYWORDS`/`BREAKFAST_SOFT_KEYWORDS`、`domain/StapleFood.kt`。
  建议抽 `FoodHeuristics` 单一真相源，`isMeat` 复用 `FoodGroup.classify(...) in {FISH,RED_MEAT,WHITE_MEAT,EGG}`，删 `MealSlotMatcher.MEAT` 重复表；`DRINK_ONLY` 精确等号改 contains；早餐"面"过宽收窄。补一致性测试。

- **③ 食材库存（ingredient/pantry）深挖**：尚未开始（列表里的第三项）。

- 推荐链路其余标记（未改，风险/较大或价值低）：
  - M5/L1 PeriodPlanner 健康 80% 达标后软加分可被应季/主食/平衡信号拉低到临界外（补对抗测试）。
  - L3 HealthRuleEngine 打分无量级锁定测试（忌口 AVOID_PENALTY=50 足够大、"忌口永远最后"稳；但"物尽其用主料优先于推荐加分"未用测试锁定）。
  - M2 Parser/PlanOrchestrator 的 JSON 提取不兼容数组根（当前 prompt 是对象根，价值低）。

- 餐食其余标记（未改）：软删 meal_record 累积膨胀（改硬删可能影响同步）、`observeTimelineCards` 逐日全表扫 pantry 的 N+1、`buildDayMealCard` 用内部 `today()`（违红线）。

## 工作节奏约定

每模块：分析 agent → 报告真 bug/可优化 → 修低风险+补测试、大改/产品决策先问用户 → 多轮回归 → commit+push（`[unattended]` 前缀）。用户在另一台机器 pull 编译。仅 Android 交付。
