# 无人值守大批次 进度快照（2026-07-16 晚，用户下班后自主执行）

## 🔖 新 SESSION 交接入口（先读这里）
> 本文件是**接续锚点**。新 session 接手时按序读：
> 1. `CLAUDE.md`（项目规范/踩坑红线）+ `.ai-context/rules/通用规则.md`（门禁）。
> 2. **`.ai-context/docs/feature/一期目标与阶段规划.md`**（北极星 + 一期方案v2：三部分[基础/营养/AI]、薄闭环+依赖DAG加厚、**营养数据=关键路径**、3横切主题、完成判据）。
> 3. **`.ai-context/docs/feature/营养数据治理方案.md`**（part2 营养·PM治理方案：现状盘点、权威源、批次0-3、口径分级、**§八用户追加长期方向**、**§三批次1执行结果 + DIABETES分支怎么做**）。
> 4. 本文件下方进度（已完成/进行中/剩余）+ `待办总览.md` + `experience/06_问题与踩坑.md`（红线经验）。
>
> **工作规则（用户定）**：中文对话；每个 UI/交互改动**先过 Apple-UX 设计门禁**、每批编码后**过 Google 代码审查门禁**（阻断项必修）；健康数据守免责红线（仅供参考·非医嘱、禁编造权威出处、无源省略/pending、一手权威才 verified）；**每完成一个阶段：审核验证测试→保存上下文→总结经验→飞书通知→下一个**；用户全权授权自主推进、不必等确认。
>
> **⏭ 当前无人值守持续推进整个 backlog（用户 2026-07-16 授权：除 iOS 外全部待办都做，不拍板、先做一版看效果、每项完成走仪式[存档+经验+飞书]、不确定走"多角色相互验证得最优方案"[已写进通用规则§十]）**。已完成 DIABETES GI(`0cdd483`) + 餐次常吃 chips(`dc67710`)。下一批候选：详情页接入 GI/嘌呤定性提示、工程性能优化(DB索引+异步seed)、批次2去pending、AI设置云端配置指南、菜系分类、库存/份数开关、P2写入Diff/P3日志/DI平台化、逐病种深挖。
> 关键工作模式记忆见 memory `cookbook-unattended-workmode`；通用规则新增 §十(无人值守特有决策)。
> 本轮已提交(至 `15194ed`)：DIABETES GI 分支 + 餐次常吃 chips + 通用规则§十 + 详情页GI/嘌呤定性提示，工作区仅剩 temp/claude 杂物。
> **已完成(本 session 无人值守)**：①DIABETES GI 分支`0cdd483` ②餐次常吃chips`dc67710` ③详情页GI/嘌呤提示`15194ed`(多角色验证门禁首次实践) ④DB索引优化`b7f1949`(v24加6个高频反查列索引,性能优化第一步)。三慢病定性现覆盖今日卡(聚合)+详情页(单菜)。
 ⑤AI配置指南入口`7b030cb`(ModalBottomSheet按厂商申请步骤+打开官网,CloudModel加applyUrl)。
> **性能优化剩余(拆分推进)**：异步seed(风险高·动seed流程+需真机验启动·单独评估)、显示优化(N+1/虚拟化)、P2写入Diff。
 ⑥库存挂钩功能开关`c707ec5`(总开关去噪·rememberPantryHookEnabled helper·三门禁·初审2阻断已修)。
> **backlog 持续推进中**，下一批候选：菜系分类(产品决策·多角色验证)、P3 KMP日志/DI平台化(工程)、慢病数值软约束、逐病种深挖、异步seed(风险高)。批次2去pending因低产出(review-only不宜单独合并)暂缓。
> **本 session 已交付 7 项、~28 提交**(DIABETES GI/餐次chips/详情页GI嘌呤/DB索引/AI配置指南/库存挂钩开关/菜系分类打磨)，全部双门禁(部分三门禁+多角色验证)、每项飞书+存档+经验。待办对账已标先前已实现项(自由搭配存为菜品/首启引导)。
> **菜系分类**`b7983a1`:多角色验证(产品+UX)定最小打磨(自建菜默认家常菜+CuisineRail去稀疏),不加必填选择器,核心早已完成故标收敛。可选增强(编辑页菜系选择器/推荐融入菜系)留后续。

> 用户授权：按通用常识/权威标准自定口径，全程 Apple-UX 设计门禁 + Google 审查门禁，分阶段独立提交，最后统一保存上下文/总结经验/提交/push/飞书通知。

## 用户本批要求（原话汇总）
1. 食材 Tab 也统一成大标题(与首页/菜品/我的一致)；选食材弹窗单独处理保持不动。"至少四大界面统一"。
2. 健康语义口径全由我定并实现：①剂量占比门槛 ②营养级别评级实装 ③今日缺口 ④忌口判定 ⑤主料语义。
3. 可直接开发的直接开始；工程/分发基础也开始。以上全部要做。

## 已完成并提交
- **Phase 2 健康语义·主料判定(任务①④⑤)** `dfdc23a`：忌口/限量/调养只按主料(role==MAIN/isMain)判定，三处消费点统一(HealthRuleEngine.evaluate + RecommendationDataSource.gatherForPlan + DishDetailViewModel)。规则层无克数，用 is_main 作占比代理；无主料标记的菜不触发定性。单测过。Google 审查无阻断(采纳详情页统一)。**跟进项**：gatherForPlan 的 DB 集成测试(防回退)未加。

## 已完成并提交(续)
- **Phase 1 食材 Tab 大标题** `<待提交>`：`IngredientPickerScreen.content` 按 selectionMode 分叉——`selectionMode=false`(Tab,asDialog=false)用 `LargeTopAppBar`「食材」Bold + scrollBehavior + Surface 挂 nestedScroll(网格滚动驱动折叠)+ 搜索行移大标题下方(pinned);`selectionMode=true`(弹窗)保留原 TopAppBar 不动。共享主体全不动。Google 审查**无阻断+弹窗选择器零回归确认**。**偏离设计点**(取稳):设计建议搜索下沉进网格头+全屏覆盖层+网格偏移+2;改为搜索 pinned(不下沉)避免动网格偏移/重建覆盖层。**真机待验**:折叠平滑度、内容不足一屏优雅退化为常驻大标题(审查确认功能不坏)。

## 本次会话结束交接(2026-07-16 无人值守，用户"按你的来吧"后续推进)
**健康口径 5 项全部落地 + 食材大标题**(用户最强调的两块)，均双门禁无阻断、增量提交：
- `dfdc23a` Phase 2 主料判定(口径①占比/④忌口/⑤主料语义)：忌口/限量/调养只按 role==MAIN，三处消费点统一。
- `4ba0f8b` Phase 1 食材大标题：LargeTopAppBar 统一四大落地页，弹窗选择器零回归。
- `6265482` Phase 3 今日缺口(口径③)：营养互补基线近7天→今日已吃(daysAgo==0)，理由文案"补今日营养"。
- 口径②营养级别评级：**早已实装**(`NutritionLevelEvaluator` P1 热量+钠，`HomeViewModel` 今日卡在用+单测)；P2-P4(GI/糖/嘌呤/脂肪)数据门控，非代码任务。
- `a67ff22` 文档收尾。

**可直接开发/工程基础项主动留待下轮**——理由：每项带 UI 设计门禁(自由搭配存菜/AI配置指南/引导浮层)或需联网权威数据(食材营养补全)或低优先(工程 KMP日志/DI/写入Diff)；本轮上下文已较满，为保质(用户设的双门禁)不在长会话尾部赶多文件 UI 特性。下轮各自走双门禁、新鲜上下文更稳。接续点见下方"未做"。

## 续(2026-07-16 全权授权后持续推进)
- ✅ `6265482` 今日缺口(健康口径③) + `8b3ab4e` 文档。健康口径 5 项全部落地(②营养级别早已实装)。
- ✅ **一期目标框架**:`decd1dc` 立三部分锚点文档 + 项目记忆 `cookbook-phase1-goal`;`cc42e03` **方案v2**(薄闭环+依赖DAG加厚、**营养数据=关键路径**、3横切主题[数据可信免责/反馈闭环学习/体验一致]、每部分完成判据)。见 `一期目标与阶段规划.md`。
- ✅ **part1 打磨·第1项 自由搭配→存为菜品** `24d442a`(双门禁无阻断,复用 NewDishPrefillBus+cookingMethodName,买抖门闩)。
- ✅ **part1 打磨·第2项 首次启动引导浮层** `45146e5`：首启自动弹功能介绍(复用 FeatureGuideScreen)+HAS_SEEN_GUIDE 标记，小改自审。part1 打磨剩:餐次块最近常吃chips(需真机验密度)。
- ✅ **用户认可 part2 为关键路径**并授权"安排适当时机开始"→ 已 pivot：**spawn 产品经理 agent 产出《营养数据治理方案》(后台运行中,agentId a3af03a…)**。返回后存成 feature 文档+审阅+据此分片权威核准补数据(守免责红线:禁编造出处、无源省略/pending、一手权威才 verified)。
- **当前**:part1 打磨基本收尾(剩餐次chips)；part2 数据治理方案编制中。等 PM 方案返回→审阅→执行第一批数据补全。
- 每阶段仪式(用户要求):审核验证测试→存上下文→总结经验→飞书→下一个。

## 续2(part2 启动 + P4 落地 + 用户追加方向)
- ✅ **PM《营养数据治理方案》** `98c243f`(存 `营养数据治理方案.md`)：关键修正=营养行覆盖已100%,真问题=162条pending/GI真需补<15/P3被schema阻断/P4零成本。采纳3决策(引入estimated三态/P3延后/verified≥85%+嘌呤仅定性)。
- ✅ **P4 痛风定性(批次0·零数据)** `98c243f`：NutritionLevelEvaluator 加 HIGH_PURINE_KEYWORDS+matchHighPurineFoods+GOUT分支(仅登记痛风+命中主料→封2+提示),HomeViewModel 今日卡按主料匹配。Google审查无阻断(删泛词凤尾/小鱼干降假阳)。解锁一个慢病维度。
- **用户追加长期治理方向(已入 `营养数据治理方案.md` §八)**：①端到端先跑通一病种(建议高血压)再扩展 ②食材/调料细分 ③全量440+食材/菜品营养可信度逼近全量verified ④逐病种深挖食材/菜品 ⑤**对健康状态本身专业深挖**(基于权威临床/膳食指南,引临床营养专业审校)。
- **用户追加工程项**:数据规模化性能优化(异步seed+DB索引+显示优化,防启动卡)——入待办B。
- **用户新提 part1 项**:首页去"最近/喜爱"(菜品页已有推荐)——**下一项做**。
- ✅ **首页去"热门/最近"发现区** `8ec29dc`(用户提,菜品页已有推荐):HomeScreen 删两块+死导入;HomeViewModel 移除 popular/recent 状态+停两路 DB 观察(兼顾性能),uiState 只留今天+未来计划。
- ✅ **批次1 GI 核准完成(诚实结果)**:GI 值大多已在库且正确,pending 的大多权威源冲突无法核准;仅 8 条有源(大麦/乌冬面/玉米糁/全麦面包/卷心菜/紫甘蓝 verified + 菜心/韭黄 estimated),**gi 值与 seed 现值一致仅升 review/ref**。**工程判断:review-only 升级不单独合并**(改 seed 内容会触发全量 re-seed耗启动、零 app 收益)——攒到有真实值变化的批次一起合并。分片存 `temp/claude/nutri_auth_1.json`(8条)。详见 `营养数据治理方案.md` §三批次1执行结果。
- **⏭ 明确下一步(健康敏感,留focused会话)**:**DIABETES GI 评级分支**——真正解锁糖尿病,用已在库 gi 值(不依赖批次1合并)。仿 P4 痛风:caller(HomeVM)按主料查 gi(`NutritionRepository.allIngredientNutrition()` 建名→gi 映射 或 按 id 查),命中 gi≥70 高GI主食→"含高GI主食·糖尿病可换低GI"提示+evaluator DIABETES 分支+单测。守免责(FAO/WHO口径、个人视角)。
- ✅ **DIABETES GI 评级分支(P2 解锁糖尿病)** `0cdd483`：仿 P4 痛风，用**已在库 gi 值**(不依赖批次1合并)。①`NutritionLevel`:GI_HIGH=70(FAO/WHO,非WS/T652)常量 + `matchHighGiFoods(names,giByName)`(按名查真实 gi≥70,非关键词——GI 有实测数值故按值判) + `evaluate` 加 `highGiFoods` 参数与 DIABETES 分支(命中封2+"含X等高GI食物·糖尿病可换低GI或控量",缺数据不下调向后兼容);②`NutritionRepository.giByName()`:名→gi(同名取较高值偏保守·宁多勿漏,key 去空格归一);③`HomeViewModel`:gate DIABETES 才查 gi 表(省无谓查询)传入 evaluate。单测6场景(命中/未登记不触发/无数据不下调/边界70/trim归一/糖尿+痛风叠加取最严)全绿。双门禁:无UI免Apple-UX;Google审查**无阻断**(采纳 trim 归一符红线 + 枚举/文档注释同步实现状态;建议1 gi缓存优化因 gate 已足够暂不做)。**三慢病维度(高血压P1/痛风P4/糖尿病P2)均进 evaluator 各有单测**。
- 剩:part1 餐次chips(真机验密度·走UX门禁)、批次2去pending(GI/宏量逐病种攒批合并)、逐病种深挖(含健康状态本身专业深挖·需临床营养专业审校)、part3 加厚、工程性能优化(异步seed+索引)。

## 未做(留待,按优先级)
- **Phase 3 营养级别评级实装(任务②)**：方案 `营养级别评级方案.md`；NutritionLevel.kt/NutritionLevelEvaluator 已有核心。缺数据退多样性。
- **Phase 4 今日缺口(任务③)**：近7天→今日缺口即时因子，权重已因子化(RecommendationWeights)。
- 可直接开发：自由搭配→存为菜品、AI设置云端配置指南、首次启动引导浮层、食材营养数据补全、餐次块最近常吃chips。
- 工程基础：P2 SQLDelight 写入 Diff、P3 KMP 日志、DI 平台化。

## 口径决策(我按常识定的,供审阅)
- 主料语义 = is_main 标记(数据已 populated,与全仓 mainIds/planMainNames 一致)。
- 剂量占比:规则层无逐食材克数→用 is_main 作代理(不新增克数透传,避免大改)。
