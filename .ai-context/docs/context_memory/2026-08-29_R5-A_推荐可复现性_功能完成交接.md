# R5-A 推荐可复现性 · 功能完成交接

> 日期：2026-08-29  
> 状态：**ARCH_ACCEPTED / AUTOMATED_GATES_PASS**  
> 批次：`RECOMMEND-REPRODUCIBILITY-01`（路线图 R5-A）

## 已完成

- 规则候选排序在既有“正常 → 最近 → 忌口 → 分数”逻辑之后，以 `dish.id` 升序解决同层同分的输入顺序不确定性。
- 增加 T-RR-01~05：固定 8/25 菜夹具、20 次重复、rotation 0..3、四种 style，以及 avoid/recent 分层。
- 实现提交：`6282d1c8`；状态与本交接的收口提交见后续 Git 历史。

## ARCH 独立验收

| 验证 | 结果 |
|---|---|
| `git diff d8244693..6282d1c8 --check` | 通过，无空白错误 |
| `:shared:testDebugUnitTest --tests "com.sxdbsm.cookbook.ai.RecommendationOrchestratorTest"` | BUILD SUCCESSFUL |
| `:shared:testDebugUnitTest --tests "com.sxdbsm.cookbook.ai.RecommendationDataSourceTest"` | BUILD SUCCESSFUL |
| `:shared:testDebugUnitTest` | BUILD SUCCESSFUL |
| `:androidApp:assembleDebug` | BUILD SUCCESSFUL |
| `feature_sync_check.py --struct` | 通过 |

## 边界与未做事项

- 未修改 RecommendationWeights、RecommendationStyle、RecommendationDataSource、Prompt、云端运行时、SQLDelight/schema 或 Android UI。
- 本批没有新增真机验证项；唯一真机清单保持为 `真机待验证清单_202608290905.md`，且任何既有待验证项仍是 `PENDING_DEVICE_VERIFICATION`。
- `feature_sync_check.py --backlog` 仍提示 `F-HEALTH` 与 `F-RECOMMEND` 的历史同步锚落后；它是文档治理待办，不是 R5-A 的功能阻断，后续按独立治理批次处理。

## 接手指引

不要重复实现 R5-A 或调整此 tie-break 的分层键。下一项按 SOL 执行顺序是 R2-B parser 规范化语料：先完成独立蓝图和权威语料/未知食材确认边界，再交 CODE；设备验证继续统一留至 R10。
