# 上下文快照：配色/今日卡/交互库审阅/B批开工（2026-07-16 下半场）

> 承接上一快照(膳食参考依据/MMR/评级核心)。本段全部已推送 master(到 fb246da)。

## 一、已落地功能（均构建+测试过、已推送）
1. **调料默认克数**(`SeasoningDefaults.defaultGramFor(name,isSeasoning)`)：盐3/酱油油10/糖5/粉末2/酱15，未命中调料8；**只对分类判定为调料(seasoningIngredientIds=调味品/油脂类)缩小**，普通食材(含油菜)仍100g。NewDishVM init 载 seasoningIds。含单测。修调料默认100g致钠算爆。
2. **数据来源页 + 我的「参考资料」组**：抽 `ReferenceScaffold` 复用；`DataSourceReference` 分类列食材分类/营养/GI/嘌呤/预设菜品来源+全部出处+免责+"用户上报(规划中)"预留位。参考资料组=食材营养表+膳食参考依据+数据来源(从实用工具拎出)。关于弹框数据来源改两链接。
3. **功能介绍页**(`FeatureGuideScreen`，我的·关于组)：Apple 欢迎/功能高亮范式(大标题hero+三章节卡+底部CTA)，三大块=①基础功能②营养膳食体系③AI能力+登录后规划。**由 Apple-UX agent 设计后重做**(原分组列表版否掉)。
4. **配色切换(7套)**：`AppPalette` 枚举(TERRACOTTA默认保留#DC6E3C + CITRUS/GARDEN/BLUEBERRY/MINT/TOMATO/GRAPE)。`Palettes.kt` 7套light/dark ColorScheme(Apple-UX 核定,向 iOS 系统色调校、鲜亮有活力、膳食意象)+代表色。`CookbookTheme(themeMode,palette)`、`PreferenceRepository.observe/setPalette`、MainActivity 读取即时重组、我的·外观·配色选择器(圆色块+选中打勾+"默认"标签)。ExtendedColors 加 macroProtein/Fat/Carb。
5. **今日卡(A-1)**：宏量条**渐变**(每段中心纯色→整条平滑过渡，非实块糊边；Brush.horizontalGradient)；统一"今日提示"区(gaps绿点+慢病concerns琥珀点`ext.warning`，无红报警+末尾免责，空则隐藏)；HomeVM 加 HealthProfileRepository 解析关注成员病种→`NutritionLevelEvaluator`(份额钠+热量)填 concerns。**色系墙保持纯结构不动(决策A)**。
6. **组合部分选**：选收藏组合展开列菜品+全选/部分选(`addComboDishes(blockId,combo,selectedIds)`)。

## 二、关键决策（用户拍板）
- **色系墙只看膳食结构、不关联热量/慢病**(延续旧决定)；热量=个人概念→只在今日卡；慢病提示(钠等食物属性)→个人视角(今日卡)。评估器热量+钠只服务个人卡。
- **营养级别评级口径**：GI 固有分级 / 钠糖脂按每日占比 / 嘌呤走定性清单(无国标数值)；缺数据退多样性。方案见 `营养级别评级方案.md`。
- **配色**：赤陶橘保留默认；柑橘橙与之明显不同故都留 → 7套。要"鲜艳明亮有活力"非莫兰迪灰。
- **UI 门禁**：界面/交互类操作**编码前先 Apple-UX 设计师过**(已写进 CLAUDE.md 体验准则强制门禁)。

## 三、交互模式库审阅(Apple-UX)——B批基准
- **§九 增补 9.12–9.17**(写进 `苹果风格UI设计方案.md`)：保存反馈Snackbar优先(Toast仅纯告知)/表单CTA与"保存并继续"/MiniStepper统一/大标题页判定(Tab落地→LargeTopAppBar,带返回→AppTopBar)/多操作收ActionSheet/未保存守卫封装。
- **`交互组件复用指南.md`**：组件对照表(需求→22组件里挑哪个)+B批逐项映射+4个待建复用组件。
- **4 个待建复用件**：✅`UnsavedGuard`(已建) / `AppSnackbarHost`(统一Snackbar宿主) / `EmptyState`+action槽 / `FormBottomBar`(保存/保存并继续)。
- 关键不一致：反馈体系乱(Toast/Snackbar混用)；删整天硬确认(HomeScreen:286/FoodTimelineScreen:200)违反§9.9须改软删+撤销；EmptyState无action槽。

## 四、B 批进度与顺序
- ✅ **B-1 餐食页未保存返回守卫**：`UnsavedGuard`(`rememberUnsavedGuard` 非包裹式返requestBack)+AddMealVM基线签名`isDirty`(loadConfiguredDate/copy 末 markBaseline)+AddDayFoodScreen 接。
- **待做顺序**(用户确认)：①`AppSnackbarHost`统一宿主(前置)②库存Tab就地加减份数(MiniStepper直接复用)③食历卡三图标收ActionSheet④删整天软删可撤销⑤食材"保存并继续"(FormBottomBar)⑥菜品页大标题+整行搜索⑦AppTopBar收敛16屏。每项先(设计确认)→复用组件→编码→构建测试。

## 五、待用户后续
- 装机验证：7套配色/今日卡渐变+慢病提示/功能介绍/数据来源/餐食页守卫。
- 待办补充项：AI设置云端模型配置指南+官网跳转、用户上报菜品(联网后)、首次启动引导浮层。
