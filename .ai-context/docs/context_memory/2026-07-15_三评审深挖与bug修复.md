# 上下文快照：三专家评审深挖 + 一批bug修复（2026-07-15 深夜，无人值守）

> 用户"睡觉了 88"授权全程无人值守。指令:深挖食材/菜品/餐食/餐次创建UX + 膳食算法优化 + Google架构审核 → 提交推送+保存上下文+总结经验+飞书。

## 一、方法
3 个专家 agent 深挖(算法/架构/创建流程UX)→ `temp/claude/{algo_review,arch_review,ux_create}.md`。主线程落地安全项、需拍板项入`待办总览.md` E节。前期还修了用户即时报的多个bug。

## 二、本轮已落地(均 commit+构建/单测过，已/待push)
**用户报的bug修复**:
- 凉皮0千卡:seedDishes"已存在预设菜即跳过"致配料关联永久残缺→改补齐式重挂(selectDishIngredientIdsByDishId)+ SEED_LOGIC_VERSION盐让老库自动跑。
- 随机推荐翻出忌口菜:rotate随机轮转到全忌口末批→轮转只在非忌口区(indexOfFirst avoidNames)取。
- 膳食统计"没吃"点了回不来:togglePresent读冻结的excluded.value→改FamilyRepository.toggleAbsent读实时DB翻转。
**即时UX**:库存页去冗余提示+空态改+采购清单引导、无食材保存提示、食材"存为菜品"、食历"今"悬浮键、主食三角"主"角标+营养行主食排前、无食材保存提示。
**高价值菜品**:编辑未保存"放弃更改"守卫(NewDishVM contentSig baseline+isDirty)、详情"记这道菜"CTA(onAddToMeal→addMealWithDishes)、首页计划(含今天)上移。
**算法安全项**:avoid 50→5(排末靠分层)、onHandMain/missing封顶min(,3)。
**架构安全项**:seed缺失项CookbookLog.w告警、deleteDay失败落日志、resolveGrams防负、Cookbook.sq版本注释。
**创建UX**:餐次块备注默认收起。

## 三、待拍板(详见待办总览E节)
- 算法:今日缺口+三支柱、MMR多样性、慢病数值软约束(钠/嘌呤/GI进打分)。
- 架构:补脆弱by-name单测、countChildren N+1、批量建食材、迁移链推演测、dish_ingredient UNIQUE待确认、大VM拆分。
- 创建UX:picker点击=直接勾选(Top1)、餐次块最近常吃chips(Top2)、长表单折叠、保存并继续、Snackbar统一、返回守卫扩展、配料按defaultUnit、自定义餐次类型。

## 四、约定
- 守健康免责红线(营养/GI/嘌呤非医嘱)、忌口保留+标红+排末。
- 无人值守:安全项直接做、高风险/涉健康语义/迁移入待确认队列;每项构建验证+独立commit。
- 收尾:上下文快照(本文)+/zongjie+push+飞书。
