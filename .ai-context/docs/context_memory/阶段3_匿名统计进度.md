# 阶段3 账号匿名统计（友盟）进度

> 一期运营看留存/活跃 = **匿名统计埋点·不登录账号**（完整账号留二期）。方案 `账号体系与用户统计方案.md` + `友盟SDK接入信息清单.md`。用户已拍板"先建抽象层·友盟留占位"。

## 已完成（push origin/master）
- **3-a 埋点抽象层地基**（`45c9707`）：`Analytics` 接口 + `AnalyticsEvent` 密封 8 事件（参数只允许枚举/布尔·**类型层杜绝健康明细越界**）+ `FeatureTag.fromRoute` 白名单（未知→null 不回传原始路由）+ `DefaultAnalytics` 同意闸门（未同意统一拦截）+ `LogSink` 本地实现 + `UmengAnalyticsSink` 友盟占位 + 匿名 UUID(expect/actual randomUuid) + DI + 启动读同意态。单测 4 项(闸门/切换/去标识化/白名单)。
- **3-b 8 事件接线**（`4d75529`）：app_open(MainActivity.onResume·30s会话去抖)/first_launch(同意后)/meal_logged(save·仅餐次)/recommend_requested+adopted(仅来源)/avoid_hit_shown(仅布尔·LaunchedEffect去重)/feature_used(路由白名单去抖)/health_profile_set(仅布尔)。Google审查健康红线通过。
- **3-c 首启合规门 + 政策页 + 我的开关**（编码完成·构建过·审查中）：
  - `PrivacyConsentDialog`（不可绕过·同意《用户协议》《隐私政策》·可选匿名统计复选框默认关·不同意二次挽留退出）
  - `PolicyScreen`(复用) + `PolicyContent`(用户协议/隐私政策分节·**含友盟合规披露标准模板**·联系 kvssxd@126.com/个人开发者 SXD)
  - `SettingSwitchRow`(公共开关行) + MineScreen"隐私与协议"组(用户协议/隐私政策/匿名统计开关)
  - MainScaffold 首启门优先于功能介绍；PreferenceKeys 加 `PRIVACY_AGREED`(删无用 ANALYTICS_CONSENT_SHOWN)
  - 政策文本源 `.ai-context/docs/feature/隐私政策与用户协议.md`（已填真实联系方式·建议法务复核）

## 待做
- **3-d 友盟 SDK 填充**：AppKey `6a5cd910cbfa6959517ccd4b`（用户已给·存 auto-memory `cookbook-umeng-appkey`·**别明文进 git**·走 local.properties+BuildConfig）。加依赖 `com.umeng.umsdk:common`+`asms`(钉版本·不加 uyumao)；`UmengAnalyticsSink` 填 `UMConfigure.preInit`(可 pre-consent) + **同意后才 UMConfigure.init**(首次 emit=已同意·懒初始化) + `MobclickAgent.onEventObject` + 关非必要采集；Koin 绑定 LogSink→UmengAnalyticsSink。真机验证上报(headless 只能构建验证)。

## 红线/口径
- 只上报 8 类去标识化事件（枚举/布尔），**绝不上报菜名/病种/身体数据/成员/日期明细**。
- 匿名统计**默认关**·首启询问式(复选框默认不勾)·"我的"随时关。
- 友盟合规三件套：延迟 init(同意后)·关非必要采集·隐私政策标准模板披露。
- AppKey 不进 git（local.properties gitignored）。
