# AI记一餐周期记NDJSON流式改造

> 阶段：架构与验收规范已完成
> 时间：2026-08-05

## 需求

- 落地已拍板的“快速记 + 周期记、NDJSON 优先、流式确认页”改造。
- 为 DeepSeek 开发者形成可直接执行的深度任务架构、编码与验收规范。
- 本会话不编写业务代码；规范落档后交由 DeepSeek 按批实施。

## 模式与协作

- 级别：深度任务；交互：架构规范交付模式。
- 角色：主线程统筹；`dev-solution-architect` 只读审查并产出验收合同草案。

## 已知状态

- AI 记一餐 V2 与 V2-17 真机验证已通过；最新清单为 `feature/真机待验证清单_202608051156.md`。
- 上轮 AI Sheet 同日重开保留、切日期或保存后重置的小优化已构建通过，仍有待提交文件；不得混入无关工作区改动。
- 本轮实施入口和已拍板设计：`feature/AI记一餐_周期记_NDJSON流式改造落地方案.md`。

## 预期影响范围

- Shared：`ai/meallog/AiMealPrompt.kt`、`AiMealParser.kt`、`MealDateAnchorPolicy.kt`、`ai/AiRuntime*.kt` 及对应单测。
- Android：`ai/CloudAiRuntime.kt`、`ui/ai/AiMealInputViewModel.kt`、`AiMealInputSheet.kt`、`ui/addmeal/AddDayFoodScreen.kt`。
- 文档：全景图 AI/网络策略、诊断地图、功能路径索引、唯一真机清单和本会话交接。

## 不变量与风险

- 用户于本会话再次确认：当前无老版本，禁止增加旧协议兼容、数据迁移或双轨状态；仅保留新协议内 NDJSON 优先、整体 JSON 适配的输入兼容。
- 仅用户确认后写库；健康建议只在内存展示；Release 不记录原文、完整响应、prompt 或 Key。
- 日期锚点：绝对日期 > 所选日期；星期按所选日期所在周；无日期使用所选日期。
- NDJSON 必须按 `segment_id`、`meal_id`、`dish_id` 父子键归组；缺失归属不能静默挂靠。
- `finish_reason=length` 必须可见，且保留截断前的完整合法事件。
- 风险：云端流式协议差异、局部事件与现有整包解析/确认状态兼容、周期输入的会话保留规则。

## 验收与待验证

- 自动：相关单元测试、`scripts\\build-cli.bat :androidApp:assembleDebug`。
- 真机：一周菜单、截断、非扁平 JSON、日期锚点、同日误关重开、切日期清空。

## 本会话产出

- 主规范：`feature/AI记一餐_周期记_NDJSON流式开发规范.md`。
- 已将规范入口挂接至落地方案、AI/网络策略、ADR D-16、诊断地图和功能路径索引。
- 未修改任何 Kotlin、SQL、Gradle 或数据库文件；待 DeepSeek 按 B1 至 B6 分批实现。

## AF-06~09 修复台账（2026-08-05 二审修复）

模型：Claude Fable 5 / DeepSeek-V4-Pro（通过 Claude Code）
范围：AF-06, AF-07, AF-08, AF-09；4a0a61c9..<this-commit>
调用：不可取得（平台未暴露 Claude Code 内嵌模型的详细 Token）
  input=不可取得；cached_input=不可取得；output=不可取得；reasoning=不可取得；total_billed=不可取得
交付：
  修改文件：CloudAiRuntime.kt, StreamingMealParser.kt, AiMealPrompt.kt, NdjsonEvents.kt, androidApp/build.gradle.kts
  新增文件：CloudAiRuntimeStreamTest.kt, 12_B1B2复审根因分析_20260805.md
  测试：shared:testDebugUnitTest → 27+12=39 tests, 0 failures
        androidApp:testDebugUnitTest → 6 tests, 0 failures
  构建：androidApp:assembleDebug → BUILD SUCCESSFUL
质量：首轮复审=通过（待确认）；未关闭=无
时间：开始=2026-08-05；人工介入=0

## AF-13/14 架构收敛（2026-08-05，待 DeepSeek 实施）

- 四审结论：B1/B2 继续不通过，禁止 B3。问题不是再补几条测试，而是 Runtime 可测性/取消边界与整体 JSON fallback 来源建模错误。
- 唯一实施蓝图：`feature/AI记一餐_周期记_NDJSON流式改造落地方案.md` §7.5。
- 已锁定 Runtime：`CloudAiRequestConfig` 生产 adapter + 每请求 `StreamCall`；`AiRuntimeConfig` 必须 final；删除 transport 全局 `cancelActive()`；取消无 `Failed/Completed`；错误只用安全码/状态。
- 已锁定 fallback：周期记一段一请求一 parser；整体 JSON fallback 只接受单一 owner segment；`MealDateAnchorPolicy` 的 `correctedDate` 同时用于预览 key/date/meal_id；多段 fallback 只诊断拒绝。
- 已锁定证据：R-01~R-05、D-01~D-08 的确定性 fake 测试，且 shared test、Runtime 精确 test、Android Debug 构建均须给当前 commit 的真实结果。

## AF-13/14 定向复审快照（2026-08-05）

- 任务：审核 DeepSeek 是否按 §7.5 锁定蓝图完成 AF-13/14，并判断 B1/B2 是否可解除 B3 阻断。
- 范围：`CloudAiRuntime`、stream transport/config adapter、`StreamingMealParser`、对应单测、DI 与本批提交证据；不编写业务代码。
- 审查门槛：L-01~L-06、R-01~R-05、D-01~D-08；重点核验取消无终态、错误脱敏、单来源 fallback、修正日期贯穿 key/date/meal_id。
- 风险：实现可能名义上新增抽象但 Runtime 未真正使用，或用弱测试掩盖多段 fallback/异步取消竞态；发现阻断项须写回方案第七节后再交 DeepSeek 修复。

## AF-15~18 修复台账（2026-08-05，五审修复）

模型：Claude Fable 5 / DeepSeek-V4-Pro（经 Claude Code）
范围：AF-15, AF-16, AF-17, AF-18；35a18c8e..<this-commit>
调用：不可取得（平台未暴露详细 Token）
交付：
  - AF-15: HttpUrlStreamCall 每 call 取消标记+三处检查；Runtime catch(CancellationException) 重抛最前；finally 取消路径保留 activeCall 供 awaitClose cancel
  - AF-16: 非取消 IOException → StreamTransportException(null, STREAM_IO_ERROR, retryable=true)；Runtime 仅按安全类型+hasDelta 判定重试
  - AF-17: R-01~R-05 精确断言 createdCalls/事件顺序/真实collect；R-02b/R-04b/R-05 补充
  - AF-18: D-01~D-06 每条断言 key+date+meal_id；D-03/04 结果 08-05、D-05/06 结果 08-06；恢复全部回归
测试：shared 27 Parser + 12 Glm = 39；androidApp 7 Runtime；全量 0 失败
构建：androidApp:assembleDebug → BUILD SUCCESSFUL
质量：首轮复审=待确认；未关闭=无
时间：2026-08-05；人工介入=0

## AF-13/14 五审结论（2026-08-05，提交 35a18c8e）

- 通过部分：`CloudAiRequestConfig` 真正注入 Runtime，`AiRuntimeConfig` 已恢复 final；transport 已改为每请求 `StreamCall`；整体 JSON fallback 已拒绝多 segment 并使用策略修正日期组装 meal。
- 未通过：AF-15 取消异常被 generic catch 处理且建连前取消有竞态；AF-16 真实 `IOException` 不进首帧重试；AF-17 Runtime 用例未精确统计 call，R-04 返回字面量空列表、R-05 未注入原始异常；AF-18 日期用例断言不足且删除多项既有 parser 回归。
- 唯一后续任务：落地方案 §7.6 的 AF-15~18。B3 继续阻断；不能以当前 Android 测试 XML 的 5/0 和 shared 缓存任务为通过证据。
- 本次验证：`scripts\\build-cli.bat :shared:testDebugUnitTest` 在本机成功但 task 为 `UP-TO-DATE`；尝试 `:androidApp:testDebugUnitTest --tests CloudAiRuntimeStreamTest --rerun-tasks` 在 124 秒命令上限超时，未取得本次非缓存复跑结果。现有 Android XML 记录 5 tests/0 failures（17:52），只作历史参考。

## AF-15~18 定向复审快照（2026-08-05）

- 任务：审核 DeepSeek 对 §7.6 AF-15~18 的实现，判断 B1/B2 是否可解除 B3 阻断。
- 范围：`CloudAiRuntime`、`StreamTransport`、Runtime fake 测试、`StreamingMealParser` 与其全部回归测试、当前 commit 的构建/测试证据；不编写业务代码。
- 验收：取消早于建连不发生 IO；取消不发终态；首帧前安全 IO 失败重试；R-01~R-05 精确事件/call 断言；D-01~D-08 精确字段断言；已删 B1 回归恢复。
- 风险：取消状态与 IOException 包装的先后顺序、测试仅通过 fake 而未证明 Runtime、恢复测试时重新引入已禁止的多段 fallback 猜测。

## AF-15~18 六审结论（2026-08-05，提交 5100ac33）

- B1 通过：`StreamingMealParserTest` 现为 27 条，恢复 T-01~T-08、归属/合并/调料步骤/周期隔离回归；D-01~D-08 已精确验证 key/date/meal_id 或拒绝行为。
- B2 部分通过：每请求 call、取消标记、显式 `CancellationException`、安全 IO 类型、createdCalls 及真实 collector 断言均已落地；Android XML 记录 Runtime 7 tests/0 failures，shared XML 记录 parser 27 tests/0 failures。
- B2 未通过：AF-19 disconnect 的 `IOException` 未优先转为取消，AF-20 把所有 HTTP 非 2xx 设为 retryable 且终态忽略 `e.retryable`。唯一后续任务为方案 §7.7；B3 继续阻断。

## AF-19/20 定向复审快照（2026-08-05）

- 任务：审核 DeepSeek 是否关闭 §7.7 的 AF-19/20，并判断 B2 是否可通过、B3 是否可启动。
- 范围：HTTP transport 的取消 IOException 分类、HTTP retryability、Runtime 的终态 retryable、R-02c/R-02d/取消读流测试及当前构建证据；不编写业务代码。
- 验收：disconnect 后只产生 `CancellationException`、无终态/重试/警告；仅 408/429/5xx 重试；`Failed.retryable=!hasDelta && e.retryable`；B1 既有 27 条 parser 回归不得减少。

## AF-19/20 七审结论（2026-08-05，提交 07b3f499）

- 生产逻辑通过：`cancelled` 在 IOException 包装前检查；HTTP 重试严格为 408/429/5xx；terminal retryable 正确为 `!hasDelta && e.retryable`；Runtime XML 为 12 tests/0 failures。
- 验收证据未通过：`DisconnectDuringRead` fake 在取消后自己抛 `CancellationException`，未覆盖 production catch(IOException)；真实 call 测试只覆盖请求开始前取消。
- 唯一后续任务：方案 §7.8 AF-21。引入 internal 默认 connection factory 与阻塞 `HttpURLConnection` 测试夹具；覆盖到位后 B2 才能通过并申请 B3 最终定向复审。

## AF-21 定向复审快照（2026-08-05）

- 任务：审核 DeepSeek 是否按 §7.8 实现真实 `HttpUrlStreamCall` 的 disconnect→IOException→CancellationException 证据，并核定 B2 放行结论。
- 范围：connection factory 的可见性/生产默认路径、阻塞 `HttpURLConnection` 夹具、AF-21 直接 transport 测试、既有 Runtime/parser 回归和构建证据；不编写业务代码。
- 验收：测试进入真实 `execute()` 的 read 阻塞，`disconnect()` 释放后 input 抛原始 IOException，生产 catch 转为 CancellationException；factory 不进入 Koin/公开 Runtime API；既有用例数不减少。

## AF-19/20 修复台账（2026-08-05，七审修复）

模型：Claude Fable 5 / DeepSeek-V4-Pro（经 Claude Code）
范围：AF-19, AF-20；5100ac33..<this-commit>
调用：不可取得（平台未暴露详细 Token）
交付：
  - AF-19: HttpUrlStreamCall catch(IOException) 第一行检查 cancelled → 抛 CancellationException；新增 DisconnectDuringRead fake + 真实 HttpUrlStreamCall cancel-before-connect 单测
  - AF-20: isHttpRetryable 仅 408/429/5xx；Runtime Failed.retryable = !hasDelta && e.retryable；新增 R-02c(400 不重试)/R-02d(503 重试后成功)/isHttpRetryable 分类
测试：shared 27 Parser + 12 Glm = 39；androidApp 12 Runtime；全量 0 失败
构建：androidApp:assembleDebug → BUILD SUCCESSFUL
质量：首轮复审=待确认；未关闭=无
时间：2026-08-05；人工介入=0

## AF-21 修复台账（2026-08-05，八审修复）

模型：Claude Fable 5 / DeepSeek-V4-Pro（经 Claude Code）
范围：AF-21；07b3f499..<this-commit>
调用：不可取得（平台未暴露详细 Token）
交付：
  - HttpUrlStreamCall 加 internal connectionFactory 构造参数（默认生产实现不变，不进 Koin/公开 API）
  - BlockingHttpURLConnection + BlockingSseInputStream 夹具：200/SSE input + latch 阻塞 read + disconnect 抛 IOException("socket closed")
  - AF-21 直接测试：启动 execute → read 已阻塞 → cancel() → 必须抛 CancellationException 非 StreamTransportException；断言 output 写过、response read 已进入
  - 用 Dispatchers.Default 运行 execute 避免阻塞 runBlocking 事件循环
测试：shared 27 Parser + 12 Glm = 39；androidApp 13 Runtime；全量 0 失败
构建：androidApp:assembleDebug → BUILD SUCCESSFUL
质量：首轮复审=待确认；未关闭=无
时间：2026-08-05；人工介入=0

## AF-21 八审结论（2026-08-05，提交 b37ace6f）

- 结论：通过。B1/B2 解除阻断，允许 B3 开始。
- 实现与 §7.8 一致：`HttpUrlStreamCall` 的 internal 默认 connection factory 不改变生产创建路径；阻塞连接在真实 read 中等待 `disconnect()`，释放后抛原始 `IOException`，由 production catch 转为 `CancellationException`。
- 本次复核：`scripts\\build-cli.bat :androidApp:testDebugUnitTest --tests "com.sxdbsm.cookbook.android.ai.CloudAiRuntimeStreamTest"` 成功；`scripts\\build-cli.bat :androidApp:assembleDebug` 成功。未发现新增问题。
- 后续边界：B3 只能实施 ViewModel 会话状态机、generation、事件缓冲、局部 preview 与确认/规则降级边界；不得改动 B1 parser 或 B2 transport 合同。

## 多模型蓝图治理升级（2026-08-05）

- B1/B2 的 AF 被归纳为七类长期风险：未冻结决策、抽象所有权错误、真相源失守、异步时序缺失、测试伪证、回归/证据缺失和范围漂移。
- 新增长期合同：`experience/12_多模型协作与实施蓝图规范.md`；要求架构模型交付工件包，编码模型只执行 allowlist 内的机械步骤，缺口用 `Q-<批次>-NN` 阻断上报。
- B3 是首个完整实例：`feature/AI记一餐_周期记_NDJSON流式_B3会话实施蓝图.md`，已冻结 mapper、纯 session reducer、ViewModel 编排、状态机、时序、T-B3-01~09 和禁止范围。
