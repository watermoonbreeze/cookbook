# Codex 用户级共享配置补齐

- 需求：以用户级 Claude 配置为参照，补齐 Codex 缺少的可迁移配置。
- 模式/级别：常规交互 / 深度；范围涉及用户级入口、技能、角色、提示词与 Hook 适配。
- 协作：主线程实施最小安全补齐；`dev-system-analyst` 独立列差异与迁移边界。
- 已知状态：共享源与 ai-share 均为 1.31.3；已知 Codex 缺 `skills/insight-add` 联接。
- 非范围：认证、token、会话/历史、缓存、IDE 状态、Claude 专属插件和未经适配验证的 Hook 行为。
- 风险/待验证：不可把 Claude 的事件载荷或插件配置直接复制到 Codex；仅建立可逆的共享目录联接或由现有脚本生成的 Codex 专属文件。

## 已实施与验证

- 新建 `~/.codex/skills/insight-add` Junction，目标为 `~/.ai-context/skills/insight-add`。
- 运行 `~/.ai-context/scripts/sync-codex-agents.ps1`，重生成 Codex 24 个 Agent TOML。
- 复核：Claude/Codex 的共享 Skill 均为 12 个、无名称差异；Codex 有 24 个 Agent TOML；`~/.codex/hooks.json` 为有效 JSON；5 个 Codex Prompt 与注册表一致。
- 未迁移：`~/.claude/commands/create-android-project.md` 是未注册的 Claude 专属命令，依赖 `~/.claude/template-registry.json` 与 Claude 语义；如需跨模型使用，应先抽为共享 Skill 后再创建 Codex Prompt，不能直接复制。
