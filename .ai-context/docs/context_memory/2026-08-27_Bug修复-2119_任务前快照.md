# Bug修复-2119 · 任务前上下文快照

- 需求：在家庭统计页将成员标记为当天“没吃”后，成员 Tab 必须与全家拆分使用同一当天在场分摊口径。
- 模式/级别：BLUEPRINT · 标准任务（当天持久化缺席状态跨 UI/统计分支，需要冻结单一数据源、补 ViewModel 回归并独立复核）。
- 基线：`ea4b5132`；Bug-3393 已 ARCH_ACCEPTED，真机 `DEV-3393-01` 保持 PENDING，不进入本批。
- Reality Verification：`FamilyStatsViewModel` 已把 `observeAbsenteeIds(today)` 纳入 stats combine，且全家 breakdown 正确排除缺席者；但成员 Tab 的 `share` 仍以全体 `ms` 系数求和，故缺席成员仍获摄入估算。
- 计划：冻结 L7 BLUEPRINT → 最小改动成员份额分支与真实 ViewModel 回归 → 自动化门禁/独立 REVIEW → 仅新增本批真机项（如需要）。
