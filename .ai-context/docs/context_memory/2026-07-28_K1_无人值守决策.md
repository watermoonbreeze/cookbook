# K1 AI快捷输入 · 无人值守决策记录

> 2026-07-28

## 决策清单

1. **depth升级**: 标准→深度（无人值守自动升级），原因：6新文件+4改造+DishRepo/IngredientRepo source参数扩展
2. **DishRepo.saveDish source参数**: 新增 `source: String = "user"` 默认参数，向后兼容，AI创建的菜品传 `source="ai"`
3. **IngredientRepo.createUserIngredient source参数**: 同上模式，默认 `source="user"`
4. **AiMealRecorder 营养推断暂缓**: 当前 MVP 只做食材/菜品自动创建（source="ai"），NutritionGuesser 链路预留但暂不写营养值——AI创建的菜品营养走"待完善"自然路径
5. **D4 追加合并实现**: 通过 loadDayMealsForEdit + mergeWithExisting + saveDayMeals(整日替换) 实现同餐次追加
6. **本地兜底正则优化**: 新增"中午/早上/晚上/下午"时间词剥离，提升兜底解析准确率
7. **构建验证**: shared 编译通过 + androidApp 编译通过 + 502 单测全绿（含 14 项 AiMealParser 新测）
8. **Google 代码审查**: 已触发，待结果

## 变更文件统计（≤15）

- 新增 8: Schema/Prompt/Parser/Recorder/VM/Sheet/Test/上下文快照
- 修改 4: DishRepo/IngredientRepo/SharedModule/AndroidModule/AddDayFoodScreen

## 未解决问题

- 语音输入（SpeechRecognizer 集成）：仅预留 UI 框架，Android SpeechRecognizer API 调用待后续实现
- NutritionGuesser 营养写入：后续优化
