# 上下文快照：搜索统一 + 从食材生成菜品 + 预设菜不可删（2026-07-16，进行中）

> 用户提 3 项、已确认方案。防 session 过长丢失。

## 一、#1 预设菜长按不可删（✅已做已提交）
- DishesScreen 长按 ActionSheet 的"删除"项：原无条件显示(预设点了才提示"无法删除")。改为 `if (d.source == "user")` 才加删除项。commit(fix dish)。

## 二、#2 搜索"点此新建"统一（进行中，用户确认"两按钮"方案）
- **菜品搜索无结果**(DishSearchOverlay，`没有匹配的菜品`)→ 改"未找到「keyword」，＋新建菜品"→预填菜名进新建菜品页。
- **首页搜索无结果**(SearchScreen，dishes+ingredients+meals 全空)→ 显"未找到「keyword」" + **两个按钮"＋新建菜品""＋新建食材"**(用户采纳:比"点此新建→弹框选"少一步)。新建菜品→预填名；新建食材→进食材页开新增表单预填名。
- 与食材搜索无结果"＋新建关键词"(已有)统一。

## 三、#3 从食材长按多选→组成菜品（进行中，确认复用选择器）
- 食材页(IngredientsScreen，查看模式 selectionMode=false)**长按食材 → 进多选态**：显勾选圈(§9.1) + 底部"已选 N · 组成菜品"栏，可继续多选。
- 点"组成菜品" → 跳**新建菜品页**、预填选中食材。本质="从食材出发生成菜品"(与"从菜品选食材"殊途同归，只是从食材出发多一个"组成菜品"按钮)。

## 四、实现机制（共享 enabler，进行中）
- 新建 `NewDishPrefillBus`(单例总线，类比 IngredientJumpBus)：`NewDishPrefill(name, ingredients: List<Ingredient>)` + request/consume。已建文件 `ui/newdish/NewDishPrefillBus.kt`。
- **待做**：①DI 注册 bus ②`NewDishViewModel.applyPrefill(name, ingredients)`(set name + addIngredient 每项 + 重置 baseline 免立即 dirty) ③NewDishScreen 进入消费 bus ④三处调用点：菜品搜索无结果、首页搜索无结果(菜品/食材)、食材页多选"组成菜品" ⑤食材页长按进多选(IngredientsScreen 加本地 multiSelect 态传给 IngredientPickerScreen，或 picker 支持"浏览态长按转多选")。
- NewDishVM 有 ingredientRepo/dishRepo；预填食材直接用选择器已加载的 Ingredient 对象(免加 by-ids 查询)。
- 设计规范见 `feature/苹果风格UI设计方案.md §九`(勾选圈/搜索即建/详情等)。

## 五、约定
- 苹果设计原则全程守；预设不可删/编辑、自建可；搜索统一；少操作。
