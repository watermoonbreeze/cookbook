# 2026-06-04 菜品编辑成功但界面空专项上下文

- 用户反馈：Toast 能显示“正在编辑：酸辣土豆丝家常菜 ID=7”，但界面表单实际仍为空。
- 判断：仓库查询和路由 id 至少曾经成功；剩余风险是加载成功后的 state 被重复 start/reset、导入/初始化覆盖，或 UI 消费到了空快照。
- 本轮策略：增加已加载编辑 id 门禁，只有目标 id 变化才允许重置表单；增加 UI 状态快照日志，确认表单字段是否真的进入 Compose。
