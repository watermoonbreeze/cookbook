# 2026-06-04 修复13 结果

- 菜品 Item 长按菜单已调整：去掉“取消”，保留弹框外部关闭；新增“编辑 / 基于此另存 / 删除”。
- 编辑操作：`DishesScreen` -> `MainScaffold.onEditDish` -> `Routes.newDish(id)`。
- 删除操作：`DishesViewModel.requestDeleteDish()` 先调用 `DishRepository.listMealReferencesByDish()` 查询餐食引用。
- 若有引用：弹框显示“被以下餐食引用”，列出日期、餐次、时间，当前不直接删除。
- 若无餐食引用：弹二次确认；确认后 `DishRepository.deleteDish()` 先清 `favorite_combo_dish` 再删 `dish`。
- 新增日志 tag：`DishActions`，覆盖删除请求、引用检查、确认删除、删除失败/成功。
- SQL 新增：`selectMealReferencesByDish`、`deleteFavoriteComboDishesByDish`。
- 验证通过：SQLDelight interface、shared/android Kotlin compile、migration verify、assembleDebug、testDebugUnitTest。
- 说明：清空某天餐食当前是 `MealRecordRepository.saveDayMeals()` 的整日替换逻辑触发，删除当天旧 meal_record_dish/meal_record 后按当前草稿重新插入；若草稿为空即清空当天。
- 说明：SAF 是 Android Storage Access Framework，适合让用户选择/授权一个目录；app 专属目录是 `/sdcard/Android/data/<package>/...`，卸载会清，访问限制更多。当前项目目标是 `/sdcard/cookbook`，后续若考虑上架合规，应评估迁到 SAF 或 app 专属目录。
