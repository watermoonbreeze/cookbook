# 无人值守：库存推荐/菜系/自由搭配 + 5轮审查（2026-07-10 第4次无人值守）

用户交互提多点后进入无人值守：先修库存推荐 bug，再做菜系(独立字段)、自由搭配(离线规则)，含周期菜名颜色、AI推荐来源标注两个细节；全做完后整体回归+5轮审核→上下文+经验+push+飞书。

## 交付（[unattended] 提交）
- **库存推荐"物尽其用"** `fc47dc7`：`HealthRuleEngine` 放宽为"用到≥1个在手非调料食材即推荐"(调料常备不算)，缺的主料/辅料全列出(missingNames)、用到的高亮(onHandNames)、齐备菜排前。修用户"库存有食材却不推荐/凑不齐就没了"。
- **周期菜名加重 + AI推荐来源标注** `340faec`：AiPlan 菜名 onSurface+Medium(原淡色看不清)；AiRecommend 标题下标"推荐来源：云端AI模型/本地模型/离线规则"(按 activeType+source, 兜底如实标)。
- **菜品菜系(独立字段)** `eb0580f`：`dish.cuisine` 列 + **14.sqm 迁移** + CREATE 同步；insertDish/updateDish/backfill 查询；Dish/DishMini/全链路透传；`Cuisines` 常量(八大菜系+家常菜)；dishes.json 51道保守归类(家常41/川4/粤5/鲁1)；seedDishes 写入+存量幂等补齐；详情展示+列表菜系筛选(viewOpts 扩4路 ViewOpts)+新建/编辑单选chip+关于页来源(守禁编造权威红线)。
- **食材自由搭配(离线规则)** `296ef99`：`FreePairingEngine`(纯逻辑)按类别(荤/素/蛋/豆/主食)+在手调味拼"组合+做法"(荤×素/蛋×素/豆×素/纯素兜底)；`RecommendationDataSource.freePairing/classifyPairRole`(按 general 大类分类)；我的>实用工具>食材自由搭配页。不依赖已有菜品/AI。
- **审查修复** `94f6258`+`9e05698`：见下。

## 5 轮审查（6 代理）
- R1 基线 clean 回归绿。
- R2 3代理(菜系迁移/推荐逻辑/Compose集成)：菜系迁移+DI+集成全通过；**修自由搭配3处**：①鲜豆类蔬菜(毛豆/四季豆)误判BEAN→蔬菜优先+BEAN只认大豆/豆制品/坚果 ②料理包因"预制菜类"含"菜"误判VEGETABLE→改"蔬/菌/藻" ③`FreePairingEngine` hint 内插了 PairIngredient 对象→`${x.name}`(单测未覆盖hint故漏)。
- R3 对抗验证 classify：ingredients.json 每食材同绑**顶层大类+叶子 code**，故"蔬/菌/藻"命中所有蔬菜，零漏判成立(仅土豆/山药无顶层蔬菜code→有意归主食)。
- R4 sweep：全闭环/无死代码/注释规范/红线合规；加端到端守护测试(RecommendationDataSourceTest)防seed演进静默退化。
- R5 clean 全量回归 SUCCESSFUL + **97 单测全绿**。
- 误入的临时扫描文件(seed/*.txt)已 `78fa273` 移除。

## 关键事实
- 迁移文件 max=13→新增 **14.sqm**(ALTER ADD cuisine, 新列安全)；CREATE TABLE 也加 cuisine(全新装 Schema.create)；列序两路径一致。红线：14.sqm 迁移脚本单测不覆盖(Schema.create 不跑迁移)，**建议真机做一次旧库升级冒烟**。
- classifyPairRole 不变量：seed 每蔬菜须绑顶层 vegetable/fungi_algae code，否则漏判 OTHER(已注释+守护测试)。
- 菜系数据为便于筛选的参考、可能地域交叉，非权威认定(关于页已标)。

## 待你验证/后续
- 真机：菜系迁移旧库升级冒烟、库存推荐物尽其用效果、自由搭配结果、周期菜名颜色。
- 自由搭配二期(AI 生成新菜谱)未做；土豆/山药归主食(非素)属分类取舍。
