# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「查看session继续/会话继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-20（无人值守续接·成员编辑页全屏化 + 成员化红绿灯Phase1 + 病种切换解读视角 三交付）**。全部已 push origin/master(最新 `fe1f3aa`)、shared单测+androidApp BUILD SUCCESSFUL、工作区仅 temp/claude + local.properties(gitignored·含友盟AppKey)。**用户在睡·授权持续无人值守把 backlog 全做完(每功能走全套门禁+push)**。

## 一、先按序读（进入状态）
1. **本文件** + `context_memory/阶段3_匿名统计进度.md` + `projectReview/07_项目现状.md`(本会话交付+真机待验)。
2. `feature/待办总览.md`(**G区=用户review追加**：忌口✅/编辑页全屏化⬜)+`一期收官路线图与决策清单.md`(阶段1-7)+`unattended_decisions.md`(忌口加法决策)。
3. `CLAUDE.md`+`.ai-context/rules/通用规则.md`(三门禁/踩坑红线/健康免责)。
4. 下一步相关方案：`苹果风格UI设计方案.md §9.26`(家庭编辑页全屏3段·待做)、`成员化红绿灯`(商业#1·待做)、`病种切换视角`(商业#3·待做)。

## 二、工作规则（用户已定·稳定·重要）
- 中文；**无人值守·全权推进·凡需拍板用推荐方案·快速全做完·不反复问确认**。真正要停：指令模糊、触红线。
- **别过度设计**：明确指令按最简实现。**先 Explore 摸现状再动**(忌口就因摸清"现状已是下厨房式"避免了盲目破坏性迁移)。
- **每功能完成走全套**：门禁(Apple-UX/copywriter/Google审查·阻断必修)→**保存上下文**→**总结经验**→**同步项目说明书(projectReview/07)**→**飞书(4段)**→commit(`[unattended]`)→**push**→下一个。(用户2026-07-19强调补齐全套)
- **🔴构建验证看输出别信exit code**：`build-cli.bat <task> *> log; grep "BUILD SUCCESSFUL"`，别用 `|tail`。
- **健康免责红线**：仅供参考·非医嘱；忌口菜绝不进推荐/一餐；匿名统计绝不上报健康明细。
- 临时文件 `temp/claude/`；**只交付 Android**。**友盟 AppKey 走 local.properties 不进 git**。

## 三、当前状态（本会话·全 push·master 编译通过）
- ✅ **首页"下一餐卡"v2·轻量单菜引流**(`05aa5ef`)：会商+用户拍板→只推1道+一句人话·整卡点AI全页·换一道·去首页记/详情·疑问式标题去歧义。
- ✅ **阶段3 账号匿名统计(友盟)全链路**：3-a抽象层(`45c9707`·8事件类型层杜绝越界+同意闸门)→3-b 8事件接线(`4d75529`)→3-c首启合规门+政策页+我的开关(`de286b0`)→3-d友盟填充(`362c8f2`·同意后init·AppKey不进git)。**AppKey已配(local.properties)**。全套文档齐。
- ✅ **忌口·具体食材(加法)**(`ac75efb`/`ecd8cf5`)：现状已是下厨房式子类chip→加法(新表member_avoid_ingredient·不破坏迁移)。数据层+编辑页"搜索添加具体食材"(复用IngredientPicker overlay)。健康红线保持+单测。

## 四、⏭ 下一步（无人值守全权推进·每步走全套+读输出验证构建）
1. ✅ **家庭编辑页全屏化+3段分段**(2026-07-20 `a96db88` 已交付)：AlertDialog→全屏MemberEditorScreen+SegmentedControl(资料/健康/忌口·N)+FormBottomBar保存+rememberUnsavedGuard。过Apple-UX门禁+Google审查(修1真数据丢失bug)。范式§9.24。**真机待验**(见下)。
2. 🔨 **阶段4 护城河·成员化红绿灯**(商业#1)：**Phase1 详情页✅已交付**(`ddf9c4b`·§9.25·"适合谁吃"逐家人红黄绿·过Apple-UX+Google门禁)。**Phase2 待做**:记菜/选菜列表逐项徽章(需缓存化)、详情全家并集avoidNames并入个人忌口(单成员场景)。
3. ✅ **阶段4 护城河·病种切换解读视角**(商业#3·`fe1f3aa`·§9.26 已交付)：详情页"关注指标"块按病种显钠/GI/嘌呤/饱脂胆固醇。过 Apple-UX+Google+copywriter 三门禁。嘌呤GI守定性。真机待验。
4. 🔜 **下一步候选**(无人值守自选推进)：商业#7 GI/嘌呤/钠三级筛选导航(S-M) / #6 人群适配红绿灯(M·可协同#1) / #4 采购清单闭环 / 中式库扩充(#5·数据) / 阶段5-7。
5. 阶段5 中式库/营养补全+药膳一期+热量国标DRIs → 阶段6 AI框架(放开限制会诊+AI对话) → 阶段7 其余P1。

> **真机待验(攒一起·装新包)**：①**首页单菜引流卡**(点整卡进AI全页/换一道/示例态/空库) ②**首启隐私合规门**(不可绕过/门内读协议/不同意退出/勾匿名统计)→我的·隐私与协议三项 ③**友盟上报**(同意后用一会→友盟后台看板有数据·关开关停·**需网络**) ④**忌口具体食材**(搜食材加忌口→该菜标红不进推荐) ⑤**成员编辑页全屏三段**(资料/健康/忌口切换·滚动保活·未保存返回守卫·忌口/健康徽标计数·异步查名窗口内点保存不清空既有忌口食材) ⑥**成员化红绿灯详情页**(≥2成员的菜品详情"适合谁吃":逐家人红黄绿差异/全绿一行/排序红→黄→绿/替代全家并集适宜性行) ⑦**病种视角详情页**(有登记病种的菜品详情"关注指标"块:钠/GI/嘌呤/饱脂胆固醇按病种显·数值+占每日上限%/定性·1-2平铺·≥3切换器·缺数据"暂无数据") + 历史批(餐次分类/多人关注等)。
> 接手：工作区干净、全push；**下一步无人值守自选(商业#7三级筛选 / #6人群适配 / 中式库扩充 等)**。构建务必读输出确认 BUILD SUCCESSFUL。**待用户补**：隐私政策法务复核、友盟依赖上架前钉版本。
