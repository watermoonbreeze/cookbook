# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「查看session继续/会话继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-19（超长会话·一期规划全拍板 + UX打磨批 #1#2 完成·无人值守推进中）**。已 push 到 origin/master(最新 `fc3b45e`)、工作区干净(仅 temp/claude)。

## 一、先按序读（进入状态）
1. **本文件** + `context_memory/UX打磨批进度.md`（**当前打磨批 #1-#4 进度·最重要**）+ `feature/一期收官路线图与决策清单.md`（一期阶段1-7+全部拍板决策）。
2. `context_memory/待拍板队列.md`（已定项记录）+ `CLAUDE.md` + `.ai-context/rules/通用规则.md`（三门禁/踩坑红线/健康免责）。
3. `feature/待办总览.md`（backlog·F区=商业汲取项+两新需求）。
4. `苹果风格UI设计方案.md §九`、`交互组件复用指南.md`（做 UI 前必读）。
5. **打磨批剩余方案**：`AI推荐页界面精简方案.md`(#3·方案A)、换一换缓存策略方案.md(阶段1)。

## 二、工作规则（用户已定·稳定）
- 中文；**无人值守·全权推进·凡需拍板用我推荐方案**（用户2026-07-19授权：睡前说"有需要我确定的都按你推荐的方案来继续推进"）。
- **每个功能完成走全套**（用户2026-07-19强调）：门禁+会诊 → 编码 → **强测试(正+逆+边界+异常)** → Google审查(阻断必修) → **保存上下文** → **总结经验** → **更新项目说明书(projectReview)** → **飞书(4段)** → commit(`[unattended]`) → **push** → 下一个。
- **飞书4段**：①做了什么 ②还有哪些 ③接下来做 ④要验证的(真机清单)。
- **提交纪律**：commit 前 `git status --short` 核对全部改动都 add;LF/CRLF warning 正常。
- **三门禁**：①UI改动先 apple_ux_designer ②每批后 google_quality_engineer ③文案多用 copywriter。小改/复用§九可免。
- **健康免责红线**：仅供参考·非医嘱、禁编造出处/医疗断言、无国标标"惯例"、慢病评级物理隔离。
- **对外文案温和化**：不显性标"三高痛风"病名，用"健康管理/慢病家庭"。
- 临时文件 `temp/claude/`。**只交付 Android**。

## 三、当前状态（本会话·已 push）
### A. 一期规划全拍板（`7b300c0`/`cf65be0`）
出 3 方案(首页推荐下一餐/账号体系/一期收官路线图)+商业汲取13项。**用户拍板**：①开发顺序阶段1→6 ②账号一期只做**匿名统计·友盟**(不逼登录·完整账号留二期) ③护城河项(成员红绿灯+病种视角)本期做 ④**AI框架一期搭好·用户侧默认关·供测试**(放开限制先会诊出方案) ⑤孕期/哺乳(care_pregnancy/lactation)不评热量 ⑥热量+活动档对照中国DRIs2023(联网核准不编造) ⑦RANDOM崩溃必修 ⑧拍照/扫码系→二期(涉三方SDK+成本)。详见 `一期收官路线图与决策清单.md`。
### B. UX打磨批(用户review期边用边反馈·详见 `UX打磨批进度.md`)
- ✅ **GLM-4.5-Flash超时修复**(`3fd18f0`)：关默认思考+max_tokens+超时45s。
- ✅ **加餐日期提示bug**(`5db9d4e`)：setDate 重选当前空日期清残留提示。
- ✅ **#1 菜品页餐次统称"加餐"**(`aecfe8c`)：上午/下午合并成"加餐"chip·仅菜品页·`DishSlotFilter`·零迁移不碰记一餐。
- ✅ **#2 搜索按分类**(`fc3b45e`)：菜品搜菜系(排"其他")/餐次、食材搜类目(dimension≠care)→按分类筛模式(淡色头部+不显新建行)。
- 均过构建+Google审查(无阻断)+经验+说明书+飞书。

## 四、⏭ 下一步（无人值守全权推进·每步走全套流程）
1. **#3 AI推荐页界面精简(方案A·用户已选·下一个)**：拆 `AiRecommendScreen.RecommendControls`→模式(库存/随机/周期计划)留固定层实心分段控件+**餐次常驻二级横滚栏(样式要和一级模式栏视觉区分·轻)**+去重周期/风格/药膳收进新 `RecommendFilterSheet`(ModalBottomSheet·切换即时)+结果头行「筛选」入口(有非默认筛选=小圆点)。**用户补充**:三模式(库存/随机/周期计划)控件外壳统一。方案 `AI推荐页界面精简方案.md`。需 Apple-UX 已satisfied(方案即产出)+Google审查。
2. **#4 菜品页餐次升为主分类**：餐次要和菜系并列成主分类(现餐次是全局二级栏、菜系Tab左侧栏让它看着像嵌套)·先 Apple-UX 出布局。
3. **打磨批清完接主线路线图**：阶段1 换一换第二批(会诊已修正·**忌口constraints每次重取红线**·关思考已提前做`3fd18f0`) → 阶段2 首页推荐下一餐 → 阶段3 账号匿名统计(友盟·客户端埋点抽象层·后端待用户服务器) → 阶段4 成员化红绿灯+病种切换视角 → 阶段5 中式库/营养补全+药膳一期+热量国标DRIs对照 → 阶段6 AI框架(放开限制先会诊+AI对话·默认关) → 阶段7 其余P1。

> **真机待验(攒着一起)**：菜品页餐次栏"全部/早餐/中餐/加餐/晚餐/宵夜"·点加餐出上午/下午菜·搜"家常菜"/"蔬菜类"等出该分类全部·记一餐仍上午/下午分开·glm-4.5-flash不超时·加餐日期提示随有无餐食显隐 + 历史批(餐次分类/个人忌口/多人关注/药膳重排等)。
> 接手：工作区干净、全 push;**从「#3 AI推荐页界面精简」接**（先读 `AI推荐页界面精简方案.md`）。
</content>
