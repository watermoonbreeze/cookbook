# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「查看session继续/会话继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-19（超长无人值守会话·一期规划全拍板 + UX打磨批4项 + 换一换缓存子步骤1 + 首页推荐下一餐 + 多个修复）**。全部已 push origin/master(最新 `d389c1a`)、androidApp BUILD SUCCESSFUL、工作区干净(仅 temp/claude)。

## 一、先按序读（进入状态）
1. **本文件** + `context_memory/UX打磨批进度.md`（打磨批#1-4进度）+ `feature/一期收官路线图与决策清单.md`（一期阶段1-7 + 全部拍板决策）。
2. `context_memory/待拍板队列.md`（已定项）+ `CLAUDE.md` + `.ai-context/rules/通用规则.md`（三门禁/踩坑红线/健康免责）。
3. `feature/待办总览.md`（backlog·F区=商业汲取项+新需求）。
4. `苹果风格UI设计方案.md §九`、`交互组件复用指南.md`（做 UI 前必读）。
5. **下一步相关方案**：`换一换缓存策略方案.md`(子步骤2·下一个候选)、`首页推荐下一餐方案.md`、`账号体系与用户统计方案.md`、`菜品页餐次升主分类方案.md`(方案A·未采用·用户要的是加一级Tab已做)。

## 二、工作规则（用户已定·稳定·**重要**）
- 中文；**无人值守·全权推进·凡需拍板用我推荐方案·快速全做完·不反复问确认**（用户2026-07-19强调，见 auto memory `cookbook-act-decisively-no-overconfirm`）。真正要停的只有：指令模糊无法解析、触红线(忌口/健康/破坏性)。
- **别过度设计**：给明确指令按**最简实现**，别扩成大重构（曾把"加个餐次Tab"做成导航重构方案被批）。
- **每功能完成走全套**：门禁+会诊 → 编码 → 强测试 → Google审查(阻断必修) → 存上下文 → 经验 → **更新项目说明书(projectReview)** → **飞书(4段)** → commit(`[unattended]`) → **push** → 下一个。
- **🔴构建验证看输出别信 exit code**（本会话踩坑·auto memory `cookbook-build-verify-not-exitcode`）：`build-cli.bat <task> > log 2>&1; grep "BUILD SUCCESSFUL|BUILD FAILED" log`，**别用 `|tail`**(管道 exit code 是 tail 的·掩盖 gradle 失败·曾致 #4 编译回归被掩盖连提几个坏commit)。改公共枚举先 grep 全仓穷尽 when 补分支。
- **三门禁**：①UI改动先 apple_ux_designer ②每批后 google_quality_engineer ③文案多用 copywriter。小改/复用§九可免。
- **健康免责红线**：仅供参考·非医嘱、禁编造出处/医疗断言、无国标标"惯例"、慢病评级物理隔离。**忌口红线**:忌口菜绝不能被推进一餐。
- 临时文件 `temp/claude/`。**只交付 Android**。

## 三、当前状态（本会话·全 push·master 编译通过）
### A. 一期规划全拍板（`7b300c0`/`cf65be0`·详见 `一期收官路线图与决策清单.md`）
3方案+商业汲取13项。**用户拍板**:①开发顺序阶段1→6 ②账号一期只做**匿名统计·友盟**(不逼登录·完整账号留二期) ③护城河项(成员红绿灯+病种视角)本期做 ④**AI框架一期搭好·用户侧默认关·供测试**(放开限制先会诊) ⑤孕期(care_pregnancy)/哺乳(care_lactation)不评热量 ⑥热量+活动档对照中国DRIs2023(联网核准不编造) ⑦RANDOM崩溃必修 ⑧拍照/扫码系→二期。
### B. UX打磨批(全4项完成·`UX打磨批进度.md`)
- ✅ GLM-4.5超时修复(`3fd18f0`·关思考+max_tokens) · 加餐日期提示bug(`5db9d4e`)
- ✅ #1 菜品页餐次统称"加餐"(`aecfe8c`·上午/下午合并·仅菜品页·DishSlotFilter·零迁移)
- ✅ #2 搜索按分类(`fc3b45e`·菜品搜菜系/餐次·食材搜类目→出该分类全部)
- ✅ #3 AI推荐页界面精简(`1ed6679`·方案A·餐次留栏+去重/风格/药膳收进筛选弹层)
- ✅ #4 菜品页餐次升为一级Tab(`76300e7`·最近/喜爱/菜系/餐次/家庭·选餐次档左栏列餐次分堆) + 移除选菜页重复"最近常吃/喜爱"(`0e3801c`)
### C. 主线路线图进行
- ✅ **阶段1 换一换第二批·子步骤1**(`f1b4dec`)：gather候选缓存(换一换省18-22 SQL)·**忌口约束每次 gatherConstraints() 重取覆盖**(红线·健康档案别页可改)·红线单测过。**子步骤2(均衡档预取多批)未做**。
- ✅ **阶段2 首页推荐下一餐**(`d389c1a`)：首页入口卡→NextMealCard(3道具体菜·按钟点·**纯规则不调云端**·整行详情+行尾记+换一批本地轮播+看更多+示例态+ON_RESUME刷新忌口即时)。**顺带修 #4 DishPicker编译回归**(恢复 master 编译)。

## 四、⏭ 下一步（无人值守全权推进·每步走全套+读输出验证构建）
1. **阶段3 账号匿名统计(友盟)**：客户端**内部埋点抽象层**(`Analytics`/`Tracker`接口·只暴露§2.1八事件·杜绝健康明细越界·SDK可换)+友盟U-App接入(同意后init+关多余采集+隐私披露)+"关闭统计"开关。后端待用户服务器(暂用友盟看板)。方案 `账号体系与用户统计方案.md`。
2. **阶段4 护城河**：成员化红绿灯(记菜/选菜/详情"这道菜对张三绿灯李四黄灯")+病种切换解读视角(糖尿病看GI/痛风看嘌呤/高血压看钠)。需 Apple-UX。
3. **换一换子步骤2(优化项·可穿插)**：关思考已做(`3fd18f0`)·剩 独立预取校验(take(N)+组间去重+本地补足)+均衡档预取多批+两视图联动+RANDOM分叉+降级链。较复杂动AI输出结构。
4. 阶段5 中式库/营养补全+药膳一期(6拍板点·方案已出)+热量国标DRIs对照 → 阶段6 AI框架(放开限制先会诊+AI对话) → 阶段7 其余P1(条码/采购闭环/人群适配/GI筛选导航)。

> **真机待验(攒着一起·装新包)**：①菜品页5Tab(含餐次·点餐次档左栏出全部/早/中/加/晚/宵·选出对应菜) ②搜"家常菜"/"蔬菜类"出该分类全部·记一餐仍上午/下午分开 ③AI推荐页精简(筛选弹层/换一批/重置) ④glm-4.5-flash不超时 ⑤加餐日期提示随有无餐食显隐 ⑥选菜页无重复"最近常吃/喜爱" ⑦**首页"下一餐"卡**(3菜/点详情/点记带菜/换一批/看更多/无库存示例/加忌口后不出忌口菜) ⑧换一换连点更快·加忌口后换一换避开该菜 + 历史批(餐次分类/个人忌口/多人关注等)。
> 接手：工作区干净、全 push;**从「阶段3 账号匿名统计」接**（先读 `账号体系与用户统计方案.md`），或穿插「换一换子步骤2」/「阶段4 护城河」。构建务必读输出确认 BUILD SUCCESSFUL。
</content>
