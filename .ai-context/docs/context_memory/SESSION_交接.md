# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「查看session继续/会话继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-20 交接（无人值守·超长会话·~34 commit·全部已 push origin/master）**。shared 单测 + androidApp BUILD SUCCESSFUL 全绿；工作区仅 temp/claude。**用户授权持续无人值守·不间断推进**。

## 一、先按序读（进入状态）
1. **本文件** + `projectReview/07_项目现状.md`（能力全景+真机待验）。
2. `feature/待办总览.md`（各功能状态·G区=用户 review 追加）+ `苹果风格UI设计方案.md §九`（**新增 §9.28 食材人群适配红绿灯 / §9.29 来源徽标显隐 / §9.30 选择编辑界面统一**）。
3. `CLAUDE.md` + `.ai-context/rules/通用规则.md`（三门禁/踩坑红线/健康免责）。
4. **经验新篇** `experience/10_云端AI校验本地规则方法论.md`（重要·见 §二规则5）。

## 二、工作规则（用户已定·稳定·重要）
1. 中文；**无人值守·全权推进·凡需拍板用推荐方案·快速全做完·不反复问确认·不间断**。真正要停：指令模糊、触红线。**先 Explore/读代码摸现状再动**·别过度设计·**动手前验证别改已正确的**。
2. **每功能走全套**：门禁（Apple-UX 界面 / copywriter 健康文案 / Google 代码审查·阻断必修复验）→保存上下文→经验→项目说明书(07)→待办→飞书(4段)→commit(`[unattended]`)→push。
3. **🔴构建看输出别信 exit code**：`scripts\build-cli.bat <task> *> log; Select-String "BUILD SUCCESSFUL"`。
4. **长跑后台 agent 必须"分批即写即存+断点续连"**（编号文件·防API断连丢全部）；**现成工具优先**（能用 Workflow 原生续连就别手工摆批次）。见 memory `agent-batch-checkpoint-strategy`+全局 CLAUDE.md。
5. **做本地规则/算法前先用云端AI(glm-4.5-flash)互相生成比较**，让规则贴合实际+挖库缺口（持续迭代非一次性）；🔴**营养数值不采信AI(会幻觉)必联网核准·AI补的标"估算·待核"用户确认才入库·别过拟合AI**。方法论见 `experience/10`。GLM key 用户在手机端配·**key 只走 header 绝不进 git/日志**。
6. **健康免责红线**：仅供参考·非医嘱；物理隔离(慢病维度不接色系墙)；嘌呤/GI只定性不给占比·钠嘌呤惯例阈值标"非国标"；忌口菜绝不进推荐/一餐；数据不编造(联网核准·ref+review·加工食品标pending)。
7. 临时文件 `temp/claude/`；**只交付 Android**。

## 三、当前状态（本会话交付全景·~34 commit·全 push·master 绿）
**商业护城河/功能**：
- ✅ #6 食材人群适配红绿灯(§9.28)｜✅ #2 来源徽标显隐(§9.29)｜🔨 #1 选择编辑界面统一(§9.30·P0/选菜完成下移已做·剩编辑页保存下移等P2需真机)
- ✅ #3 AI推荐更贴合日常：QW-1离线走"一餐组合"+QW-3修早餐误配+**中西不混搭+中式优先**(`b09c6a3`·用GLM对比验证方向)
- 🔄 #5 库扩充：西式/速食/早餐/地方+常见家常菜→**658菜/495食材**；营养补全**胆固醇→98%**(喂高血脂红绿灯)
- ✅ DRIs国标热量参照(WS/T578.1-2017·家庭档案并列Mifflin)｜✅ AI对话前置基建(AiRuntime.chat多轮`8869950`)
- ✅ UX走查headless批(数字输入"30."不丢值/文案一致)｜✅ 我的页排序优化｜✅ 拼音字母条放开全分类
**规则/方法论**：长跑agent分批续连(全局)｜云端AI×本地规则互校方法论(experience/10)
**待建(已记待办+我的意见)**：AI营养补全(自建缺值云端补·我的-AI助手)｜端侧免费OCR拍营养成分表(一期零成本+速度disclaimer)｜库缺~50道日常菜(GLM挖掘·`temp/claude/glm_missing_common.txt`)

## 四、⏭ 下一步（无人值守自选·每步走全套+读输出验证）
1. 🔜 **#5 补GLM挖出的~50道日常菜**（headless·用现有食材组·营养已覆盖·`glm_missing_common.txt`）。
2. 🔜 **AI营养补全功能**（自建缺值云端AI联网补·我的-AI助手·模拟营养表页·标估算待核·前置基建已就绪·需真机）。
3. 🔜 **#1 界面统一 P2**（编辑菜品保存下移底部/必填降级·触建菜核心保存链路·**需真机全回归**）。
4. 🔜 **AI对话生成MVP**（用户拍板一期·前置基建已就绪·对话UI需真机·XL独立批）。
5. 🔜 **端侧OCR营养成分表**（一期零成本ML Kit·需真机+依赖）｜**MID-1家常度列**（迁移·解锁推荐排序·精细）。
6. 持续用**方法论(experience/10)**：改规则前先GLM对比、挖库缺口。

> **真机待验（攒一起·装新包·老库先"我的→更新基础数据"跑 seed v7）**：
> ⑩人群适配红绿灯 ⑪库扩充新菜 ⑫徽标显隐 ⑬推荐组合+早餐纠偏+**中西不混搭** ⑭编辑菜品折叠头 ⑮选菜新建入口+**完成下移底部** ⑯DRIs国标参照 ⑰UX批(数字输入/超长文本) ⑱高血脂红绿灯(补胆固醇后) ⑲我的页新排序 ⑳食材拼音字母条(各分类)；历史⑤-⑨见 git。
> 接手：工作区干净·全 push；**下一步自选(补日常菜/AI营养补全/#1 P2)**。构建务必读输出确认 BUILD SUCCESSFUL。
