# 🔖 SESSION 交接入口（新会话先读这里）

> 本文件是**会话交接的唯一固定入口**，永远指向"最新一次交接状态"。
> **触发词**（见 `CLAUDE.md·会话交接协议`）：用户说「查看session继续 / 会话继续 / 读session / 接续session」→ 读本文件接续；说「切换session / 会话交接 / 保存session / 交接」→ 落地文档+覆盖更新本文件+git 提交。
> 更新时间：2026-07-16（无人值守大批次·9 项交付后交接）。

---

## 一、先按序读（3 分钟进入状态）
1. **本文件**（当前状态 + 下一步）。
2. `CLAUDE.md` + `.ai-context/rules/通用规则.md`（项目规范/门禁/**§十无人值守特有决策**/踩坑红线）。
3. **`.ai-context/docs/feature/2026-07-16_无人值守交付总览.md`**（本轮 9 项验收清单：每项操作逻辑+怎么验证+提交号+详细文档链接）。
4. `.ai-context/docs/context_memory/2026-07-16_无人值守大批次_进度快照.md`（更细的进度流水）。
5. 需动手时：`.ai-context/docs/feature/待办总览.md` + `.ai-context/docs/experience/06_问题与踩坑.md`（红线经验）。

## 二、工作规则（用户已定，无人值守持续）
- 中文对话；按 **backlog 顺序自主推进、不拍板**（"先做一版看效果"）；全权授权。
- **不确定方案 → 多角色相互验证**（spawn 多身份 agent 从不同角度收敛最优方案再执行）——通用规则§十。
- **双/三门禁**：UI 改动先 Apple-UX 设计门禁；每批编码后 Google 代码审查门禁（阻断必修复验）；产品/算法不确定加多角色验证。
- **健康数据守免责红线**：仅供参考·非医嘱、禁编造出处、无源省略/pending、一手权威才 verified；GI 用 FAO/WHO(非WS/T652)、嘌呤无国标数值走定性。
- **每完成一项走完整仪式**：审核验证测试 → 存上下文快照 → 总结经验 → 飞书通知 → 下一项（不攒批）。
- **算法/数据类落地文档必须含【操作逻辑】(公式/阈值/数据来源/何时生效) + 【用户验证方法】(真机/数据怎么判断对不对)**——通用规则§十。
- 构建：`scripts\build-cli.bat :shared:testDebugUnitTest` / `:androidApp:assembleDebug`。临时文件放 `temp/claude/`。
- **只交付 Android**（iOS 暂不做，DI平台化/KMP日志等 iOS 向工程项本期不做）。

## 三、当前状态（2026-07-16 无人值守本轮）
- 本 session 已交付 **9 项、~36 提交**，工作区干净（仅 temp/claude 杂物）。9 项见交付总览：DIABETES GI 分支 / 餐次常吃 chips / 详情页 GI 嘌呤提示 / DB 索引 / AI 配置指南入口 / 库存挂钩开关 / 菜系分类打磨 / 慢病数值软约束 / classify 单测硬化。
- **三慢病定性维度**（高血压/痛风/糖尿病）均进评级器，覆盖今日卡(聚合)+详情页(单菜)+推荐软降(营养风格)。
- **首次固化多角色验证机制**（本轮用了 4 次）+ 算法/数据文档"操作逻辑+验证方法"规范。

## 四、⏭ 下一步（按序，均属"需新上下文/专业审校/真机"类，新会话做更稳）
1. **逐病种深挖**（part2 关键路径）：每个慢病维度补足够的适宜/避免食材 + 对应菜品覆盖；**需引入临床营养专业审校**（基于权威临床/膳食指南，非泛化常识）。建议先高血压端到端跑通再扩。见 `营养数据治理方案.md §八`。
2. **工程性能·异步 seed**：首装/内容变更 seed 全量写耗时→后台化+首屏 loading。**高风险**（动 seed 流程 + 需真机验启动），做前推演旧库迁移。
3. **批次2 去 pending**（营养 verified 率↑）：低产出（batch1 发现多为源冲突/review-only 不宜单独合并），攒到有真实值变化再批量合并。
4. **可选增强**（不阻塞）：菜系编辑页可选菜系选择器、软降现场透明提示(加了可把默认权重从0提到对病种生效)、钠"高本底主料关键词"窄口径软降、软约束 P3 高血脂(需扩 schema)。

> 接手做法：从第 1 项起（先 spawn 多角色/临床营养审校 agent 摸现状+定方案），或用户另指方向。每项走完整仪式。
