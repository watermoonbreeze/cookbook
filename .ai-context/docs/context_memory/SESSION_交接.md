# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「会话继续/查看session继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-18**（超长会话·多批交付，全部构建/单测过门禁、**已 push 到 `88efc20`**，工作区干净·仅 temp/claude 临时文件）。

## 一、先按序读（进入状态）
1. **本文件**（当前状态 + ⏭下一步）。
2. `CLAUDE.md` + `.ai-context/rules/通用规则.md`（规范/三门禁/踩坑红线/**文案准则**/健康免责红线）。
3. `.ai-context/docs/feature/待办总览.md`（**backlog 单一入口**）。
4. `苹果风格UI设计方案.md §九`、`交互组件复用指南.md`（做 UI 前必读·22 组件必复用）。
5. 本会话新增方案（按要接的活选读）：`多人关注设计方案.md`、`特殊人群健康状态方案.md`、`健康状态深挖分析.md`、`推荐算法深挖分析.md`、`UX核心流深挖.md`、`对话食材补充清单.md`。

## 二、工作规则（用户已定·稳定）
- 中文；**无人值守·全权推进·按 backlog 顺序自主做·先做一版看效果**（不拍板医学/产品决策，涉口径/schema/大改**先出方案带选项让用户选**，别让用户出方案）。
- **三门禁**：①UI/交互/配色改动**编码前先 Apple-UX**（spawn `apple_ux_designer`）②每批编码后 **Google 代码审查**（`google_quality_engineer`·阻断必修复验）③文案多/新页用 **`copywriter`**。小改/复用已确立 §九 可免。
- **健康免责红线（更严）**：仅供参考·非医嘱、禁编造出处/医疗断言、无国标标"惯例"、GI 用 FAO/WHO、嘌呤定性；**孕期/婴幼儿遵儿保科/产科**；食养/特殊人群**物理隔离慢病评级**（不进 NutritionLevelEvaluator/色系墙）。
- **每功能完整仪式**：门禁 → 构建(`scripts\build-cli.bat`)/单测 → **存档(feature/待办)+刷项目说明书(`projectReview/07`等·用户要求实时刷)+经验(`06_问题与踩坑`)** → **飞书**(每完成一项发) → 提交(`[unattended]`) → **push**（本会话用户要求做完 push）。临时文件 `temp/claude/`。**只交付 Android**。
- **持续收集食材**：对话中冒出的缺库食材记入 `对话食材补充清单.md`，攒批权威补库(分片 agent 联网《成分表》·守红线)。
- **数据/权威健康数据**用分片后台 agent 联网核准、python 覆盖式合并(只填缺失/物理约束校验如饱脂≤总脂)、禁编造；DB 加列走红线全套。
- **记一餐保留预览确认**（用户 2026-07-18 明确要·别回退）。

## 三、当前状态（本超长会话已交付·全绿·已 push `88efc20`）
**功能/修复**：计时器到点全屏多计时器逐个显示+停止生效(双响机制收敛单一真相源+并发锁) · 菜名推食材(推出即自动加入+库外"待自建"保存自动建库+做法词打磨堵"醋溜/烧鸡"误当食材) · 连续多段倒计时(手动接力·被杀恢复段) · A14全屏引导 · **A12通知点击闪退修复(openPi 补 NEW_TASK)** · 我的模块排序(档案/回顾/实用工具/AI助手/数据/外观/参考资料/通用/关于)。
**健康体系**：P3高血脂饱脂/胆固醇数据补齐(15%→37%·分片agent联网) · 健康状态参考页(我的·参考) · **特殊人群**(备孕/孕期/哺乳/婴幼儿/学龄前/学龄儿童·282条权威care规则+参考页6节+健康档案慢病/生命阶段分组·复用crowd维度零schema)。
**算法深挖**：推荐MMR多样性扩维(主料0.6+菜系0.2+做法0.2·避免一批全川菜/全红烧)+全链路分析文档。
**UX深挖**：收藏图标矢量化+日期锁定纯文本+AI推荐勾选圈统一+核心6流走查文档。
**方案(待实现/待审)**：多人关注(方向已批·待Apple-UX布局细则)、特殊人群(已实现)、算法/UX分级增强路线。

## 四、⏭ 下一步（待做·按优先/用户意愿接）
1. **算法3项(用户已拍板·待实现·中等)**——见 `推荐算法深挖分析.md`：①**时间衰减**(仅"偏新鲜"风格·久没做的常做菜降权·需"每菜距上次做天数"查询) ②**负反馈"踩"**(菜卡显式标不推荐→降权·需存储+**Apple-UX出踩手势位置**+评分负因子) ③**口味画像**(学历史菜系/做法/主料频次→taste-match加分·纯本地统计)。
2. **UX 🟡项(用户"按你来"·门禁走Apple-UX规范)**——见 `UX核心流深挖.md`：搜索食材直开详情(对齐§9.2·最明确优先)、今日已有卡加"再记一餐"直达、AI推荐加"看详情"路、AI控件低频项折叠、搜索结果统一纵向分组。
3. **多人关注实现**(🔴大重构·方向已批·>15文件)——先spawn Apple-UX出布局细则(今日卡/报告成员切换chip)再编码,见 `多人关注设计方案.md`。
4. **华为A12计时仍闪退**——用户定**暂不做,等其明确+真机logcat**(app有installCrashHandler写日期日志·可adb pull)。
5. 低优/待拍板：食材撞名提示(scenario1)、推演接AI、报告二期、药膳四气二期、GI/纤维补齐(已验证碳水主食GI够·多为香辛料无意义可缓)。

> **待用户真机验证**：A12通知点击不闪退、我的新排序、健康档案选孕期/婴幼儿生效忌口推荐、多段计时接力/被杀恢复、菜名自动加入+待自建落库、收藏图标/日期锁定/AI勾选圈。
> 接手：工作区干净、全部已 push；从"算法3项"或"UX 🟡项"接最顺，或按用户指定。
