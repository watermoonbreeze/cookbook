# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「会话继续/查看session继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-18 夜（无人值守长跑·checkpoint）**。全部构建/单测过门禁、**逐批已 push**，工作区干净（仅 temp/claude）。

## 一、先按序读（进入状态）
1. **本文件** + `context_memory/一期推进总规划.md`（总规划+进度+已拍板结论）+ `context_memory/待拍板队列.md`（**攒下待你定夺的项·含我的推荐**）。
2. `CLAUDE.md` + `.ai-context/rules/通用规则.md`（规范/三门禁/踩坑红线/文案准则/健康免责红线）。
3. `.ai-context/docs/feature/待办总览.md`（backlog 单一入口）。
4. `苹果风格UI设计方案.md §九`(§9.1–9.20)、`交互组件复用指南.md`（做 UI 前必读）。
5. 相关方案：`推荐算法深挖分析.md`、`多人关注设计方案.md`、`药膳食补维度方案.md`。

## 二、工作规则（用户已定·稳定）
- 中文；**无人值守·全权推进·多方会诊·先做一版看效果**；涉口径/schema/大改先记 `待拍板队列.md` 不擅动。
- **标准无人值守流程**（用户 2026-07-18 确认）：门禁+会诊 → 编码 → **强测试(正向+逆向+边界+异常防护·多环境)** → Google 审查(阻断必修) → 存上下文 → 总结经验 → **飞书** → commit(`[unattended]`) → **push** → 继续下一个。
- **三门禁**：①UI改动先 `apple_ux_designer` ②每批后 `google_quality_engineer` ③文案多用 `copywriter`。小改/复用§九可免。
- **健康免责红线**：仅供参考·非医嘱、禁编造出处/医疗断言、无国标标"惯例"、GI 用 FAO/WHO、嘌呤定性、特殊人群物理隔离慢病评级。
- **新角色**：`algorithm_engineer`(算法/营养/健康/膳食/基础)、`ai_engineer`(提示词/LLM)——可组"AI推荐会诊团队"。
- 临时文件 `temp/claude/`。**只交付 Android**。

## 三、当前状态（本夜无人值守已交付·全绿·逐批已 push）
**算法3项(用户拍板)全完**：①时间衰减(FRESH·`d85ede2`) ②口味画像(`d85ede2`) ③负反馈"踩"(§9.20长按不再推荐+灰态+撤销+详情恢复·dish_dislike表·27.sqm·gather过滤)。
**膳食推荐质量**：A1组合级荤素/主食搭配+A2营养互补首餐兜底+D1荤素多样性(`d85ede2`批)。**云端AI**：R1忌口正确性红线(剔除出模型可选集)+R5免责+R2近吃标签。**热量达标(A4已实现)**：补红线 A1未成年不评/B1/B2域防护。
**搜索就地新建**(`f24660a`)、**AI推荐仅手动刷新**(`08e0a9e`)、**AI推荐规则说明图标**(`343a78e`)。
均过门禁+构建/单测。角色 algorithm_engineer/ai_engineer 已建。

## 四、⏭ 下一步（按`一期推进总规划.md`顺序·全权推进）
1. **阶段A3 营养数据补全**(65%→更高·分片 agent 联网《成分表》权威核准→python 覆盖式合并→validateNutritionSeed+单测·守红线·data活)。
2. **阶段B2/B3**：algorithm_engineer 出复杂度/热点地图(预防式性能·埋点)、逐菜推荐理由展示(P3剩余)；技术债 MealCompositionScorer(荤素评分三处同构抽共享)。
3. **阶段C UX核心流**：Apple-UX 走查"推荐→选菜→记一餐"主链 + 撞名提示 scenario1。
4. **阶段D 大功能**(用户已拍板纳入)：D1 多人关注重构(照`多人关注设计方案.md`·>15文件·先Apple-UX布局→编码→审查) → D2 药膳一期最小集(涉schema) → D3 自定义餐次(meal_type source列迁移)。
5. **阶段E 收尾**：真机验证批 → 性能到阈值优化 → AI协作经验总结 → **AI模型推荐待办方案(用户要求·多方算法会诊后出·仅文档·含放开限制phased)**。

> **待用户拍板**：见 `待拍板队列.md`（忌口放宽辅料/换默认模型glm-4.5-flash+JSON/换一换缓存/餐次差异/孕期哺乳不评热量/默认活动档）。
> **待真机验证**：算法3项效果、负反馈踩全流程、搜索新建、AI推荐不自动刷+规则说明、热量达标。
> 接手：工作区干净、全部已 push；从阶段A3 或按用户指定接。
