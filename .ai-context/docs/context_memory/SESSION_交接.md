# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「会话继续/查看session继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-19（长会话·多批交付+多方案会商）**。全部构建/单测过门禁、**逐批已 push**，工作区干净（仅 temp/claude）。

## 一、先按序读（进入状态）
1. **本文件** + `context_memory/一期推进总规划.md`（总规划+进度+已拍板结论）+ `context_memory/待拍板队列.md`（仅剩少数开放项+我的推荐）。
2. `CLAUDE.md` + `.ai-context/rules/通用规则.md`（规范/三门禁/踩坑红线/文案准则/健康免责红线）。
3. `.ai-context/docs/feature/待办总览.md`（backlog 单一入口）。
4. `苹果风格UI设计方案.md §九`(§9.1–9.20)、`交互组件复用指南.md`（做 UI 前必读）。
5. **待实现方案（按下一步选读）**：`菜品餐次分类方案.md`、`AI对话生成菜品餐食方案.md`、`个人忌口设置方案.md`、`换一换缓存策略方案.md`、`多人关注设计方案.md`、`推荐算法深挖分析.md`、`性能优化推荐链路地图.md`。

## 二、工作规则（用户已定·稳定）
- 中文；**无人值守·全权推进·多方会诊·先做一版看效果**；涉口径/schema/大改先记 `待拍板队列.md` 或出方案带选项，别让用户凭空定。
- **标准无人值守流程**（每项都走）：门禁+会诊 → 编码 → **强测试(正向+逆向+边界+异常防护·多环境)** → Google 审查(阻断必修) → 存上下文 → 总结经验 → **飞书** → commit(`[unattended]`) → **push** → 继续下一个。
- **飞书消息必按 4 段结构**（用户2026-07-19定）：①做了什么 ②还有哪些 ③接下来做 ④要我验证的（尤其真机验证清单）。
- **三门禁**：①UI改动先 `apple_ux_designer` ②每批后 `google_quality_engineer` ③文案多用 `copywriter`。小改/复用§九可免。
- **健康免责红线**：仅供参考·非医嘱、禁编造出处/医疗断言、无国标标"惯例"、GI 用 FAO/WHO、嘌呤定性、特殊人群物理隔离慢病评级。**食材营养数据不编造**(权威核准或估算标"待核")。
- **会诊角色**：`algorithm_engineer`(算法/营养/健康/膳食/基础)、`ai_engineer`(提示词/LLM)、apple_ux_designer/copywriter/google_quality_engineer/apple_operations_designer/DEV_product_manager。
- 临时文件 `temp/claude/`。**只交付 Android**。

## 三、当前状态（本会话已交付·全绿·逐批已 push）
- **算法3项全完**：时间衰减(FRESH)·口味画像·负反馈"踩"(长按不再推荐+撤销+详情恢复·dish_dislike表)。
- **膳食推荐质量**：A1组合级荤素/主食搭配·A2营养互补首餐兜底·D1荤素多样性。
- **云端AI**：R1忌口正确性红线(剔除出模型可选集)·R5免责·R2近吃标签·**R3换默认模型glm-4.5-flash+JSON强约束**。
- **B1忌口从严**：忌口放宽到含辅料(限量/调养仍守主料·组合非此即彼)·三处消费点一致。
- **热量达标(早已实现)补红线**：未成年不评+域防护+B1中性。
- **UX**：搜索就地新建·AI推荐仅手动刷新·AI推荐规则说明图标。
- **新角色**：algorithm_engineer·ai_engineer。**性能地图**(会诊·发现🔴RANDOM崩溃隐患待修)。
- **已出方案(待实现)**：菜品餐次分类·AI对话生成菜品餐食·个人忌口(按分类)·换一换缓存(均衡档)。

## 四、⏭ 下一步（用户2026-07-19 定的实现顺序·全权推进）
1. **菜品餐次分类**(方案 `菜品餐次分类方案.md`·D1-D5全采纳) + **配套中西式餐次菜品/食材数据扩充**：现早餐推炒菜不合理→补中式(粥/豆浆/包子/油条)+西式(牛奶/面包/沙拉/意面)按餐次+菜系双维预标·**守食材营养不编造红线**(优先用已有食材·缺的估算标待核或权威核准·引用完整性单测)。先建机制(schema `dish_meal_slot`+打标+编辑chip+二级筛选栏+搜索按餐次+AI推荐/记一餐查关联)再批量补数据。
2. **AI对话MVP**(方案 `AI对话生成菜品餐食方案.md`)：聊→列食材→出一道菜→确认入库·前置扩AiRuntime多轮·三红线(忌口校验/确认才入库/营养本地估算)·JSON-intent信封·MVP不做多天PLAN。=放开限制云端档会话外壳。
3. **就绪队列**：R6换一换均衡档缓存(药膳本地重排零风险先做) → 个人忌口设置(按分类·member_avoid_category) → 多人关注重构(照方案·先Apple-UX布局)。
4. **其余**：搜索按餐次(并入餐次分类)·营养数据补全·MealCompositionScorer技术债·竞品对比+商业化(运营总监+PM·一期稳定后)。

> **均需 Apple-UX 门禁(新UI)+Google 审查(迁移/质量)**。餐次分类+数据扩充是涉 schema+批量 seed 的大批次·作完整批次做。
> **开放待用户定**(见 `待拍板队列.md`)：A2孕期哺乳不评热量·热量参国标·🔴RANDOM崩溃(需真机确认食材库量级)。
> **待真机验证**：算法3项/负反馈踩/搜索新建/AI推荐不自动刷+规则说明+glm4.5默认/B1忌口含辅料/热量达标未成年不评。
> 接手：工作区干净、全部已 push；从"菜品餐次分类"接。
