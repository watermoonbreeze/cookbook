# 🔖 SESSION 交接入口（新会话先读这里）

> 本文件是**会话交接的唯一固定入口**，永远指向"最新一次交接状态"（每次交接覆盖，历史流水在 git + 同目录日期快照）。
> 触发词：「会话继续 / 查看session继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接 / 切换session」→落地文档+覆盖本文件+git提交。
> 更新时间：**2026-07-18**（超长会话·多批交付，全部构建/单测过门禁、**已 push 到 `a0c8242`**，工作区干净）。

---

## 一、先按序读（进入状态）
1. **本文件**（当前状态 + ⏭下一步）。
2. `CLAUDE.md` + `.ai-context/rules/通用规则.md`（规范/三门禁/踩坑红线/**文案准则**/健康免责红线）。
3. `.ai-context/docs/feature/待办总览.md`（**backlog 单一入口**，含所有待做+已做状态）。
4. `苹果风格UI设计方案.md §九`（交互模式库·做 UI 前必读）、`交互组件复用指南.md`（22 组件必复用）。
5. 相关方案文档（按要接的活选读）：`报告模块方案.md`、`食材输入智能推演方案.md`、`药膳食补维度方案.md`。

## 二、工作规则（用户已定，稳定）
- 中文；**无人值守·全权推进·按 backlog 顺序自主做·先做一版看效果**（不拍板，除非"需用户拍板"类）。
- **三门禁**：①UI/交互/配色改动**编码前先 Apple-UX 设计**（spawn `apple_ux_designer`）②每批编码后 **Google 代码审查**（`google_quality_engineer`·阻断必修复验）③文案多/新页用 **`copywriter`**。小改可免。
- **健康免责红线**：仅供参考·非医嘱、禁编造出处/医疗断言、无国标标"惯例"、GI 用 FAO/WHO、嘌呤/中医食养定性；食养(药食同源)/慢病维度**物理隔离**——食养只正向展示、绝不接忌口/评级。
- **每项走完整仪式**：门禁 → 构建(`scripts\build-cli.bat`)/单测 → 存档(feature doc/待办) → **飞书**(每完成一项发) → 提交(`[unattended]`)。**push 仅用户要求时**（本会话用户常要 push，做完可主动问）。临时文件 `temp/claude/`。**只交付 Android**。
- 数据/权威健康数据用**分片后台 agent 联网核准**、禁编造；DB 加列走红线全套（CREATE 同步+`N.sqm` ALTER+所有构造点+迁移推演单测）。

## 三、当前状态（2026-07-18 本超长会话已交付，全绿·已 push `a0c8242`）
> 早期两大批：**(A) 执行队列 1–7**(`5c4408d`→`122532e`：高血脂DB迁移/记一餐去预览→后又改回全预览/UX深挖6项/selectionMode重构/说明书跳转/报告方案/两新待办方案) + **(B) 用户5条**(`09ed463`→`44c2051`：常吃即时/全保存预览/**报告模块MVP**/**食材智能推演**/药膳PM方案)。

**后续批次（本会话主体，均已实现+过门禁+push）**：
- **高血脂评级维度**：ingredient_nutrition 加饱和脂肪/胆固醇(`25.sqm`)+评级器负向分支+90食材权威数据+单测。
- **报告模块 MVP**：我的→回顾→饮食报告（周/月×家庭/个人·概览/发现/概况/菜品/食材/结构/营养·自绘图·空态）。
- **食材智能推演**：自建食材按名预填分类+营养(三级兜底·善意提示条·可撤)；**土豆修复**(coreName 曾把"土"当修饰词剥成"豆"→匹不上)。
- **药食同源一期(食养)**：`MedicinalFoods`(卫健委目录106项·资源免迁移)+食材详情"食养"标签+AI推荐"食养"筛选(只正向排序·**不接慢病评级**)+**食养参考页**(我的·参考资料)。术语：食材标签叫"药食同源"、维度/过滤叫"食养"(不叫"食补")。
- **UX 深挖6项**：出/入库可撤销、餐次横滚 PrimaryTabRow、首页今日/计划分区、日期快捷chip、收藏组合 ModalBottomSheet。
- **图片改造**：全屏可滑查看器 `FullScreenImageViewer`(HorizontalPager)+层叠缩略图 `StackedThumbnail`(单张/多张叠加+共N张)+详情图片收敛到名称前一处(删散落多图区)+**缩略图清晰度**(存储 360→800、显示解码 /4→/2、缓存改按字节)。
- **倒计时增强①②**：计时中屏幕常亮(keepScreenOn)；到点全屏**多计时器实时增删(StateFlow驱动·修"第2个没进全屏/停不掉")+每个各自停止+布局重排(Apple-UX·名称省略不挤按钮)**。
- **菜名推食材**：`DishNameIngredientGuesser`(纯函数)+新建菜品输"土豆牛腩"推"＋土豆 ＋牛腩"点确认加入。
- **说明书册间跳转**：projectReview 10 册交叉引用改可点 markdown 链接。

## 四、⏭ 下一步（待做，详 `待办总览.md`；按优先/用户意愿接）
1. 🔄 **倒计时③ 连续多段**（数据层已 done `502f5f9`：`segments_json`加列+`26.sqm`+`TimerSegment`模型+repo+单测·向后兼容）。**待做=运行流+创建UI**：
   - 运行流(`CookingTimerScreen`)：跑 `template.runSegments`，**当前段到点响铃、用户点停止后才启动下一段**（用户明确：不是自动链，是"停了上一段才开始下一段"）直到最后段；`CookingTimerItem` 需带 segments + currentIndex。
   - 创建 UI(`CookingTimerEditRow`)：默认单段，可展开"添加下一段"(段名可选+分秒)；保存写 `template.segments`(单段留空)。守 elapsedRealtime 墙钟红线。
2. **华为不弹到点全屏引导**：华为/鸿蒙对 `USE_FULL_SCREEN_INTENT`(A14+需授权)+"后台弹出界面/锁屏通知"限制严(小米默认给了才弹)。加：A14+ 检 `canUseFullScreenIntent()` 否则引导去系统设置授权；铃声+通知本就是兜底。
3. **家庭档案·同时关注多人**（🔴 大重构·**建议先出 Apple-UX+PM 方案**）：现 `is_focus` 单选、`pickFocus` 取一个，身体数据/达标/份额/今日卡/报告个人视角都围绕单人。改多关注涉一串 `observeFocus*` 单人接口→多人 + "多人怎么展示"UX。忌口/病种已是全家并集不受影响。
4. **推演类接入 AI**（🟡）：现有 NutritionGuesser/DishNameIngredientGuesser 等本地启发式，AI 可用时由 AI 推演更准更全(本地规则兜底·用户确认·守免责)。
5. **报告二期 / 药膳四气二期 / UX #14·#6**（低优先/待拍板，详待办）。
6. **#1 候选建议(可选)**：自建食材撞已有/预设名时提示"已有『土豆』，直接用"(防重复建)——菜名推食材已覆盖菜品侧。

> **待用户真机验证**：到点全屏(多计时器/停止)、图片全屏+层叠+缩略图清晰度、菜名推食材、食养标签/参考页、高血脂提示、报告页。
> 接手：工作区干净、全部已 push；从第 1 项(倒计时③运行流+UI)接着干最顺，或按用户指定。
