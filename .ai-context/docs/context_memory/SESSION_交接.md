# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「查看session继续/会话继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-19（超长会话·商业规划+餐次分类+个人忌口+多人关注等 6 大项交付）**。全部过门禁+单测+构建绿、**19 个提交已全部 push 到 origin/master**、工作区干净(仅 temp/claude)。

## 一、先按序读（进入状态）
1. **本文件** + `context_memory/一期推进总规划.md`（总规划+进度）+ `context_memory/待拍板队列.md`（开放项+推荐）。
2. `CLAUDE.md` + `.ai-context/rules/通用规则.md`（规范/三门禁/踩坑红线/文案准则/健康免责红线）。
3. `.ai-context/docs/feature/待办总览.md`（backlog 单一入口）。
4. `苹果风格UI设计方案.md §九`(§9.1–**9.23**)、`交互组件复用指南.md`（做 UI 前必读）。
5. **下一步相关方案**：`换一换缓存策略方案.md`(R6第二批·下一个)、`AI对话生成菜品餐食方案.md`、`推荐算法深挖分析.md`、`性能优化推荐链路地图.md`、`竞品对比与商业化规划.md`(战略)。

## 二、工作规则（用户已定·稳定）
- 中文；**无人值守·全权推进·多方会诊·先做一版看效果**；涉口径/schema/大改先记 `待拍板队列.md` 或出方案带选项。
- **标准无人值守流程**（每项都走）：门禁+会诊 → 编码 → **强测试(正向+逆向+边界+异常防护)** → Google 审查(阻断必修) → 存上下文 → 经验 → **飞书** → commit(`[unattended]`) → **push**(本会话用户已授权 push·默认逐项/逐段推) → 下一个。
- **提交纪律(本会话教训)**：commit 前先 `git status --short` 核对**全部**改动文件都已 `git add`——本会话曾漏 add 两个 shared 文件致 push 的 master 编译不过，补提交修复。
- **飞书必按 4 段**（用户2026-07-19定）：①做了什么 ②还有哪些 ③接下来做 ④要验证的(真机清单)。
- **三门禁**：①UI改动先 `apple_ux_designer` ②每批后 `google_quality_engineer` ③文案多用 `copywriter`。小改/复用§九可免。
- **健康免责红线**：仅供参考·非医嘱、禁编造出处/医疗断言、无国标标"惯例"、GI 用 FAO/WHO、嘌呤定性、慢病评级物理隔离特殊人群。**食材营养不编造**(权威核准或估算标"待核")。
- **对外文案温和化(用户2026-07-19定)**：面向用户不显性标"三高痛风"病名，用"健康管理/慢病家庭"；病名只在健康档案/忌口设置的中性功能语境出现。(见 auto memory + 商业规划§2.5)
- **会诊角色**：algorithm_engineer/ai_engineer/apple_ux_designer/copywriter/google_quality_engineer/apple_operations_designer/DEV_product_manager。
- 临时文件 `temp/claude/`。**只交付 Android**。

## 三、当前状态（本会话 6 大项已交付·全绿·已 push）
- **✅商业规划**(`92adca1`/`58df056`)：`竞品对比与商业化规划.md`(PM+运营会诊+2轮联网调研+§三·五数据源战略)。用户5点拍板:①对外文案温和化 ②一期不变现 ③AI双轨(内置订阅+自带Key免费) ④账号最小化可选 ⑤**不做电商/toB保持纯粹**。数据源结论:家常菜食材线(权威成分表·本地·主战场)vs预包装(轻量条码OCR)·**不接超市**。事实素材 temp/claude/competitor_research.md+datasource_research.md。
- **✅餐次分类全功能**(`5972e87`+`24f55d3`+`b755e15`)：dish_meal_slot表+28.sqm+Matcher降默认+seed打标SEED_LOGIC_VERSION**v6**+编辑chip+二级栏+搜索按餐次+AI推荐查关联+记一餐预筛+**中西式51道数据**(全用已有食材·引用完整性0错误)。§9.21。
- **✅个人忌口设置(按分类)**(`a83b6b2`+`02fade8`)：29.sqm+member_avoid_category+成员编辑弹层15分类chip(共享ToggleChip)+gather并集(个人忌口对调料真避开·含即命中·与健康忌口正交)。§9.22。
- **✅多人关注重构**(`e0b3679`+`851b252`+`70dfb3c`+`89aa5a6`)：**零schema迁移**·is_focus改多选+当前查看指针(偏好JSON)·resolveViewing收口·toggleFocus(下限1·事务化)·今日卡/报告成员切换器·家庭星标多选·**1人零变化**·老库无感。§9.23。
- **✅R6第一批药膳本地重排**(`f75edf3`)+**✅R2口味汇总**(`02ed415`)：缓存复用免全量gather+云端·踩菜失效；常吃top2菜系喂云端prompt(偏新鲜/空不喂)。
- 期间：修漏提交(`3b4359a`)保 master 可编译；auto memory 存"对外文案温和化"+"商业化路线已定"。

## 四、⏭ 下一步（就绪队列大功能已全做完·剩以下·全权推进·每步发飞书）
1. **R6第二批「换一换=均衡档」(下一个·方案`换一换缓存策略方案.md`)**：首次云端预取多批(模型基于前20-30候选给3-4组)→前几次换一换本地轮播+规则rotate兜底→用尽才再联网。**动核心推荐编排/AI输出结构·敏感·需谨慎**(先做gather结果缓存复用[换一换/同形状切风格免重gather]较低风险、再做云端预取多批)。失效守卫:切餐次/窗口/mode/踩菜。
2. **营养数据补全**：分片后台agent联网核准《成分表》→temp/*_N.json→python覆盖升级式合并→validateNutritionSeedForTest+单测(权威流程见踩坑红线)。
3. **AI对话MVP**(方案`AI对话生成菜品餐食方案.md`)：聊→列食材→出一道菜→确认入库·三红线(忌口校验/确认才入库/营养本地估算)·JSON-intent信封。
4. **持续打磨**(用户定·"都做完就参考其他app取长补短")：跨模块体验提升扫描·MealCompositionScorer技术债·餐次数据继续扩·商业规划P0汲取项(成员化红绿灯/病种解读视角)。

> **均需 Apple-UX 门禁(新UI)+Google 审查(迁移/质量)+强测试**。每步:门禁→编码→测试→审查→存档→经验→**飞书**→commit(`[unattended]`)→**push**→下一步。
> **开放待用户定**(见`待拍板队列.md`)：A2孕期哺乳不评热量·热量参国标·🔴RANDOM崩溃(食材库过千IN变量超限·需真机确认量级)；商业化(AI订阅定价待用户AI套餐定·成分表商用授权走CDC申请还是买商业库·电商/toB已定不做)。
> **待真机验证(攒着一起·装新包+老库需触发迁移28/29.sqm+seed v6)**：①餐次分类5项(二级栏/搜早餐/记一餐预筛/编辑预选/AI推早餐) ②个人忌口(选分类→含该类食材的菜标忌口·葱蒜勾香辛料) ③多人关注(关注≥2人→今日卡/报告切换器+同步·取消最后一个被拦·1人零变化) ④药膳本地重排瞬时响应 ⑤历史项(算法3项/负反馈踩等)。
> 接手：工作区干净、19提交全已 push；**从「R6第二批 换一换均衡档」接**（先读该方案）。
