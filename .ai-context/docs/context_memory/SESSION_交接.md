# 🔖 SESSION 交接入口（新会话先读这里）

> 会话交接唯一固定入口（每次交接覆盖，历史流水在 git）。
> 触发词：「查看session继续/会话继续」→读本文件按"先读清单"补上下文、按"⏭下一步"接着干；「交接/保存session」→落地文档+覆盖本文件+git 提交。
> 更新时间：**2026-07-20 交接（超长无人值守会话·本轮连交付 5 个功能）**。全部已 push origin/master（功能最新 `4b119fa`、含本次交接提交）、shared 单测 + androidApp BUILD SUCCESSFUL、工作区仅 temp/claude + local.properties（gitignored·含友盟AppKey）。**用户授权持续无人值守把 backlog 全做完（每功能走全套门禁+push）**。

## 一、先按序读（进入状态）
1. **本文件** + `projectReview/07_项目现状.md`（能力成熟度全景+本会话交付+真机待验）。
2. `feature/待办总览.md`（各功能状态·F/G 区=用户 review 追加）+ `一期收官路线图与决策清单.md`（阶段1-7）+ `unattended_decisions.md`。
3. `CLAUDE.md` + `.ai-context/rules/通用规则.md`（三门禁/踩坑红线/健康免责）。
4. 设计范式：`苹果风格UI设计方案.md §九`（**本轮新增 §9.24 全屏三段编辑页 / §9.25 成员化红绿灯 / §9.26 病种关注指标块 / §9.27 指标分级筛选**）。UI 经验沉淀在 `experience/05_UI组件.md` 末尾对应四条。

## 二、工作规则（用户已定·稳定·重要）
- 中文；**无人值守·全权推进·凡需拍板用推荐方案·快速全做完·不反复问确认**。真正要停：指令模糊、触红线。
- **别过度设计**：明确指令按最简实现。**先 Explore/读代码摸现状再动**（红绿灯/病种视角/三级筛选都先摸清现有基建避免重复造轮子）。
- **每功能完成走全套**：门禁（Apple-UX 界面门禁 / copywriter 健康文案门禁 / Google 代码审查·阻断必修复验）→**保存上下文**→**总结经验**（experience）→**同步项目说明书**（projectReview/07）→**更新待办总览**→**飞书(4段)**→commit(`[unattended]`)→**push**→下一个。
- **🔴构建验证看输出别信 exit code**：`scripts\build-cli.bat <task> *> log; grep "BUILD SUCCESSFUL"`，别用 `|tail`。
- **健康免责红线**：仅供参考·非医嘱；**物理隔离**（慢病维度不接色系墙营养级别）；**嘌呤/GI 只给定性不给占比**（无国标不自造标准）；钠/嘌呤惯例阈值必标"非国标·惯例口径"；忌口菜绝不进推荐/一餐；匿名统计绝不上报健康明细。
- 临时文件 `temp/claude/`；**只交付 Android**。**友盟 AppKey 走 local.properties 不进 git**。

## 三、当前状态（本会话·全 push·master 编译通过·shared 单测绿）
**本轮连交付 5 个功能**（每个过门禁+构建绿+文档全套+push）：
1. ✅ **成员编辑页全屏化+3段分段**（`a96db88`·§9.24）：AlertDialog→全屏 `MemberEditorScreen`（Dialog overlay）+顶部 SegmentedControl（资料/健康/忌口·N 徽标）+FormBottomBar 保存+rememberUnsavedGuard。Google 审查修 1 真数据丢失 bug（异步查名未完成前保存会清空既有忌口食材→save 加 ingLoaded gate）。
2. ✅ **成员化红绿灯 Phase1**（`ddf9c4b`·§9.25·商业#1护城河）：菜品详情"适合谁吃"逐家人红/黄/绿+原因（成员≥2 显·排序红→黄→绿·全绿一行·替代全家并集适宜性行）。shared `MemberDishVerdict`+`MemberDishHealthUseCase`+`gatherConstraintsForMember`（镜像生产 gatherConstraints·8 单测）。
3. ✅ **病种切换解读视角**（`fe1f3aa`·§9.26·商业#3护城河）：详情页"关注指标"块按登记病种显钠/GI/嘌呤/饱脂胆固醇（钠饱脂胆固醇给数值+占每日上限%+分级；嘌呤GI守定性；缺数据"暂无数据"）。吸收删除原散落 🍖📈 行。shared `NutritionInterpreter`（7 单测）。三门禁。
4. ✅ **GI/钠/嘌呤三级筛选导航**（`10ffe1e`·§9.27·商业#7）：营养表顶栏 Tune→ModalBottomSheet（选指标+多选级别）+摘要 chip+无数据计数+N=0 兜空态。shared `NutrientLevelFilter`（7 单测）+VM combine 拆层。三门禁。
5. ✅ **采购清单·家人忌口标注**（`4b119fa`·商业#4）：采购项是某家人个人忌口→琥珀"X 忌口·为其他家人采购"。复用 #1 `gatherConstraintsForMember` 基建（只个人忌口防噪音）。小改自审。

> 上一会话已交付（历史，见 git）：首页下一餐卡 v2、阶段3 匿名统计全链路（友盟）、忌口具体食材（加法）。

## 四、⏭ 下一步（无人值守自选推进·每步走全套+读输出验证构建）
1. 🔜 **下一个功能候选**（自选其一开工）：
   - **中式食材库/预设菜扩充**（#5·差异化地基·持续）：以《中国食物成分表》补覆盖率+扩预设菜；分片后台 agent 联网核准营养（**守不编造红线**·标免责·引用完整性校验+`:shared:testDebugUnitTest`）。
   - **#6 人群适配红绿灯**（M）：食材详情"三高/痛风慎食·孕产适宜"红绿灯，协同 #1（可复用逐成员评估思路）。需 Apple-UX。
   - **#9"换更适合的菜"替代推荐**（Yuka 式·P2）：详情/推荐"这道嘌呤偏高→换一道更适合的"，复用换一换+忌口/病种基建。
2. **两个护城河的 Phase2**（可与上并行）：①红绿灯/病种视角**列表逐项徽章**（记菜/选菜·需大批量评估缓存化）②详情单成员场景全家并集 avoidNames 并入个人忌口。
3. 阶段5 营养补全+药膳一期+热量国标 DRIs → 阶段6 AI 框架（放开限制会诊+AI 对话，方案已出）→ 阶段7 其余 P1。

> **真机待验（攒一起·装新包·本会话新增 ⑤–⑨）**：
> ⑤**成员编辑页全屏三段**（资料/健康/忌口切换·滚动保活·未保存返回守卫·忌口/健康徽标计数·**异步查名窗口内点保存不清空既有忌口食材**）
> ⑥**成员化红绿灯详情页**（≥2 成员菜品详情"适合谁吃"逐家人红黄绿差异/全绿一行/排序/替代全家并集行）
> ⑦**病种视角详情页**（有登记病种的菜品详情"关注指标"块·数值+占每日上限%/定性·1-2 平铺·≥3 切换器·缺数据"暂无数据"）
> ⑧**营养表三级筛选**（我的·食材营养表右上 Tune→按 GI/钠/嘌呤筛低中高·多选·摘要 chip 清筛·无数据计数·N=0 兜空态）
> ⑨**采购清单忌口标注**（有个人忌口的家庭·采购清单对应食材下显琥珀"X 忌口·为其他家人采购"）
> 历史批：①首页单菜引流卡 ②首启隐私合规门 ③友盟上报（需网络）④忌口具体食材 + 餐次分类/多人关注等。
> 接手：工作区干净、全 push；**下一步自选（中式库扩充 / #6 人群适配 / #9 替代推荐）**。构建务必读输出确认 BUILD SUCCESSFUL。**待用户补**：隐私政策法务复核、友盟依赖上架前钉版本、挑一批真机验证 ⑤–⑨。
