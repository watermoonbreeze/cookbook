# 家族化统一升级·专项决策与进度（本会话建立 2026-07-20）

> 用户确立：**家族化是必须闭环的专项**——每页做完即测；关联页做完做联动验证（操作逻辑/界面体验/数据流转）；**全部完成后至少 5 轮回测 + 多方审核**；整体测完才继续其他待办。基调总纲见 `feature/App操作基调_设计系统.md`。

## 一、用户已拍板的 4 个推进决策（AskUserQuestion 2026-07-20）
1. **推进节奏**：全速铺 P2→P3→P4，不等真机验（但下方载体阻断使实际按安全落法调整）。
2. **P2 食材编辑页载体**：升级为路由页（统一 Snackbar·一步到位）——**但发现架构阻断（见二），实际落法调整**。
3. **草稿持久化**：完整表单草稿持久化（所有字段·rememberSaveable + 自定义 Saver）。
4. **真机验证节奏**：攒 2-3 页一批，推 git 后用户远程一次性验。

## 二、载体路由化的架构阻断（本会话读码发现·关键）
`IngredientPickerScreen` 一体两用：既是**食材 Tab 落地页**（有 nav），又是 `asDialog=true` 的**全屏 Compose Dialog 选择器**（selectionMode，选食材进菜品/餐食）。食材编辑器从两种模式都能打开。
- **阻断**：从 Compose `Dialog{}` 内 push nav 路由会异常；且此时全屏 Dialog 会**遮住** MainScaffold 的统一 Snackbar 宿主（现有代码正因此在 dialog 内用 Toast）。
- **推荐落法**（无人值守·凡拍板用推荐方案）：先抽共享 `IngredientEditorContent`；Tab 入口做真 nav 路由页（得统一 Snackbar）；选择模式内保持原地全屏 surface 复用同一 Content（Snackbar 被遮→Toast 占位·架构限制）。**载体完全统一需先把"选择器本身"路由化（更大重构·易砸核心选食材流程）→ 拆为紧随子批**。

## 进度快照（2026-07-20）
- **P1 菜品编辑页**：✅ 家族化落地 + 闪退根因(inline Column 内 `return@Column`)已修(`c82336c`)。真机待验四链路。
- **P2 食材编辑页·增量一**：✅(`757db5e`) 单 moreExpanded 拆 4 独立折叠段(营养数值/更多信息/做法说明/调养建议)+去营养大类必选红字+营养 Decimal 键盘+CareRuleEditor 去重复标题+新增 `FoldSection` 助手(if/else 无提前 return·守崩溃红线)。真机待验。
- **P2 增量二**：✅ 已落地(2026-07-20·androidApp 构建绿+shared 单测绿)——
  - 单件克重上移基础卡(`showPieceGram`=计件单位或已有值时显·重量/体积单位 g/kg/两/斤/ml/L/勺 不显·`WEIGHT_VOLUME_UNIT_NAMES` 按名判)；别名/图片下沉「更多信息」折叠段(预设食材无折叠→留基础卡)。
  - `EditorSection`→`InsetGroup` 白卡(页面灰底·4 折叠段各包白卡)；内联 `TopAppBar`→`AppTopBar`；自绘 `confirmDiscard`→`rememberUnsavedGuard`；`FoldSection` 加横向 16 内距适配卡内。
  - **完整 `rememberSaveable` 草稿**：文件级 Saver(`StringListSaver`/`LongSetSaver`/`StringSetSaver`/`GroupSaver`/`CareRulesSaver`)；营养字段改按 `hydrated` 守卫 `LaunchedEffect` **只首次水合填、进程被杀恢复不覆盖草稿**(VM 已确认 editorLoading=false 与 editor* 原子更新)；`editedN` 集合替代 `mutableStateMapOf`。
  - §五质量阻断：①保存失败错误条上移顶栏下**固定可见**(errorContainer 卡) ②保存成功统一反馈(`IngredientPickerScreen` LaunchedEffect·Tab=Snackbar/选择态=Toast 降级) ⑤`ImagePickerButton.onProcessingChange`→`imageProcessing`→压缩中禁保存+"图片处理中…"；营养 Decimal 键盘沿用。
  - **真机待验**：编辑既有食材不崩(守崩溃红线·if/else 平衡)+新建/保存并继续+旋转/进程杀草稿不丢+单件克重条件显+保存成功反馈。
- **P2 增量二·剩余(留后)**：🔴**载体路由化**(全屏 Dialog→路由页·选择器嵌入阻断见「二」·风险子批·上下文充裕时做)。
- **崩溃兜底**：✅(`ceed26a`) CrashActivity 友好界面+CrashReporter 上报抽象层。真机待验。

## 三、当前进度 / 阻塞
- **P1 菜品编辑页**：`0d0f6dd` 首版**编辑态点开即闪退**（用户真机复现：首页餐次→详情→编辑）。已回退到良好版 `fef91fd`（commit `a461b8d`·构建绿·已 push），恢复功能。**P1 转为家族化专项第一页待重做**，重做前须先定位崩因（google_quality_engineer agent 诊断中）。
- **共用件风险**：`ImagePickerButton`（含新增 coverStyle）为**菜品/食材编辑共用**；崩因若在其中，P2 食材编辑页同受影响 → **诊断结论出前不盲铺 P2**。

## 四、P2 食材编辑页落地要点（§四/§五·待做）
- 现状：`IngredientEditorDialogs.kt` 的 `IngredientEditorDialog`（全屏 Dialog·单 `moreExpanded` 折叠）。
- 目标：基础卡(名称*+营养大类chip+单位+单件克重[仅计件条件显·从营养区上移]) → 拆 4 独立折叠段(营养数值/更多信息[别名/图片/其它分类]/做法说明/调养建议) → InsetGroup 白卡视觉 → 完整 rememberSaveable 草稿 → rememberUnsavedGuard → 底部 FormBottomBar。
- 质量 5 阻断（§五）：①保存失败错误顶部固定可见 ②保存成功统一反馈(编辑器关闭后由 picker 页发 Snackbar) ③进程被杀完整草稿 ④克数 remember(key) ⑤图片压缩中禁保存(ImagePickerButton 外露 processing) + 营养 KeyboardType.Decimal。
