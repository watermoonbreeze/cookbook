# 2026-06-06 食材最近使用排查前置上下文

## 任务编排

- 模式：标准级别 / 缺陷排查修复模式。[AI生成]
- 原因：用户反馈食材选择中的“最近使用”不准确，需要检查 SQL 查询、Repository、ViewModel 和测试覆盖。[AI生成]

## 关注点

- “最近使用”目前是虚拟分类，不写入 `food_category`。[AI生成]
- 预期应展示最近被餐食引用过的食材，并按最近引用时间倒序。[AI生成]
- 需要确认排序字段是否只用 `meal_record.created_at`，以及同一天/同一保存时间时是否缺少 `date/meal_time/id` 作为排序兜底。[AI生成]
