# 会话快照：P0存储合规 / 备份导出 / 双设备同传 / 端侧自测（2026-07-09）

> [AI生成] 本会话大块工作的结论性快照，供跨会话续接。当前进入**无人值守**：审核+测试+优化。

## 已完成并提交（部分未 push）
1. **AI 推荐打磨**（前段）：删除指定日期餐食；周期计划"配了AI以纯AI为准、缺口补规则(fromRule标注)"；每餐2~5道随机；进AI推荐页配了模型不自动推荐(手动"开始推荐")；规则/兜底补充如实标注。
2. **P0 存储合规**：数据从 `/sdcard/cookbook`(公共+MANAGE) 改 app 专属 `getExternalFilesDir/cookbook`；移除 MANAGE_EXTERNAL_STORAGE/WRITE/READ/legacy + 权限门禁页；`CookbookStorage.init(context)` 于 Application.onCreate 存 app Context；开发阶段不迁旧公共数据。
3. **图片相对路径**：`ImagePickerButton` 存文件名；`StoredImage.resolveImageFile` 按当前 img 目录解析(历史绝对路径存在则直用，否则按文件名自愈)。
4. **完整备份**：`.ckbk`(zip)= db(+wal/shm)+img全部+manifest(schema版本校验)；restore 解压覆盖；`exportTo/importFrom` 流式供 SAF。
5. **SAF 导出/导入**：MineScreen 备份弹框「导入文件」(OpenDocument)+每条「导出」(CreateDocument)。
6. **双设备局域网同传**：`DeviceSyncViewModel`(ServerSocket 发送端+校验码握手, 接收端 importFrom)；`DeviceSyncDialog` 发送/接收两档；二维码扫码(zxing)自动填入；`PortraitCaptureActivity` 固定竖屏；声明 CAMERA 权限→拍照前加运行时申请。
7. **端侧设备自测 Step1**：`DeviceAiCapability` 按内存/CPU/arm64 预估流畅度等级(流畅/可用/偏慢/不建议)，AI设置端侧档下「测试本机能否流畅运行」。

## 待确认队列（无人值守不做，需用户/真机）
- **端侧 AI Step2**：接 native 运行时(建议先 MediaPipe LLM Inference `tasks-genai`，后迁 LiteRT-LM)+模型导入/下载(不打进APK)+实装 `OnDeviceAiRuntime.complete()`+自测升级实测。需真机+大模型，无法在此验证。
- **真机验证**：P0 重装、备份导出/导入、双设备同传(两台同WiFi)、拍照(CAMERA授权)、删除一致性、图片持久化。
- **iOS shared 编译**、**selectionMode 完整拆分**(高风险)：暂缓。

## 关键约束/红线（本会话涉及）
- 改 Manifest 权限后必须**重装 APK**。
- 声明 CAMERA 后 `ACTION_IMAGE_CAPTURE`(TakePicture) 也需运行时授予该权限，否则拍照失败——已在拍照前加申请。
- KDoc 注释里出现 `/*`(如 `img/*`) 会被 Kotlin 当嵌套块注释起始 → 编译"Unclosed comment"，禁用。
- 图片存相对文件名(非绝对路径)，跨设备/备份才可携带。

## 关键文件
- `shared/androidMain/.../platform/CookbookStorage.android.kt`、`BackupManager.android.kt`、`DatabaseDriverFactory.android.kt`
- `androidApp/.../ui/sync/`(DeviceSyncViewModel/Dialog/SyncQr/PortraitCaptureActivity)
- `androidApp/.../ai/DeviceAiCapability.kt`、`ui/component/StoredImage.kt`/`ImagePickerButton.kt`
- 方案文档：`.ai-context/docs/feature/备份导出与双设备同传方案.md`、`端侧AI能力接入方案.md`
