# Bug修复-3393 · 任务前上下文快照

- 需求：按既定优先级无人值守处理 `Bug修复-3393`，修复成员设为不在场后今日营养卡成员选项卡全部消失；后续继续 `Bug修复-2119`。
- 模式/级别：BLUEPRINT · 深度任务（成员缺席状态影响家庭与营养两个功能，需确认数据流、补最小诊断、冻结实施合同、测试和真机验证）。
- 已知状态：`BLUEPRINT_STATE.md` 顶部批次均为 `TURN=NONE`；本批尚无条目或蓝图。2026-08-27 已 `git pull --ff-only`，工作树干净。
- 计划：Reality Verification（先审日志覆盖）→ 冻结 BLUEPRINT-LITE/FULL（按风险结论）→ 状态握手 → 最小实现与单测 → 构建/质量审查 → 真机项/项目文档/会话交接。
- 预期范围：`F-NUTRITION`、`F-FAMILY` 的 ViewModel/Compose 状态链路及其 Android 单测；可能新增最小安全 `BusinessTrace` 事件。禁止改 schema、营养计算口径、成员缺席持久化语义、DI、导航及无关 UI。
- 风险/待验证：不能将不在场成员纳入营养分摊；成员列表在所有成员缺席、查看指针失效、异步更新时必须有唯一、安全的选择状态；不记录成员名、日期或摄入数据明文；真机验证后置且不得伪造 PASS。
