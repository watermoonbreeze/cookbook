# 2026-08-27 · 待办分类与文档治理快照

- 需求：将全部未完成待办按大类归并并依次完成；所有真机验证统一置于所有代码/文档批次之后。
- 模式：BLUEPRINT；标准任务。当前首批为纯文档治理，按项目蓝图协议降级为 L3（不改产品代码、运行时、schema、依赖或用户行为）。
- 已知状态：`BLUEPRINT_STATE.md` 顶部已验收 Bug-2119，`TURN=NONE`；工作树开始时干净。唯一真机清单为 `真机待验证清单_202608271041.md`，其中项目运行时项均保持 `PENDING_DEVICE_VERIFICATION`。
- 分类：A 文档治理（横轴陈旧、生成视图配置错误、Feature 回写欠账）；B 已有方向的功能实施（须一项一蓝图）；C 待产品/UX/健康口径决策（只登记，不擅自实现）；D 真机/OEM 依赖验证（全部最后统一执行）。
- 首批范围：只纠正项目地图文档的事实锚、生成视图配置与已发生提交的功能回写；不改代码、功能语义、蓝图协议、数据库、真机清单状态或历史提交。
- 风险：生成段不得手改；不得将静态/文档检查冒充运行时或真机通过；历史回写需按代码与提交事实，不可凭待办文字推断。
- 当前进度：已清除 Feature 历史回写欠账（9 个功能），重生成 `07`/`_INDEX`/功能路径索引生成段；修复 freshness 脚本将生成视图误判 `CONFIG-ERROR` 的配置冲突。01/03/04/20/21 已逐提交复核并回写：03 记录统一新增入口，04/20 记录餐食读写投影边界，21 复核流式会话、云端同意与诊断链。`feature_sync_check.py --backlog`、`--struct`、`--emit-index --write` 全部通过，`review_freshness.py --md` 六册均为 FRESH；真机项未改动，仍待最后统一执行。
