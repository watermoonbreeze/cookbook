# 待拍板队列（用户 2026-07-19 已批量定夺·仅留少数开放项）

> [AI生成] 无人值守期间攒下。**2026-07-19 用户已把绝大部分定掉**，本文件更新为最新。

## ✅ 已定(2026-07-19 用户拍板)——已实现或就绪实现
- **B1 忌口从严**：忌口放宽到含辅料、限量/调养仍守主料(组合非此即彼)。→ ✅已实现(`本轮`)。
- **R3 换默认模型 glm-4.5-flash + JSON模式**：key 同厂通用不用重设。→ ✅已实现(`本轮`)。
- **R6 换一换=均衡档**：首次云端预取多批+本地轮播+规则兜底·用尽再联网。→ 就绪待实现(方案 `换一换缓存策略方案.md`)。
- **口味画像喂prompt(R2)**：用户"按你的来"→做轻量 top-1 菜系汇总。→ 就绪。
- **餐次差异化**：用户"按你的来"→暂不做午晚硬规则(早餐保留关键词+软硬搭配)。→ 无需动。
- **热量达标**：用户"按你的来"+"参国标再斟酌"。→ ✅本轮已补红线(未成年/域防护);热量口径参国标斟酌记待办↓。
- **个人忌口设置**：用户定=**按分类选**(肉/蔬/菌/豆/奶/口味调味类+鸡羊牛子类·不选具体食材)+对调料真避开+含即命中。→ 就绪(方案 `个人忌口设置方案.md` 已改分类版)。
- **多人关注重构**：早已定=照 `多人关注设计方案.md`。→ 就绪(先 Apple-UX 布局)。
- **AI推荐放开限制定向**：云端档=只给健康+忌口·食材菜品全云端推+自动建库;本地档=从本地取。→ 记入待办(会诊出"AI模型推荐待办方案")。

## ✅ 已定(2026-07-19 第二轮·用户拍板)
- **A组1 首页推荐 D1–D10**：✅ **全按方案推荐**执行(升级入口卡为推荐卡/3道/整行详情+行尾记/记进编辑页/卡底免责/不就地切餐次/新用户示例/依据说明/色系墙下移/首页预览+全页全量)。
- **A组2 账号首启同意策略**：✅ **默认开+可关**(非询问式·样本更大·仍透明可关+不采健康明细)。
- **A组3 换一换预取组数**：✅ 用默认(9组·pending 待埋点)。
- **C组8=A2 孕期/哺乳不评热量**：✅ **以健康状态名称 gate**——成员启用 `care_pregnancy「孕期」`/`care_lactation「哺乳期」`(必要时含 `care_infant「婴幼儿(0-2岁)」`)时热量评级中性不评(Mifflin 仅普通成人)。care 分类**确认存在**。做热量相关阶段落地。
- **C组9+C组11 合并=热量/活动档国标对照**：✅ **要参国标·对照 RNI 并列+标参考**。把热量口径(现 Mifflin BMR)与活动档(现默认 MODERATE×1.55·对慢病中老年偏高)都对照《中国居民膳食营养素参考摄入量(2023版 DRIs)》能量 RNI + PAL 分级(轻/中/重)并列展示。**DRIs 确切系数/RNI 须分片 agent 联网核准**(健康红线·不编造权威数值)，标"参考·非医嘱·据中国营养学会 DRIs"。
- **C组10 🔴RANDOM 崩溃**：✅ **必修**(用户:食材库后续逐步增大)。RANDOM 模式 `IN` 全食材展开→SQLite 999 变量上限崩溃→改无 IN 候选查询。→ ✅**已修**(2026-07-21 算法打磨轮·commit `34e49d3`)：新增 `selectAllDishesForRandom`(无 IN·纯查询无迁移)+`DishRepository.listAllDishMinis`，RANDOM 走全库 DishMini(语义等价:整库都可做)。构建+单测绿。**真机待验**(RANDOM 推荐在大食材库不崩)。

## ⬜ 仍开放(低优先·记待办·看情况做)
### 🆕 2026-07-21 AI 推荐算法打磨轮·会诊出的需拍板/待跟进项（3镜头 algorithm_engineer + Google 质量审）
> **✅ 用户 2026-07-21 已逐项拍板·落地跟踪见 `context_memory/算法拍板落地_2026-07-21.md`**：F4=b已实现/F5=D4已实现(`6142aba`·结合营养线重评)/A#4=最小化做GI一维或不做(分析已出待拍)/A#6=A立即做+B排A2后+C不做(方案已出待拍)/C#F1=✅已实现(supplementComposition)/B#6=✅已实现(封顶)/C#F2=实施方案就绪待确认/C#F3=✅核实RandomMode已覆盖+可选空态CTA。以下原始记录留档：
> 本轮已落地"安全自主"批(RANDOM修复/MealCompositionScorer抽取/MMR重油族/一餐主料不重复/组合完整性喂prompt·commit `34e49d3`+`15ba793` 区间)。以下为**需你拍板或涉产品口径/新字段/身体数据**的项，未擅自做：

- **慢病软降默认微开(F4/F5)**：现 `chronicDiseaseNutrition` 默认 0(仅"偏营养"风格开)是**刻意防操纵设计**(静默默认开有操纵感)。oracle 镜头指出"痛风家庭用默认综合风格时高嘌呤菜(care 未覆盖但关键词命中)不额外靠后"。**两选**:(a) 综合/偏熟悉给很小默认值 0.2(弱靠后·量级0.14远低于 avoid5.0)；(b) 保持默认关+对已登记痛风/糖尿病用户在推荐页一次性引导"想让高嘌呤/高GI菜自动靠后?→切偏营养风格"。**oracle 与我都倾向 (b) 知情引导**(守透明准则不擅改默认)。**需你拍板 a/b**。
- **周计划(PeriodPlanner)接入 GI/嘌呤梯度(F5)**：单餐 evaluate 有慢病软降(高GI/嘌呤),周计划 score 无(口径不一致·仅弱 LIMIT_PENALTY)。~~重油族去重~~✅**已做**(2026-07-21 算法打磨第2轮·`PlanDish.cookingMethodNames`+`usedHeavyToday` 按天去重·与单餐 MMR 同 cookingHeaviness 口径)。**剩 GI/嘌呤梯度**：需给 `PlanDish` 补 GI/嘌呤命中字段+按 F4 同口径开关(默认关否)。**需 F4 定口径后做**。
- **菜级营养字段打散高钠/高GI(A#4)**：现 MMR 重油族只是烹饪代理,真"避免一批全高钠/全高GI"需给 `DishCandidate` 补菜级营养摘要(highFat/highSodium/highGiStaple·由 NutritionLevelEvaluator 派生·守免责)。改数据契约+全构造点+依赖营养覆盖率。**需拍板是否值得**。
- **当日能量目标(A#6)**：营养互补只调宏量占比结构、不看总量是否超标。控总能量需身体数据 DRI(与 C组9 热量档 DRIs 联动)。**需身体数据+产品口径**。
- **模型输出 validate 后补菜(C#F1 强版)**：本轮做了 prompt 引导(软);强版=validate 后对"两荤无素无主食"的餐从候选补一道(算法二次干预模型结果)。**属"算法是否二次改模型输出"口径·需拍板**。
- **recommend 因子封顶(B#6)**：`recommend * recommendHits.size` 是唯一未封顶因子(onHandMain/missing 已封顶),多味调养主料可累加。封顶 min(,3) 与其余一致、但改默认排序、价值低(仅≥4味调养主料罕见菜)+跨镜头张力(营养镜头或主张不封顶)。**需拍板**。
- **早餐软硬搭配透传单餐(C#F2)**：周计划有"偶数位硬/奇数位软",单餐 Orchestrator 无→糖尿病早餐可能推"白粥+豆浆"两软食无蛋。需给 `DishCandidate` 透传 breakfastSoft/含蛋白信号(现只 PlanDish 有)。**需新字段**。
- **无库存单餐兜底(C#F3)**：`evaluate` 首行剔除"没用到在手食材的菜";无库存/"想吃什么"模式若走同 evaluate 会空/退化。**需先核实现有数据源有无独立入口**(可能已存在),确认漏洞再改。

## 就绪待实现队列(方案+拍板都齐·可直接做)
1. R6 换一换均衡档缓存(第一批药膳本地重排零风险先做)。
2. R2 口味汇总喂 prompt(轻量)。
3. 个人忌口设置(按分类·新表 member_avoid_category·先 Apple-UX 细化 chip 布局)。
4. 多人关注重构(照方案·>15文件·先 Apple-UX 布局)。
5. 搜索按餐次(复用 MealSlotMatcher)。
6. 阶段A3 营养数据补全(权威核准·web agent)。
