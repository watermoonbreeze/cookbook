# 待拍板队列（无人值守期间攒下·用户早上定夺）

> [AI生成] 2026-07-18 夜 无人值守。这些项**涉健康口径/模型选型/成本/产品方向**，我不擅自决定，先记这里、继续做可自主项。每条给我的推荐。定完我落地。

## 来自"膳食推荐会诊"(algorithm_engineer + AI会诊)

1. **B1 忌口漏判是否放宽到辅料**（健康口径·会改已拍板的"只按主料判定"）
   - 现状：忌口/限量/调养只按**主料(is_main)**判定，无主料标记的菜不触发忌口（"宁可不误报"）。
   - 会诊建议：**忌口(avoid)方向从严**——放宽到全部非调料食材（漏判代价高：该避的没提示；误报代价低：本就列出+标红不隐藏由用户判断）；限量/调养仍守主料（避免"木耳50g显忌木耳"误伤）。
   - **我的推荐**：采纳（忌口从严更安全、符合家庭 App"告知不替决策"）。但它反转了你 2026-07-16 拍板的"剂量占比门槛"，故等你确认。

2. **R3 换云端默认模型 `glm-4-flash`→`glm-4.5-flash` + 开 JSON 输出模式**（模型选型·用户可见默认变更）
   - 事实：现默认 `glm-4-flash` **不支持** `response_format:json_object`；同免费档 `glm-4.5-flash`/`glm-4.7-flash` **支持**且更强。现在解析靠正则截 `{...}`，脆。
   - **我的推荐**：换 `glm-4.5-flash`（免费·更强·支持 JSON 强约束→解析成功率↑），保留老型号可选。JSON Schema 强约束(minItems 等)留二期。
   - 待你确认"是否换默认模型"。

3. **R2 口味画像喂 prompt 的粒度**（token 成本 + "操纵感"边界）
   - 已自主做**保守版**：候选加"N天前吃过"标签（帮模型避近吃）。
   - 待定：是否再加"你家最近偏好：川菜/红烧"一句汇总（喂太重会让推荐固化在熟悉口味→操纵感）。**我的推荐**：加轻量 top-1 菜系汇总即可，别逐候选喂强度。等你定。

4. **R6 "换一换"缓存策略**（成本）：首次云端拿 N 组后本地轮播 vs 每次调云端。**我的推荐**：先做"药膳过滤切换不重调云端"(纯本地重排·已可自主)，换一换缓存等你定"新鲜感 vs 成本"。

5. **A4 餐次差异化**（产品口径）：午/晚餐"清淡 vs 丰盛"无权威共识。**我的推荐**：暂不做硬规则，早餐保留关键词筛+软硬搭配即可。等你有明确偏好再说。

## 来自"每日热量目标+达标评级"会诊(A4·已实现·2026-07-14)
6. **A2 孕期/哺乳成人不评热量达标**（健康红线·需 care 检测+拍板）
   - 现状：A4 热量达标已实现且质量高；本夜已修 **A1 未成年(<18)不评热量**(Mifflin 仅成人·已 push)+B1/B2 防护。
   - 剩 **A2**：孕期/哺乳期成人按常规 TDEE 会被判"偏低"(实际应+285~500kcal)→错误焦虑。**我的推荐**：孕期/哺乳成员**不评热量达标**(同缺数据退化·不对特殊人群下能量结论)。需：`FamilyMember.careCategoryIds` 里检出"孕期/哺乳"care 类别 → HomeViewModel 给该成员 skip 热量评估。
   - **待你确认**：①孕期/哺乳在 care 分类里的确切名称/code（特殊人群 282 条 care 规则里有）②是否按此 gate。确认后我落地(FamilyRepository 暴露 focus 成员是否孕期/哺乳 + evaluator gate)。
7. **B3 默认活动档**（调参）：现默认 MODERATE(1.55)对久坐家庭偏高、易普遍判"偏低"。真机验收若普遍偏低→降 LIGHT(1.375)。等真机数据。

## 其他（低优先·记录）
- A3 营养密度维度(纤维/钾/钙富集度)：**依赖营养数据补全**，数据到位再开。
- onHandMain 全量归一化(C2)：收益边际，低优先。
- 缺数据菜"营养因子中性反占优"(E2)：先观察 A2 修复后分布，暂不动。

## 已自主落地(不需拍板·本夜已做/在做)
- ✅ 算法批 A1(组合级荤素/主食搭配)+A2(营养互补首餐绝对均衡)+D1(MMR荤素维度)。
- 🔄 云端 AI 批 R1(忌口剔除出模型可选集·正确性红线)+R5(prompt禁医疗断言免责)+R2保守(近吃标签)。
- 🔄 负反馈"踩"(§9.20 UX规范已出·长按ActionSheet+就地灰态+撤销·需DB加列+负因子)。
