# 一期推进总规划（2026-07-18 用户全量拍板·无人值守全速跑）

> 用户定：**全做**，我排顺序、全权处理、多方会诊；标准无人值守流程=每完成一项 → 保存上下文 + 总结经验 + 飞书通知 + 继续下一个。北极星=健康推荐餐食。

## 已拍板结论（本轮范围+口径）
1. **本轮四项全纳入**：①每日热量目标+达标评级 ②多人关注重构 ③药膳维度一期最小集 ④自定义餐次类型。
2. **热量达标口径**：用健康档案身体数据(身高/体重/年龄)自动算每日热量目标(BMR)+把"热量达标度"纳入健康评级；标"仅供参考·非医嘱"守红线。
3. **多人关注**：照 `多人关注设计方案.md` 已出方案直接做（多选关注+"当前查看"指针·非聚合·下限1·指针今日卡与报告共用·不改表 is_focus 可多·零 schema 迁移）。
4. **推荐多样性**：综合风格也开 MMR 打散（温和 λ），不再仅偏新鲜。
5. **放开 AI 推荐限制**（AI 自由创菜+自动建食材）：**暂只记待办**，启动时先 `ai_engineer`+`algorithm_engineer`+健康会诊出 phased 方案（红线：忌口校验/营养估算不编造/去重+确认入库/结构化输出）。

## 执行顺序（三轨交织·以北极星牵引·逐项走门禁+构建+存档+飞书+commit+push）
- **阶段A 膳食推荐质量(先做)**：A1 AI推荐会诊(ai_engineer+algorithm_engineer+健康)出"离线规则质量+prompt优化+P4画像/缺口喂模型"清单 → A2 落地 → A3 营养数据补全(65%→更高·分片agent) → A4 每日热量目标+达标评级。
- **阶段B 算法质量+性能地图**：B1 负反馈"踩"(算法3项最后一项·需Apple-UX踩位置+加列迁移+负因子) → B2 algorithm_engineer出复杂度/热点地图+埋点(预防式) → B3 逐菜推荐理由。
- **阶段C UX核心流**：C1 Apple-UX走查"推荐→选菜→记一餐"主链落地 → C2 撞名提示scenario1+散落🟡。
- **阶段D 大功能**：D1 多人关注重构(照方案·>15文件·先Apple-UX布局→编码→审查) → D2 药膳维度一期最小集(涉schema) → D3 自定义餐次类型(涉meal_type source列迁移)。
- **阶段E 收尾**：真机验证批 → 性能到阈值优化 → AI协作经验总结 →(放开AI限制方案·已defer)。

## 已完成(本次会话·截至进入本规划前)
- 算法：时间衰减(FRESH)+口味画像(`d85ede2`)。UX：搜索就地新建(`f24660a`)、AI推荐仅手动刷新(`08e0a9e`)、AI推荐规则说明图标(`343a78e`)。经验沉淀(`8ab4b9d`)。
- 新增公共角色：`algorithm_engineer`(算法/营养/健康/膳食/基础四镜头)、`ai_engineer`(提示词/LLM应用)——可组"AI推荐会诊团队"。

## 标准无人值守流程(用户2026-07-18确认·每项都走)
门禁(Apple-UX/copywriter 按需 + 多方会诊) → 编码 → **强测试(用户强调)**：多环境、**正向+逆向都测**、边界/异常操作(防坏数据/坏界面)、构建+单测 → Google 代码审查(阻断必修复) → 保存上下文(本文件+context_memory) → 总结经验(experience) → 飞书通知 → commit(`[unattended]`) → push → 继续下一个。

## ⭐ 用户专项待办(不要忘了)
- **"AI 模型推荐待办方案"**：用户要求经**多方算法工程师会诊**(+ai_engineer/健康/copywriter)后**出方案(仅文档·待办方案,不实现)**。内容=云端/AI 模型推荐的优化路线，含"放开限制(AI自由创菜+自动建食材)"phased 方案(红线见待办总览该项)。**时机**：当前 A-E 队列做完后出；出完继续算法+UX 深挖优化(持续)。**多方算法会诊是硬要求。**

## 无人值守进度(2026-07-18 夜·逐批 push)
- ✅综合风格多样性(核对代码早已全开·仅修旧注记)。
- ✅**批1 算法** A1(组合级荤素/主食搭配)+A2(营养互补首餐绝对均衡)+D1(MMR荤素维度)——过审查+单测·commit已push。
- ✅**批2 云端AI** R1(忌口剔除出模型可选集·正确性红线)+R5(prompt禁医疗断言)+R2(近吃标签)——过审查+单测·commit已push。
- ✅**批3 负反馈"踩"**(算法3项全完)：独立 `dish_dislike` 表(仿 dish_favorite·`27.sqm`·零牵连 dish 表)+查询+DishRepository+gather/gatherForPlan 过滤+VM setDisliked+DishRow 长按 ActionSheet+灰态+Snackbar撤销+菜品详情恢复闭环(§9.20)。shared 单测(DishDislikeTest)+androidApp 构建过·Google 审查中→过后 commit。
- ✅**A4 每日热量目标+达标评级**：会诊发现**早已实现**(2026-07-14·质量高);本夜补健康红线 A1(未成年<18不评·Mifflin仅成人)+B1(target≤0中性)+B2(离谱身体数据域防护)+单测。**待拍板** A2(孕期/哺乳成人不评热量·需care检测)。
- ✅**阶段B2 性能地图**：`性能优化推荐链路地图.md`(algorithm_engineer会诊)——链路健康(N+1已消),热点=全表/全窗口聚合(需缓存)。**🔴发现正确性隐患**:RANDOM 模式 `IN` 全食材展开→SQLite 999变量上限崩溃(食材库过千时·建议尽早修·已记待办⑤)。缓存/埋点均记就绪待办。
- **下一步**：🔴RANDOM IN 崩溃隐患修(正确性·尽早) → 阶段A3营养数据补全(web agent权威核准·data活) → 阶段B3逐菜理由/MealCompositionScorer技术债 → 阶段C UX核心流(推荐→选菜→记一餐主链·Apple-UX) → 阶段D 多人关注/药膳/自定义餐次(大·先设计门禁) → 阶段E收尾+AI推荐待办方案(多方算法会诊)。
- 待拍板见 `待拍板队列.md`。每项完成更新本文件+待办+经验+飞书。
