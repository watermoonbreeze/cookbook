# 上下文快照：五项调整 + 家庭档案 P1（2026-07-15）

> 承接健康膳食方案后，完成用户 5 项调整 + 家庭档案 P1 全量落地。均已 push gitee master。

## 五项调整（全部完成）
1. **色系墙今天格显示日期**：`NutritionWall` 今天格无论有无餐食都显示日期(足强度色)。
2. **补菜品操作步骤**：364 道原无步骤的菜补 3~6 步家常做法(4片agent并行→合并校验)，516 道全有步骤。
3. **补食材营养 + 详情热量**：营养 103→440(100%覆盖，4片agent)；配料用量 842 行补齐(4片agent，只用克/毫升/勺/两保证折算)；516 道菜热量全部可算(区间41~2815,均值548)。营养/用量为AI家常估算、review=pending。
4. **单位库自定义单位**：`measurement_unit` 加 selectByName/softDeleteUser 查询(免迁移)；`ensureMeasurementUnit`；编辑器 UnitDropdown 加"＋自定义单位"入口。
5. **自定义食材归类**：方案文档 `自定义食材归类方案.md`(P1: food_group 列+营养大类单选关键词预选+映射分类树)。未实现，待后续。
- 另：拆分 营养色系/热量数值显示 两独立开关(`CALORIE_NUMBER_ENABLED`)；活动水平点选内联说明(`ActivityLevel.desc`)。

## 家庭档案 P1（全量落地，分3阶段）
- **Stage1 数据层**：`family_member`(身体数据/饭量系数/is_self/is_focus)+`member_care`(病种) 表 + `20.sqm`(纯新表) + `FamilyRepository`(ensureInitialized 幂等建「我」迁旧身体数据+旧病种/CRUD/setFocus/allEnabledCareIds并集) + `FamilyMember` 模型(defaultCoefficient 男1.2/女1.0/老0.8/小孩0.5) + DI + MainActivity 首启调用。单测绿。
- **Stage2 成员管理页**：`FamilyScreen`(成员卡/⭐设关注/增删改/编辑器含昵称·性别·身高体重年龄·活动水平说明·饭量系数预选·病种多选) + `FamilyViewModel` + 路由 FAMILY + 功能设置「健康膳食」分组(营养色系+热量数值+家庭成员入口，每日目标改标「我的」)。
- **Stage3a 忌口并集**：`allEnabledCareIds` = member_care 全员 ∪ user_health_profile 启用；`RecommendationDataSource` 忌口改用之。
- **Stage3b 达标按关注成员**：`observeFocusBody/observeFocusShare/observeFocusName`；DayMealCardView+HomeViewModel 用关注成员身体数据算目标、个人摄入=全家餐×饭量系数份额；设置"我的目标"同步成员「我」。

## 关键设计
- **忌口用并集(做菜决策) / 热量用个人切换(评估)** 二分主轴(见 `健康膳食功能设置与说明.md`)。
- 记菜仍全家不变(零操作)；个人摄入是派生估算(标注约/估算，非医嘱)。

## 回归
多轮增量 + clean 全量 + 全单测(含 FamilyRepositoryTest 默认成员迁移/CRUD/关注/忌口并集)均绿。

## 待办
- ⑤自定义食材归类 P1(food_group 列，与后续迁移一起)。
- 家庭档案 P2(记菜「谁没吃」缺席微调/统计切换页)、P3(分餐精确/成员口味画像)。
- MineScreen「个人健康档案」与家庭成员病种的展示口径统一(当前 union 已覆盖忌口，展示可后续收敛)。
- 可视化 UX 真机验证(色系墙/家庭档案/开关/单位)。
