# 功能路径索引（feature → 文件路径 · 快速定位省 token）

> [AI生成] 2026-07-26。**用途**：用户提需求/功能名时，先查本索引直接跳文件，避免 grep/glob/read 重新定位。**只列路径 + 一句话职责，不塞代码**。
> **约定**：`shared/…` = `shared/src/commonMain/kotlin/com/sxdbsm/cookbook/`；`android/…` = `androidApp/src/main/java/com/sxdbsm/cookbook/android/`。SQLDelight schema = `shared/src/commonMain/sqldelight/com/sxdbsm/cookbook/db/Cookbook.sq`（改表结构要加 `N.sqm` 迁移·见踩坑红线）。
> **踩坑细节**看 `CLAUDE.md 踩坑红线` + `experience/`，功能方案看 `feature/`。

## 🔧 维护触发（强制·防索引过时）
> **定位先查本索引**（用户提功能/需求 → 先查这里跳文件，别直接 grep/glob 重找·省 token）。**并在下列操作发生时、与代码改动同一 commit 同步维护本索引**：

- **触发时机（命中任一即更新）**：
  1. **新增**文件——Screen / ViewModel / Repository / UseCase / Engine / 真相源 / 领域模型 / 复用组件 → 在对应分区补一行（`路径 — 一句话职责`）；新功能模块无对应分区则新建分区。
  2. **重命名 / 移动目录**——同步改本索引对应行的路径（漏改=索引指错、比没索引更坑）。
  3. **删除**文件 → 删本索引对应行。
  4. **新增真相源 / 单一真相源**（多处依赖的核心）→ 除分区行外，补进文末「常用真相源速记」表。
- **操作**：改动落盘后回本索引核对对应分区行，增/改/删后**并入同一 commit**（提交信息可含"同步功能路径索引"）。
- **自检口令**：凡动了 `shared/ai|domain|data` 或 `android/ui` 下的**文件结构**（增删改名·非纯内部逻辑），提交前问一句"本索引要不要改？"。纯改某文件内部实现（不动文件名/职责）**不必**更新。
- **批量校准（可选）**：怀疑索引漂移时，`find shared/src/commonMain/kotlin android/src/main/java -name "*.kt"` 对比本索引查缺漏。

---

## 1. 记录（记一餐 / 食历 / 复用）
- `android/ui/addmeal/AddDayFoodScreen.kt` + `AddMealViewModel.kt` — 记一餐/编辑某日餐食主界面
- `android/ui/timeline/FoodTimelineScreen.kt` + `TimelineViewModel.kt` — 历史食历（时间线）
- `android/ui/home/HomeScreen.kt` + `HomeViewModel.kt` — 首页（今日卡+计划区+色系墙入口）
- `android/ui/component/DayMealCardView.kt` — 每日餐食卡（今日卡/食历/周计划共用·含餐次结构提示）
- shared 数据：`shared/data/repository/MealRecordRepository.kt`、`shared/domain/model/MealRecord.kt`

## 2. 食材体系（食材库 / 分类 / 属性 / care忌口 / 库存）
- 食材库 UI：`android/ui/ingredients/IngredientsScreen.kt`、选择器 `android/ui/picker/IngredientPicker*.kt` / `IngredientDetailSheet.kt` / `IngredientEditorDialogs.kt`
- shared：`shared/data/repository/IngredientRepository.kt`、`IngredientGroupRepository.kt`、`shared/domain/model/Ingredient.kt`、`IngredientGroup.kt`
- 分类归类：`shared/domain/FoodGroup.kt`（九大类·尾词classify）、`shared/domain/model/Category.kt`、`android/ui/picker/CategoryDialogs.kt`
- 属性标签体系：`shared/domain/FoodAttribute.kt`、`AttributeGuesser.kt`
- care忌口（属性层压制）：`shared/domain/IngredientCrowdCare.kt`
- 库存：`shared/data/repository/PantryRepository.kt`、`shared/pantry/PantryAllocation.kt`、`PantryPlanAnnotator.kt`、`android/ui/component/PantryHook.kt`
- 药膳/食养：`shared/domain/MedicinalFoods.kt`
- seed 数据（JSON in `shared/src/commonMain/resources/`）：`shared/data/seed/PresetDataSeeder.kt`、`SeedResourceLoader.kt`、`SeedUpdateCenter.kt`

## 3. 推荐（库存 / 随机 / 一周计划 / AI / 自由搭配）
- 单餐推荐（库存/随机）：`android/ui/ai/AiRecommendScreen.kt` + `AiRecommendViewModel.kt`；引擎 `shared/ai/RecommendationOrchestrator.kt`、`RecommendationDataSource.kt`
- 周期/一周计划：`android/ui/ai/AiPlanScreen.kt`(内 `AiPlanBody`) + `AiPlanViewModel.kt`；引擎 `shared/ai/PlanOrchestrator.kt`、`PeriodPlanner.kt`；已排周 `android/ui/weekplan/WeekPlanScreen.kt` + `WeekPlanViewModel.kt`
- 组合评分（荤素/主食/餐次差异·单一真相源）：`shared/ai/MealCompositionScorer.kt`
- 菜数/份量：`shared/ai/MealPortion.kt`；餐次 `shared/ai/MealSlot.kt`
- 自由搭配：`android/ui/pairing/FreePairingScreen.kt` + `FreePairingViewModel.kt`；`shared/ai/FreePairingEngine.kt`
- AI 云端/端侧：`shared/ai/AiRuntime*.kt`、`CloudModel.kt`、`GlmProtocol.kt`、`RecommendationPrompt.kt`/`Parser.kt`；`android/ai/CloudAiRuntime.kt`、`OnDeviceAiRuntime.kt`、`DeviceAiCapability.kt`；设置 `android/ui/ai/AiSettingsScreen.kt`
- 风格/口味/去重：`shared/ai/RecommendationStyle.kt`、`TasteProfile.kt`、`CuisineFamily.kt`、`CookingHeaviness.kt`

## 4. 营养评估（今日卡 / 色系墙 / 热量 / 摄入折算 / 营养线 / 趋势）
- **摄入折算单一真相源**：`shared/domain/model/IntakeCalculator.kt`（整份×eaten_ratio×成员share·三维正交·守恒）
- 今日营养卡：`android/ui/home/HomeViewModel.kt`(todayNutrition)；食用比例调整 `android/ui/home/EatenAdjustSheet.kt`
- 色系墙（膳食结构·不关联热量慢病）：`android/ui/home/NutritionWall.kt`；评级 `shared/domain/NutritionLevel.kt`、`NutritionInterpreter.kt`、`NutrientLevelFilter.kt`
- 热量/营养值：`shared/domain/model/Nutrition.kt`（resolveGrams/折算）、`shared/data/repository/NutritionRepository.kt`
- 周计划营养线（宝塔四层缺口）：`shared/domain/NutritionLine.kt`、`NutritionLineAdvisor.kt`；UI `android/ui/component/NutritionLineCard.kt`
- 营养趋势折线：`android/ui/component/NutritionTrendChart.kt`；宏量条 `android/ui/component/NutritionMacro.kt`、`DishNutritionLine.kt`
- 营养表：`android/ui/nutrition/NutritionTableScreen.kt` + `NutritionTableViewModel.kt`
- 饮食报告：`android/ui/report/DietReportScreen.kt` + `DietReportViewModel.kt`；`shared/domain/model/DietReport.kt`
- 热量数值开关等偏好：`shared/domain/model/Preference.kt`（PreferenceKeys.DEFAULT_*）

## 5. 健康判定 / 忌口引擎（数值层 + 属性层 + 成员化）
- **忌口/限量引擎**：`shared/ai/HealthRuleEngine.kt`（按主料判·调料转提示·多成员并集）
- 成员化菜品健康判定：`shared/ai/MemberDishHealthUseCase.kt`、`shared/domain/model/MemberDishVerdict.kt`
- 慢病罚分（推荐）：`shared/ai/ChronicDiseasePenalty.kt`
- 膳食结构权威真相源（宝塔/餐盘/三餐分配·份量）：`shared/domain/DietaryGuideline.kt`
- 主食判定/调料默认：`shared/domain/StapleFood.kt`、`SeasoningDefaults.kt`
- GI/嘌呤定性、营养推演：`shared/domain/NutritionGuesser.kt`、`DishNameIngredientGuesser.kt`

## 6. 健康档案（家庭成员 / 生命阶段 / 慢病 / 餐食系数）
- 家庭档案 UI：`android/ui/family/FamilyScreen.kt` + `FamilyViewModel.kt`；统计 `FamilyStatsScreen.kt` + `FamilyStatsViewModel.kt`
- **成员/餐食系数/缺席归一**：`shared/data/repository/FamilyRepository.kt`（portion_coefficient 归一·缺席微调）、`shared/domain/model/HealthMetrics.kt`（FamilyMember/BodyMetrics/portionCoefficient）
- 慢病档案：`shared/data/repository/HealthProfileRepository.kt`

## 7. 参考资料页（我的·下·透明/权威口径）
- `android/ui/reference/`：`DietaryReference.kt`+`DietaryReferenceScreen.kt`（膳食参考依据·份量口径）、`DataSourceReference.kt`+`DataSourceScreen.kt`（数据来源·联网核准必加）、`NutritionRuleReferenceScreen.kt`（营养怎么算）、`HealthConditionReferenceScreen.kt`、`HealthScienceReferenceScreen.kt`、`TcmReferenceScreen.kt`、`VitaminReferenceScreen.kt`、`UpdateLogScreen.kt`

## 8. 工具（厨房小助手 / 计时 / 采购清单 / 搜索 / 复用）
- 厨房/做菜模式：`android/ui/cook/CookModeScreen.kt` + `CookModeViewModel.kt`；步骤模板 `shared/data/repository/StepTemplateRepository.kt`
- 计时器：`android/ui/kitchen/CookingTimerScreen.kt`；服务 `android/kitchen/CookingTimerService.kt`、`TimerAlarm.kt`、`TimerAlertActivity.kt`；`shared/data/repository/CookingTimerRepository.kt`
- 采购清单：`android/ui/shopping/ShoppingListScreen.kt` + `ViewModel`；`shared/data/repository/ShoppingListRepository.kt`、`shared/domain/model/ShoppingList.kt`
- 全局搜索：`android/ui/search/SearchScreen.kt` + `SearchViewModel.kt`
- 菜品库/详情：`android/ui/dishes/DishesScreen.kt`、`android/ui/dishdetail/DishDetailScreen.kt`、`android/ui/newdish/NewDishScreen.kt`；`shared/data/repository/DishRepository.kt`、`shared/domain/model/Dish.kt`
- 收藏组合/复用：`shared/data/repository/FavoriteComboRepository.kt`、`shared/domain/model/FavoriteCombo.kt`

## 9. 导航 / 主题 / 通用组件 / 设置
- 导航：`android/ui/nav/MainScaffold.kt`（NavHost·所有路由 wire）、`Destinations.kt`（路由常量 Routes）
- 主题/配色：`android/ui/theme/Theme.kt`、`Palettes.kt`、`Color.kt`、`ExtendedColors.kt`
- 复用组件（§九交互模式库）：`android/ui/component/`（`AppTopBar`/`AppSearchField`/`CapsuleButton`/`SegmentedControl`/`InsetGroup`/`ActionSheet`/`MiniStepper`/`EmptyState`/`UnsavedGuard`/`FoldSection`/`AppSnackbar` 等）
- 功能设置（开关）：`android/ui/settings/FeatureSettingsScreen.kt` + `FeatureSettingsViewModel.kt`
- 我的：`android/ui/mine/MineScreen.kt` + `MineViewModel.kt`
- 首启/隐私/引导：`android/ui/onboarding/PrivacyConsentDialog.kt`、`android/ui/guide/FeatureGuideScreen.kt`、`android/ui/policy/`

## 10. 数据层 / 平台 / 同步
- SQLDelight schema：`shared/src/commonMain/sqldelight/…/Cookbook.sq` + 迁移 `*.sqm`（改表必加迁移）
- Repository 全在 `shared/data/repository/`（见各功能）；DI：`shared/di/SharedModule.kt`、`android/di/AndroidModule.kt`
- 偏好：`shared/data/repository/PreferenceRepository.kt`（小配置存 JSON 免迁移）
- 平台适配（expect/actual）：`shared/platform/`（`DatabaseDriverFactory`/`BackupManager`/`CookbookLog`/`Pinyin`/`Dispatchers`/`Uuid`）
- 双设备同步：`shared/sync/`、`android/ui/sync/`、`android/sync/SelectiveSyncBundler.kt`
- 埋点：`shared/analytics/`、`android/analytics/UmengAnalyticsSink.kt`
- 崩溃兜底/日志：`android/util/CrashReporter.kt`、`LogFileManager.kt`、`android/ui/crash/CrashActivity.kt`

---

## 常用真相源速记（改这些要格外小心·多处依赖）
| 真相源 | 路径 | 管什么 |
|---|---|---|
| IntakeCalculator | `shared/domain/model/IntakeCalculator.kt` | 摄入折算(整份×eaten×share) |
| DietaryGuideline | `shared/domain/DietaryGuideline.kt` | 膳食宝塔权威口径(份量/层/三餐) |
| HealthRuleEngine | `shared/ai/HealthRuleEngine.kt` | 忌口/限量判定 |
| MealCompositionScorer | `shared/ai/MealCompositionScorer.kt` | 荤素/主食/餐次组合评分 |
| FoodGroup | `shared/domain/FoodGroup.kt` | 九大类分类(尾词classify) |
| FamilyRepository | `shared/data/repository/FamilyRepository.kt` | 成员份额归一/缺席 |
| Preference | `shared/domain/model/Preference.kt` | 开关默认值 DEFAULT_* |
| MainScaffold | `android/ui/nav/MainScaffold.kt` | 所有路由接线 |
