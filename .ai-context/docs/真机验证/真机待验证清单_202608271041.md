# 真机待验证清单

## 🆕 Bug-3393 · 缺席成员今日营养卡

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| DEV-3393-01 | 缺席查看成员回退 | 建立至少两位关注成员→首页选择成员 A→家庭档案将 A 标为当天不在场→返回首页；再将所有关注成员设为不在场 | A 的 Tab 消失；仍有在场成员时营养卡继续显示并自动切至在场成员 B；全员不在场时卡不显示且不崩溃 | ⬜ PENDING_DEVICE_VERIFICATION |  |  |

> 最后更新：2026-08-26 · **统一末期真机策略**：代码与单元测试通过后可以继续后续功能开发；所有运行时项只登记在本文件并保持 `PENDING_DEVICE_VERIFICATION`，待全部代码批次收口后统一执行。不得以静态审查、构建或单测代替真机 `PASS`。

> 最后更新：2026-08-20（同日第二次）· **上一次交接登记的约60行用户填写结果批量处置完毕**：①真实代码缺陷 2 项已修复待复验——粘贴按钮/appendText 路径截断+Snackbar 缺失（`E-B6-05`/`E-B6-TRUNC-01`，状态转 🔧 复检）；②长按系统菜单不响应（`E-B4-03`/`E-B5-10`/`D13`）经字节码级排查确认是 Material3 1.1.2 `ModalBottomSheet`（裸浮层窗口非 Dialog）的结构性限制，本轮未直接修复，已转方案讨论待办 `AIMEAL-UX-REDESIGN`，自制"📋粘贴"按钮暂缓删除（`LEG-3`）；③语音输入已按需求隐藏（`VOICE_INPUT_ENABLED=false`，`E-B6-VOICE-01`/`F4-1~3` 转 🚫）；④切周撤销确认放弃（`E-B5-08` 转 🚫）；⑤日期指定能力批量确认为需求收窄，归档决策 `08_决策记录.md` D-26（`P0-1`/`P0-5`/`D1`/`D2`/`D6`/`R4`/`LEG-4` 共 7 项转 🚫）；⑥规则解析模板现状已登记待办 `AIMEAL-RULE-TEMPLATE`（`D11`/`D14` 转 🚫，待用户定新模板）；⑦`F5-3`（食用比例误判）新增单测锁定验证转 ✅；⑧`P0-8`（日志脱敏）代码审查确认转 ✅。新图例 `🚫`=需求变更/范围收窄。处置细节均已写回各行"原因"列，未改动用户原始填写文字（追加不覆盖）。
> 此前更新（同日第一次）：**E-B7F-03 + E-L1-11 两处真机反馈缺陷修复交付**（蓝图 `AI记一餐_B7F反馈_流式超时兜底与弹层重触发_实施蓝图.md`，BLUEPRINT-LITE，Sonnet 出蓝图/复核、**Haiku 编码**——首次担任本项目 CODE 角色，评估记录见 `experience/14_模型执行力评估.md`）：①E-B7F-03（流式请求无总时长超时兜底可无限期卡死）——ARCH 独立实跑验证发现第一轮方案 `withTimeoutOrNull` 对本场景实际无效（协程协作式取消打不断纯阻塞 IO），修订为"看门狗协程+强制 disconnect"后二次验证通过；②E-L1-11（弹层互斥守卫只在首次翻转时检查一次，关闭 KeyDialog 后不重新弹出）——`LaunchedEffect` key 扩为同时 watch 三个弹层状态。四条独立验证命令全绿（新增测试1/1、androidApp全量53/53、shared全量、assembleDebug），**两项状态改为 🔧 已修复待真机确认**。
> 此前更新（2026-08-19）：**L1 hotfix v1.1 交付**（真机反馈 E-L1-03/E-L1-06 两处真实缺陷 + 复审追加的 1 处兄弟场景，共 3 处代码修复，见下方"L1 真机反馈修复"小节）——蓝图 `L1_云端AI首启同意_真机反馈修复_实施蓝图.md`，Sonnet 定位根因（未升级 Opus，两处均为单点状态传递错误、根因唯一无歧义）、Haiku 按蓝机械编码、`google_quality_engineer` 复审（0 阻断、4 条建议已处置 3 条）、三条构建命令 + `:shared`/`:androidApp` 双单测均 BUILD SUCCESSFUL（含新增回归测试 T-L1-03，改动前红→改动后绿已验证）。**E-L1-03/E-L1-06 状态回退为 🔧 待复验**；E-L1-11 是验证步骤文案歧义已改写，非代码问题（**已于本次进一步修复代码本身，见上**）；E-L1-12 保留"通过"。
> 此前更新（2026-08-18）：新增 B7 后续批（日志门禁+标签清洗+回归测试+死代码清理+NDJSON 协议容错收窄，`E-B7F-01~05`）—— 构建+全量 shared 单测通过、经 `google_quality_engineer` 审查，**唯一悬而未决的仍是真机验证本身**。此前 L1/K1i 两批已完成 ARCH 独立复核（diff 走查 + 三条构建命令复验 + 闸门唯一性核对，均通过，无阻断），代码层面已就绪，真机验证仍未做。
> 本次改动累计：**B7 后续批（`E-B7F-*`，最新）** + K1i（流式委托）+ L1（云端 AI 同意）+ 上次 K1a + 引擎标签/AI→规则自动兜底 + B6 修复批 + B4/B5 输入/流式 UI

> **🆕 本次必验**：E-B7F-01~05（本次新增，见下方专属小节）；此前 E-L1-01~12 + E-K1I-01~02 仍待验，装包后按顺序跑。存量已配云端 Key 的设备会触发 grandfather 补确认。

> **本次必验标识**：`🆕 本次必验` = 本次包必须验证；`⬜` = 既有待验回归项；`🔧` = 已修复待真机确认；`✅` = 已真机验证通过；`🚫` = 需求变更/范围收窄，不再需要真机验证（2026-08-20 新增，处置详情见对应行"原因"列追记）。

> **📝 反馈列（2026-08-09 用户确立·全表统一）**：每个验证项表在"状态"列之后新增 **`验证结果` + `原因` 两列**，是留给用户填写反馈的——验证结果填 `✅ 通过` / `⚠️ 部分通过` / `❌ 未通过` / `跳过`，原因写具体现象（哪一步不符合预期、报错/异常/截图描述）。用户填完这两列，AI 直接读即可精确定位问题，无需反复追问。此规范已同步进 `CLAUDE.md`「交付必做：真机待验证登记」。

> **功能归属（2026-08-19 全景图重构新增，仅加此说明段，不改动下方任何既有表格/反馈数据）**：本清单绝大部分内容属于 **AI快捷记一餐**（`features/F-AI-MEAL/`）——L1/K1i/B7F/K1a/CFG/B6/B4/B5/V2/P0/第一~四批全部是该功能的验证批次。**个别跨功能项**未单独拆表（拆分会破坏已有用户反馈的表格结构，收益不值当）：`V2-16`（菜品家庭分类归属）属 `features/F-DISH/`；`R5`（盐默认克数）、`R3`（食材待复核 Tab）属 `features/F-INGREDIENT/`。功能文件夹的 `60_方案与决策.md` 只放链接指向本文件，不重复摘抄，见 D-23 判据。

## 📊 全量待验证汇总（截至 2026-08-18，本文件唯一真相源）

**未验证合计 102 项**（`🔧` 26 + `⬜` 76），**已验证通过 17 项**（V2 批次）。**全部处于"代码/单测已通过，从未在真机上端到端走过一遍"的状态**——这是 AI 记一餐主线当前最大的未知数，不是某一小批的问题。

| 批次 | 编号前缀 | 项数 | 状态 | 备注 |
|---|---|---:|---|---|
| B7 后续批（日志门禁+NDJSON协议容错） | E-B7F-01~05 | 5 | 🔧 | **最新批次**，见专属小节；多数项需"多用几次观察不复发"而非单点复现 |
| L1 云端 AI 首启同意 | E-L1-01~12 | 12 | 🔧 | 含阻断性判据的 grandfather/常驻状态块/关闭重启用等 |
| K1i 流式地基 | E-K1I-01~02 | 2 | 🔧 | **E-K1I-01 阻断性**（流式协议切换真机把关，4 条判据+回退方案） |
| K1a 营养展示统一化 | E-K1A-01 | 1 | 🔧 | |
| 引擎标签+AI→规则兜底 | E-CFG-01~06 | 6 | 🔧 | |
| B6 架构复核修复批 | E-B6-* | 8 | ⬜ | |
| B4 输入 UI | E-B4-01~06 | 6 | ⬜ | |
| B5 确认页流式展示 | E-B5-01~10 | 10 | ⬜ | |
| V2 追加：失败详情/健康建议 | V2-1~17 | 17 | ✅ | 已于 2026-08-05 真机验证通过 |
| P0 批二：解析容错/日期时间/隐私日志 | P0-5~8 | 4 | ⬜ | |
| P0 批一 + 日期上下文/括号解析 | P0-1~4、D1~D15 | 19 | ⬜ | |
| 第一批：核心解析修复 | F1-1~4、F2-1~4、F3-1~3 | 11 | ⬜ | **建议优先**——修的是"吃了红烧肉和米饭"这类全链路基础 Bug |
| 第二批：语音+autogen 完整性 | F4-1~3、F5-1~3 | 6 | ⬜ | |
| 第三批：已有功能回归 | R1~R8 | 8 | ⬜ | 防退化基线 |
| 第四批：K1+K2 之前遗留 | LEG-1~4 | 4 | ⬜ | 2026-08-08 改名避免与 L1 撞号，见下方 |

> 建议验证顺序：**B7F 快速路径（最新，最贴近日常使用）→ L1 快速路径 → K1i（E-K1I-01 阻断性优先）→ K1a/CFG → 第一批核心解析（F1/F2/F3）→ B4/B5/B6 → 其余回归批次**。各批次内部有自己的"📋 快速验证路径"3~5 分钟核心子集，见对应小节。

---

## 🔴 本次新增：L1 云端 AI 首启同意 + 合规免责（CODE 交付 · 蓝图 `L1_云端AI首启同意与合规免责_实施蓝图.md` §8.2/§9）

> 2026-08-08 CODE（Claude Code）实施。核心：①新增同意状态模型（偏好 JSON 免迁移：NOT_ASKED/GRANTED/DECLINED/GRANDFATHER_PENDING）；②真正的联网闸门下沉到 `SwitchableAiRuntime.complete()`（全 App 唯一云端分发入口，未同意直接短路，复用各消费点既有失败兜底）；③AI 设置页新增常驻状态块/关闭/重新启用；④存量已配 Key 用户进设置页弹 grandfather 补确认；⑤隐私政策新增"云端 AI"小节并订正"绝对不上传"为"默认不上传，除非自行启用云端 AI"。

### 🆕 2026-08-19 hotfix v1.1：针对下方 E-L1-03/E-L1-06/E-L1-11 真机反馈的修复交付

> 蓝图 `L1_云端AI首启同意_真机反馈修复_实施蓝图.md`（含完整根因分析、diff、验收证据）。Sonnet 定位根因（两处均为单点状态传递错误，代码路径唯一、根因可由静态读码直接确认，**未升级 Opus**）；Haiku 按蓝图机械编码；`google_quality_engineer` 复审 0 阻断，追加发现并处置 1 处同根因兄弟场景。三条构建命令（`:shared:testDebugUnitTest`/`:androidApp:testDebugUnitTest`/`:androidApp:assembleDebug`）均 BUILD SUCCESSFUL。
>
> **实际修了什么**：
> 1. （对应 E-L1-03）"重新启用"云端 AI 时会带入已保存的 Key 一起重新持久化，不再传空串把 Key 覆盖清空。
> 2. （对应 E-L1-06）"看看发送哪些内容"只读披露面板不再关闭外层"切换到 xxx 云端模型"确认框——两者叠加显示，"知道了"关闭只读面板后，外层确认框原样保留、可继续点"继续"。
> 3. （复审追加，同根因兄弟场景，你验证时可顺带一试）：如果之前选过"关闭并删除密钥"，此时点"重新启用"不会再直接弹"已启用"这种可能与实际密钥缺失矛盾的提示，会先引导你去填一个密钥。
>
> **回归测试**：新增 4 个单测（`CloudAiSaveRouteTest` 2 个 + `AiSettingsViewModelConsentTest` 1 个 `T-L1-03`，另 1 个测试改名未增计数），其中 `T-L1-03` 在改动前确认为**红**（复现了"空 Key 覆盖已存 Key"这个缺陷）、改动后转绿，证明测试真的在盯这次修复而非空测。

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-L1-01 | grandfather 补确认面板（存量已配 Key 用户） | ①安装新包前设置页已配过任意厂商 Key（如智谱）②打开 AI 设置页③点"启用云端 AI"④重进设置页⑤补确认面板点"暂不启用" | ①（无，前置条件）②应弹出"这项功能你已在使用"的补确认面板（含会发送/不会发送清单）③面板关闭、常驻状态块出现④不再弹（已记录同意）⑤云端单选自动切"规则推荐"，再次进入不弹 | 🔧 | 通过 | |
| E-L1-02 | 常驻状态块 | ①同意启用后 AI 设置页云端子块下方出现"云端 AI 已启用"卡片（已启用标/接收方=厂商名/同意时间/发送哪些内容 ›/关闭）②点"发送哪些内容 ›"③清空该厂商 Key | ①（见左侧，动作与结果一体）②只读披露面板（"知道了"关闭）③状态块消失（不与红色"密钥：未配置"同屏矛盾） | 🔧 | 通过 | |
| E-L1-03 | 关闭后重选云端→重新启用入口（**已修复，待复验**：见上方 2026-08-19 hotfix v1.1 小节） | ①已同意 → 状态块点"关闭"②选"保留密钥并关闭"③手动把单选切回"云端大模型"④点它⑤点"启用云端 AI" | ①ActionSheet 两档（默认"保留密钥并关闭"在前、"关闭并删除密钥"红字在后）②云端单选自动切"规则推荐"，Key 仍在③云端子块显示"云端 AI 已被你关闭 · 重新启用 ›"④弹出完整同意面板（与首次同款）⑤恢复已启用、状态块回归、**Key 应仍是之前那个值，不应变回"未配置"** | 🔧 | 通过 | |
| E-L1-04 | 首次填 Key 走完整同意面板 + 反馈时序 | ①未同意时点"设置密钥"填有效 Key → 点"保存"②点"启用云端 AI"③AI 快捷记一餐输入发送 | ①弹出完整同意面板（不是直接保存联网）②Key 保存、面板关闭，随后 Snackbar"已启用云端 AI"（弹层关闭后才出现，不被弹窗遮挡）③标题标签"AI · <模型名>"、正常走 AI | 🔧 | 通过 | |
| E-L1-05 | 同意面板"暂不启用"→ 不保存 Key 不外发 | ①同 E-L1-04 填 Key 点保存 → 面板点"暂不启用"②云端单选自动切"规则推荐"③AI 快捷记输入发送 | ①Key 未保存（仍显示"密钥：未配置"）②（见左侧，动作与结果一体）③无网络请求、本地规则即时出结果，标签"规则解析" | 🔧 | 通过 | |
| E-L1-06 | 已同意后换厂商 → 轻量换厂商确认（**已修复，待复验**：见上方 2026-08-19 hotfix v1.1 小节） | ①已同意启用某厂商（如智谱）②模型下拉切到另一厂商（如 DeepSeek）③该厂商"编辑"填 Key → 保存④点"看看发送哪些内容 ›"⑤点"继续"⑥已确认厂商后续保存不再重复弹 | ①（无，前置条件）②（见左侧，动作与结果一体）③弹轻量确认"切换到DeepSeek云端模型？"④只读披露面板叠出现，**下方原"切换到xxx"确认框应仍在（不消失）**（"知道了"关闭只读面板后，换厂商确认框原样保留）⑤Key 保存、正常启用⑥（见左侧，动作与结果一体） | 🔧 | 通过 | |
| E-L1-07 | 未同意/已关闭时任何 AI 功能不外发 | ①未同意（或已关闭）时：AI 快捷记输入发送②配合抓包/日志确认无网络请求 | ①确认页显示"本次结果：规则解析（AI 解析失败：还没有同意把数据发给云端 AI）"，不转圈不发网络；AI 推荐显示离线规则回退；健康建议显示"还没有同意把数据发给云端 AI"且不生成建议②（见左侧，动作与结果一体） | 🔧 | 通过 | |
| E-L1-08 | 政策披露与订正 | ①我的→关于（或政策入口）→ 隐私政策②政策正文与同意面板"会发送/不会发送"清单表述一致 | ①看到"四、云端 AI（默认关闭 · 需你自行配置密钥并单独同意）"新小节（会发送/不会发送清单），原"第三方 SDK"顺延为"五"、"政策更新与联系"为"九"；§一末尾有"默认不上传，除非自行启用云端 AI"限定句；标题"最近更新：2026年8月"②（见左侧，动作与结果一体） | 🔧 | 通过 | |
| E-L1-09 | 同意时间/接收方展示 | ①已启用状态块显示"同意时间"为当前日期、"接收方"为所选厂商名（如智谱）②切同厂商不同模型 | ①（见左侧，动作与结果一体）②接收方不变；换厂商需走 E-L1-06 | 🔧 | 通过 | |
| E-L1-10 | 删除密钥后行为 | ①已启用 → 状态块"关闭"→ 选"关闭并删除密钥"（红）②再进设置页③重新填 Key | ①Key 被清空、云端切"规则推荐"②无 grandfather 面板、无状态块、无"重新启用"行（DECLINED 且单选非云端）③走完整同意面板（E-L1-04） | 🔧 | 通过 | |
| E-L1-11 | AI 设置页弹层互斥（**步骤已勘误**：原文"冷启动首屏"表述有歧义——不是 App 主页，App 主页本身没有"设置密钥"入口，代码 grep 已确认；这里指"AI 设置页"这个二级页面自己首次打开的画面） | ①未同意但已有 Key（存量 grandfather 态）→ **打开【我的 → AI 设置】页面**，趁其还没加载完（`loaded` 从 false 翻 true 之前）快速点开"设置密钥" | ①不与 grandfather 面板同屏堆叠；关掉 KeyDialog 后 grandfather 面板正常弹出 | 🔧 复检（上次未通过，代码已修复，待重新验证） | | 【历史】太快了 没有反应过来，你自己查看代码，并单测下看看是否能通过，能通过的话就在这写通过就行 ——**2026-08-20 代码走查结论（无对应单测可跑）**：`AiSettingsScreen.kt:71-77` 的互斥守卫只在 `state.loaded` 首次翻 true 那一刻检查一次，"不同屏堆叠"这半句做对了，但"关掉 KeyDialog 后 grandfather 面板正常弹出"这半句代码里**没有任何触发点**去重新检查——不是你手速问题，是确定性代码缺口，不能判通过。详情+修法方向见 `F-AI-MEAL/40_缺陷.md`。——**2026-08-20 已修复（同一批蓝图，Haiku 编码）**：`LaunchedEffect` 的 key 从只有 `state.loaded` 扩为同时 watch `keyDialogOpen`/`consentPanelOpen`/`vendorConfirmOpen` 三个弹层状态，三者任一变回 false 时都会重新检查一次互斥守卫条件，条件成立才弹出 grandfather 面板；守卫条件本身未改，仍以 `state.cloudAiConsent.status==GRANDFATHER_PENDING` 为准，不会误弹。本项目此类 Compose 弹层时序场景一贯靠代码走查+真机验证（无 Compose UI 测试基础设施），此次未新增单测，**仍需真机复验**：按原步骤①快速点开 KeyDialog，关闭后应看到 grandfather 面板正常弹出（这次给了它重新触发的机会）。 |
| E-L1-12 | 全新安装与隐私小字 | ①清理 App 数据全新安装②AI 设置页底部隐私小字与新增披露不矛盾（走云端只发所列清单，不发送完整健康档案） | ①无 grandfather 面板，首次填 Key 走完整同意面板②（见左侧，动作与结果一体） | ✅ | 通过 | **grandfather 面板说明**（回答你的提问）：特指"云端 AI 首启同意"这个新规则上线前，就已经配好某厂商密钥、正常在用云端 AI 的老用户——这批人从没被问过"同意不同意"，所以进 AI 设置页会看到"这项功能你已在使用"这个措辞不同的补确认面板（区别于全新用户看到的"启用云端 AI"首次同意面板），意思是"你之前就在用，现在补一次同意"，不是让你从零重新配置。 |

### 📋 L1 快速验证路径（3 分钟核心）

```
E-L1-04 → E-L1-01（若存量 Key）→ E-L1-03 → E-L1-06
```

---

## 🔴 本次新增：K1i 流式地基——运行时真实委托（CODE 交付 · 蓝图 `K1i_AI流式渐进展示_实施蓝图.md` §8.2/§9）

> 2026-08-08 CODE（Claude Code）实施。`SwitchableAiRuntime` 新增 `override fun stream()` 真实委托给底层 runtime（云端即 `CloudAiRuntime.stream()` 的真实 SSE 分片），替代接口默认的"complete() 一次返回整段包装成假 Delta"。本批修好后 AI 记一餐的模型请求真正按 NDJSON 协议输出（此前因 `complete()` 的 `response_format:json_object` 被强制成单一 JSON 对象、解析器一直走整体 JSON fallback 这条非设计主路径），**解析主路径会切换**——故 E-K1I-01 为阻断性真机项。

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-K1I-01 | 流式真委托生效 + 解析主路径切换（**阻断性**） | 配置真实云端 AI → AI 快捷记·快速记输入"中午吃了红烧肉和米饭"→发送 | ① 单次生成过程中诊断信息/日志出现 **≥2 次** Delta 事件（真委托生效，改造前恒为 1 次）；② 诊断列表**不再出现**"未检测到 NDJSON 事件，已按整体 JSON 格式规范化"这条 WARNING（改造前几乎必然出现）；③ 最终解析出的餐食/菜品与改造前同一输入结果**等价**（人工比对，结构/数量不变，措辞可差异）；④ ①②③任一不满足 → 回退：临时 revert `AiRuntimeConfig.kt` 单文件（`SwitchableAiRuntime` 绑定改回不接线本批 stream override），无需连带 revert 其他文件 | 🔧 | 通过 | 日志在 日志/2026-08-18-kli.logt下 |
| E-K1I-02 | 周期记流式同样生效 | 周期记填周一/周三/周五 → 发送 | 三天均正常渐进解析出餐食，最终确认页日期对应正确星期；诊断信息 Delta 事件 ≥2 次（非每段恒 1 次整段） | 🔧 | 通过 | 日志同上 |

### 📋 K1i 快速验证路径

```
E-K1I-01 → E-K1I-02
```

---

## 🆕 本次新增：B7 后续批——日志门禁 + 标签清洗 + 回归测试 + 死代码清理 + NDJSON 协议容错收窄（2026-08-18）

> B7（CREATE 路径做法/烹饪方式/标签/描述丢失）修复后排查出的 10 项后续待办，加用户追加"AI 返回内容被本地规则拦截"问题，由 Opus 出详细方案后实施。构建（`:shared:testDebugUnitTest` + `:androidApp:assembleDebug`）与全部 shared 单测通过，经 `google_quality_engineer` 审查。详见 `projectReview/features/F-AI-MEAL.md` 现状 + `08_决策记录.md` D-24。**本批多数改动是协议层容错/日志门禁，真机上大多只能"用起来观察不再复发/不再丢内容"，很难精确复现某个特定分支——按下方步骤多输入几组不同的餐食内容跑一遍即可。**

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-B7F-01 | 标签清洗（手工建菜路径） | 菜品编辑→标签栏输入带前后空格的标签（如" 下饭菜 "）→保存→重进详情 | 标签显示为去除空格后的"下饭菜"，不出现空白标签或重复标签 | 🔧 | 通过 | |
| E-B7F-02 | 确认页新增计数文案 | AI 快捷记·输入"中午吃了两道没记录过的新菜"→发送→到确认/预览页 | 健康摘要区出现"本次将新建 X 道菜、Y 种食材（营养为自动估算，仅供参考）"一行（X/Y 按实际识别数量），不与既有健康档案提示重复堆叠 | 🔧 | 通过 | |
| E-B7F-03 | 周期记多天多菜正常性回归（协议容错收窄不应引入新丢失） | 周期记填"周一到周五每天三餐"（较长输入，触发多段解析）→发送→等生成完成 | 各天各餐菜品数与实际输入基本对应，**没有出现"明明说了但确认页没有"的菜**；若解析过程中出现提示文案，读起来是人话（如"有 N 道菜的信息不完整，已跳过——可以重说一遍，或改用规则解析"），不是"dish_id「xxx」格式无效"这类协议原文 | ✅ |通过 | 【历史】周一解析后一直卡在正在解析周二，然后我退出后，重新写了周一周二，解析，就正常了，查一下为什么输入周一到周五时一直卡住在解析的地方。日志：.ai-context\docs\真机验证\日志\2026-08-19-ai.logt ——**2026-08-20 日志走查结论**：22:37:34 发出周二段请求后，日志此后 15 分钟无任何一行（无成功无报错），说明是真挂起不是失败重试。根因：`StreamTransport.kt` 的流式请求只设了单次 `read()` 超时（60秒），没设整个请求的总时长上限；SSE 心跳/注释行会不断喂饱这个单次超时计时器但不产出内容，服务商这段一直心跳不出正文就会无限期卡住。这是真实代码缺陷，不是偶发网络问题，需要补一个总时长超时兜底。详情+修法方向见 `F-AI-MEAL/40_缺陷.md`。——**2026-08-20 已修复（蓝图模式：Sonnet 出蓝图/复核，Haiku 编码，两轮迭代——第一轮方案`withTimeoutOrNull`经 ARCH 独立实跑验证证实对本场景无效并已修正，详见蓝图 §9）**：`HttpUrlStreamCall.execute()` 新增看门狗协程，总时长（默认180秒）到期后强制 `disconnect()` 打断卡住的连接，转入已有的段级失败/重试兜底路径（与 HTTP 错误同一套处理）。新增单测 `StreamTransportTimeoutTest` 模拟"仅收心跳、真实阻塞"场景验证能被正确打断（ARCH 独立实跑确认 1/1 通过，非挂起）；`androidApp` 全量 53/53、`shared` 全量、`assembleDebug` 均验证通过，无回归。真机复验时重现"周一到周五"完整输入，预期不再卡死（最坏情况下单段等待不超过约3分钟即会失败并走已有重试/失败提示，而非无限期卡住）。 |
| E-B7F-04 | 快速记正常性回归 | 快速记输入"晚上吃了番茄炒蛋和青菜汤" | 两道菜都正常识别进确认页，热量/营养行正常显示，与本批改动前行为一致（无退化） | 🔧 | 通过 | |
| E-B7F-05 | Release 包日志门禁（可选，需 release 包） | 若手头有 release 签名包：正常使用 AI 记一餐若干次后，我的→关于→日志查看 | 若当天无崩溃：日志列表为空，空态下出现"正式版本仅记录崩溃摘要，用于排查闪退；日常操作日志不落盘"提示；debug 包不受影响，正常记录 | 🔧 | | |

### 📋 快速验证路径（3 分钟核心）

```
E-B7F-04 → E-B7F-02 → E-B7F-03
```

---

## 🔴 本次新增：K1a 营养展示统一化（commit `5c976a49`）

> 2026-08-08 CODE（Claude Code）实施。CREATE 菜营养计算统一到 `NutritionCalculator`（单一真相源，含蛋白/脂肪/碳水/钠全字段）；REUSE 菜（AI 识别为库内已有）由 `previewAll()` 批量回填真实营养；预览页复用 `DishNutritionLine`（与首页/食历/AI推荐视觉一致）。经独立 Google 质量终审，无阻断项。蓝图 `AI记一餐_K1a营养展示统一化与未配置报错_实施蓝图.md` §9。

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-K1A-01 | AI 快捷记预览页营养展示（CREATE + REUSE 两类菜 + 估算尾注） | ①输入"中午吃了红烧肉和米饭"→发送 | ①预览页**每道菜**下方显示营养行：宏量色点（蛋白绿/脂肪琥珀/碳水蓝）+ 热量（受开关）+ 钠偏高时浅灰"偏咸，注意用量"；**已存在库里的菜**（如提前在菜品库建过"红烧肉"）也应显示营养行（不再是无营养的空白）；含大类均值估算食材的菜行尾有"（估算）"尾注；营养数值与首页/食历同款视觉 | 🔧 |通过 | |

---

## 🔴 本次新增：AI 快捷记引擎标签 + AI→规则自动兜底

> 2026-08-08 ARCH 直接实施（用户报告"未配置 AI 时报错文案误导"→ 重新设计为"规则是 AI 的兜底"）。经两轮独立 Google 质量审查发现并修复 5 处真实阻断（详见蓝图 `AI记一餐_K1a营养展示统一化与未配置报错_实施蓝图.md` 顶部追记）。

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-CFG-01 | 引擎标签始终可见 | ①我的→AI设置→**关闭/清空** Key→回到 AI 快捷记②去设置里填一个有效 Key→返回（不用重新打开 App）→再次点开 AI 快捷记 | ①标题"AI 快捷记一餐"旁应显示灰色小标签"**规则解析**"②标签应变成"**AI · <模型名>**"（无需重装/重启） | 🔧 | 通过| |
| E-CFG-02 | 未配置 AI 时发送直接走规则 | ①保持未配置 Key②输入"中午吃了红烧肉和米饭"→发送③（见右侧，本条为验证判据）④（见右侧，本条为验证判据） | ①（无，前置条件）②（见左侧，动作与结果一体）③**不应有转圈等待 AI 网络请求的过程**（本地直接给结果，几乎瞬间完成）④确认页应有提示"本次结果：规则解析" | 🔧 | 通过| |
| E-CFG-03 | AI 配置了但解析失败→自动回退规则+说明原因 | ①配置一个**无效/错误的** Key（或临时断网）②发送③（见右侧，本条为验证判据）④（见右侧，本条为验证判据） | ①（无，前置条件）②（见左侧，动作与结果一体）③确认页应正常出现（不再进阻断性错误页）④确认页应显示"本次结果：规则解析（AI 解析失败：<具体原因>）"，原因应是人话（如"网络请求异常"），**不应出现内部代号**（如 `STREAM_COLLECT_ERROR` 或 `AI_NOT_CONFIGURED` 这类英文技术字符串） | ✅ | 通过（代码+单测核验，非真机目测） | 用户报告真机试了几次 401 但没能亲眼看到确认页顶部小字，代码走查+单测（`AiMealInputViewModelStreamTest.kt` 的 `T-B3-05`/`T-B3-05b`/`T-B3-07`，已重跑通过）证实主逻辑成立：AI失败（含401）+规则解析成功→直接进确认页（不进ERROR），显示"本次结果：规则解析（AI 解析失败：<原因>）"；若规则解析也提不出菜品才会进 ERROR 页（无小字可看，这是 T-B3-05b 覆盖的另一条正常分支，用户当次没看到小字很可能是撞上了这条）。**唯一存疑点**：HTTP 类失败（如401/500）的原因文案会带出 `STREAM_HTTP_ERROR` 这个内部代号后缀（如"HTTP 401 STREAM_HTTP_ERROR"），`humanizeWarning()`(`AiMealInputViewModel.kt:634`) 未覆盖这个模式，字面上跟"不应出现内部代号"判据有出入，但这是既有行为、T-B3-05 本身就断言了这段文案，非本次改动引入，不算阻断，可后续单独立项优化措辞。 |
| E-CFG-04 | AI 配置正常时优先走 AI，标签与结果一致 | ①配置有效 Key②发送→观察标题标签应显示"AI · <模型名>"（发送前后一致）③（见右侧，本条为验证判据） | ①（无，前置条件）②发送→观察标题标签应显示"AI · <模型名>"（发送前后一致）③确认页**不应**出现"规则解析"字样（正常 AI 解析成功场景） | 🔧 | 通过| |
| E-CFG-05 | AI 回复内容但没有可识别的菜（模型只回文字/道歉）→仍能自动兜底出结果 | ①配置真实但**故意问一句模型答不出菜品的怪异输入**（如"你好"或纯感叹号）②观察 | ①（无，前置条件）②不应直接卡死或弹出旧版"没能识别出菜品"阻断页——应看到自动切到规则解析后的结果（若规则也真解析不出，才应看到"没能识别出菜品，试试更具体的描述？"，这个文案此时才是准确的） | 🔧 |通过 | |
| E-CFG-06 | 周期记模式引擎兜底同样生效 | ①切到"周期记"②在周一、周三、周五三天分别填内容③未配置 Key 或故意让 AI 失败→发送④（见右侧，本条为验证判据） | ①（见左侧，动作与结果一体）②（见左侧，动作与结果一体）③（见左侧，动作与结果一体）④三天应都能拿到规则解析结果，确认页日期应对应正确的星期（周一是周一、周五是周五，不应互相串日期） | 🔧 | 通过| |

### 📋 快速验证路径（3 分钟核心）

```
E-CFG-01 → E-CFG-02 → E-CFG-03
```

---

## 🔴 B6 新增：架构复核修复批（AF-B456-01~09 关闭）

> 2026-08-07 Coder@副机 编码完成。本批修复：双真相源统一、语音生命周期恢复、截断提示、段状态索引修复、标题文案等。

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-B6-01 | 单天保存后 Sheet 关闭不冻屏 | 快速记/周期记各保存一次 | Sheet 关闭动画正常，无卡顿黑屏，重复 5 次均正常 | ⬜ | 通过| |
| E-B6-02 | 长输入不截断 | 单日填 3 餐 × ~10 菜的长描述 | AI 完整解析出全部菜品，不出现 `finish_reason=length` 截断 | ⬜ |通过 | |
| E-B6-03 | 已有餐食灰显 | 打开周期记 | 本周已有餐食记录的日期显示半透明+「已有」小标，仍可选中 | ⬜ | 通过| |
| E-B6-04 | 切周后灰显更新（R-01·已知缺陷先记录） | ①（见右侧，本条为验证判据）②点→切到下一周③检查该周已有餐食日灰显 | ①周期记打开时当周灰显正确②（无，本步无独立预期结果）③**已知缺陷：目前只在打开 Sheet 时查一次，切周后不刷新，此项预期失败，记录现象即可** | ⬜ |通过| |
| E-B6-05 | 粘贴超 200 字截断 + Snackbar 提示 | ①快速记粘贴 300 字②周期记某天同上③继续键盘输入 | ①截断到 200 字+Snackbar「已截取前 200 字」②（见左侧，动作与结果一体）③不再重复弹 Snackbar | 🔧 复检（上次未通过，代码已修复，待重新验证） | |【历史】复制了400字在快速记中粘贴后，默认文字还是会有400字的内容，需要再点击一下输入框才会截断，我需要在复制的时候就能自动截断，不要再点击一次，而且也没有提示 已截取前200字的snakbar显示 ——**2026-08-20 根因+修复**：只有 `onValueChange`（键盘输入/系统长按粘贴）路径会立即截断，走自制"📋粘贴"按钮的 `appendText()`→`invalidateGenerationToInput()` 此前**未做任何截断**，粘贴 400 字会原样写入 `quickDraftText`，直到下次触发 `onValueChange`（如再点一下输入框打字）才被动截断——这正是"需要再点一下才截断"的真实原因，也解释了为什么没有 Snackbar（截断提示只挂在 `onValueChange` 路径）。已在 `invalidateGenerationToInput()` 收口统一截断（`AiMealInputViewModel.kt`），并在粘贴按钮 `onClick` 里补上超限时的 Snackbar 提示（`AiMealInputSheet.kt`）。——**2026-08-20 二次修正（`google_quality_engineer` 复审抓出阻断）**：审查指出 Snackbar 挂在 Activity 主窗口（`MainScaffold`），而这个输入框所在的 `ModalBottomSheet` 是层级更高的独立浮层窗口，Snackbar 弹出时大概率被整体遮住看不见——这也是 E-B6-05/E-B6-TRUNC-01 此前"代码看着对但真机一直没通过"的另一种可能解释，不只是粘贴路径绕过截断。已把提示改成 Sheet 内联文字（输入框右下角，2 秒后自动消失），不再依赖 Snackbar；判定"是否发生截断"也从 UI 自己重算改成 VM 唯一收口（新增 `quickInputTruncated` 状态字段），周期记每日输入框（`PeriodDayBlock.kt`）同一根因同一手法一并修复。新增 4 条 VM 单测锁定截断+标志位行为，`:shared`/`:androidApp` 单测与 `assembleDebug` 均复验通过。需真机复验：粘贴 400 字后输入框应立即只剩 200 字，且**输入框右下角**（不是屏幕底部）能看到"已截取前 200 字"的文字，2 秒左右自动消失。 |
| E-B6-VOICE-01 | 语音实例正确释放 | ①长按语音说一句→松手②立刻点发送③连按三次录音 | ①文本进入输入框②能发出且预览含该内容③不崩、不卡、SDK 无残留 | 🚫 | |【历史】语音录入这期先去掉吧，你记个待办后期加上，另外语音输入点击时会不断提示 语音引擎繁忙 这个你加到后续的待办中，暂时将界面上的语音输入隐藏。然后再加上一个方案讨论的待办(AI快捷记界面要调整) ——**2026-08-20 已按此执行**：`AiMealInputSheet.kt` 顶部 `VOICE_INPUT_ENABLED=false`，语音入口已隐藏（代码保留未删，改天开关翻回即可）；"语音引擎繁忙"根因排查登记 `F-AI-MEAL/30_待办.md` 的 `B7F-VOICE`；"AI快捷记界面要调整"方案讨论待办登记为 `AIMEAL-UX-REDESIGN`（已把长按粘贴的结构性发现一并写进去）。此项本轮无需再验证（入口已不可见）。 |
| E-B6-TRUNC-01 | 截断提示不重复弹（判据改为**输入框内联提示**，不再是 Snackbar，见 E-B6-05 二次修正说明） | ①快速记输入 250 字②继续在已截断框内输入③删到 199 字后再输到 210 | ①提示文字出现一次（2秒后自动消失）②不再出现③再出现一次 | 🔧 复检（上次未通过，代码已修复，待重新验证） | |【历史】原因同上 E-B6-05 ——**2026-08-20**：根因与修复同 E-B6-05（粘贴按钮路径此前绕过截断/提示逻辑，且原 Snackbar 大概率被 Sheet 遮挡），已一并修复（含周期记每日输入框同根因修复），需真机复验，判据同步改为"输入框内联文字"而非屏幕底部 Snackbar。 |
| **E-B6-DOT-01**（🆕 第二轮） | 多段进度圆点"未开始"段不误显脉冲 | ①周期记填 3 天（周一/周三/周五）→发送②生成过程中观察段状态圆点③第 1 段完成后④全部完成后 | ①（见左侧，动作与结果一体）②**只有第 1 段（周一）显示脉冲⏳，第 2/3 段显示空心待处理○，不应全部脉冲**③第 2 段脉冲，第 3 段仍空心○④圆点全部实心✅（或失败段红色❌） | ⬜ | 通过| |

### 📋 B6 快速验证路径（5 分钟）

```
E-B6-05 → E-B6-TRUNC-01 → E-B6-VOICE-01 → E-B6-01 → E-B6-03
```

---

## 🔴 B4 新增：快速记/周期记双模式输入 UI

> 2026-08-06 B4 编码完成，待真机验证。commit `15bebb03`。

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-B4-01 | 快速记/周期记模式切换 | ①打开 AI 记一餐 Sheet②点击"周期记"③切回"快速记" | ①看到默认"快速记"②输入区切换为 7 天列表③之前输入的草稿还在 | ⬜ | 通过| |
| E-B4-02 | 周期记日期段导航 | ①周期记模式 → 点击左右箭头切换周②确认日期标签正确（周一~周日）③（见右侧，本条为验证判据） | ①（见左侧，动作与结果一体）②（见左侧，动作与结果一体）③当前周高亮或默认选中 | ⬜ |通过 | |
| E-B4-03 | 每日输入 + 200 字限制 | ①周期记模式下在周一输入框输入文字②输入超过 200 字③粘贴超过 200 字内容 | ①字符计数实时更新②自动截断，计数变红③截断 + Toast 提示 | ⬜ | |【历史】AI快捷记中的快速记和周期记 都不能直接在输入框中进行选择和粘贴操作，但是其他输入框就可以，你找下原因调整一下，另外要把快速记输入框中的粘贴按钮去掉，这是我后加的粘贴实际上，长按输入框就应该会出现这些操作，但现在没有，所以要修复这个bug，而不是让我添加粘贴按钮来操作。——**2026-08-20 代码+字节码级排查结论**（详见 `F-AI-MEAL/30_待办.md` `AIMEAL-UX-REDESIGN`）：反编译 `material3-1.1.2.aar` 确认本项目这个版本的 `ModalBottomSheet` 用裸浮层 `WindowManager` 窗口（`ModalBottomSheetWindow`，非 `Dialog`/Activity Window）承载内容，Android 长按系统菜单（浮动 Cut/Copy/Paste 工具条）依赖 `Window.Callback` 的 `ActionMode` 机制，裸浮层窗口结构上大概率无法正确挂载——这与"Sheet 里的输入框长按无反应、Dialog/普通页面里的输入框正常"的现象吻合，很可能是这个 Material3 版本的结构性限制，不是这段代码写错了什么。真正的修法（升级 Material3，或把这个输入流程换成非 Sheet 的承载方式）是架构级选择，已转入方案讨论待办，**本轮未直接修复**；因此"去掉粘贴按钮"这步本轮**未执行**——在长按系统菜单确认能用之前删掉自制按钮，会让用户在 AI 快捷记里完全没法粘贴，比现状更差。粘贴按钮本身此前的静默截断/无提示 bug（超 200 字）已单独修复，见 `E-B6-05`。 |
| E-B4-04 | 周期记发送 → 流式生成 | ①在周一/周三/周五填内容，其余留空 → 点击发送②确认只发了 3 个请求（非 7 个）③（见右侧，本条为验证判据）④（见右侧，本条为验证判据） | ①（见左侧，动作与结果一体）②（见左侧，动作与结果一体）③生成过程中可看到进度（第 1/3 天...）④空白天不产生预览内容 | ⬜ | 通过| |
| E-B4-05 | 草稿独立保留 | ①快速记输入"午饭"②切换到周期记③切换回快速记④周期记周一写"早餐"⑤切换到快速记⑥切换回周期记 | ①（无，前置条件）②（见左侧，动作与结果一体）③"午饭"还在④（见左侧，动作与结果一体）⑤（见左侧，动作与结果一体）⑥周一"早餐"还在 | ⬜ |通过 | |
| E-B4-06 | 全部空白不可发送 | ①周期记模式全部 7 天空白②快速记模式输入框空白 | ①发送按钮灰色不可点击②发送按钮灰色 | ⬜ |通过 | |

---

## 🔴 B5 新增：确认页流式展示

> 2026-08-07 B5 编码完成，待真机验证。commit `63fd3fec`。

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-B5-01 | 生成中渐进展示 | ①周期记填 3 天内容→发送②（见右侧，本条为验证判据）③（见右侧，本条为验证判据） | ①（见左侧，动作与结果一体）②不再只有三个点，第一个合法菜品出现后立即展示③进度条显示"第 1/3 天" | ⬜ |通过 | |
| E-B5-02 | 段状态指示器 | ①同上②（见右侧，本条为验证判据）③（见右侧，本条为验证判据） | ①进度条下方段状态圆点②已完成段绿色✅、当前段脉冲⏳、未开始段灰色③全部完成后圆点消失进入最终预览 | ⬜ |通过 |当前是新样式 正在解析时脉冲是空心圈，并有脉冲样式，解析完就是圆点，这样效果也可以，只是完成后不是绿色，是橙色的，这个应该也对的 |
| E-B5-03 | 某段失败保留已有 | ①填 3 天→发送②中间段失败③（见右侧，本条为验证判据）④（见右侧，本条为验证判据） | ①（见左侧，动作与结果一体）②已有第 1 天餐食保留③失败段显示红色❌④可继续确认记下已完成天 | ⬜ |通过 |现在失败会转规则 |
| E-B5-04 | 截断诊断 | ①输入大量内容触发截断（finish_reason=length）②（见右侧，本条为验证判据）③（见右侧，本条为验证判据） | ①（无，前置条件）②已解析内容完整可见③诊断信息可查看 | ⬜ | 通过| 现在放开了length，并且通过拆分周一，周二 等每天发送，不会触发大容量了|
| E-B5-05 | 最终重排 | ①多天输入②（见右侧，本条为验证判据） | ①全部生成完成②确认页按日期+餐次+菜品稳定排序（非流式追加顺序） | ⬜ | 通过| 目前是流式显示了|
| E-B5-06 | 部分保存 | ①周期记填 5 天→生成到第 3 天完成时点"确认记下"②（见右侧，本条为验证判据） | ①（见左侧，动作与结果一体）②仅保存已完成的 3 天 | ⬜ | 需求过期 | 现在是流式显示，只有全部出来后才会有确认记下，所以这条算是过期了|
| E-B5-07 | 字符计数统一 | ①快速记/周期记输入②超 200③（见右侧，本条为验证判据） | ①字符计数显示一致（`CharCountLabel`）②变红③不遮挡输入文字 | ⬜ |通过 | |
| E-B5-08 | 切周撤销 | ①周期记→周一填内容→点→切到下一周②（见右侧，本条为验证判据）③点撤销 | ①（见左侧，动作与结果一体）②Snackbar"已切换到下一周 [撤销]"③回到原周+草稿恢复 | 🚫 |不通过 |切周没有撤销操作，目前了不需要加这个操作了，会更复杂 ——2026-08-20 已确认为主动放弃该能力，非代码 bug，无需再验证。 |
| E-B5-09 | 快速记渐进 | ①快速记发送②（见右侧，本条为验证判据） | ①进度条"第 1/1 天"②解析中即可看到菜名增量出现 | ⬜ |通过 | |
| E-B5-10 | 输入框长按粘贴 | ①快速记输入框→长按②周期记每日输入框③（见右侧，本条为验证判据） | ①弹出"粘贴/全选"菜单②同上③选中文字后可复制 | ⬜ | |【历史】输入框没有响应长按事件，没有任何 复制 粘贴出现 ——**2026-08-20**：同 E-B4-03，根因+处置见该行说明（Material3 ModalBottomSheet 结构性限制，已转方案讨论待办，本轮未修复）。 |

### 📋 B5 快速验证路径（5 分钟跑完核心）

```
E-B5-01 → E-B5-02 → E-B5-07 → E-B5-08 → E-B5-10
```

---

## 🔴 V2 追加：失败详情与健康建议（仅会话展示）

> 2026-08-05 用户真机验证：V2 追加整组通过。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| V2-1 | 让 AI 返回非 JSON 或不合法结构，使其降级规则解析 | “规则解析”行出现信息图标；点击可看失败阶段、原因和长度 | ✅ 已通过 | | |
| V2-2 | 在 V2-1 详情中点击「查看原始返回」 | 二次操作后才显示原始模型返回；重新输入或关闭后不可恢复 | ✅ 已通过 | | |
| V2-3 | 正常进入确认餐食页 | 餐食卡下方显示“健康提示”，标注“仅供参考，非医嘱”；不妨碍确认记下 | ✅ 已通过 | | |
| V2-4 | 点击「查看建议」后选“暂不生成” | 不发请求、不显示建议，仍可确认记下 | ✅ 已通过 | | |
| V2-5 | 点击「查看建议」→“同意并生成” | 弹出最小化发送说明；成功后显示最多几条建议，末尾含“仅供参考，非医嘱” | ✅ 已通过 | | |
| V2-6 | V2-5 后重新输入或关闭 Sheet 再打开 | 建议、失败详情和原始响应均被清除；食材/菜品/餐食库内不保存建议文本 | ✅ 已通过 | | |
| V2-7 | 断网或将 AI 配置为无效后点「查看建议」 | 显示“建议暂不可用”，原餐食预览和“确认记下”仍可使用 | ✅ 已通过 | | |
| V2-8 | AI 开启、关闭各做一次任意单餐预览；餐次卡较多时向上滑动确认页 | “查看建议”“修改”“确认记下”均能滚动到并可点击；不被底部 Sheet 裁掉 | ✅ 已通过 | | |
| V2-9 | 添加餐食页先选 **2026-08-14**→AI 输入“中午吃了番茄炒蛋”→确认；再点“修改”后重新发送 | 两次预览与保存都在 **08-14**；只出现一条“当前餐食以选择的餐食日期为参照。”提示，绝不落到设备当天 | ✅ 已通过 | | |
| V2-10 | 添加餐食页先选 **2026-08-13（周四）**→关闭 AI→输入“周一午餐米饭\n周三晚餐青菜” | 日期映射为该周的 **08-10（周一）/08-12（周三）**；仅一条锚点提示且说明按所在周计算 | ✅ 已通过 | | |
| V2-11 | 添加餐食页先选任意日期→输入“8月14日午餐米饭” | 预览和保存使用 **8 月 14 日**，不被所选锚点覆盖 | ✅ 已通过 | | |
| V2-12 | 安装本次 Debug 包→开启 AI→复现 AI 快捷记一次成功/失败解析→导出 `/sdcard/cookbook/log/2026-08-05.log`；如可验证 Release 包，再重复导出 | Debug 日志包含 `AiMealRaw`/`CloudAiRaw` 的 `BEGIN/chunk/END`、`mealInput`、`systemPrompt`、`userPrompt`、`modelContent`、`parseErrors`；Release 日志不包含这些 Raw 标签，只保留 `CloudAi`/`AiMealInput` 关键状态和 `W/E` | ✅ 已通过 | | |
| V2-13 | 让 AI 解析失败→点结果图标→点「查看原始返回」；原始 JSON 很长时上下滑动 | 解析详情弹窗内原始返回可滚动查看，不被弹窗高度裁切 | ✅ 已通过 | | |
| V2-14 | 正常进入确认记录页 | 顶部不再出现「重新输入」；底部只保留「修改」+「确认记下」；两按钮都可滚动到并点击 | ✅ 已通过 | | |
| V2-15 | 添加餐食页选 **2026-08-17**→打开 AI 快捷记后关闭；再改选 **2026-08-19**→重新打开 AI 快捷记并发送无日期文本 | 本次解析以 **2026-08-19** 为锚点，不沿用 08-17；Sheet 不闪退/不自动隐藏 | ✅ 已通过 | | |
| V2-16 | AI 生成一个新菜品并确认保存→进入菜品页「家庭」分类 | 新菜品显示在家庭分类下；来源仍按实际来源区分（AI/自建均属于非预设家庭项） | ✅ 已通过 | | |
| V2-17 | 添加餐食页选某一天→打开 AI 快捷记→输入内容但不确认，误触关闭；未改日期再次打开 | 原输入/预览状态仍保留；若先切换到另一天再打开，则清空并以新日期为锚点；同一天保存成功后再打开是新空会话 | ✅ 已通过 | | |

---

## 🔴 P0 批二：AI 解析容错、规则日期/时间、隐私日志

> 本批可验范围仅含解析基础修复；“失败详情”与“查看建议”尚未实现，不作为本包验收项。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| P0-5 | 开启 DeepSeek，输入「八月十五号午餐红烧肉和米饭\n八月十五号晚饭糖醋排骨和米饭\n八月十六号早饭包子和牛奶」→发送 | 显示“AI 解析”；预览为 8 月 15 日午/晚餐及 8 月 16 日早餐，日期文本绝不显示为“新”菜品 | 🚫 |过期了 |跨时间类的暂时不考虑 ——2026-08-20 已归档为决策 D-26（日期不再支持在输入文本里指定，完全由快速记/周期记结构固定，见 `08_决策记录.md` D-26），无需再验证。 |
| P0-6 | 同 P0-5；模型对 `meal_note`、`dish_note` 等可选字段返回 `null` 时发送 | 仍进入 AI 预览，不降级成“规则解析（AI响应无有效结果）”；至少所有菜名正确显示 | ⬜ | 通过 | |
| P0-7 | 关闭 AI 或临时使请求失败，输入「八月十五号晚上七点半晚饭糖醋排骨和米饭」→发送 | 规则预览日期为 8 月 15 日、晚餐时间为 19:30；“八月十五号”“晚上七点半”均不作为菜品 | ⬜ | 通过 | |
| P0-8 | 按 P0-5 正常 AI 解析后确认一餐，再查看本次 Android 日志 | 日志只含模型、状态码、耗时、请求/响应长度；不含完整输入、请求 JSON、原始模型响应或 Key | ✅ |✅ 通过（代码审查确认，未新增单测） | 你在代码逻辑和单元测试中测试通过即可 ——**2026-08-20 代码审查**：`AppLogger.kt` 的 `write()`（`i`/`w`/`e` 唯一落盘入口）以 `if (!isDebuggable()) return` 收口，release 包（非 debuggable）**完全不落盘** `i`/`w`/`e` 级别日志，只有崩溃摘要（`writeSync`，专走未捕获异常处理器）例外——比"只含模型/状态码/耗时/长度"更严格（release 包压根没有这些日志文件）。debug 包不受限（供开发排查），符合预期。未找到覆盖 `AppLogger` 的专门单测（`androidApp`/`shared` 均无），本项目该工具耦合 `Context`，历史上也一直靠代码审查而非自动化测试把关，维持既有方式。 |

---

## 🔴 最新批次：日期上下文+括号解析+AI Token+刷新

### P0 批一：AI Flat 协议与安全确认

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| P0-1 | 开启 AI，食历切到 2026-10-24 后输入「2026-08-03 午餐米饭；2026-08-03 晚餐青菜」 | 预览仅显示 **2026-08-03** 一天，且有午餐、晚餐两个餐次；不被当前日期覆盖 | 🚫 | 过期|快速记中简单点只记录传入的日期，不在输入框内指定日期，否则太乱 ——2026-08-20 已归档为决策 D-26（日期不再支持在输入文本里指定，完全由快速记/周期记结构固定，见 `08_决策记录.md` D-26），无需再验证。 |
| P0-2 | 开启 AI，输入模型可返回的错误日期（如 2026-02-30）或非法份量 | 不进入可保存预览；保留原输入并显示可理解的规则降级/失败提示 | ⬜ |通过 | |
| P0-3 | 选择已有午餐记录的日期，AI 再识别一份午餐 | 预览明确提示已有餐食；首次按钮为「我已知晓，继续合并」，再次确认才保存；原记录保留 | ⬜ | 通过| |
| P0-4 | 输入缺少日期的单餐文本 | 预览显示“按当前选择日期记录”的提示，保存到当前食历日期 | ⬜ |通过 | |

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| **D1** | 食历切到 **2026-10-24**→AI 记一餐→输入"中午吃了红烧肉"→确认 | 红烧肉出现在 **10-24 的午餐**中（非今天 08-02） | 🚫 |过期 |原因见P0-1的原因说明 ——2026-08-20 已归档为决策 D-26（日期不再支持在输入文本里指定，完全由快速记/周期记结构固定，见 `08_决策记录.md` D-26），无需再验证。 |
| **D2** | 同上→输入多天文本"周一/周二/周三..."→确认 | 周一/周二/周三的日期按 **10-24 所在周**推算（非 08-02 所在周） | 🚫 |过期 |已经有周期记，不需要在快速记中加这些 ——2026-08-20 已归档为决策 D-26（日期不再支持在输入文本里指定，完全由快速记/周期记结构固定，见 `08_决策记录.md` D-26），无需再验证。 |
| **D3** | AI 记一餐→输入"凉皮（黄瓜丝+绿豆芽）"→发送预览 | 食材含"黄瓜丝""绿豆芽"（括号内被正确拆为食材） | ⬜ | 通过| |
| **D4** | 同上→确认记下 | 食历立即刷新显示新餐食 | ⬜ |通过 | |
| **D5** | **开启 AI 模型**（DeepSeek）→输入 3 天菜单→发送 | AI 完整返回（不截断）·预览正常·语法待完善 | ⬜ |通过 | |
| **D6** | 目标日设为 **2026-08-03（周一）**，关闭 AI，输入周三/周二/周一三天菜单（可倒序） | 预览为 08-03/08-04/08-05，不出现 07-28/07-29 | 🚫 |过期 | 原因同D2原因 ——2026-08-20 已归档为决策 D-26（日期不再支持在输入文本里指定，完全由快速记/周期记结构固定，见 `08_决策记录.md` D-26），无需再验证。 |
| **D7** | 确认 D6 | 添加餐食页关闭，进入该周周计划，三天餐食都可见 | ⬜ | 通过| |
| **D8** | 单日输入「中午吃了红烧肉和米饭」并确认 | 当前日期立即刷新；不跳周计划 | ⬜ |通过 | |
| **D9** | 输入 `凉皮（黄瓜丝+绿豆芽）+番茄青椒炒鸡蛋虾仁+苹果`，关闭 AI | 预览三道菜；凉皮名完整，含黄瓜丝、绿豆芽 | ⬜ | 通过| 凉皮命中已有菜品，未按照新的食材产生，这也算正常吧|
| **D10** | AI 未配置/无效时发送 | 预览显示「本次结果：规则解析（原因）」 | ⬜ | 通过| |
| **D11** | 可用 AI 时发送同类菜单 | 预览显示「本次结果：AI 解析」；日期和括号规则正确 | 🚫 |过期 | 规则解析的模板需要更新 你记到待办中，待办中先注明当前规则解析模板怎么样的，我看下如何调整比较好 ——2026-08-20 已登记 `F-AI-MEAL/30_待办.md` 的 `AIMEAL-RULE-TEMPLATE`（含当前模板现状原文摘录+已知与 D-26 决策矛盾之处），待你看完定新模板，本轮未改文案/未改解析规则。|
| **D12** | 用不存在食材的菜确认后，在食材页「家庭」查找 | 显示「家庭 · AI 创建」，可编辑/删除；预设不可删除 | ⬜ | 通过| |
| **D13** | 输入框已有文字后长按 | 系统菜单可复制/粘贴；右上「粘贴」按钮仍可用 | ⬜ | 过期|下一步 粘贴按钮要去掉了，修正输入框无法响应长按事件 |
| **D14** | 查看 AI 记一餐右上说明 | 有多天模板；说明菜之间用 `+`、配料写在 `（）` | 🚫 |过期 |模板要重新规划 见D11 ——2026-08-20 见 D11 处置说明。|
| **D15** | 多天预览连续点两次确认 | 同日期同餐次不重复写入菜/餐次 | ⬜ | 通过| |

---

## 🔴 第一批：核心解析修复

> **必验·装包后第一件事**。以下用例修复了"吃了红烧肉和米饭"→菜名含"吃了"+"和"不拆菜+"肉和米饭"成食材的全链路 Bug。

### FIX-1 吃动词剥离 + 软分隔修复

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F1-1 | ①首页→AI快捷记一餐→输入"**中午吃了红烧肉和米饭**"→发送 | 预览页两道菜：**菜名都不含"吃了"**+"和"已正确拆菜 | ⬜ | 通过| |
| F1-2 | 同上→看每道菜热量 | 每道菜下方**有营养值**（"整份约 X 千卡"）·**不显示"营养待完善"** | ⬜ |通过 | |
| F1-3 | 同上→点**确认记下** | Sheet关闭→食历今日**午餐出现红烧肉+米饭两道菜** | ⬜ | 通过| |
| F1-4 | ①食材管理→搜索"**吃了**" | **不应存在**名为"吃了"的食材 | ⬜ | 通过| |

### FIX-2 吃/喝动词同类覆盖

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F2-1 | AI输入"**中午准备吃红烧鹅炒青菜煮玉米和白米饭**"→发送 | 菜名不含"准备吃" | ⬜ | 通过| |
| F2-2 | AI输入"**中午想吃红烧肉**"→发送 | 菜名"红烧肉"·不含"想吃" | ⬜ |通过 | |
| F2-3 | AI输入"**晚上打算吃火锅**"→发送 | 菜名"火锅"·不含"打算吃" | ⬜ | 通过| |
| F2-4 | AI输入"**早餐要喝牛奶**"→发送 | 菜名"牛奶"·不含"要喝" | ⬜ |通过 | |

### FIX-3 AI 模式 Schema 修复

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F3-1 | **开启 AI 模型**（我的→AI设置→打开云端模型）→输入"中午吃了红烧肉和米饭"→发送 | 预览正常·菜名正确·热量有值 | ⬜ |通过 | |
| F3-2 | **关闭 AI 模型**→同上输入→发送 | 预览同样正常（规则兜底） | ⬜ | 通过| |
| F3-3 | 对比 F3-1 和 F3-2 结果 | 两次结果合理一致（AI 模式下解析更精准是期望的） | ⬜ | 通过| |

---

## 🟡 第二批：语音 + autogen 数据完整性

### FIX-4 语音录音修复

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F4-1 | AI快捷记→**长按🎤按钮** | 按钮变主色+**波形条出现**+文字"正在聆听…松手结束" | 🚫 |过期 |录音功能先去除了 ——2026-08-20 已隐藏（见 E-B6-VOICE-01），此项无需再验证。 |
| F4-2 | 说话→松手 | 文字"识别中…"→**识别结果填入输入框** | 🚫 | 过期|同上（见 E-B6-VOICE-01） |
| F4-3 | 首次长按（无权限时） | 弹出**麦克风权限请求** | 🚫 |过期 |同上（见 E-B6-VOICE-01） |

### FIX-5 autogen 数据完整性

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F5-1 | AI输入任意菜→预览→点确认记下→**再输同样内容**→再确认 | 同名菜不重复创建（`dish` 表 `name` 查不重复） | ⬜ | 通过| |
| F5-2 | 食材管理→搜 AI 建的食材 | 无空白名称食材·无不正常名称食材 | ⬜ | 通过| |
| F5-3 | AI输入"差不多一碗面"→预览 | 食用比例不为 0.75（"差不多"不再被误判为吃了一半） | ✅ | ✅ 通过（代码+单测核验，非真机目测）|【历史】未知，这是什么功能？没找到 ——**2026-08-20 回复**：这条不是一个能在界面上点开看到的功能——"食用比例"是规则解析器从"差不多一碗面"这类口语里试图识别"吃了多少"的一个内部数值，直接影响这道菜记进营养统计时的折算比例，AI 快捷记的确认页目前没有任何地方把这个数值显示出来（吃了多少的调整入口按项目现有规范只在"今日卡"暴露），所以真机上确实找不到对应的界面操作，不是你没找到，是这项本身不可见。代码走查：`RuleMealParser.kt` 的 `EATEN_PATTERNS` 只匹配"一半/大半/少量"类关键词，不含"差不多"，所以"差不多一碗面"不会被误判成"吃了一半"（当前代码是对的）；新增单测 `差不多一碗面不被误判为吃了一半`（`RuleMealParserRegressionTest.kt`）锁定这个行为，`:shared:testDebugUnitTest` 通过。 |

---

## 🟢 第三批：已有功能回归（防退化）

> 以下为之前批次已验证的功能，本次改动不应影响。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| R1 | AI输入"中午吃了红烧肉和米饭"→预览→**不点确认**→点外部关闭 | 食材库/餐食**无新增记录**·预览零写库 | ⬜ | 通过| |
| R2 | 食材详情→AI建的食材 | 营养 `ref` 显示"**自动估算**" | ⬜ |通过 | |
| R3 | 食材管理页→**待复核 Tab** | Tab 正常显示+Badge 数量正确 | ⬜ |通过 | |
| R4 | AI输入"**周三**午餐吃了红烧肉"（今天周四） | 日期推算为昨天(周三) | 🚫 |过期 |日期不在输入框中显示，快速记和周期记已经固定了日期 ——2026-08-20 已归档为决策 D-26（日期不再支持在输入文本里指定，完全由快速记/周期记结构固定，见 `08_决策记录.md` D-26），无需再验证。 |
| R5 | AI输入含"**盐**"的菜 | 盐默认 2-3g（非 100g） | ⬜ | 通过| |
| R6 | AI输入"**西红柿炒蛋**" | 食材不重复建"番茄"（已有→复用） | ⬜ | 通过| |
| R7 | AI输入"**宫保鸡丁**" | 不被拆分为多道菜（"保"不是分隔词） | ⬜ |通过 | |
| R8 | AI预览→点**重新输入**→改文字→再发送 | 正常重新解析·不残留旧状态 | ⬜ | 通过| |

---

## ⚪ 第四批：K1+K2 之前遗留

> 来自前次 session，非本次改动，择机验证。
> ⚠️ 编号 2026-08-08 由 `L1~L4` 改为 `LEG-1~4`（legacy 遗留项），避免与本文件"L1 云端 AI 首启同意"批次（`E-L1-01~12`）撞名混淆。

| # | 操作 | 预期 | 状态 | 验证结果 | 原因 |
|----|------|------|------|------|------|
| LEG-1 | AI预览→热量数字开关 | 我的→功能设置→关"热量数值"→预览不显千卡 | ⬜ |通过 | |
| LEG-2 | AI预览→「新」标 | 库里没有的菜→右侧"新"小标签；库里有的→无标 | ⬜ |通过 | |
| LEG-3 | 粘贴按钮 | 剪贴板有文字→AI输入框右上角出现"📋粘贴" | ⬜ |过期 |【历史】现在不需要了 ——**2026-08-20**：本想按此意见删掉，但 `E-B4-03` 排查发现长按系统菜单在这个 Sheet 里大概率结构性不可用（Material3 版本限制），删掉按钮前必须先确认有替代方案，否则用户会完全没法粘贴——**本轮暂缓删除**，跟 `AIMEAL-UX-REDESIGN` 待办一起讨论定。 |
| LEG-4 | 多天预览 | 输入"周一吃了X 周三吃了Y"→预览多天 | 🚫 |过期 |现在快速记和周期记中固定了日期 ——2026-08-20 已归档为决策 D-26（日期不再支持在输入文本里指定，完全由快速记/周期记结构固定，见 `08_决策记录.md` D-26），无需再验证。 |

---

## 📋 快速验证路径（15 分钟跑完核心）

```
F1-1 → F1-4 → F2-2 → F3-1 → F3-2 → F4-1 → F4-2 → R1 → R3
```

---

## ⚠️ 已知：数据库脏数据

以下为本次 Bug 产生的历史脏数据（在 `GCL0220212004523` 真机上）：

| 表 | ID | 名称 | 建议 |
|----|----|------|------|
| ingredient | 1207 | 吃了 | 手动删除 |
| ingredient | 1208 | 肉和米饭 | 手动删除 |
| dish | 774 | 吃了红烧肉和米饭 | 软删 |
| dish | 776 | 吃了土豆粉 | 软删 |

---

## 🔵 MEAL-UX-CONSOLIDATION-01：本批新增真机待验证项

> 代码/单测/构建已完成；以下项目未在本环境执行真机交互，不标记为通过。

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-UEN-17 | Single+AI 生成到 Preview → 切 Manual → 切回 AI | 无放弃确认，原 Preview 恢复 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-UEN-18 | Period+AI Preview → Manual → AI | 无放弃确认，原 Preview 恢复 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-UEN-19 | AI Preview 时尝试切 Single/Period | Range 选择器置灰且范围不变 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-UEN-20 | AI Generating/Saving 时尝试切换两个 selector | 两个 selector 均不可切，状态不变化 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-UEN-21 | Unified 入口有 dirty 草稿时按返回并确认离开 | 原 UnsavedGuard 出现；确认后可 cancel/pop | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-DC-01 | 添加餐食 → 单天 → 手动选择 | 页面无昨天/今天/明天快捷 chips | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-DC-02 | 打开添加餐食日期月历 | 与食历使用同一共享月历，已有餐食日期点击前有圆点 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-DC-03 | 点击已有餐食日期/空日期 | 已有日期仍由 VM 拒绝并提示；空日期切换且草稿保留 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-DC-04 | 月历选择空日期与当前日期 | 空日期可选；当前日期 selected 明显 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-DC-05 | 点击已有餐食日期 | 日期不切换，并保留“去食历编辑”提示 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-DC-06 | 从食历打开月历后返回 | 原 meal-only navigation 行为无回归 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-DC-07 | Single+Manual 选菜后保存 | CTA 可用并能完整保存 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-DC-08 | 深色/浅色查看日期 marker | dot 与 selected 都可辨识 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-HM-01 | 首页无今天记录但有未来记录 | 今天占轻量槽位，未来最多显示两天，查看全部食历始终可达 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-HM-02 | 首页今天卡与未来卡 | 今天角标橘底白字；未来角标透明底灰字 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-HM-03 | 删除今天全部餐食 | 今天位置显示轻量“今天还没记 / 记一餐”占位卡 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-HM-04 | 未来无安排 | 页面不显示“接下来还没安排” | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-HM-05 | 开启营养色系后查看今天/未来卡 | 角标与日期色仍能区分今天和未来 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-HM-06 | 点击“查看全部食历” | 进入食历页 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-HM-07 | 新用户点击“查看全部食历” | 食历空态可用，不是死页面 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |
| E-HM-08 | fontScale 调至最大档 | 查看全部食历行与今天占位卡文字不重叠/不裁切 | PENDING_DEVICE_VERIFICATION | | 本环境无真机交互 |

## 🔵 UEN：统一添加入口 + 悬浮导航栏（E-UEN-01~21）

> 代码批次：`AIMEAL-UNIFIED-ENTRY-NAVCOMPACT-01`。以下项目需在真机上逐项确认；未完成前不得将对应状态改为通过。
> **2026-08-20 ARCH 复核发现 2 项阻断（AF-UEN-01/02，详见蓝图 §22.1）**：`E-UEN-01`（单天+手动选择实测无法保存）与新增 `E-UEN-16`（AI保存后无跳转+假性未保存提示）**在 CODE 二轮修复交付前不可能验证通过**，跳过本轮真机验证，等 `luna` 修复后再统一验一遍。

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-UEN-01 | 首页点击独立悬浮“+”，切到“手动选择”选完菜后保存 | 进入“记录饮食”全屏页默认“单天+AI快捷”；切手动选择后能看到“保存”按钮并成功保存 | 🔧 待真机确认 | | AF-UEN-01 已由代码修复，待回归 |
| E-UEN-02 | “单天/周期”切换 | 只切换范围，AI/手动选择保持不变，草稿不丢失 | ⬜ | | |
| E-UEN-03 | “AI快捷/手动选择”切换 | 只切换输入方式，当前范围保持不变 | ⬜ | | |
| E-UEN-04 | AI 已有预览时切换范围 | Range 选择器置灰，不弹放弃确认，范围保持不变 | 🔧 待真机确认 | | AF-UEN-03 修订后的新语义 |
| E-UEN-05 | AI 保存中切换范围/方式 | 选择器置灰，状态不变化 | ⬜ | | |
| E-UEN-06 | AI 输入草稿后切换到手动再切回 AI | 原输入草稿仍在 | ⬜ | | |
| E-UEN-07 | 手动已选菜后切换到周期再切回单天 | 原手动选择仍在 | ⬜ | | |
| E-UEN-08 | 周期 + 手动点击空日“安排这天” | 进入该日期的单天手动编辑页 | ⬜ | | |
| E-UEN-09 | 从周期手动子页保存/返回 | 返回统一入口，仍处于“周期 + 手动” | ⬜ | | |
| E-UEN-10 | 统一入口有草稿时按物理返回 | 弹未保存确认；确认后退出并取消 AI 生成 | ⬜ | | |
| E-UEN-11 | 所有草稿为空时按物理返回 | 直接退出，不弹确认框 | ⬜ | | |
| E-UEN-12 | 食历“编辑某天餐食” | 仍直接进入原单天编辑页，不经过统一入口 | ⬜ | | |
| E-UEN-13 | AI 输入框长按 | 弹出系统复制/粘贴菜单 | ⬜ | | |
| E-UEN-14 | 三键导航与手势导航设备 | 悬浮胶囊、“+”按钮均不被系统导航栏遮挡 | ⬜ | | |
| E-UEN-15 | 浅色/深色主题 | 胶囊层级、“+”对比度、标签文字均清晰可读 | ⬜ | | |
| E-UEN-16 | 完整走一遍 AI 快捷（单天或周期）保存 | 保存成功后自动离开统一入口并有反馈（如 Snackbar）；随后再次打开统一入口/按返回不应出现“你的改动还没保存”的假提示 | 🔧 待真机确认 | | AF-UEN-02 已由代码修复，待回归 |

## 🔵 OBS-NEXT：运行时 Trace 对齐与隐私收口（E-OBS-NEXT-01~07）

> 自动化状态：`OBS-NEXT-A/B=AUTOMATED_GATES_PASS`。已通过 shared/Android debug/release 单测、assemble、结构质量与 8 类隐私负例；以下仍必须在最终真机批次执行。

| # | 验证标题 | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|---|
| E-OBS-NEXT-01 | 会话与 JSONL envelope | 安装 debug，强停后启动，完成一次普通操作，从“我的→日志查看”导出当天 JSONL。 | 每行可独立解析；首条含 `system.session_started`；同一 session 的 `seq` 严格递增。 | PENDING_DEVICE_VERIFICATION | | 最终统一真机执行 |
| E-OBS-NEXT-02 | Release 普通日志门禁 | 安装 release，正常浏览、搜索、记一餐后导出日志目录。 | 不产生普通 INFO/WARN/ERROR 日志；若存在普通操作日志即失败。 | PENDING_DEVICE_VERIFICATION | | 最终统一真机执行 |
| E-OBS-NEXT-03 | Crash 单出口与隐私 | debug 制造可控崩溃后重启并导出当天 JSONL/目录。 | 只允许当天 JSONL 内的崩溃标记；不存在 `crash_reported.log` 或原始 exception message。 | PENDING_DEVICE_VERIFICATION | | 最终统一真机执行 |
| E-OBS-NEXT-04 | AI 推荐 Trace 连续性 | 从两个 AI 推荐入口分别进入并连续触发两次推荐，导出 JSONL。 | 首次操作沿用路由 UUID；后续操作生成新 trace；交错操作不串链。 | PENDING_DEVICE_VERIFICATION | | 最终统一真机执行 |
| E-OBS-NEXT-05 | 保存/推荐终态唯一性 | 各执行一次成功、失败与取消路径，导出 JSONL。 | 每次操作只有一个 terminal 与一个 duration；取消不伪装成失败。 | PENDING_DEVICE_VERIFICATION | | 最终统一真机执行 |
| E-OBS-NEXT-06 | MDC3 结构化投影链 | 编辑菜品、食材与餐食关联后导出 JSONL，再回到相关展示页。 | 仅出现结构化 `meal_data.stage(source,count)`；不含日期原文、token 或列表；投影/UI 更新正确。 | PENDING_DEVICE_VERIFICATION | | 最终统一真机执行 |
| E-OBS-NEXT-07 | 日志全路径隐私 | 使用语音、搜索、建菜、加餐、AI 推荐并导出 JSONL。 | 不含识别文本、菜名、路由、日期、dish ID 列表、analytics params、异常 message/stack 或 UI error 原文。 | PENDING_DEVICE_VERIFICATION | | 最终统一真机执行 |

## 🔵 FOUNDATION-OBSERVABILITY-01：结构化日志（E-OBS-01~03）

> 代码/单测/构建已完成；以下项目必须真机逐项确认，Foundation 与 MDC3 项均不得用静态证据代替运行时证据。

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-OBS-01 | 安装 debug，强停后启动，从“我的→日志查看”导出当天文件 | 每行均为 JSON；首条含 `system.session_started`、session、连续 seq；无敏感字段 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-OBS-02 | 本批不接真实业务埋点，检查 Foundation 状态 | 状态固定为 `DEFERRED_TO_FIRST_INSTRUMENTATION_BATCH`，不得标 PASS | DEFERRED_TO_FIRST_INSTRUMENTATION_BATCH | | 按蓝图约束留待首个埋点批次 |
| E-OBS-03 | 清除旧日志，安装 release 并正常操作后导出目录 | 本次 session 无普通日志记录；出现 INFO/WARN/ERROR 普通文件记录即失败；crash 另测 | PENDING_DEVICE_VERIFICATION | | 本环境未执行 release 真机交互 |

## FOUNDATION-OBSERVABILITY-02：业务 Trace（E-OBS-BIZ-01）
| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-OBS-BIZ-01 | 安装 debug，进入“记录饮食”完成一次 Meal Save，并从日志查看导出当天文件 | 同一 `trace_id` 依次可查 `meal.save.clicked`、`operation.started`、`repository.operation`、`operation.finished`；成功/失败/取消终态分别准确；无原始饮食内容 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
## FOUNDATION-OBSERVABILITY-02-R1：Business Action Trace
| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-OBS-ACTION-01 | 在首页下一餐卡点击 AI 推荐入口 | 同一 `trace_id` 可查 `ui.action.clicked`，不含用户输入或饮食明细 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-OBS-NAV-01 | 从首页/我的页进入 AI 推荐 | 可查 `navigation.started`、`navigation.completed`、`screen.entered`、`screen.loaded` | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-OBS-STATE-01 | AI 推荐页加载、成功和失败 | 可查 `state.changed`，状态转移与结果一致 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-OBS-AI-01 | Meal Edit → AI Recommend；Record Meal → Manual Select → AI Recommend | 两个入口均可串联 Action → Navigation → Screen → State → Operation → Result | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |

## COOKBOOK-OVERNIGHT-01：Observability Completion（E-OVN-01~03）

> 本批仅完成诊断契约与自动化证据；未执行真机交互，不将运行时日志项目标记为通过。

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-OVN-01 | 真机执行一个可失败的诊断操作并导出 JSONL | 可按同一 `trace_id` 找到 `operation.failed`、`operation.error` 与 `operation.duration`；错误项只有类型/代码标识，不含异常消息或业务输入 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-OVN-02 | 真机执行一个成功的诊断操作并导出 JSONL | 可按同一 `trace_id` 找到成功终态与非负耗时 `duration_ms` | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-OVN-03 | 检查错误与耗时事件字段 | 不出现原始饮食文本、异常 message、密钥或用户标识；自动测试已覆盖脱敏契约 | PENDING_DEVICE_VERIFICATION | | 字段运行时核对待真机 |

## COOKBOOK-OVERNIGHT-02：AI Recommend Diagnostic（E-OVN-04~05）

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-OVN-04 | 从 Meal Edit 进入 AI Recommend，导出同一 trace 的 JSONL | 可识别入口 `meal_edit`，并与 `recommend.route`、导航和推荐操作共享同一 `trace_id` | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-OVN-05 | 从 Record Meal → Manual Select 进入 AI Recommend，导出同一 trace 的 JSONL | 可识别入口 `record_meal_manual`，与 E-OVN-04 不混淆；不出现输入文本或菜名 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |

## COOKBOOK-OVERNIGHT-03：Trace Quality Testing（E-OVN-06）

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-OVN-06 | 真机导出一组包含 UI、导航、操作、错误和耗时事件的 JSONL | 每个 `event` 均属于 `TraceEventContract`，字段类别与事件名匹配；未知事件应被识别为契约错误 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |

## COOKBOOK_ARCHITECTURE_EVOLUTION_PHASE_BATCH：Meal Domain Governance（E-AEP-01~05）

> 本批只生成验证资产，未执行真机验证；下列项目均保持 `PENDING_DEVICE_VERIFICATION`。

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| E-AEP-01 | 记录饮食→手动→AI 推荐→返回并选菜 | 原餐次草稿保留，结果只消费一次 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-AEP-02 | 搜索→新建菜品→保存返回 | 回到搜索页并收到受控结果 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-AEP-03 | 添加餐食→库存选择→返回 | 原页面状态保留并完成 MERGE | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-AEP-04 | 添加餐食→新建菜品→保存 | 父页面收到 `createdDishId` | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| E-AEP-05 | 食历→编辑餐食→保存 | 完整 Trace 可诊断为 `FLOW_PASS` | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |

## P5-A Repository Mutation Boundary

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| DEV-P5-01 | 首页、食历、一周计划删除并撤销同一天 | 仅删除/恢复一次，餐次、时间、备注、菜品顺序一致 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| DEV-P5-02 | 删除后撤销并检查常做排序 | 撤销不增加 preference | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| DEV-P5-03 | 调整单菜与整餐食用比例后重启 | 比例 clamp 并持久化 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| DEV-P5-04 | AI 快捷记餐 preview/确认/merge | Phase 4 行为和比例回填不退化 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
| DEV-P5-05 | 计划冲突确认前新增第二冲突 | 旧确认失效且确认前零写 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |

## Bug-5305：手动营养大类优先

| 编号 | 操作 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---|---|---|---|---|
| DEV-5305-01 | 食材管理→新增或编辑“鸡蛋”→确认自动预选→手动点“蔬菜类”→修改名称/等待分类加载后保存并重开；再点“保存并继续”新建下一食材 | 名称变化和分类加载后仍显示并持久化手选“蔬菜类”；新表单可重新自动预选 | PENDING_DEVICE_VERIFICATION | | 本环境未执行真机交互 |
