# 真机待验证清单

> 最后更新：2026-08-18 · 新增 B7 后续批（日志门禁+标签清洗+回归测试+死代码清理+NDJSON 协议容错收窄，`E-B7F-01~05`）—— 构建+全量 shared 单测通过、经 `google_quality_engineer` 审查，**唯一悬而未决的仍是真机验证本身**。此前 L1/K1i 两批已完成 ARCH 独立复核（diff 走查 + 三条构建命令复验 + 闸门唯一性核对，均通过，无阻断），代码层面已就绪，真机验证仍未做。
> 本次改动累计：**B7 后续批（`E-B7F-*`，最新）** + K1i（流式委托）+ L1（云端 AI 同意）+ 上次 K1a + 引擎标签/AI→规则自动兜底 + B6 修复批 + B4/B5 输入/流式 UI

> **🆕 本次必验**：E-B7F-01~05（本次新增，见下方专属小节）；此前 E-L1-01~12 + E-K1I-01~02 仍待验，装包后按顺序跑。存量已配云端 Key 的设备会触发 grandfather 补确认。

> **本次必验标识**：`🆕 本次必验` = 本次包必须验证；`⬜` = 既有待验回归项；`🔧` = 已修复待真机确认；`✅` = 已真机验证通过。

> **📝 反馈列（2026-08-09 用户确立·全表统一）**：每个验证项表在"状态"列之后新增 **`验证结果` + `原因` 两列**，是留给用户填写反馈的——验证结果填 `✅ 通过` / `⚠️ 部分通过` / `❌ 未通过` / `跳过`，原因写具体现象（哪一步不符合预期、报错/异常/截图描述）。用户填完这两列，AI 直接读即可精确定位问题，无需反复追问。此规范已同步进 `CLAUDE.md`「交付必做：真机待验证登记」。

## 📊 全量待验证汇总（截至 2026-08-18，本文件唯一真相源）

**未验证合计 102 项**（`🔧` 26 + `⬜` 76），**已验证通过 17 项**（V2 批次）。**全部处于"代码/单测已通过，从未在真机上端到端走过一遍"的状态**——这是 AI 记一餐主线当前最大的未知数，不是某一小批的问题。

| 批次 | 编号前缀 | 项数 | 状态 | 备注 |
|---|---|---:|---|---|
| B7 后续批（日志门禁+NDJSON协议容错） | E-B7F-01~05 | 5 | 🔧 | **最新批次**，见专属小节；多数项需"多用几次观察不复发"而非单点复现 |
| L1 云端 AI 首启同意 | E-L1-01~12 | 12 | 🔧 | 含阻断性判据的 grandfather/常驻状态块/关闭重启用等 |
| K1i 流式地基 | E-K1I-01~02 | 2 | 🔧 | **E-K1I-01 阻断性**（流式协议切换真机把关，4 条判据+回退方案） |
| K1a 营养展示统一化 | E-K1A-01 | 1 | 🔧 | |
| 引擎标签+AI→规则兜底 | E-CFG-01~06 | 6 | 🔧 | |
| B6 架构复核修复批 | E-B6-* | 8 | ⬜ | |
| B4 输入 UI | E-B4-01~06 | 6 | ⬜ | |
| B5 确认页流式展示 | E-B5-01~10 | 10 | ⬜ | |
| V2 追加：失败详情/健康建议 | V2-1~17 | 17 | ✅ | 已于 2026-08-05 真机验证通过 |
| P0 批二：解析容错/日期时间/隐私日志 | P0-5~8 | 4 | ⬜ | |
| P0 批一 + 日期上下文/括号解析 | P0-1~4、D1~D15 | 19 | ⬜ | |
| 第一批：核心解析修复 | F1-1~4、F2-1~4、F3-1~3 | 11 | ⬜ | **建议优先**——修的是"吃了红烧肉和米饭"这类全链路基础 Bug |
| 第二批：语音+autogen 完整性 | F4-1~3、F5-1~3 | 6 | ⬜ | |
| 第三批：已有功能回归 | R1~R8 | 8 | ⬜ | 防退化基线 |
| 第四批：K1+K2 之前遗留 | LEG-1~4 | 4 | ⬜ | 2026-08-08 改名避免与 L1 撞号，见下方 |

> 建议验证顺序：**B7F 快速路径（最新，最贴近日常使用）→ L1 快速路径 → K1i（E-K1I-01 阻断性优先）→ K1a/CFG → 第一批核心解析（F1/F2/F3）→ B4/B5/B6 → 其余回归批次**。各批次内部有自己的"📋 快速验证路径"3~5 分钟核心子集，见对应小节。

---

## 🔴 本次新增：L1 云端 AI 首启同意 + 合规免责（CODE 交付 · 蓝图 `L1_云端AI首启同意与合规免责_实施蓝图.md` §8.2/§9）

> 2026-08-08 CODE（Claude Code）实施。核心：①新增同意状态模型（偏好 JSON 免迁移：NOT_ASKED/GRANTED/DECLINED/GRANDFATHER_PENDING）；②真正的联网闸门下沉到 `SwitchableAiRuntime.complete()`（全 App 唯一云端分发入口，未同意直接短路，复用各消费点既有失败兜底）；③AI 设置页新增常驻状态块/关闭/重新启用；④存量已配 Key 用户进设置页弹 grandfather 补确认；⑤隐私政策新增"云端 AI"小节并订正"绝对不上传"为"默认不上传，除非自行启用云端 AI"。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| E-L1-01 | grandfather 补确认面板（存量已配 Key 用户） | ①安装新包前设置页已配过任意厂商 Key（如智谱）②打开 AI 设置页 → 应弹出"这项功能你已在使用"的补确认面板（含会发送/不会发送清单）③点"启用云端 AI"→ 面板关闭、常驻状态块出现；④重进设置页 → 不再弹（已记录同意）；⑤补确认面板点"暂不启用"→ 云端单选自动切"规则推荐"，再次进入不弹 | 🔧 | | |
| E-L1-02 | 常驻状态块 | ①同意启用后 AI 设置页云端子块下方出现"云端 AI 已启用"卡片（已启用标/接收方=厂商名/同意时间/发送哪些内容 ›/关闭）②点"发送哪些内容 ›"→ 只读披露面板（"知道了"关闭）③清空该厂商 Key → 状态块消失（不与红色"密钥：未配置"同屏矛盾） | 🔧 | | |
| E-L1-03 | 关闭后重选云端→重新启用入口 | ①已同意 → 状态块点"关闭"→ ActionSheet 两档（默认"保留密钥并关闭"在前、"关闭并删除密钥"红字在后）②选"保留密钥并关闭"→ 云端单选自动切"规则推荐"，Key 仍在③手动把单选切回"云端大模型"→ 云端子块显示"云端 AI 已被你关闭 · 重新启用 ›"④点它 → 弹出完整同意面板（与首次同款）⑤点"启用云端 AI"→ 恢复已启用、状态块回归 | 🔧 | | |
| E-L1-04 | 首次填 Key 走完整同意面板 + 反馈时序 | ①未同意时点"设置密钥"填有效 Key → 点"保存"→ 弹出完整同意面板（不是直接保存联网）②点"启用云端 AI"→ Key 保存、面板关闭，随后 Snackbar"已启用云端 AI"（弹层关闭后才出现，不被弹窗遮挡）③AI 快捷记一餐输入发送 → 标题标签"AI · <模型名>"、正常走 AI | 🔧 | | |
| E-L1-05 | 同意面板"暂不启用"→ 不保存 Key 不外发 | ①同 E-L1-04 填 Key 点保存 → 面板点"暂不启用"→ Key 未保存（仍显示"密钥：未配置"）②云端单选自动切"规则推荐"③AI 快捷记输入发送 → 无网络请求、本地规则即时出结果，标签"规则解析" | 🔧 | | |
| E-L1-06 | 已同意后换厂商 → 轻量换厂商确认 | ①已同意启用某厂商（如智谱）②模型下拉切到另一厂商（如 DeepSeek）③该厂商"编辑"填 Key → 保存 → 弹轻量确认"切换到DeepSeek云端模型？"④点"看看发送哪些内容 ›"→ 只读披露面板（"知道了"关闭后不回弹换厂商框）⑤点"继续"→ Key 保存、正常启用；⑥已确认厂商后续保存不再重复弹 | 🔧 | | |
| E-L1-07 | 未同意/已关闭时任何 AI 功能不外发 | ①未同意（或已关闭）时：AI 快捷记输入发送 → 确认页显示"本次结果：规则解析（AI 解析失败：还没有同意把数据发给云端 AI）"，不转圈不发网络；AI 推荐显示离线规则回退；健康建议显示"还没有同意把数据发给云端 AI"且不生成建议②配合抓包/日志确认无网络请求 | 🔧 | | |
| E-L1-08 | 政策披露与订正 | ①我的→关于（或政策入口）→ 隐私政策 → 看到"四、云端 AI（默认关闭 · 需你自行配置密钥并单独同意）"新小节（会发送/不会发送清单），原"第三方 SDK"顺延为"五"、"政策更新与联系"为"九"；§一末尾有"默认不上传，除非自行启用云端 AI"限定句；标题"最近更新：2026年8月"②政策正文与同意面板"会发送/不会发送"清单表述一致 | 🔧 | | |
| E-L1-09 | 同意时间/接收方展示 | ①已启用状态块显示"同意时间"为当前日期、"接收方"为所选厂商名（如智谱）②切同厂商不同模型 → 接收方不变；换厂商需走 E-L1-06 | 🔧 | | |
| E-L1-10 | 删除密钥后行为 | ①已启用 → 状态块"关闭"→ 选"关闭并删除密钥"（红）→ Key 被清空、云端切"规则推荐"②再进设置页 → 无 grandfather 面板、无状态块、无"重新启用"行（DECLINED 且单选非云端）③重新填 Key → 走完整同意面板（E-L1-04） | 🔧 | | |
| E-L1-11 | 冷启动首屏弹层互斥 | ①未同意但已有 Key（存量 grandfather 态）→ 冷启动首屏快速点开"设置密钥"→ 不与 grandfather 面板同屏堆叠；关掉 KeyDialog 后 grandfather 面板正常弹出 | 🔧 | | |
| E-L1-12 | 全新安装与隐私小字 | ①清理 App 数据全新安装 → 无 grandfather 面板，首次填 Key 走完整同意面板②AI 设置页底部隐私小字与新增披露不矛盾（走云端只发所列清单，不发送完整健康档案） | 🔧 | | |

### 📋 L1 快速验证路径（3 分钟核心）

```
E-L1-04 → E-L1-01（若存量 Key）→ E-L1-03 → E-L1-06
```

---

## 🔴 本次新增：K1i 流式地基——运行时真实委托（CODE 交付 · 蓝图 `K1i_AI流式渐进展示_实施蓝图.md` §8.2/§9）

> 2026-08-08 CODE（Claude Code）实施。`SwitchableAiRuntime` 新增 `override fun stream()` 真实委托给底层 runtime（云端即 `CloudAiRuntime.stream()` 的真实 SSE 分片），替代接口默认的"complete() 一次返回整段包装成假 Delta"。本批修好后 AI 记一餐的模型请求真正按 NDJSON 协议输出（此前因 `complete()` 的 `response_format:json_object` 被强制成单一 JSON 对象、解析器一直走整体 JSON fallback 这条非设计主路径），**解析主路径会切换**——故 E-K1I-01 为阻断性真机项。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| E-K1I-01 | 流式真委托生效 + 解析主路径切换（**阻断性**） | 配置真实云端 AI → AI 快捷记·快速记输入"中午吃了红烧肉和米饭"→发送 → ① 单次生成过程中诊断信息/日志出现 **≥2 次** Delta 事件（真委托生效，改造前恒为 1 次）；② 诊断列表**不再出现**"未检测到 NDJSON 事件，已按整体 JSON 格式规范化"这条 WARNING（改造前几乎必然出现）；③ 最终解析出的餐食/菜品与改造前同一输入结果**等价**（人工比对，结构/数量不变，措辞可差异）；④ ①②③任一不满足 → 回退：临时 revert `AiRuntimeConfig.kt` 单文件（`SwitchableAiRuntime` 绑定改回不接线本批 stream override），无需连带 revert 其他文件 | 🔧 | | |
| E-K1I-02 | 周期记流式同样生效 | 周期记填周一/周三/周五 → 发送 → 三天均正常渐进解析出餐食，最终确认页日期对应正确星期；诊断信息 Delta 事件 ≥2 次（非每段恒 1 次整段） | 🔧 | | |

### 📋 K1i 快速验证路径

```
E-K1I-01 → E-K1I-02
```

---

## 🆕 本次新增：B7 后续批——日志门禁 + 标签清洗 + 回归测试 + 死代码清理 + NDJSON 协议容错收窄（2026-08-18）

> B7（CREATE 路径做法/烹饪方式/标签/描述丢失）修复后排查出的 10 项后续待办，加用户追加"AI 返回内容被本地规则拦截"问题，由 Opus 出详细方案后实施。构建（`:shared:testDebugUnitTest` + `:androidApp:assembleDebug`）与全部 shared 单测通过，经 `google_quality_engineer` 审查。详见 `projectReview/features/F-AI-MEAL.md` 现状 + `08_决策记录.md` D-24。**本批多数改动是协议层容错/日志门禁，真机上大多只能"用起来观察不再复发/不再丢内容"，很难精确复现某个特定分支——按下方步骤多输入几组不同的餐食内容跑一遍即可。**

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| E-B7F-01 | 标签清洗（手工建菜路径） | 菜品编辑→标签栏输入带前后空格的标签（如" 下饭菜 "）→保存→重进详情 | 标签显示为去除空格后的"下饭菜"，不出现空白标签或重复标签 | 🔧 | | |
| E-B7F-02 | 确认页新增计数文案 | AI 快捷记·输入"中午吃了两道没记录过的新菜"→发送→到确认/预览页 | 健康摘要区出现"本次将新建 X 道菜、Y 种食材（营养为自动估算，仅供参考）"一行（X/Y 按实际识别数量），不与既有健康档案提示重复堆叠 | 🔧 | | |
| E-B7F-03 | 周期记多天多菜正常性回归（协议容错收窄不应引入新丢失） | 周期记填"周一到周五每天三餐"（较长输入，触发多段解析）→发送→等生成完成 | 各天各餐菜品数与实际输入基本对应，**没有出现"明明说了但确认页没有"的菜**；若解析过程中出现提示文案，读起来是人话（如"有 N 道菜的信息不完整，已跳过——可以重说一遍，或改用规则解析"），不是"dish_id「xxx」格式无效"这类协议原文 | 🔧 | | |
| E-B7F-04 | 快速记正常性回归 | 快速记输入"晚上吃了番茄炒蛋和青菜汤" | 两道菜都正常识别进确认页，热量/营养行正常显示，与本批改动前行为一致（无退化） | 🔧 | | |
| E-B7F-05 | Release 包日志门禁（可选，需 release 包） | 若手头有 release 签名包：正常使用 AI 记一餐若干次后，我的→关于→日志查看 | 若当天无崩溃：日志列表为空，空态下出现"正式版本仅记录崩溃摘要，用于排查闪退；日常操作日志不落盘"提示；debug 包不受影响，正常记录 | 🔧 | | |

### 📋 快速验证路径（3 分钟核心）

```
E-B7F-04 → E-B7F-02 → E-B7F-03
```

---

## 🔴 本次新增：K1a 营养展示统一化（commit `5c976a49`）

> 2026-08-08 CODE（Claude Code）实施。CREATE 菜营养计算统一到 `NutritionCalculator`（单一真相源，含蛋白/脂肪/碳水/钠全字段）；REUSE 菜（AI 识别为库内已有）由 `previewAll()` 批量回填真实营养；预览页复用 `DishNutritionLine`（与首页/食历/AI推荐视觉一致）。经独立 Google 质量终审，无阻断项。蓝图 `AI记一餐_K1a营养展示统一化与未配置报错_实施蓝图.md` §9。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| E-K1A-01 | AI 快捷记预览页营养展示（CREATE + REUSE 两类菜 + 估算尾注） | ①输入"中午吃了红烧肉和米饭"→发送→预览页**每道菜**下方显示营养行：宏量色点（蛋白绿/脂肪琥珀/碳水蓝）+ 热量（受开关）+ 钠偏高时浅灰"偏咸，注意用量"；**已存在库里的菜**（如提前在菜品库建过"红烧肉"）也应显示营养行（不再是无营养的空白）；含大类均值估算食材的菜行尾有"（估算）"尾注；营养数值与首页/食历同款视觉 | 🔧 | | |

---

## 🔴 本次新增：AI 快捷记引擎标签 + AI→规则自动兜底

> 2026-08-08 ARCH 直接实施（用户报告"未配置 AI 时报错文案误导"→ 重新设计为"规则是 AI 的兜底"）。经两轮独立 Google 质量审查发现并修复 5 处真实阻断（详见蓝图 `AI记一餐_K1a营养展示统一化与未配置报错_实施蓝图.md` 顶部追记）。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| E-CFG-01 | 引擎标签始终可见 | ①我的→AI设置→**关闭/清空** Key→回到 AI 快捷记→标题"AI 快捷记一餐"旁应显示灰色小标签"**规则解析**"②去设置里填一个有效 Key→返回（不用重新打开 App）→再次点开 AI 快捷记→标签应变成"**AI · <模型名>**"（无需重装/重启） | 🔧 | | |
| E-CFG-02 | 未配置 AI 时发送直接走规则 | ①保持未配置 Key②输入"中午吃了红烧肉和米饭"→发送③**不应有转圈等待 AI 网络请求的过程**（本地直接给结果，几乎瞬间完成）④确认页应有提示"本次结果：规则解析" | 🔧 | | |
| E-CFG-03 | AI 配置了但解析失败→自动回退规则+说明原因 | ①配置一个**无效/错误的** Key（或临时断网）②发送→③确认页应正常出现（不再进阻断性错误页）④确认页应显示"本次结果：规则解析（AI 解析失败：<具体原因>）"，原因应是人话（如"网络请求异常"），**不应出现内部代号**（如 `STREAM_COLLECT_ERROR` 或 `AI_NOT_CONFIGURED` 这类英文技术字符串） | 🔧 | | |
| E-CFG-04 | AI 配置正常时优先走 AI，标签与结果一致 | ①配置有效 Key②发送→观察标题标签应显示"AI · <模型名>"（发送前后一致）③确认页**不应**出现"规则解析"字样（正常 AI 解析成功场景） | 🔧 | | |
| E-CFG-05 | AI 回复内容但没有可识别的菜（模型只回文字/道歉）→仍能自动兜底出结果 | ①配置真实但**故意问一句模型答不出菜品的怪异输入**（如"你好"或纯感叹号）②观察：不应直接卡死或弹出旧版"没能识别出菜品"阻断页——应看到自动切到规则解析后的结果（若规则也真解析不出，才应看到"没能识别出菜品，试试更具体的描述？"，这个文案此时才是准确的） | 🔧 | | |
| E-CFG-06 | 周期记模式引擎兜底同样生效 | ①切到"周期记"②在周一、周三、周五三天分别填内容③未配置 Key 或故意让 AI 失败→发送④三天应都能拿到规则解析结果，确认页日期应对应正确的星期（周一是周一、周五是周五，不应互相串日期） | 🔧 | | |

### 📋 快速验证路径（3 分钟核心）

```
E-CFG-01 → E-CFG-02 → E-CFG-03
```

---

## 🔴 B6 新增：架构复核修复批（AF-B456-01~09 关闭）

> 2026-08-07 Coder@副机 编码完成。本批修复：双真相源统一、语音生命周期恢复、截断提示、段状态索引修复、标题文案等。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| E-B6-01 | 单天保存后 Sheet 关闭不冻屏 | 快速记/周期记各保存一次 → Sheet 关闭动画正常，无卡顿黑屏，重复 5 次均正常 | ⬜ | | |
| E-B6-02 | 长输入不截断 | 单日填 3 餐 × ~10 菜的长描述 → AI 完整解析出全部菜品，不出现 `finish_reason=length` 截断 | ⬜ | | |
| E-B6-03 | 已有餐食灰显 | 打开周期记 → 本周已有餐食记录的日期显示半透明+「已有」小标，仍可选中 | ⬜ | | |
| E-B6-04 | 切周后灰显更新（R-01·已知缺陷先记录） | ①周期记打开时当周灰显正确②点→切到下一周③检查该周已有餐食日灰显——**已知缺陷：目前只在打开 Sheet 时查一次，切周后不刷新，此项预期失败，记录现象即可** | ⬜ | | |
| E-B6-05 | 粘贴超 200 字截断 + Snackbar 提示 | ①快速记粘贴 300 字→截断到 200 字+Snackbar「已截取前 200 字」②周期记某天同上③继续键盘输入不再重复弹 Snackbar | ⬜ | | |
| E-B6-VOICE-01 | 语音实例正确释放 | ①长按语音说一句→松手→文本进入输入框②立刻点发送→能发出且预览含该内容③连按三次录音，不崩、不卡、SDK 无残留 | ⬜ | | |
| E-B6-TRUNC-01 | 截断提示不重复弹 | ①快速记输入 250 字→Snackbar 出现一次②继续在已截断框内输入→Snackbar 不再弹③删到 199 字后再输到 210→Snackbar 再弹一次 | ⬜ | | |
| **E-B6-DOT-01**（🆕 第二轮） | 多段进度圆点"未开始"段不误显脉冲 | ①周期记填 3 天（周一/周三/周五）→发送②生成过程中观察段状态圆点：**只有第 1 段（周一）显示脉冲⏳，第 2/3 段显示空心待处理○，不应全部脉冲**③第 1 段完成后→第 2 段脉冲，第 3 段仍空心○④全部完成后圆点全部实心✅（或失败段红色❌） | ⬜ | | |

### 📋 B6 快速验证路径（5 分钟）

```
E-B6-05 → E-B6-TRUNC-01 → E-B6-VOICE-01 → E-B6-01 → E-B6-03
```

---

## 🔴 B4 新增：快速记/周期记双模式输入 UI

> 2026-08-06 B4 编码完成，待真机验证。commit `15bebb03`。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| E-B4-01 | 快速记/周期记模式切换 | ①打开 AI 记一餐 Sheet → 看到默认"快速记"②点击"周期记"→ 输入区切换为 7 天列表③切回"快速记"→ 之前输入的草稿还在 | ⬜ | | |
| E-B4-02 | 周期记日期段导航 | ①周期记模式 → 点击左右箭头切换周②确认日期标签正确（周一~周日）③当前周高亮或默认选中 | ⬜ | | |
| E-B4-03 | 每日输入 + 200 字限制 | ①周期记模式下在周一输入框输入文字 → 字符计数实时更新②输入超过 200 字 → 自动截断，计数变红③粘贴超过 200 字内容 → 截断 + Toast 提示 | ⬜ | | |
| E-B4-04 | 周期记发送 → 流式生成 | ①在周一/周三/周五填内容，其余留空 → 点击发送②确认只发了 3 个请求（非 7 个）③生成过程中可看到进度（第 1/3 天...）④空白天不产生预览内容 | ⬜ | | |
| E-B4-05 | 草稿独立保留 | ①快速记输入"午饭"②切换到周期记③切换回快速记 → "午饭"还在④周期记周一写"早餐"⑤切换到快速记⑥切换回周期记 → 周一"早餐"还在 | ⬜ | | |
| E-B4-06 | 全部空白不可发送 | ①周期记模式全部 7 天空白 → 发送按钮灰色不可点击②快速记模式输入框空白 → 发送按钮灰色 | ⬜ | | |

---

## 🔴 B5 新增：确认页流式展示

> 2026-08-07 B5 编码完成，待真机验证。commit `63fd3fec`。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| E-B5-01 | 生成中渐进展示 | ①周期记填 3 天内容→发送②不再只有三个点，第一个合法菜品出现后立即展示③进度条显示"第 1/3 天" | ⬜ | | |
| E-B5-02 | 段状态指示器 | ①同上→进度条下方段状态圆点②已完成段绿色✅、当前段脉冲⏳、未开始段灰色③全部完成后圆点消失进入最终预览 | ⬜ | | |
| E-B5-03 | 某段失败保留已有 | ①填 3 天→发送②中间段失败→已有第 1 天餐食保留③失败段显示红色❌④可继续确认记下已完成天 | ⬜ | | |
| E-B5-04 | 截断诊断 | ①输入大量内容触发截断（finish_reason=length）②已解析内容完整可见③诊断信息可查看 | ⬜ | | |
| E-B5-05 | 最终重排 | ①多天输入→全部生成完成②确认页按日期+餐次+菜品稳定排序（非流式追加顺序） | ⬜ | | |
| E-B5-06 | 部分保存 | ①周期记填 5 天→生成到第 3 天完成时点"确认记下"②仅保存已完成的 3 天 | ⬜ | | |
| E-B5-07 | 字符计数统一 | ①快速记/周期记输入→字符计数显示一致（`CharCountLabel`）②超 200 变红③不遮挡输入文字 | ⬜ | | |
| E-B5-08 | 切周撤销 | ①周期记→周一填内容→点→切到下一周②Snackbar"已切换到下一周 [撤销]"③点撤销→回到原周+草稿恢复 | ⬜ | | |
| E-B5-09 | 快速记渐进 | ①快速记发送→进度条"第 1/1 天"②解析中即可看到菜名增量出现 | ⬜ | | |
| E-B5-10 | 输入框长按粘贴 | ①快速记输入框→长按→弹出"粘贴/全选"菜单②周期记每日输入框→同上③选中文字后可复制 | ⬜ | | |

### 📋 B5 快速验证路径（5 分钟跑完核心）

```
E-B5-01 → E-B5-02 → E-B5-07 → E-B5-08 → E-B5-10
```

---

## 🔴 V2 追加：失败详情与健康建议（仅会话展示）

> 2026-08-05 用户真机验证：V2 追加整组通过。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| V2-1 | 让 AI 返回非 JSON 或不合法结构，使其降级规则解析 | “规则解析”行出现信息图标；点击可看失败阶段、原因和长度 | ✅ 已通过 | | |
| V2-2 | 在 V2-1 详情中点击「查看原始返回」 | 二次操作后才显示原始模型返回；重新输入或关闭后不可恢复 | ✅ 已通过 | | |
| V2-3 | 正常进入确认餐食页 | 餐食卡下方显示“健康提示”，标注“仅供参考，非医嘱”；不妨碍确认记下 | ✅ 已通过 | | |
| V2-4 | 点击「查看建议」后选“暂不生成” | 不发请求、不显示建议，仍可确认记下 | ✅ 已通过 | | |
| V2-5 | 点击「查看建议」→“同意并生成” | 弹出最小化发送说明；成功后显示最多几条建议，末尾含“仅供参考，非医嘱” | ✅ 已通过 | | |
| V2-6 | V2-5 后重新输入或关闭 Sheet 再打开 | 建议、失败详情和原始响应均被清除；食材/菜品/餐食库内不保存建议文本 | ✅ 已通过 | | |
| V2-7 | 断网或将 AI 配置为无效后点「查看建议」 | 显示“建议暂不可用”，原餐食预览和“确认记下”仍可使用 | ✅ 已通过 | | |
| V2-8 | AI 开启、关闭各做一次任意单餐预览；餐次卡较多时向上滑动确认页 | “查看建议”“修改”“确认记下”均能滚动到并可点击；不被底部 Sheet 裁掉 | ✅ 已通过 | | |
| V2-9 | 添加餐食页先选 **2026-08-14**→AI 输入“中午吃了番茄炒蛋”→确认；再点“修改”后重新发送 | 两次预览与保存都在 **08-14**；只出现一条“当前餐食以选择的餐食日期为参照。”提示，绝不落到设备当天 | ✅ 已通过 | | |
| V2-10 | 添加餐食页先选 **2026-08-13（周四）**→关闭 AI→输入“周一午餐米饭\n周三晚餐青菜” | 日期映射为该周的 **08-10（周一）/08-12（周三）**；仅一条锚点提示且说明按所在周计算 | ✅ 已通过 | | |
| V2-11 | 添加餐食页先选任意日期→输入“8月14日午餐米饭” | 预览和保存使用 **8 月 14 日**，不被所选锚点覆盖 | ✅ 已通过 | | |
| V2-12 | 安装本次 Debug 包→开启 AI→复现 AI 快捷记一次成功/失败解析→导出 `/sdcard/cookbook/log/2026-08-05.log`；如可验证 Release 包，再重复导出 | Debug 日志包含 `AiMealRaw`/`CloudAiRaw` 的 `BEGIN/chunk/END`、`mealInput`、`systemPrompt`、`userPrompt`、`modelContent`、`parseErrors`；Release 日志不包含这些 Raw 标签，只保留 `CloudAi`/`AiMealInput` 关键状态和 `W/E` | ✅ 已通过 | | |
| V2-13 | 让 AI 解析失败→点结果图标→点「查看原始返回」；原始 JSON 很长时上下滑动 | 解析详情弹窗内原始返回可滚动查看，不被弹窗高度裁切 | ✅ 已通过 | | |
| V2-14 | 正常进入确认记录页 | 顶部不再出现「重新输入」；底部只保留「修改」+「确认记下」；两按钮都可滚动到并点击 | ✅ 已通过 | | |
| V2-15 | 添加餐食页选 **2026-08-17**→打开 AI 快捷记后关闭；再改选 **2026-08-19**→重新打开 AI 快捷记并发送无日期文本 | 本次解析以 **2026-08-19** 为锚点，不沿用 08-17；Sheet 不闪退/不自动隐藏 | ✅ 已通过 | | |
| V2-16 | AI 生成一个新菜品并确认保存→进入菜品页「家庭」分类 | 新菜品显示在家庭分类下；来源仍按实际来源区分（AI/自建均属于非预设家庭项） | ✅ 已通过 | | |
| V2-17 | 添加餐食页选某一天→打开 AI 快捷记→输入内容但不确认，误触关闭；未改日期再次打开 | 原输入/预览状态仍保留；若先切换到另一天再打开，则清空并以新日期为锚点；同一天保存成功后再打开是新空会话 | ✅ 已通过 | | |

---

## 🔴 P0 批二：AI 解析容错、规则日期/时间、隐私日志

> 本批可验范围仅含解析基础修复；“失败详情”与“查看建议”尚未实现，不作为本包验收项。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| P0-5 | 开启 DeepSeek，输入「八月十五号午餐红烧肉和米饭\n八月十五号晚饭糖醋排骨和米饭\n八月十六号早饭包子和牛奶」→发送 | 显示“AI 解析”；预览为 8 月 15 日午/晚餐及 8 月 16 日早餐，日期文本绝不显示为“新”菜品 | ⬜ | | |
| P0-6 | 同 P0-5；模型对 `meal_note`、`dish_note` 等可选字段返回 `null` 时发送 | 仍进入 AI 预览，不降级成“规则解析（AI响应无有效结果）”；至少所有菜名正确显示 | ⬜ | | |
| P0-7 | 关闭 AI 或临时使请求失败，输入「八月十五号晚上七点半晚饭糖醋排骨和米饭」→发送 | 规则预览日期为 8 月 15 日、晚餐时间为 19:30；“八月十五号”“晚上七点半”均不作为菜品 | ⬜ | | |
| P0-8 | 按 P0-5 正常 AI 解析后确认一餐，再查看本次 Android 日志 | 日志只含模型、状态码、耗时、请求/响应长度；不含完整输入、请求 JSON、原始模型响应或 Key | ⬜ | | |

---

## 🔴 最新批次：日期上下文+括号解析+AI Token+刷新

### P0 批一：AI Flat 协议与安全确认

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| P0-1 | 开启 AI，食历切到 2026-10-24 后输入「2026-08-03 午餐米饭；2026-08-03 晚餐青菜」 | 预览仅显示 **2026-08-03** 一天，且有午餐、晚餐两个餐次；不被当前日期覆盖 | ⬜ | | |
| P0-2 | 开启 AI，输入模型可返回的错误日期（如 2026-02-30）或非法份量 | 不进入可保存预览；保留原输入并显示可理解的规则降级/失败提示 | ⬜ | | |
| P0-3 | 选择已有午餐记录的日期，AI 再识别一份午餐 | 预览明确提示已有餐食；首次按钮为「我已知晓，继续合并」，再次确认才保存；原记录保留 | ⬜ | | |
| P0-4 | 输入缺少日期的单餐文本 | 预览显示“按当前选择日期记录”的提示，保存到当前食历日期 | ⬜ | | |

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| **D1** | 食历切到 **2026-10-24**→AI 记一餐→输入"中午吃了红烧肉"→确认 | 红烧肉出现在 **10-24 的午餐**中（非今天 08-02） | ⬜ | | |
| **D2** | 同上→输入多天文本"周一/周二/周三..."→确认 | 周一/周二/周三的日期按 **10-24 所在周**推算（非 08-02 所在周） | ⬜ | | |
| **D3** | AI 记一餐→输入"凉皮（黄瓜丝+绿豆芽）"→发送预览 | 食材含"黄瓜丝""绿豆芽"（括号内被正确拆为食材） | ⬜ | | |
| **D4** | 同上→确认记下 | 食历立即刷新显示新餐食 | ⬜ | | |
| **D5** | **开启 AI 模型**（DeepSeek）→输入 3 天菜单→发送 | AI 完整返回（不截断）·预览正常·语法待完善 | ⬜ | | |
| **D6** | 目标日设为 **2026-08-03（周一）**，关闭 AI，输入周三/周二/周一三天菜单（可倒序） | 预览为 08-03/08-04/08-05，不出现 07-28/07-29 | ⬜ | | |
| **D7** | 确认 D6 | 添加餐食页关闭，进入该周周计划，三天餐食都可见 | ⬜ | | |
| **D8** | 单日输入「中午吃了红烧肉和米饭」并确认 | 当前日期立即刷新；不跳周计划 | ⬜ | | |
| **D9** | 输入 `凉皮（黄瓜丝+绿豆芽）+番茄青椒炒鸡蛋虾仁+苹果`，关闭 AI | 预览三道菜；凉皮名完整，含黄瓜丝、绿豆芽 | ⬜ | | |
| **D10** | AI 未配置/无效时发送 | 预览显示「本次结果：规则解析（原因）」 | ⬜ | | |
| **D11** | 可用 AI 时发送同类菜单 | 预览显示「本次结果：AI 解析」；日期和括号规则正确 | ⬜ | | |
| **D12** | 用不存在食材的菜确认后，在食材页「家庭」查找 | 显示「家庭 · AI 创建」，可编辑/删除；预设不可删除 | ⬜ | | |
| **D13** | 输入框已有文字后长按 | 系统菜单可复制/粘贴；右上「粘贴」按钮仍可用 | ⬜ | | |
| **D14** | 查看 AI 记一餐右上说明 | 有多天模板；说明菜之间用 `+`、配料写在 `（）` | ⬜ | | |
| **D15** | 多天预览连续点两次确认 | 同日期同餐次不重复写入菜/餐次 | ⬜ | | |

---

## 🔴 第一批：核心解析修复

> **必验·装包后第一件事**。以下用例修复了"吃了红烧肉和米饭"→菜名含"吃了"+"和"不拆菜+"肉和米饭"成食材的全链路 Bug。

### FIX-1 吃动词剥离 + 软分隔修复

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F1-1 | ①首页→AI快捷记一餐→输入"**中午吃了红烧肉和米饭**"→发送 | 预览页两道菜：**菜名都不含"吃了"**+"和"已正确拆菜 | ⬜ | | |
| F1-2 | 同上→看每道菜热量 | 每道菜下方**有营养值**（"整份约 X 千卡"）·**不显示"营养待完善"** | ⬜ | | |
| F1-3 | 同上→点**确认记下** | Sheet关闭→食历今日**午餐出现红烧肉+米饭两道菜** | ⬜ | | |
| F1-4 | ①食材管理→搜索"**吃了**" | **不应存在**名为"吃了"的食材 | ⬜ | | |

### FIX-2 吃/喝动词同类覆盖

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F2-1 | AI输入"**中午准备吃红烧鹅炒青菜煮玉米和白米饭**"→发送 | 菜名不含"准备吃" | ⬜ | | |
| F2-2 | AI输入"**中午想吃红烧肉**"→发送 | 菜名"红烧肉"·不含"想吃" | ⬜ | | |
| F2-3 | AI输入"**晚上打算吃火锅**"→发送 | 菜名"火锅"·不含"打算吃" | ⬜ | | |
| F2-4 | AI输入"**早餐要喝牛奶**"→发送 | 菜名"牛奶"·不含"要喝" | ⬜ | | |

### FIX-3 AI 模式 Schema 修复

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F3-1 | **开启 AI 模型**（我的→AI设置→打开云端模型）→输入"中午吃了红烧肉和米饭"→发送 | 预览正常·菜名正确·热量有值 | ⬜ | | |
| F3-2 | **关闭 AI 模型**→同上输入→发送 | 预览同样正常（规则兜底） | ⬜ | | |
| F3-3 | 对比 F3-1 和 F3-2 结果 | 两次结果合理一致（AI 模式下解析更精准是期望的） | ⬜ | | |

---

## 🟡 第二批：语音 + autogen 数据完整性

### FIX-4 语音录音修复

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F4-1 | AI快捷记→**长按🎤按钮** | 按钮变主色+**波形条出现**+文字"正在聆听…松手结束" | ⬜ | | |
| F4-2 | 说话→松手 | 文字"识别中…"→**识别结果填入输入框** | ⬜ | | |
| F4-3 | 首次长按（无权限时） | 弹出**麦克风权限请求** | ⬜ | | |

### FIX-5 autogen 数据完整性

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| F5-1 | AI输入任意菜→预览→点确认记下→**再输同样内容**→再确认 | 同名菜不重复创建（`dish` 表 `name` 查不重复） | ⬜ | | |
| F5-2 | 食材管理→搜 AI 建的食材 | 无空白名称食材·无不正常名称食材 | ⬜ | | |
| F5-3 | AI输入"差不多一碗面"→预览 | 食用比例不为 0.75（"差不多"不再被误判为吃了一半） | ⬜ | | |

---

## 🟢 第三批：已有功能回归（防退化）

> 以下为之前批次已验证的功能，本次改动不应影响。

| # | 操作步骤 | 预期结果 | 状态 | 验证结果 | 原因 |
|---|---------|---------|------|------|------|
| R1 | AI输入"中午吃了红烧肉和米饭"→预览→**不点确认**→点外部关闭 | 食材库/餐食**无新增记录**·预览零写库 | ⬜ | | |
| R2 | 食材详情→AI建的食材 | 营养 `ref` 显示"**自动估算**" | ⬜ | | |
| R3 | 食材管理页→**待复核 Tab** | Tab 正常显示+Badge 数量正确 | ⬜ | | |
| R4 | AI输入"**周三**午餐吃了红烧肉"（今天周四） | 日期推算为昨天(周三) | ⬜ | | |
| R5 | AI输入含"**盐**"的菜 | 盐默认 2-3g（非 100g） | ⬜ | | |
| R6 | AI输入"**西红柿炒蛋**" | 食材不重复建"番茄"（已有→复用） | ⬜ | | |
| R7 | AI输入"**宫保鸡丁**" | 不被拆分为多道菜（"保"不是分隔词） | ⬜ | | |
| R8 | AI预览→点**重新输入**→改文字→再发送 | 正常重新解析·不残留旧状态 | ⬜ | | |

---

## ⚪ 第四批：K1+K2 之前遗留

> 来自前次 session，非本次改动，择机验证。
> ⚠️ 编号 2026-08-08 由 `L1~L4` 改为 `LEG-1~4`（legacy 遗留项），避免与本文件"L1 云端 AI 首启同意"批次（`E-L1-01~12`）撞名混淆。

| # | 操作 | 预期 | 状态 | 验证结果 | 原因 |
|----|------|------|------|------|------|
| LEG-1 | AI预览→热量数字开关 | 我的→功能设置→关"热量数值"→预览不显千卡 | ⬜ | | |
| LEG-2 | AI预览→「新」标 | 库里没有的菜→右侧"新"小标签；库里有的→无标 | ⬜ | | |
| LEG-3 | 粘贴按钮 | 剪贴板有文字→AI输入框右上角出现"📋粘贴" | ⬜ | | |
| LEG-4 | 多天预览 | 输入"周一吃了X 周三吃了Y"→预览多天 | ⬜ | | |

---

## 📋 快速验证路径（15 分钟跑完核心）

```
F1-1 → F1-4 → F2-2 → F3-1 → F3-2 → F4-1 → F4-2 → R1 → R3
```

---

## ⚠️ 已知：数据库脏数据

以下为本次 Bug 产生的历史脏数据（在 `GCL0220212004523` 真机上）：

| 表 | ID | 名称 | 建议 |
|----|----|------|------|
| ingredient | 1207 | 吃了 | 手动删除 |
| ingredient | 1208 | 肉和米饭 | 手动删除 |
| dish | 774 | 吃了红烧肉和米饭 | 软删 |
| dish | 776 | 吃了土豆粉 | 软删 |
