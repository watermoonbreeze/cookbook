# 操作步骤删除按钮与食材主分类布局修复完成

- 时间：2026-06-21

## 完成内容

- 菜品新增/编辑页操作步骤 item 的删除入口移到右下角，改为 `删除` 文本按钮，释放顶部空间。
- 食材浏览器顶部主分类从固定均分 `TabRow` 改为 `ScrollableTabRow`。
- 主分类 Tab 文本设置为单行不换行，并设置最小宽度，避免“自定义”被挤成两行。

## 验证

- `./gradlew :androidApp:assembleDebug` 通过。
