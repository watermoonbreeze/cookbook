# 2026-06-06 任务5前置上下文

## 任务编排

- 模式：深度级别 / 预测试可观测性落地模式。[AI生成]
- 原因：用户准备预测试，需要把关键界面流转日志落到 `/sdcard/cookbook/log/`，并规划内测埋点采集数据。[AI生成]

## 用户需求

- 刚才增加的 UI 流转日志需要同步写入 `/sdcard/cookbook/log/`。[AI生成]
- 日志文件按日期创建，方便导出几天内日志给 Codex 分析。[AI生成]
- 需要规划开始内测时应用应埋哪些数据点，用于后续判断是否有 bug。[AI生成]

## 初步约束

- 日志应优先打印 route/date/id/count/blockId 等摘要，避免图片路径、大文本、隐私数据。[AI生成]
- 需要复用当前 `/sdcard/cookbook/` 权限和目录初始化逻辑，避免新增分散路径。[AI生成]
- 埋点阶段先建议本地化、低侵入，后续再决定是否上传服务端。[AI生成]
