# 2026-05-24 后续待处理列表

- 图片能力升级：
  - 当前食材/菜品图片以 URI 字符串拼接后写入现有 `image_path` 字段，最多 3 张用 `|` 分隔。
  - 后续需要改为：拍照或相册选择后复制到 app 私有持久目录，避免 cache 清理或外部 URI 权限失效。
  - 建议新增独立图片表或结构化字段，支持菜品/食材多图、排序、封面图、删除图片。
  - 同步考虑 iOS 端图片持久化目录和 shared 层字段模型，避免只做 Android 单端方案。
