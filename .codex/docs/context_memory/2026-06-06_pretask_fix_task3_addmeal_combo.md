# 2026-06-06 任务3前置上下文

## 任务编排

- 模式：标准级别 / 缺陷修复模式。[AI生成]
- 原因：问题集中在添加餐食页面的弹框状态、菜品新增回填、组合命名，影响用户主流程但不涉及大范围架构调整。[AI生成]

## 用户需求

- 添加餐食页中，从“添加到餐次”弹框进入新建菜品并保存后，菜品已回填到添加餐食页，但弹框没有关闭，需要关闭。[AI生成]
- 再次点击餐次 `+` 进入添加弹框时，新增加的菜品需要能显示在菜品库/候选列表中。[AI生成]
- 保存组合默认名称改为 `日期+餐次+时间 (几道菜)`，替换现在的 `餐次 组合 (几道菜)`。[AI生成]

## 验收点

- 新建菜品保存返回添加餐食页后，添加到餐次弹框自动隐藏。[AI生成]
- 再次打开添加到餐次弹框，新菜品出现在列表中并可选。[AI生成]
- 保存组合默认名称能明确包含日期、餐次、时间、菜品数量。[AI生成]
