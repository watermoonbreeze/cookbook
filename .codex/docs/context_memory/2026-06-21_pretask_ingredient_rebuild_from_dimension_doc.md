# 食材体系推翻重做任务前快照

- 时间：2026-06-21
- 用户需求：推翻当前食材分类方案，按 `日常食材维度.md` 中的分类重新设计；尽可能利用文档中的所有食材和分类；不要拘泥于现有代码和架构。
- 关键要求：
  - 菜品中的选择食材与食材界面必须统一，都按新的食材体系和交互来。
  - 顶部主分类顺序调整为：最近、常规、营养、调养、自定义。
  - `调养` 包含所有按病种/人群区分的食材选择，不仅是慢病。
  - 最近使用统一放到 `最近` 主标签，其他标签下不再显示最近使用入口。
  - 管理分类统一放到 `自定义` 主标签，其他标签下不再显示 `+管理分类`。
  - 后续用户新增分类全部进入 `自定义` 标签下。
- 任务类型/深度：Feature 重构，深度。
- 计划角色：主线程模拟 DEV_PM、DEV_SA、DEV_ARCH、DEV_DB、DEV_UI、DEV_REVIEW；不真实 spawn 子代理。
- 当前已知状态：
  - 食材页刚完成第一步 Tab：常规/营养/慢病/自定义。
  - 现有 seed 分类只覆盖粗粒度分类，未完整吸收 `日常食材维度.md`。
  - 当前选择器和食材管理页仍在同一 `IngredientPickerScreen/ViewModel` 中。
- 预计影响：
  - `food_categories.json`、`ingredients.json`、`crowd_rules.json` 需要整体重构。
  - `IngredientPickerViewModel` 的主分类、虚拟入口、分类树逻辑需要重做。
  - `IngredientPickerScreen` 需要统一选择模式和管理模式的分类入口。
  - 可能需要调整分类层级支持，从二级升级到多级。
- 风险：
  - 文档食材量和分类量很大，若一次全量手工落库，容易出现重复、别名混乱、分类过细导致 UI 难用。
  - 需要先确定分类树策略和 seed 数据生成策略，再进入实现。
