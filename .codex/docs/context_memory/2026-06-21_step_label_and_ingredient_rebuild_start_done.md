# 操作步骤标签移除与食材重构启动完成摘要

- 时间：2026-06-21

## 完成内容

- 菜品新增/编辑页的操作步骤不再显示固定 `第 x 步` 标签。
- 菜品详情页的操作步骤不再显示固定 `第 x 步` 标签。
- 食材浏览器顶部主分类调整为：最近、常规、营养、调养、自定义。
- 菜品选择食材弹窗和食材页统一显示同一套顶部主分类，不再保留旧选择器分类入口。
- 最近使用统一进入 `最近` 主分类，不再作为其他分类下的左侧入口。
- 管理分类入口只在 `自定义` 主分类下显示。
- 默认进入 `最近` 主分类时加载最近使用食材。

## 验证

- `./gradlew :androidApp:assembleDebug` 通过。

## 后续建议

- 下一步进入食材体系重构阶段 1：把 `temp/claude/日常食材维度.md` 转成结构化分类和食材 seed。
- 随后升级左侧分类树为真正多级树，并重做新增/编辑食材完整表单。
