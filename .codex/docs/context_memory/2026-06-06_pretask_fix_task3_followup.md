# 2026-06-06 任务3后续修正前置上下文

## 任务编排

- 模式：标准级别 / 回归修正模式。[AI生成]
- 原因：修正上一次添加餐食弹框处理的交互方向，并排查编辑餐食返回后未保存编辑状态被旧数据覆盖的问题。[AI生成]

## 用户反馈

- 新增菜品后不需要返回添加餐食界面，应保留在菜品选择界面，并能显示新增菜品。[AI生成]
- 保存组合默认名称不需要再带 `(几道菜)`，因为弹框中已有菜品数量语义。[AI生成]
- 编辑餐食时，删除已有菜品后再从新建菜品返回，界面出现原始未编辑菜品，疑似返回时重新加载了数据库旧数据。[AI生成]

## 验收点

- 从菜品选择弹框进入新建菜品，保存返回后仍停留在菜品选择弹框，且新菜品可见/已选。[AI生成]
- 组合默认名称格式为 `日期 餐次 时间`。[AI生成]
- 编辑餐食期间的未保存改动不会因为新建菜品返回而被 `editDate` 重新加载覆盖。[AI生成]
